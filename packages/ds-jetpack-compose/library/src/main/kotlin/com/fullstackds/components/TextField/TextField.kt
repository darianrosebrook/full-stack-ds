// @generated by ds-codegen from components/TextField/TextField.contract.json — do not edit by hand.
package com.fullstackds.components.textfield

// @generated:start imports
import androidx.compose.foundation.layout.Arrangement
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.layout.requiredSizeIn
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.foundation.text.BasicTextField
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.unit.dp
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsDp
// @generated:end

// @generated:start component
@Composable
fun TextField(
    modifier: Modifier = Modifier,
    value: String? = null,
    defaultValue: String = "",
    onChange: ((String) -> Unit)? = null,
    label: (@Composable () -> Unit)? = null,
    description: (@Composable () -> Unit)? = null,
    error: (@Composable () -> Unit)? = null,
    invalid: Boolean = false,
    enabled: Boolean = true,
) {
    var uncontrolledValue by remember { mutableStateOf(defaultValue) }
    val resolvedValue = value ?: uncontrolledValue
    val fsdsTheme = LocalFsdsTheme.current
    fun layeredSlot(slotName: String): String? {
        val def = textFieldTokenScopes["root"]?.get(slotName)
        return def?.let { fsdsTheme.resolve(it) }
    }
    val fieldRadius = layeredSlot("text-field.border.radius")?.toFsdsDp() ?: 4.dp
    val fieldGap = layeredSlot("box-model.gap")?.toFsdsDp() ?: 0.dp
    val fieldPadding = PaddingValues(start = layeredSlot("box-model.padding-inline-start")?.toFsdsDp() ?: 0.dp, end = layeredSlot("box-model.padding-inline-end")?.toFsdsDp() ?: 0.dp, top = layeredSlot("box-model.padding-block-start")?.toFsdsDp() ?: 0.dp, bottom = layeredSlot("box-model.padding-block-end")?.toFsdsDp() ?: 0.dp)
    val fieldMinWidth = layeredSlot("box-model.min-width")?.toFsdsDp() ?: 0.dp
    val fieldMinHeight = layeredSlot("box-model.min-height")?.toFsdsDp() ?: 0.dp

    val slotShape = RoundedCornerShape(fieldRadius)
    val inputModifier = Modifier
        .requiredSizeIn(minWidth = fieldMinWidth, minHeight = fieldMinHeight)
        .padding(fieldPadding)
    Column(
        modifier,
        verticalArrangement = Arrangement.spacedBy(fieldGap),
    ) {
        label?.let { region -> region() }
        description?.let { region -> region() }
        error?.let { region -> if (invalid) region() }
        BasicTextField(
            value = resolvedValue,
            onValueChange = { next ->
                if (value == null) { uncontrolledValue = next }
                onChange?.invoke(next)
            },
            modifier = inputModifier,
            enabled = enabled,
            textStyle = TextStyle(color = Color.Unspecified),
        )
    }
}
// @generated:end
