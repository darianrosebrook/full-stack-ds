// @generated by ds-codegen from components/Skeleton/Skeleton.contract.json — do not edit by hand.
package com.fullstackds.components.skeleton

// @generated:start imports
import androidx.compose.foundation.layout.Box
import androidx.compose.foundation.layout.height
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsDp
// @generated:end

// @generated:start component
/** variant axis lowered from the contract's variant variant. */
enum class SkeletonVariant { Block, Text, Avatar, Media, Dataviz, Actions }

/** animate axis lowered from the contract's animate variant. */
enum class SkeletonAnimate { Shimmer, Wipe, Pulse, None }

/** density axis lowered from the contract's density variant. */
enum class SkeletonDensity { Compact, Regular, Spacious }

@Composable
fun Skeleton(
    modifier: Modifier = Modifier,
    variant: SkeletonVariant = SkeletonVariant.Block,
    animate: SkeletonAnimate = SkeletonAnimate.Shimmer,
    density: SkeletonDensity = SkeletonDensity.Regular,
    content: @Composable () -> Unit,
) {
    val fsdsTheme = LocalFsdsTheme.current
    fun layeredSlot(slotName: String): String? {
        for (key in listOf("variant_" + variant.name.lowercase(), "variant_" + animate.name.lowercase(), "variant_" + density.name.lowercase(), "root")) {
            val def = skeletonTokenScopes[key]?.get(slotName)
            if (def != null) return fsdsTheme.resolve(def)
        }
        return null
    }
    val paddingInlineStart = layeredSlot("box-model.padding-inline-start")?.toFsdsDp() ?: 0.dp
    val paddingInlineEnd = layeredSlot("box-model.padding-inline-end")?.toFsdsDp() ?: 0.dp
    val paddingBlockStart = layeredSlot("box-model.padding-block-start")?.toFsdsDp() ?: 0.dp
    val paddingBlockEnd = layeredSlot("box-model.padding-block-end")?.toFsdsDp() ?: 0.dp
    val minHeight = layeredSlot("box-model.min-height")?.toFsdsDp()

    val chromeModifier = Modifier
        .padding(start = paddingInlineStart, end = paddingInlineEnd, top = paddingBlockStart, bottom = paddingBlockEnd)
        .then(if (minHeight != null) Modifier.height(minHeight) else Modifier)
    Box(modifier.then(chromeModifier)) { content() }
}
// @generated:end
