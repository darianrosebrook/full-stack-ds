// @generated by ds-codegen from components/NavTree/NavTree.contract.json — do not edit by hand.
package com.fullstackds.components.navtree

// @generated:start imports
import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.layout.requiredSizeIn
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.semantics.clearAndSetSemantics
import androidx.compose.ui.unit.dp
import com.fullstackds.glyph.FsdsGlyphCatalog
import com.fullstackds.glyph.FsdsGlyphIcon
import com.fullstackds.tokens.LocalFsdsContentColor
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsDp
// @generated:end

// @generated:start component
@Composable
fun NavTree(
    modifier: Modifier = Modifier,
    icon: String,
    iconSize: NavTreeIconSize = NavTreeIconSize.Sm,
) {
    val fsdsTheme = LocalFsdsTheme.current
    val glyphSize = when (iconSize) {
        NavTreeIconSize.Sm -> 16.dp
        NavTreeIconSize.Md -> 20.dp
    }
    val glyphModifier = modifier
        .requiredSizeIn(minWidth = fsdsTheme.resolve(navTreeTokenScopes["root"]?.get("box-model.min-width"))?.toFsdsDp() ?: 0.dp, minHeight = fsdsTheme.resolve(navTreeTokenScopes["root"]?.get("box-model.min-height"))?.toFsdsDp() ?: 0.dp)
        .padding(PaddingValues(start = fsdsTheme.resolve(navTreeTokenScopes["root"]?.get("box-model.padding-inline-start"))?.toFsdsDp() ?: 0.dp, end = fsdsTheme.resolve(navTreeTokenScopes["root"]?.get("box-model.padding-inline-end"))?.toFsdsDp() ?: 0.dp, top = fsdsTheme.resolve(navTreeTokenScopes["root"]?.get("box-model.padding-block-start"))?.toFsdsDp() ?: 0.dp, bottom = fsdsTheme.resolve(navTreeTokenScopes["root"]?.get("box-model.padding-block-end"))?.toFsdsDp() ?: 0.dp))
    FsdsGlyphIcon(
        named = icon,
        size = glyphSize,
        tint = LocalFsdsContentColor.current,
        modifier = glyphModifier.then(
            // Decorative-by-default per catalog semantics: the glyph
            // carries no semantics node unless the surface gives it one.
            if (FsdsGlyphCatalog.decorativeDefaults.contains(icon)) {
                Modifier.clearAndSetSemantics { }
            } else {
                Modifier
            },
        ),
    )
}
// @generated:end
