// @generated by ds-codegen from components/Icon/Icon.contract.json — do not edit by hand.
package com.fullstackds.components.icon

// @generated:start imports
import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.layout.requiredSizeIn
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.semantics.clearAndSetSemantics
import androidx.compose.ui.unit.dp
import com.fullstackds.glyph.FsdsGlyphCatalog
import com.fullstackds.glyph.FsdsGlyphIcon
import com.fullstackds.tokens.LocalFsdsContentColor
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsDp
// @generated:end

// @generated:start component
/** Size axis synthesized from the iconGlyph size hints. */
enum class IconSize { Sm, Md, Lg, Xl }

@Composable
fun Icon(
    modifier: Modifier = Modifier,
    name: String,
    size: IconSize = IconSize.Md,
) {
    val fsdsTheme = LocalFsdsTheme.current
    val glyphSize = when (size) {
        IconSize.Sm -> fsdsTheme.resolve(iconTokenScopes["root"]?.get("icon.size.sm"))?.toFsdsDp() ?: 16.dp
        IconSize.Md -> fsdsTheme.resolve(iconTokenScopes["root"]?.get("icon.size.md"))?.toFsdsDp() ?: 20.dp
        IconSize.Lg -> fsdsTheme.resolve(iconTokenScopes["root"]?.get("icon.size.lg"))?.toFsdsDp() ?: 24.dp
        IconSize.Xl -> fsdsTheme.resolve(iconTokenScopes["root"]?.get("icon.size.xl"))?.toFsdsDp() ?: 32.dp
    }
    val glyphModifier = modifier
        .requiredSizeIn(minWidth = fsdsTheme.resolve(iconTokenScopes["root"]?.get("box-model.min-width"))?.toFsdsDp() ?: 0.dp, minHeight = fsdsTheme.resolve(iconTokenScopes["root"]?.get("box-model.min-height"))?.toFsdsDp() ?: 0.dp)
        .padding(PaddingValues(start = fsdsTheme.resolve(iconTokenScopes["root"]?.get("box-model.padding-inline-start"))?.toFsdsDp() ?: 0.dp, end = fsdsTheme.resolve(iconTokenScopes["root"]?.get("box-model.padding-inline-end"))?.toFsdsDp() ?: 0.dp, top = fsdsTheme.resolve(iconTokenScopes["root"]?.get("box-model.padding-block-start"))?.toFsdsDp() ?: 0.dp, bottom = fsdsTheme.resolve(iconTokenScopes["root"]?.get("box-model.padding-block-end"))?.toFsdsDp() ?: 0.dp))
    FsdsGlyphIcon(
        named = name,
        size = glyphSize,
        tint = LocalFsdsContentColor.current,
        modifier = glyphModifier.then(
            // Decorative-by-default per catalog semantics: the glyph
            // carries no semantics node unless the surface gives it one.
            if (FsdsGlyphCatalog.decorativeDefaults.contains(name)) {
                Modifier.clearAndSetSemantics { }
            } else {
                Modifier
            },
        ),
    )
}
// @generated:end
