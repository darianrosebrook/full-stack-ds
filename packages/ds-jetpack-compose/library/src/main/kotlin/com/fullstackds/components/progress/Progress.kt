// @generated by ds-codegen from components/Progress/Progress.contract.json — do not edit by hand.
package com.fullstackds.components.progress

// @generated:start imports
import androidx.compose.foundation.layout.height
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.runtime.CompositionLocalProvider
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.fullstackds.components.progress.FsdsProgressIndicator
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsColor
import com.fullstackds.tokens.toFsdsDp
import com.fullstackds.tokens.toFsdsMs
import com.fullstackds.tokens.LocalFsdsContentColor
// @generated:end

// @generated:start component
/** variant axis lowered from the contract's variant variant. */
enum class ProgressVariant { Linear, Circular }

/** size axis lowered from the contract's size variant. */
enum class ProgressSize { Sm, Md, Lg }

/** intent axis lowered from the contract's intent variant. */
enum class ProgressIntent { Info, Success, Warning, Danger }

@Composable
fun Progress(
    modifier: Modifier = Modifier,
    value: Float? = null,
    label: String? = null,
    showValue: Boolean = false,
    variant: ProgressVariant = ProgressVariant.Linear,
    size: ProgressSize = ProgressSize.Sm,
    intent: ProgressIntent = ProgressIntent.Info,
    content: @Composable () -> Unit = {},
) {
    val fsdsTheme = LocalFsdsTheme.current
    fun layeredSlot(slotName: String): String? {
        for (key in listOf("variant_" + variant.name.lowercase(), "variant_" + size.name.lowercase(), "variant_" + intent.name.lowercase(), "root")) {
            val def = progressTokenScopes[key]?.get(slotName)
            if (def != null) return fsdsTheme.resolve(def)
        }
        return null
    }
    val paddingInlineStart = layeredSlot("box-model.padding-inline-start")?.toFsdsDp() ?: 0.dp
    val paddingInlineEnd = layeredSlot("box-model.padding-inline-end")?.toFsdsDp() ?: 0.dp
    val paddingBlockStart = layeredSlot("box-model.padding-block-start")?.toFsdsDp() ?: 0.dp
    val paddingBlockEnd = layeredSlot("box-model.padding-block-end")?.toFsdsDp() ?: 0.dp
    val minHeight = layeredSlot("box-model.min-height")?.toFsdsDp()
    val textColor = layeredSlot("progress.color.text.default")?.toFsdsColor()
    val trackColor = layeredSlot("progress.color.track.background")?.toFsdsColor()
    val fillColor = layeredSlot(
        when (intent) {
            ProgressIntent.Info -> "progress.color.fill.info"
            ProgressIntent.Success -> "progress.color.fill.success"
            ProgressIntent.Warning -> "progress.color.fill.warning"
            ProgressIntent.Danger -> "progress.color.fill.danger"
        },
    )?.toFsdsColor()
    val durationMs = layeredSlot("progress.motion.duration.indeterminate")?.toFsdsMs() ?: 1200

    val chromeModifier = Modifier
        .padding(start = paddingInlineStart, end = paddingInlineEnd, top = paddingBlockStart, bottom = paddingBlockEnd)
        .then(if (minHeight != null) Modifier.height(minHeight) else Modifier)

    FsdsProgressIndicator(
        progress = value?.let { it / 100f },
        linear = ProgressVariant.Linear == variant,
        size = 24.dp,
        strokeWidth = 4.dp,
        trackColor = trackColor ?: Color(0xFFD0D0D0),
        fillColor = fillColor ?: Color(0xFF0566FE),
        durationMs = durationMs,
        contentDescription = label,
        modifier = modifier.then(chromeModifier),
    )

    if (showValue) {
        CompositionLocalProvider(LocalFsdsContentColor provides (textColor ?: Color.Unspecified)) {
            content()
        }
    }
}
// @generated:end
