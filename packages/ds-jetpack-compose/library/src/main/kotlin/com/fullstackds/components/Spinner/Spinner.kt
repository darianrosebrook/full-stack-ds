// @generated by ds-codegen from components/Spinner/Spinner.contract.json — do not edit by hand.
package com.fullstackds.components.spinner

// @generated:start imports
import androidx.compose.foundation.layout.height
import androidx.compose.foundation.layout.padding
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.fullstackds.components.progress.FsdsProgressIndicator
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsDp
// @generated:end

// @generated:start component
/** size axis lowered from the contract's size variant. */
enum class SpinnerSize { Xs, Sm, Md, Lg }

/** thickness axis lowered from the contract's thickness variant. */
enum class SpinnerThickness { Hairline, Regular, Bold }

@Composable
fun Spinner(
    modifier: Modifier = Modifier,
    label: String? = null,
    size: SpinnerSize = SpinnerSize.Xs,
    thickness: SpinnerThickness = SpinnerThickness.Hairline,
) {
    val fsdsTheme = LocalFsdsTheme.current
    fun layeredSlot(slotName: String): String? {
        for (key in listOf("variant_" + size.name.lowercase(), "variant_" + thickness.name.lowercase(), "root")) {
            val def = spinnerTokenScopes[key]?.get(slotName)
            if (def != null) return fsdsTheme.resolve(def)
        }
        return null
    }
    val paddingInlineStart = layeredSlot("box-model.padding-inline-start")?.toFsdsDp() ?: 0.dp
    val paddingInlineEnd = layeredSlot("box-model.padding-inline-end")?.toFsdsDp() ?: 0.dp
    val paddingBlockStart = layeredSlot("box-model.padding-block-start")?.toFsdsDp() ?: 0.dp
    val paddingBlockEnd = layeredSlot("box-model.padding-block-end")?.toFsdsDp() ?: 0.dp
    val minHeight = layeredSlot("box-model.min-height")?.toFsdsDp()
    val spinnerSize = layeredSlot(
        when (size) {
            SpinnerSize.Xs -> "spinner.size.xs"
            SpinnerSize.Sm -> "spinner.size.sm"
            SpinnerSize.Md -> "spinner.size.md"
            SpinnerSize.Lg -> "spinner.size.lg"
        },
    )?.toFsdsDp()
    val spinnerThickness = layeredSlot(
        when (thickness) {
            SpinnerThickness.Hairline -> "spinner.thickness.hairline"
            SpinnerThickness.Regular -> "spinner.thickness.regular"
            SpinnerThickness.Bold -> "spinner.thickness.bold"
        },
    )?.toFsdsDp()

    val chromeModifier = Modifier
        .padding(start = paddingInlineStart, end = paddingInlineEnd, top = paddingBlockStart, bottom = paddingBlockEnd)
        .then(if (minHeight != null) Modifier.height(minHeight) else Modifier)

    FsdsProgressIndicator(
        progress = null,
        linear = false,
        size = spinnerSize ?: 24.dp,
        strokeWidth = spinnerThickness ?: 3.dp,
        trackColor = Color(0xFFD0D0D0),
        fillColor = Color(0xFF0566FE),
        durationMs = 1200,
        contentDescription = label,
        modifier = modifier.then(chromeModifier),
    )
}
// @generated:end
