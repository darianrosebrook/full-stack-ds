// @generated by ds-codegen from components/ToggleSwitch/ToggleSwitch.contract.json — do not edit by hand.
package com.fullstackds.components.toggleswitch

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.fullstackds.components.toggle.FsdsToggle
import com.fullstackds.components.toggle.FsdsToggleStyle
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsDp

/** Size axis lowered from the contract's ToggleSwitchSize type. */
enum class ToggleSwitchSize { Small, Medium, Large }

@Composable
fun ToggleSwitch(
    checked: Boolean? = null,
    defaultChecked: Boolean = false,
    onChange: ((Boolean) -> Unit)? = null,
    size: ToggleSwitchSize = ToggleSwitchSize.Medium,
    enabled: Boolean = true,
    ariaLabel: String? = null,
    ariaDescribedby: String? = null,
    contentDescription: String? = null,
    modifier: Modifier = Modifier,
) {
    var uncontrolledChecked by remember { mutableStateOf(defaultChecked) }
    val resolvedChecked = checked ?: uncontrolledChecked
    val fsdsTheme = LocalFsdsTheme.current
    val checkedTrackColor: Color? = null
    val checkedThumbColor: Color? = null
    val uncheckedTrackColor: Color? = null
    val uncheckedThumbColor: Color? = null
    val disabledTrackColor: Color? = null
    val disabledThumbColor: Color? = null
    val trackBorderColor: Color? = null
    val focusRingColor: Color? = null
    val minTouchWidth = fsdsTheme.resolve(toggleSwitchTokenScopes["root"]?.get("box-model.min-width"))?.toFsdsDp() ?: 32.dp
    val minTouchHeight = fsdsTheme.resolve(toggleSwitchTokenScopes["root"]?.get("box-model.min-height"))?.toFsdsDp() ?: 32.dp
    val togglePadding = PaddingValues(start = fsdsTheme.resolve(toggleSwitchTokenScopes["root"]?.get("box-model.padding-inline-start"))?.toFsdsDp() ?: 0.dp, end = fsdsTheme.resolve(toggleSwitchTokenScopes["root"]?.get("box-model.padding-inline-end"))?.toFsdsDp() ?: 0.dp, top = fsdsTheme.resolve(toggleSwitchTokenScopes["root"]?.get("box-model.padding-block-start"))?.toFsdsDp() ?: 0.dp, bottom = fsdsTheme.resolve(toggleSwitchTokenScopes["root"]?.get("box-model.padding-block-end"))?.toFsdsDp() ?: 0.dp)


    val (trackWidth, trackHeight) = when (size) {
        ToggleSwitchSize.Small -> 36.dp to 18.dp
        ToggleSwitchSize.Medium -> 48.dp to 24.dp
        ToggleSwitchSize.Large -> 60.dp to 30.dp
    }

    val toggleStyle = FsdsToggleStyle(
        trackWidth = trackWidth,
        trackHeight = trackHeight,
        trackColorChecked = checkedTrackColor ?: Color(0xFFD92D2E),
        trackColorUnchecked = uncheckedTrackColor ?: Color(0xFFB8B8B8),
        trackColorDisabled = disabledTrackColor ?: uncheckedTrackColor ?: Color(0xFFB8B8B8),
        thumbColorChecked = checkedThumbColor ?: Color(0xFFFFFFFF),
        thumbColorUnchecked = uncheckedThumbColor ?: Color(0xFFFFFFFF),
        thumbColorDisabled = disabledThumbColor ?: uncheckedThumbColor ?: Color(0xFFFFFFFF),
        trackBorderColor = trackBorderColor,
        focusRingColor = focusRingColor,
        minTouchWidth = minTouchWidth,
        minTouchHeight = minTouchHeight,
        padding = togglePadding,
    )

    FsdsToggle(
        checked = resolvedChecked,
        onCheckedChange = { next ->
            if (checked == null) {
                uncontrolledChecked = next
            }
            onChange?.invoke(next)
        },
        style = toggleStyle,
        enabled = enabled,
        contentDescription = contentDescription,
        modifier = modifier,
    )
}
