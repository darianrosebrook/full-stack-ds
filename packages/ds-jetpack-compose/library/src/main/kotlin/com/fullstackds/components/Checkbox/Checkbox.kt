// @generated by ds-codegen from components/Checkbox/Checkbox.contract.json — do not edit by hand.
package com.fullstackds.components.checkbox

// @generated:start imports
import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.fullstackds.components.controls.FsdsCheckbox
import com.fullstackds.components.controls.FsdsCheckboxStyle
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsColor
import com.fullstackds.tokens.toFsdsDp
import com.fullstackds.tokens.toFsdsMs
// @generated:end

// @generated:start component
/** Size axis lowered from the contract's CheckboxSize type. */
enum class CheckboxSize { Sm, Md, Lg }

@Composable
fun Checkbox(
    modifier: Modifier = Modifier,
    checked: Boolean? = null,
    defaultChecked: Boolean = false,
    onChange: ((Boolean) -> Unit)? = null,
    size: CheckboxSize = CheckboxSize.Md,
    enabled: Boolean = true,
    contentDescription: String? = null,
) {
    var uncontrolledChecked by remember { mutableStateOf(defaultChecked) }
    val resolvedChecked = checked ?: uncontrolledChecked
    val fsdsTheme = LocalFsdsTheme.current
    val boxColorUnchecked = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("checkbox.color.background.default"))?.toFsdsColor()
    val boxBorderColor = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("checkbox.color.border.default"))?.toFsdsColor()
    val focusRingColor = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("checkbox.focus.ring.color"))?.toFsdsColor()
    val boxBorderWidth = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("checkbox.border.width"))?.toFsdsDp() ?: 1.dp
    val boxRadius = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("checkbox.border.radius"))?.toFsdsDp() ?: 4.dp
    val transitionDurationMs = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("checkbox.transition.duration"))?.toFsdsMs() ?: 150
    val focusRingWidth = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("checkbox.focus.ring.width"))?.toFsdsDp() ?: 2.dp
    val minTouchWidth = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("box-model.min-width"))?.toFsdsDp() ?: 32.dp
    val minTouchHeight = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("box-model.min-height"))?.toFsdsDp() ?: 32.dp
    val checkboxPadding = PaddingValues(start = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("box-model.padding-inline-start"))?.toFsdsDp() ?: 0.dp, end = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("box-model.padding-inline-end"))?.toFsdsDp() ?: 0.dp, top = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("box-model.padding-block-start"))?.toFsdsDp() ?: 0.dp, bottom = fsdsTheme.resolve(checkboxTokenScopes["root"]?.get("box-model.padding-block-end"))?.toFsdsDp() ?: 0.dp)

    val checkboxStyle = FsdsCheckboxStyle(
        boxSize = when (size) {
            CheckboxSize.Sm -> 14.dp
            CheckboxSize.Md -> 18.dp
            CheckboxSize.Lg -> 22.dp
        },
        boxColorChecked = Color(0xFF0566FE), // ledgered constant — no checked-scope slot in the token graph yet
        boxColorUnchecked = boxColorUnchecked ?: Color(0xFFFFFFFF),
        boxColorDisabled = boxColorUnchecked ?: Color(0xFFE4E4E5),
        checkColorChecked = Color(0xFFFFFFFF),
        checkColorDisabled = Color(0xFF9A9A9C),
        borderColor = boxBorderColor,
        borderWidth = boxBorderWidth,
        boxRadius = boxRadius,
        focusRingColor = focusRingColor,
        focusRingWidth = focusRingWidth,
        transitionDurationMs = transitionDurationMs,
        minTouchWidth = minTouchWidth,
        minTouchHeight = minTouchHeight,
        padding = checkboxPadding,
    )

    FsdsCheckbox(
        checked = resolvedChecked,
        onCheckedChange = { next ->
            if (checked == null) {
                uncontrolledChecked = next
            }
            onChange?.invoke(next)
        },
        style = checkboxStyle,
        enabled = enabled,
        contentDescription = contentDescription,
        modifier = modifier,
    )
}
// @generated:end
