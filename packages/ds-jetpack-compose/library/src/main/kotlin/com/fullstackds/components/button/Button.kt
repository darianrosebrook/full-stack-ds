// @generated by ds-codegen from components/Button/Button.contract.json — do not edit by hand.
package com.fullstackds.components.button

// @generated:start imports
import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.fullstackds.components.button.FsdsButton
import com.fullstackds.components.button.FsdsButtonScope
import com.fullstackds.components.button.FsdsButtonStyle
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsColor
import com.fullstackds.tokens.toFsdsDp
import com.fullstackds.tokens.toFsdsMs
// @generated:end

// @generated:start component
/** Size axis lowered from the contract's size variant. */
enum class ButtonSize { Small, Medium, Large }

/** Intent axis lowered from the contract's variant variant. */
enum class ButtonVariant { Primary, Secondary, Tertiary, Ghost, Destructive, Outline }

@Composable
fun Button(
    size: ButtonSize = ButtonSize.Medium,
    variant: ButtonVariant = ButtonVariant.Primary,
    disabled: Boolean = false,
    loading: Boolean = false,
    accessibilityLabel: String? = null,
    onClick: (() -> Unit)? = null,
    modifier: Modifier = Modifier,
    content: @Composable FsdsButtonScope.() -> Unit,
) {
    val fsdsTheme = LocalFsdsTheme.current
    fun layeredSlot(slotName: String): String? {
        for (key in listOf("variant_" + variant.name.lowercase(), "variant_" + size.name.lowercase(), "root")) {
            val def = buttonTokenScopes[key]?.get(slotName)
            if (def != null) return fsdsTheme.resolve(def)
        }
        return null
    }
    val containerColor = layeredSlot("button.color.background.default")?.toFsdsColor()
    val containerHoverColor = layeredSlot("button.color.background.hover")?.toFsdsColor()
    val containerActiveColor = layeredSlot("button.color.background.active")?.toFsdsColor()
    val containerDisabledColor = layeredSlot("button.color.background.disabled")?.toFsdsColor()
    val contentColor = layeredSlot("button.color.foreground.default")?.toFsdsColor()
    val contentDisabledColor = layeredSlot("button.color.foreground.disabled")?.toFsdsColor()
    val borderColor = layeredSlot("button.color.border.default")?.toFsdsColor()
    val focusRingColor = layeredSlot("button.color.border.focus")?.toFsdsColor()
    val cornerRadius = layeredSlot("button.size.radius")?.toFsdsDp() ?: 4.dp
    val borderWidth = layeredSlot("button.size.border")?.toFsdsDp() ?: 1.dp
    val pressDurationMs = layeredSlot("button.motion.duration.fast")?.toFsdsMs() ?: 100
    val minHeight = layeredSlot("button.size.minHeight.medium")?.toFsdsDp() ?: 32.dp
    val minWidth = layeredSlot("box-model.min-width")?.toFsdsDp() ?: 32.dp
    val paddingInline = layeredSlot("button.size.padding-inline.medium")?.toFsdsDp() ?: 8.dp
    val paddingBlock = layeredSlot("button.size.padding-block.medium")?.toFsdsDp() ?: 4.dp

    val buttonStyle = FsdsButtonStyle(
        containerColor = containerColor,
        containerColorHover = containerHoverColor,
        containerColorActive = containerActiveColor,
        containerColorDisabled = containerDisabledColor,
        contentColor = contentColor,
        contentColorDisabled = contentDisabledColor,
        borderColor = borderColor,
        borderWidth = borderWidth,
        cornerRadius = cornerRadius,
        focusRingColor = focusRingColor,
        pressDurationMs = pressDurationMs,
        minHeight = minHeight,
        minWidth = minWidth,
        padding = PaddingValues(horizontal = paddingInline, vertical = paddingBlock),
    )

    FsdsButton(
        onClick = onClick,
        style = buttonStyle,
        modifier = modifier,
        enabled = !disabled && !loading,
        contentDescription = accessibilityLabel,
        content = content,
    )
}
// @generated:end
