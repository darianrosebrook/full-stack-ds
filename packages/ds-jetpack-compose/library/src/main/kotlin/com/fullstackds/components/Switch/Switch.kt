// @generated by ds-codegen from components/Switch/Switch.contract.json — do not edit by hand.
package com.fullstackds.components.switch

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.fullstackds.components.toggle.FsdsToggle
import com.fullstackds.components.toggle.FsdsToggleStyle
import com.fullstackds.tokens.LocalFsdsTheme
import com.fullstackds.tokens.toFsdsColor
import com.fullstackds.tokens.toFsdsDp

/** Size axis lowered from the contract's SwitchSize type. */
enum class SwitchSize { Sm, Md, Lg }

@Composable
fun Switch(
    checked: Boolean? = null,
    defaultChecked: Boolean = false,
    onChange: ((Boolean) -> Unit)? = null,
    size: SwitchSize = SwitchSize.Md,
    enabled: Boolean = true,
    name: String? = null,
    value: String? = null,
    contentDescription: String? = null,
    modifier: Modifier = Modifier,
) {
    var uncontrolledChecked by remember { mutableStateOf(defaultChecked) }
    val resolvedChecked = checked ?: uncontrolledChecked
    val fsdsTheme = LocalFsdsTheme.current
    val checkedTrackColor = fsdsTheme.resolve(switchTokenScopes["checked"]?.get("switch.color.track.background.default"))?.toFsdsColor()
    val checkedThumbColor = fsdsTheme.resolve(switchTokenScopes["checked"]?.get("switch.color.thumb.background.default"))?.toFsdsColor()
    val uncheckedTrackColor = fsdsTheme.resolve(switchTokenScopes["root"]?.get("switch.color.track.background.default"))?.toFsdsColor()
    val uncheckedThumbColor = fsdsTheme.resolve(switchTokenScopes["root"]?.get("switch.color.thumb.background.default"))?.toFsdsColor()
    val disabledTrackColor = fsdsTheme.resolve(switchTokenScopes["disabled"]?.get("switch.color.track.background.default"))?.toFsdsColor()
    val disabledThumbColor: Color? = null
    val trackBorderColor = fsdsTheme.resolve(switchTokenScopes["root"]?.get("switch.color.track.border.default"))?.toFsdsColor()
    val focusRingColor: Color? = null
    val minTouchWidth = fsdsTheme.resolve(switchTokenScopes["root"]?.get("box-model.min-width"))?.toFsdsDp() ?: 32.dp
    val minTouchHeight = fsdsTheme.resolve(switchTokenScopes["root"]?.get("box-model.min-height"))?.toFsdsDp() ?: 32.dp
    val togglePadding = PaddingValues(start = fsdsTheme.resolve(switchTokenScopes["root"]?.get("box-model.padding-inline-start"))?.toFsdsDp() ?: 0.dp, end = fsdsTheme.resolve(switchTokenScopes["root"]?.get("box-model.padding-inline-end"))?.toFsdsDp() ?: 0.dp, top = fsdsTheme.resolve(switchTokenScopes["root"]?.get("box-model.padding-block-start"))?.toFsdsDp() ?: 0.dp, bottom = fsdsTheme.resolve(switchTokenScopes["root"]?.get("box-model.padding-block-end"))?.toFsdsDp() ?: 0.dp)


    val (trackWidth, trackHeight) = when (size) {
        SwitchSize.Sm -> 36.dp to 18.dp
        SwitchSize.Md -> (fsdsTheme.resolve(switchTokenScopes["root"]?.get("switch.size.md.track.width"))?.toFsdsDp() ?: 48.dp) to (fsdsTheme.resolve(switchTokenScopes["root"]?.get("switch.size.md.track.height"))?.toFsdsDp() ?: 24.dp)
        SwitchSize.Lg -> 60.dp to 30.dp
    }

    val toggleStyle = FsdsToggleStyle(
        trackWidth = trackWidth,
        trackHeight = trackHeight,
        trackColorChecked = checkedTrackColor ?: Color(0xFFD92D2E),
        trackColorUnchecked = uncheckedTrackColor ?: Color(0xFFB8B8B8),
        trackColorDisabled = disabledTrackColor ?: uncheckedTrackColor ?: Color(0xFFB8B8B8),
        thumbColorChecked = checkedThumbColor ?: Color(0xFFFFFFFF),
        thumbColorUnchecked = uncheckedThumbColor ?: Color(0xFFFFFFFF),
        thumbColorDisabled = disabledThumbColor ?: uncheckedThumbColor ?: Color(0xFFFFFFFF),
        trackBorderColor = trackBorderColor,
        focusRingColor = focusRingColor,
        minTouchWidth = minTouchWidth,
        minTouchHeight = minTouchHeight,
        padding = togglePadding,
    )

    FsdsToggle(
        checked = resolvedChecked,
        onCheckedChange = { next ->
            if (checked == null) {
                uncontrolledChecked = next
            }
            onChange?.invoke(next)
        },
        style = toggleStyle,
        enabled = enabled,
        contentDescription = contentDescription,
        modifier = modifier,
    )
}
