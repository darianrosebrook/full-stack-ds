{
  "$schema": "../../component.contract.schema.json",
  "name": "CodeBlock",
  "description": "Styled preformatted code block for displaying a literal source string with a declared programming or markup language.",
  "layer": "primitive",
  "category": "display",
  "anatomy": {
    "parts": [
      "root",
      "code",
      "token"
    ],
    "details": {
      "root": {
        "tag": "pre",
        "description": "Preformatted code container that preserves whitespace and provides the visual block surface.",
        "role": "root"
      },
      "code": {
        "tag": "code",
        "description": "Literal source text rendered through the highlight content transform: one token span per syntax token when the highlight gate is enabled, a single plain text run otherwise. Never interpreted markup.",
        "role": "content"
      },
      "token": {
        "tag": "span",
        "description": "Single syntax-token span produced by the highlight transform. Carries the token's literal text and a data-token kind attribute from the CodeBlockTokenType vocabulary; text-only by construction.",
        "role": "content",
        "multiple": true
      }
    },
    "dom": {
      "tag": "pre",
      "part": "root",
      "bindings": {
        "data-language": "prop:language"
      },
      "children": [
        {
          "tag": "code",
          "part": "code",
          "attrs": {
            "spellcheck": "false"
          },
          "bindings": {
            "data-language": "prop:language"
          },
          "content": {
            "transform": "highlight",
            "source": "prop:code",
            "language": "prop:language",
            "gate": "prop:highlight",
            "tokenPart": "token"
          }
        }
      ]
    }
  },
  "props": {
    "designed": {
      "members": [
        {
          "name": "code",
          "propType": { "kind": "string" },
          "description": "Literal source text to display. The value is rendered as text and must not be treated as HTML.",
          "required": true
        },
        {
          "name": "language",
          "propType": { "kind": "ref", "to": "CodeBlockLanguage" },
          "description": "Declared language driving the highlight content transform's tokenizer, and retained for documentation and downstream adapters.",
          "required": true
        },
        {
          "name": "highlight",
          "propType": { "kind": "boolean" },
          "description": "Gate for the highlight content transform. When true (default) the source is rendered as one span per syntax token; when false it is rendered as a single plain text run.",
          "default": true
        }
      ]
    },
    "passthrough": {
      "extends": "pre"
    }
  },
  "types": {
    "CodeBlockLanguage": {
      "kind": "union",
      "values": [
        "bash",
        "css",
        "html",
        "javascript",
        "json",
        "jsx",
        "markdown",
        "plaintext",
        "tsx",
        "typescript"
      ]
    },
    "CodeBlockTokenType": {
      "kind": "union",
      "values": [
        "comment",
        "definition",
        "keyword",
        "plain",
        "property",
        "punctuation",
        "static",
        "string",
        "tag"
      ]
    }
  },
  "variants": {},
  "states": {
    "dimensions": {
      "pointer": {
        "category": "interaction",
        "values": [
          "default"
        ],
        "initial": "default",
        "exclusive": true
      }
    }
  },
  "slots": {
    "root": {
      "required": true,
      "selector": "[data-slot=\"code-block\"]"
    },
    "code": {
      "required": true,
      "selector": "[data-slot=\"code-block-code\"]"
    }
  },
  "ssr": {
    "hydrateOn": "none"
  },
  "rtl": {
    "flipIcon": false
  },
  "a11y": {
    "labeling": [],
    "keyboard": []
  },
  "a2ui": {
    "category": "content",
    "usageHints": [
      "Use code for the literal source string; do not pass pre-rendered HTML",
      "Set language to the closest supported language enum value",
      "Use plaintext when the content has no meaningful syntax language",
      "Set highlight to false to render the source as a single plain text run without token spans",
      "Use CodeSnippet for inline code, keyboard keys, or sample output"
    ],
    "children": {
      "allowed": false,
      "slot": "children",
      "accepts": []
    }
  }
}
