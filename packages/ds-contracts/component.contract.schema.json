{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "component.contract.schema.json",
  "title": "Full-Stack DS Component Contract",
  "description": "Defines the contract for a component: anatomy, props, tokens, a11y, behavior, and usage. Implementation-language-agnostic — any target that can read these fields can reconstruct the component.",
  "type": "object",
  "required": ["name", "layer"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path to this JSON Schema file for editor validation."
    },
    "schemaVersion": {
      "type": "string",
      "description": "Contract schema version. Absent or \"1\" indicates the current iteration."
    },
    "codegen": {
      "type": "object",
      "description": "Codegen behavior overrides for this contract. All fields optional; unset uses defaults.",
      "additionalProperties": false,
      "properties": {
        "tests": {
          "type": "boolean",
          "description": "Whether to emit a test file. Default true. Set false for components with hand-authored tests that codegen should not overwrite."
        }
      }
    },
    "name": {
      "type": "string",
      "pattern": "^[A-Z][a-zA-Z0-9]+$",
      "description": "PascalCase component name (e.g. \"Button\", \"ListItem\")."
    },
    "description": {
      "type": "string",
      "description": "Brief human-readable summary of the component's purpose."
    },
    "layer": {
      "type": "string",
      "enum": ["primitive", "compound", "composer", "assembly"],
      "description": "Architecture layer per the component-complexity blueprint: 'primitive' (irreducible to the user, e.g. Button, Input, Icon), 'compound' (blessed bundle of primitives with ARIA wiring but no state orchestration, e.g. TextField, Card, Chip), 'composer' (orchestrates state, focus, dismissal, or multi-step interaction, e.g. Dialog, Popover, Field, Tabs), or 'assembly' (product flow; assemblies live in the app, not the system)."
    },
    "category": {
      "type": "string",
      "enum": ["action", "input", "surface", "feedback", "glyph", "display", "structure"],
      "description": "Behavioral category, used to derive box-model defaults from category-keyed semantic tokens (semantic.<category>.size.*). 'action' (button-like: Button, IconButton, Switch), 'input' (form controls: Input, TextField, Select, Checkbox), 'surface' (containers: Card, Sheet, Dialog, Popover, Tooltip, Toast), 'feedback' (status: Alert, Banner, Progress, Skeleton, Status), 'glyph' (atomic visual at a fixed square extent: Icon, Avatar, Badge, ProfileFlag), 'display' (inline content / structural visual that sizes to context: Text, Label, Chip, Image, Links, Stat, Truncate, Divider), 'structure' (collection layout: Table, Tabs, Accordion, NavList, List, Breadcrumbs). Codegen does not consume this field today — the migration script reads it to seed box-model defaults. See docs/box-model-primitive.md."
    },
    "cssPrefix": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "BEM block name override. Defaults to kebab-cased component name."
    },
    "anatomy": {
      "description": "Named sub-parts that make up the component. Legacy forms (string array or {parts, description}) remain valid; prefer the detailed form which attaches roles and metadata to each part.",
      "oneOf": [
        {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "description": "Simple list of anatomy part names (legacy)."
        },
        {
          "type": "object",
          "required": ["parts"],
          "additionalProperties": false,
          "properties": {
            "parts": {
              "type": "array",
              "items": { "type": "string" },
              "minItems": 1,
              "description": "Ordered canonical list of anatomy part names."
            },
            "description": {
              "type": "string",
              "description": "Prose explaining how the parts relate to each other."
            },
            "details": {
              "type": "object",
              "description": "Per-part metadata keyed by part name. Keys should appear in `parts`.",
              "additionalProperties": { "$ref": "#/$defs/partDetails" }
            },
            "dom": {
              "$ref": "#/$defs/domNode",
              "description": "Optional DOM tree describing how parts compose into rendered elements with attribute and event bindings. Framework emitters walk this tree when present; otherwise they fall back to single-root output."
            }
          }
        }
      ]
    },
    "props": {
      "type": "object",
      "description": "Component prop API in the six-bucket form. New contracts should use `designed` + `passthrough` and (where applicable) `constrained` / `restricted`. The `styled` and `hook` buckets are accepted as legacy surfaces during migration; both carry the same shape they had under the open-keyed schema.",
      "additionalProperties": false,
      "properties": {
        "designed": {
          "type": "object",
          "description": "Props the component intentionally exposes as part of its designed API. Every member must have a non-empty description.",
          "required": ["members"],
          "additionalProperties": false,
          "properties": {
            "members": {
              "type": "array",
              "items": { "$ref": "#/$defs/designedPropMember" }
            }
          }
        },
        "passthrough": {
          "type": "object",
          "description": "HTML attribute surface the component deliberately forwards to its root element. `extends` names the HTML element whose attribute surface is inherited (e.g. \"button\", \"input\", \"div\"); the emitter uses this to forward attributes without guessing.",
          "additionalProperties": false,
          "properties": {
            "extends": {
              "type": "string",
              "description": "HTML element whose attribute surface is forwarded, e.g. 'button', 'input', 'div'."
            },
            "allowed": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Explicit allowlist of forwarded attribute names. If absent, every attribute of the extended element is forwarded."
            }
          }
        },
        "constrained": {
          "type": "object",
          "description": "Props that are valid only under specific conditions. Each member must describe its constraint.",
          "required": ["members"],
          "additionalProperties": false,
          "properties": {
            "members": {
              "type": "array",
              "items": { "$ref": "#/$defs/constrainedPropMember" }
            }
          }
        },
        "restricted": {
          "type": "object",
          "description": "Props that must NOT be passed by consumers. Each member must describe why.",
          "required": ["members"],
          "additionalProperties": false,
          "properties": {
            "members": {
              "type": "array",
              "items": { "$ref": "#/$defs/designedPropMember" }
            }
          }
        },
        "styled": {
          "description": "Legacy surface — migrate to `designed` + `passthrough` when updating this contract.",
          "type": "object",
          "required": ["members"],
          "additionalProperties": false,
          "properties": {
            "description": { "type": "string" },
            "members": {
              "type": "array",
              "items": { "$ref": "#/$defs/propMember" }
            }
          }
        },
        "hook": {
          "description": "Props surfaced via a companion hook (e.g. useDialog). Migrate to `designed` when updating this contract.",
          "type": "object",
          "required": ["members"],
          "additionalProperties": false,
          "properties": {
            "description": { "type": "string" },
            "members": {
              "type": "array",
              "items": { "$ref": "#/$defs/propMember" }
            }
          }
        }
      }
    },
    "channels": {
      "type": "object",
      "description": "Controlled/uncontrolled prop pairings. Each channel groups the props that together drive one piece of externally observable state.",
      "additionalProperties": { "$ref": "#/$defs/controlledChannel" }
    },
    "types": {
      "type": "object",
      "description": "Named type definitions referenced by prop members.",
      "additionalProperties": {
        "$ref": "#/$defs/typeDef"
      }
    },
    "dataModel": {
      "description": "Shapes of domain objects the component consumes (e.g. option records, tree nodes). Use when props reference array/object types that need structural definition for non-TS implementers.",
      "$ref": "#/$defs/dataModel"
    },
    "variants": {
      "type": "object",
      "description": "Variant dimensions keyed by prop name, each listing allowed values.",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" },
        "minItems": 1
      }
    },
    "states": {
      "description": "Interaction/visual states. Prefer the dimensional form when states span multiple orthogonal axes (e.g. interaction vs selection vs validity).",
      "oneOf": [
        {
          "type": "array",
          "items": { "type": "string" },
          "description": "Flat list of states (legacy)."
        },
        {
          "type": "object",
          "required": ["dimensions"],
          "additionalProperties": false,
          "properties": {
            "dimensions": {
              "type": "object",
              "description": "Orthogonal state axes keyed by dimension name. By default each dimension is mutually exclusive (one active value at a time).",
              "additionalProperties": { "$ref": "#/$defs/stateDimension" }
            },
            "description": {
              "type": "string"
            }
          }
        }
      ]
    },
    "keyframes": {
      "type": "object",
      "description": "CSS @keyframes definitions. Keys are animation names, values are objects mapping keyframe selectors (from, to, 50%, etc.) to CSS property-value pairs.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": {
          "$ref": "#/$defs/styleBlock"
        }
      }
    },
    "events": {
      "type": "object",
      "description": "Structured signatures for user-facing callbacks (onChange, onOpen, onClose, etc.). Keys are event names — not prop names — and may be emitted through one or more props listed in `emittedVia`.",
      "additionalProperties": { "$ref": "#/$defs/eventSignature" }
    },
    "stateMachine": {
      "$ref": "#/$defs/stateMachine"
    },
    "form": { "$ref": "#/$defs/formContract" },
    "motion": { "$ref": "#/$defs/motionContract" },
    "focus": { "$ref": "#/$defs/focusContract" },
    "portal": { "$ref": "#/$defs/portalContract" },
    "dismissal": {
      "description": "How a dismissable surface can be closed. Accepts either the structured form ({ triggers: [...] }) or a legacy flat array of event names — the flat form lets older portfolio contracts import unchanged.",
      "oneOf": [
        {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["escape", "overlayClick", "outsideClick", "blur", "routeChange", "triggerReclick", "userDismiss", "selection"]
          },
          "description": "Legacy flat form: array of event names. Equivalent to { triggers: [{ event: <name> }, ...] }."
        },
        { "$ref": "#/$defs/dismissalContract" }
      ]
    },
    "surface": { "$ref": "#/$defs/surfaceContract" },
    "slots": {
      "type": "object",
      "description": "Named slot anchors keyed by slot name. Each entry declares whether the slot is required and an optional CSS selector consumers or test harnesses can use to locate the slot in rendered output. Complements anatomy.details — anatomy describes the part's semantic role; slots describes its DOM anchor.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["required"],
        "properties": {
          "required": { "type": "boolean" },
          "selector": {
            "type": "string",
            "description": "CSS selector that identifies the slot anchor in rendered DOM (e.g. \"[data-slot=\\\"trigger\\\"]\")."
          }
        }
      }
    },
    "ssr": {
      "type": "object",
      "description": "Server-side rendering and hydration contract. Tells the codegen and consumers whether the component must be wrapped in a client boundary.",
      "additionalProperties": false,
      "required": ["hydrateOn"],
      "properties": {
        "hydrateOn": {
          "type": "string",
          "enum": ["interaction", "load", "none"],
          "description": "When the component hydrates. \"interaction\" defers until first user interaction (lazy); \"load\" hydrates on initial page load; \"none\" is fully server-rendered with no client behavior."
        }
      }
    },
    "rtl": {
      "type": "object",
      "description": "Right-to-left behavior contract. Currently a single field; structured as an object so additional RTL concerns can be added without a breaking change.",
      "additionalProperties": false,
      "properties": {
        "flipIcon": {
          "type": "boolean",
          "description": "Whether directional icons (e.g. chevrons, arrows) should be horizontally mirrored under dir=\"rtl\"."
        }
      }
    },
    "relationships": {
      "type": "array",
      "description": "Cross-part wiring (ID references) that implementers must maintain so assistive tech works.",
      "items": { "$ref": "#/$defs/relationship" }
    },
    "a11y": {
      "$ref": "#/$defs/a11y"
    },
    "a2ui": {
      "$ref": "#/$defs/a2uiContract",
      "description": "Agent-facing authoring metadata. Keep lean — the runtime deriver pulls props, enum, default, events, form from the existing contract blocks."
    }
  },
  "$defs": {
    "domNode": {
      "type": "object",
      "required": ["tag"],
      "additionalProperties": false,
      "description": "Single DOM node in a component's render tree. Framework emitters map this into idiomatic JSX/SFC/template/html output.",
      "properties": {
        "tag": {
          "type": "string",
          "description": "HTML tag name (e.g. \"label\", \"input\"), or special placeholder \"slot\" / \"children\" where consumer-provided content renders."
        },
        "name": {
          "type": "string",
          "description": "Named-slot identifier — only meaningful when `tag === \"slot\"`. Maps to framework idioms: React `slots.<name>`, Vue/Lit `<slot name=\"<name>\">`, Svelte `{@render <name>?.()}`, Angular `<ng-content select=\"[slot=<name>]\">`. When absent, the slot is the default/unnamed slot.",
          "pattern": "^[a-z][a-zA-Z0-9]*$"
        },
        "part": {
          "type": "string",
          "description": "Optional reference to an `anatomy.parts` entry. The emitter derives a BEM `__part` class from this."
        },
        "attrs": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Static attribute values rendered verbatim (e.g. {\"type\": \"checkbox\", \"role\": \"switch\"})."
        },
        "bindings": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Dynamic *attribute* bindings keyed by attribute name (e.g. `aria-label`, `disabled`, `src`). Values use the binding-expression mini-language: \"prop:<name>\", \"channel:<name>.value\", \"channel:<name>.onChange\", \"channel:<name>.defaultValue\", or \"literal:<text>\". Event handlers belong under `events`; inner content under `content`. Legacy authoring with `bindings.onX` or `bindings.children`/`bindings.textContent` is auto-translated by the IR builder but new contracts should use the canonical fields."
        },
        "events": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Event handlers keyed by *unprefixed* event name (`click`, `input`, `change`, `keydown`, ...). Values use the same binding-expression mini-language as `bindings`. Each framework emitter lowers to its idiom: React `onClick={...}`, Vue `@click=\"...\"`, Svelte `on:click={...}`, Angular `(click)=\"...\"`, Lit `@click=${...}`. Distinct from attribute bindings because Angular and Lit reject event-shaped attribute bindings."
        },
        "content": {
          "type": "string",
          "description": "Single inner-content binding (one binding expression). Lowers to the framework's content-projection idiom (React `{prop}`, Vue/Angular `{{ prop }}`, Svelte `{prop}`, Lit `${this.prop}`). Mutually exclusive with `children` on the same node — wrap a content value in its own child if you need both."
        },
        "children": {
          "type": "array",
          "items": { "$ref": "#/$defs/domNode" },
          "description": "Child nodes. Empty/absent for leaf elements. Mutually exclusive with `content` on the same node."
        },
        "if": {
          "type": "string",
          "description": "Render guard. \"<prop>\" renders when prop is truthy; \"!<prop>\" renders when prop is FALSY; \"children\" renders when consumer-provided children exist."
        },
        "iterate": {
          "type": "object",
          "additionalProperties": false,
          "required": ["source", "kind"],
          "description": "Renders this node N times, driven by a prop. Note: index-keyed iteration only — not suitable for sortable/reorderable lists.",
          "properties": {
            "source": {
              "type": "string",
              "pattern": "^(prop:[A-Za-z_$][\\w$-]*|channel:[A-Za-z_$][\\w$-]*\\.value)$",
              "description": "Iteration source as a binding expression. Either 'prop:X' referencing a contract prop, or 'channel:X.value' referencing a declared channel's resolved value. 'prop:length' for count-driven; 'prop:items' or 'channel:selection.value' for array-driven."
            },
            "kind": {
              "type": "string",
              "enum": ["count", "array"],
              "description": "'count' renders N copies (source prop must be number-typed). 'array' renders one copy per item (source prop must be array-typed)."
            },
            "indexVar": {
              "type": "string",
              "pattern": "^[a-z][a-zA-Z0-9]*$",
              "default": "index",
              "description": "Local variable name carrying the zero-based loop index. Available to nested binding/content/attr expressions through render-context interpolation."
            },
            "itemVar": {
              "type": "string",
              "pattern": "^[a-z][a-zA-Z0-9]*$",
              "default": "item",
              "description": "Local variable name carrying the current item (only meaningful when kind='array')."
            },
            "itemType": {
              "type": "string",
              "description": "TypeScript type of each item, referencing a `types` entry or a builtin. Required when kind='array'."
            }
          }
        },
        "cssVariableBindings": {
          "type": "object",
          "patternProperties": {
            "^--fsds-[a-z][a-z0-9-]+$": {
              "type": "string",
              "description": "Binding expression (same prop:/channel:/literal: grammar as `bindings`)."
            }
          },
          "additionalProperties": false,
          "description": "CSS custom-property bindings on this element's runtime `style` attribute. Keys must match --fsds-<component>-<name>. Lowers to: React/Vue/Lit object-style; Svelte `style:--x={v}` directive; Angular `[style.--x]` binding. Mutually exclusive with a literal `style` attr on the same node."
        }
      }
    },
    "propMember": {
      "type": "object",
      "description": "A single prop exposed by the component.",
      "required": ["name", "type", "description"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Prop name as it appears in the component API."
        },
        "type": {
          "type": "string",
          "description": "TypeScript type expression (e.g. \"string\", \"ReactElement\", \"TooltipPlacement\")."
        },
        "required": {
          "type": "boolean",
          "description": "Whether the prop must be supplied by the consumer."
        },
        "default": {
          "oneOf": [
            { "type": "string" },
            { "type": "number" },
            { "type": "boolean" },
            {
              "type": "array",
              "items": {
                "oneOf": [
                  { "type": "string" },
                  { "type": "number" },
                  { "type": "boolean" }
                ]
              }
            }
          ],
          "description": "Default value when the prop is omitted. Arrays of primitives are accepted for collection-typed props (e.g. `string[]`); the codegen emits them as JSON literals in the destructure (`= [\"a\", \"b\"]`)."
        },
        "description": {
          "type": "string",
          "description": "What this prop controls."
        },
        "nodeKind": {
          "type": "string",
          "enum": ["node-ref", "icon-ref"],
          "description": "A2UI disambiguation hint for props whose TS type can't distinguish a nested component from a registered icon (`ReactNode`, `ReactElement`). When present, the A2UI deriver uses this as the prop's `accepts` value instead of guessing. Leave unset for non-node props."
        }
      }
    },
    "designedPropMember": {
      "type": "object",
      "description": "A prop in the `designed` or `restricted` bucket. Identical shape to `propMember` except `description` is required to be non-empty — new contracts must explain every designed prop.",
      "required": ["name", "type", "description"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "type": { "type": "string" },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Non-empty description of what this prop controls."
        },
        "required": { "type": "boolean", "default": false },
        "default": {
          "oneOf": [
            { "type": "string" },
            { "type": "number" },
            { "type": "boolean" }
          ]
        },
        "nodeKind": {
          "type": "string",
          "enum": ["node-ref", "icon-ref"]
        }
      }
    },
    "constrainedPropMember": {
      "type": "object",
      "description": "A prop in the `constrained` bucket. Adds a required `constraint` field documenting the condition under which the prop is valid.",
      "required": ["name", "type", "description", "constraint"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "type": { "type": "string" },
        "description": { "type": "string", "minLength": 1 },
        "constraint": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable condition under which this prop is valid (e.g. 'only when asChild=true')."
        },
        "required": { "type": "boolean", "default": false },
        "default": {
          "oneOf": [
            { "type": "string" },
            { "type": "number" },
            { "type": "boolean" }
          ]
        }
      }
    },
    "propGroup": {
      "type": "object",
      "description": "A named group of related props (e.g. styled component props, hook options).",
      "required": ["members"],
      "additionalProperties": false,
      "properties": {
        "description": {
          "type": "string",
          "description": "What this group of props is for."
        },
        "members": {
          "type": "array",
          "items": { "$ref": "#/$defs/propMember" },
          "description": "The props in this group."
        }
      }
    },
    "controlledChannel": {
      "type": "object",
      "description": "Declares a controlled/uncontrolled pairing. References existing prop names inside `props.*.members`; does not redefine them.",
      "required": ["value", "onChange"],
      "additionalProperties": false,
      "properties": {
        "description": {
          "type": "string",
          "description": "What external state this channel represents (e.g. \"selected value\", \"open state\", \"search query\")."
        },
        "value": {
          "type": "string",
          "description": "Name of the controlled value prop."
        },
        "defaultValue": {
          "type": "string",
          "description": "Name of the uncontrolled default-value prop, if one exists."
        },
        "onChange": {
          "type": "string",
          "description": "Name of the change handler prop."
        },
        "enabledBy": {
          "type": "string",
          "description": "Name of a sibling prop that must be truthy for this channel to be active (e.g. a search channel enabled by `isSearchable`)."
        },
        "valueType": {
          "type": "string",
          "description": "Short type hint for the channel's value (e.g. \"number\", \"string | string[]\"). Use when the prop's TS type alone is not descriptive cross-language."
        },
        "notes": {
          "type": "string",
          "description": "Free-form notes about semantics (e.g. \"in multi mode the value is an array\")."
        },
        "omitWhen": {
          "type": "string",
          "description": "Condition under which this channel's value is omitted from form submission. Mirrors `form.serialization.omitWhen`. Documents the case rather than implementing it; the codegen reads this to comment the emitted serialization logic."
        }
      }
    },
    "typeDef": {
      "type": "object",
      "description": "A named type used by one or more props.",
      "required": ["kind"],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["union", "enum", "alias"],
          "description": "What flavor of type this is."
        },
        "values": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Allowed values (required for union and enum kinds)."
        },
        "alias": {
          "type": "string",
          "description": "The type expression this aliases (required for alias kind)."
        }
      }
    },
    "dataModel": {
      "type": "object",
      "description": "Domain entity shapes consumed by the component. Entity fields use simple type expressions; suffix `?` marks optional.",
      "additionalProperties": false,
      "properties": {
        "entities": {
          "type": "object",
          "description": "Named entities keyed by entity name.",
          "additionalProperties": { "$ref": "#/$defs/entityDef" }
        },
        "semantics": {
          "type": "object",
          "description": "Prose notes about semantic behaviors over the data model (selection, filtering, ordering, identity).",
          "additionalProperties": { "type": "string" }
        }
      }
    },
    "entityDef": {
      "type": "object",
      "required": ["fields"],
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "fields": {
          "type": "object",
          "description": "Field name → type expression. Append \"?\" to the name for optional fields (e.g. \"disabled?\").",
          "additionalProperties": { "type": "string" }
        }
      }
    },
    "styleBlock": {
      "type": "object",
      "description": "CSS property-value pairs for a single selector.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "partDetails": {
      "type": "object",
      "description": "Metadata about a single anatomy part: what role it plays, whether it's focusable, whether it renders many times, etc.",
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "tag": {
          "type": "string",
          "description": "Native HTML tag this part is realized as. When set, the codegen uses this tag for the part's compound subcomponent (skipping Stack wrapping for tags in the table composition set) and for the root component's anatomy.dom node (when the part appears in that tree). If anatomy.dom also declares a tag for the same part, the two must agree."
        },
        "role": {
          "type": "string",
          "description": "Conceptual role this part plays in the component's interaction model.",
          "enum": [
            "root",
            "trigger",
            "content",
            "overlay",
            "item",
            "group",
            "label",
            "decoration",
            "region",
            "context"
          ]
        },
        "multiple": {
          "type": "boolean",
          "description": "True if this part is rendered many times (e.g. Tabs.tab, Dropdown.option)."
        },
        "focusable": {
          "oneOf": [
            { "type": "boolean" },
            { "type": "string", "enum": ["roving"] }
          ],
          "description": "Whether this part receives DOM focus. \"roving\" means it participates in a roving-tabindex group."
        },
        "interactive": {
          "type": "boolean",
          "description": "Whether this part handles user events directly (click, key, hover)."
        },
        "portalTarget": {
          "type": "boolean",
          "description": "Whether this part renders into a portal rather than inline."
        },
        "aria": {
          "type": "object",
          "additionalProperties": false,
          "description": "Static ARIA contract for this part.",
          "properties": {
            "role": { "type": "string" },
            "attributes": {
              "type": "array",
              "items": { "type": "string" },
              "description": "ARIA attributes the implementation must set on this part (e.g. \"aria-expanded\", \"aria-selected\")."
            }
          }
        },
        "collapsibleTo": {
          "type": "string",
          "description": "Declares that this part's purpose is fully served by a target's native primitive on platforms that ship one. Emitters consult a per-target intent-to-primitive table: targets with a mapping collapse the part into the native primitive; targets without one render the full multi-part anatomy. Web emitters ignore this field. The vocabulary is closed — add new intents in lockstep with the emitters that consume them.",
          "enum": [
            "native-toggle-affordance"
          ]
        }
      }
    },
    "stateDimension": {
      "type": "object",
      "required": ["values"],
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "values": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Allowed values on this dimension."
        },
        "initial": {
          "type": "string",
          "description": "Default value when the component first renders."
        },
        "exclusive": {
          "type": "boolean",
          "description": "Whether values on this dimension are mutually exclusive. Defaults to true.",
          "default": true
        }
      }
    },
    "formContract": {
      "type": "object",
      "description": "Declares how this component participates in HTML forms. Omit for non-form components; set `participates: false` to explicitly record that a component with form-like props does not serialize.",
      "required": ["participates"],
      "additionalProperties": false,
      "properties": {
        "participates": {
          "type": "boolean",
          "description": "Whether this component contributes to a native <form>'s FormData on submit."
        },
        "inputType": {
          "type": "string",
          "enum": ["text", "number", "email", "password", "search", "tel", "url", "date", "time", "datetime-local", "month", "week", "color", "file", "checkbox", "radio", "select", "hidden", "combobox", "custom"],
          "description": "Platform input type that backs this control."
        },
        "name": { "$ref": "#/$defs/formPropRef", "description": "Prop driving the `name` attribute used as the FormData key." },
        "formProp": { "$ref": "#/$defs/formPropRef", "description": "Prop that overrides the owning <form> id." },
        "required": {
          "type": "object",
          "additionalProperties": false,
          "required": ["prop"],
          "properties": {
            "prop": { "type": "string", "description": "Prop that marks the field as required." },
            "constraintValidation": {
              "type": "boolean",
              "description": "True if the component participates in HTML5 native validation."
            }
          }
        },
        "serialization": {
          "type": "object",
          "additionalProperties": false,
          "required": ["valueType", "valueSource"],
          "properties": {
            "valueType": {
              "type": "string",
              "description": "Type of the serialized value (e.g. \"string\", \"number\", \"File\", \"string[]\")."
            },
            "valueSource": {
              "type": "string",
              "description": "Where the form value originates. Use \"channel:<name>\" to reference a controlled/uncontrolled pair; \"prop:<name>\" for a single prop; \"static:<literal>\" for a fixed value."
            },
            "omitWhen": {
              "type": "string",
              "enum": ["unchecked", "empty", "disabled", "never"],
              "description": "Condition under which the control contributes no entry to FormData."
            },
            "multiple": {
              "type": "boolean",
              "description": "True for controls that contribute multiple entries (multi-select, multi-file)."
            }
          }
        },
        "validity": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "model": {
              "type": "string",
              "enum": ["native", "props", "hybrid"],
              "description": "\"native\": uses platform validationMessage/setCustomValidity. \"props\": validity driven purely by props. \"hybrid\": both, with props taking precedence when set."
            },
            "invalidProp": { "type": "string", "description": "Prop that forces an invalid visual/AT state." },
            "errorMessageProp": { "type": "string", "description": "Prop that carries the error text shown to users and AT." },
            "errorSurface": {
              "type": "string",
              "enum": ["aria-describedby", "aria-errormessage", "live-region", "none"],
              "description": "How the error text is exposed to assistive tech."
            },
            "ariaAttributes": {
              "type": "array",
              "items": { "type": "string" },
              "description": "ARIA attributes the implementation must set on the focusable part to reflect validity."
            }
          }
        },
        "labeling": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "strategy": {
              "type": "string",
              "enum": ["associated-label", "self-labeling", "wrapped-label"]
            },
            "required": { "type": "boolean" },
            "via": {
              "type": "array",
              "items": { "type": "string" }
            }
          }
        },
        "autofill": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "autocomplete": { "type": "string" },
            "hints": {
              "type": "array",
              "items": { "type": "string" }
            }
          }
        }
      }
    },
    "formPropRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["prop"],
      "properties": {
        "prop": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "motionContract": {
      "type": "object",
      "description": "Motion/animation contract. Durations and easings are authored as token references so they resolve through the cascade and honor brand overrides.",
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "transitions": {
          "type": "array",
          "items": { "$ref": "#/$defs/motionTransition" },
          "description": "Named animated transitions this component performs."
        },
        "reducedMotion": {
          "type": "string",
          "enum": ["respect", "disable", "reduce", "ignore"],
          "description": "\"respect\": honor `prefers-reduced-motion`. \"disable\": always skip. \"reduce\": shorter variants. \"ignore\": do not adapt (strongly discouraged)."
        },
        "reducedMotionStrategy": {
          "type": "string",
          "description": "How the reduced-motion adaptation is applied. Free-form."
        }
      }
    },
    "motionTransition": {
      "type": "object",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "trigger": {
          "type": "string",
          "description": "Event or state transition that starts this animation."
        },
        "properties": {
          "type": "array",
          "items": { "type": "string" },
          "description": "CSS properties animated (e.g. \"opacity\", \"transform\", \"height\")."
        },
        "duration": {
          "type": "string",
          "description": "Token reference driving the duration."
        },
        "easing": {
          "type": "string",
          "description": "Token reference driving the easing."
        },
        "phase": {
          "type": "string",
          "enum": ["enter", "exit", "move", "attention"],
          "description": "Semantic phase — useful for reduced-motion heuristics."
        }
      }
    },
    "focusContract": {
      "type": "object",
      "description": "How focus is managed inside and around the component.",
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "strategy": {
          "type": "string",
          "enum": ["trap", "roving", "auto", "manual", "none"],
          "description": "\"trap\": contain focus inside (dialogs). \"roving\": tabindex moves through siblings. \"auto\": component sets initial focus on mount/open. \"manual\": caller controls. \"none\": no managed focus."
        },
        "orientation": {
          "type": "string",
          "enum": ["horizontal", "vertical", "both"]
        },
        "initialFocus": {
          "type": "string",
          "description": "Anatomy part name or prop name (prefix prop names with \"prop:\") that receives focus when first established."
        },
        "returnFocus": {
          "type": "string",
          "description": "Where focus returns when the component closes/unmounts."
        },
        "wrap": {
          "type": "boolean",
          "description": "For roving focus, whether arrow navigation wraps at the ends."
        },
        "scrollLock": {
          "type": "boolean",
          "description": "Whether body scroll is locked while focus is managed here (modals)."
        }
      }
    },
    "portalContract": {
      "type": "object",
      "description": "Whether and how the component escapes its parent DOM container.",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
        "defaultTarget": { "type": "string" },
        "targetProp": { "type": "string" },
        "positioning": {
          "type": "string",
          "enum": ["anchored", "centered", "fixed", "none"]
        },
        "layering": {
          "type": "string",
          "enum": ["modal", "popover", "tooltip", "menu", "toast", "none"]
        },
        "backdrop": { "type": "boolean" },
        "collision": {
          "type": "string",
          "enum": ["flip", "shift", "flip-shift", "none"]
        }
      }
    },
    "dismissalContract": {
      "type": "object",
      "description": "How a dismissable component can be closed.",
      "additionalProperties": false,
      "properties": {
        "triggers": {
          "type": "array",
          "items": { "$ref": "#/$defs/dismissalTrigger" }
        }
      }
    },
    "dismissalTrigger": {
      "type": "object",
      "required": ["event"],
      "additionalProperties": false,
      "properties": {
        "event": {
          "type": "string",
          "enum": ["escape", "overlayClick", "outsideClick", "blur", "routeChange", "triggerReclick", "userDismiss", "selection"]
        },
        "enabledBy": { "type": "string" },
        "defaultEnabled": { "type": "boolean" },
        "description": { "type": "string" }
      }
    },
    "surfaceContract": {
      "type": "object",
      "description": "Declares this component as a presence surface (see docs/presence-surfaces.md). Coordinates anatomy part roles with presence, modality, anchoring, positioning, and controller selection. Does NOT duplicate the existing portal, dismissal, or focus blocks — those remain the detailed policy knobs; surface is the semantic and controller-selection descriptor.",
      "additionalProperties": false,
      "required": ["kind", "presence", "modality"],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["tooltip", "popover", "dialog", "menu", "select", "toast", "coachmark", "sheet"],
          "description": "Semantic presence-surface family. Selects the controller family (e.g. AnchoredSurfaceController for tooltip/popover/menu/select; DialogSurfaceController for dialog; ToastSurfaceController for toast)."
        },
        "presence": {
          "type": "string",
          "enum": ["ephemeral", "persistent"],
          "description": "Lifecycle expectation. Ephemeral surfaces close when their trigger condition expires or a timing budget elapses. Persistent surfaces remain until explicitly dismissed."
        },
        "modality": {
          "type": "string",
          "enum": ["blocking", "non-blocking"],
          "description": "Whether the surface blocks interaction with content outside itself. Blocking surfaces require focus trap, outside inertness, and overlay scrim (specifics live in the top-level focus and portal blocks)."
        },
        "anchor": {
          "type": "object",
          "description": "Relationship to an invoking element. Omit for unanchored surfaces (centered modal dialogs, viewport-edge toasts).",
          "additionalProperties": false,
          "required": ["part", "relation"],
          "properties": {
            "part": {
              "type": "string",
              "description": "Anatomy part name that registers the invoking element. Must reference a part whose details.role === 'trigger'."
            },
            "relation": {
              "type": "string",
              "enum": ["describedby", "controls-expanded", "labelledby", "activedescendant", "none"],
              "description": "Semantic ARIA relation between the trigger element and the surface content."
            }
          }
        },
        "content": {
          "type": "object",
          "description": "Surface content part.",
          "additionalProperties": false,
          "required": ["part", "interactive"],
          "properties": {
            "part": {
              "type": "string",
              "description": "Anatomy part name that registers the rendered surface element. Expected to reference a part whose details.role is one of 'content', 'region', or 'overlay' depending on kind."
            },
            "interactive": {
              "type": "boolean",
              "description": "Whether the content accepts direct user interaction (focusable controls, links). False for tooltips and toasts."
            }
          }
        },
        "positioning": {
          "type": "object",
          "description": "Placement strategy family. Detailed portal target/layering lives in the top-level portal block.",
          "additionalProperties": false,
          "required": ["strategy"],
          "properties": {
            "strategy": {
              "type": "string",
              "enum": ["anchored", "centered", "viewport-edge", "inline"],
              "description": "anchored: relative to the trigger element. centered: viewport-centered. viewport-edge: pinned to a viewport edge (toasts). inline: rendered in document flow (no portal)."
            },
            "placementProp": {
              "type": "string",
              "description": "Name of the consumer-facing prop that controls preferred placement (e.g. 'placement', 'side')."
            },
            "collision": {
              "type": "string",
              "enum": ["none", "flip", "shift", "flip-shift"],
              "description": "Edge-avoidance behavior when anchored placement would overflow the viewport."
            }
          }
        },
        "dismissal": {
          "type": "array",
          "description": "Semantic dismissal modes expected for this surface. The existing top-level dismissal block carries prop-level enablement (e.g. closeOnEscape: boolean enabledBy). The two compose; they do not replace each other.",
          "items": {
            "type": "string",
            "enum": ["escape", "outside-click", "blur", "pointer-leave", "close-button", "timeout"]
          }
        },
        "openTriggers": {
          "type": "array",
          "description": "Interactions on the anchor element that open the surface. Required by IR validation when surface.kind === 'tooltip' or surface.positioning.strategy === 'anchored'; otherwise optional (e.g. toasts and centered dialogs do not have an anchor-driven open trigger).",
          "items": {
            "type": "string",
            "enum": ["hover", "focus", "click"]
          },
          "minItems": 1,
          "uniqueItems": true
        },
        "timing": {
          "type": "object",
          "description": "Names of the consumer-facing props that control surface timing. Default values live in the props.styled.members[].default field, as today.",
          "additionalProperties": false,
          "properties": {
            "openDelayProp": {
              "type": "string",
              "description": "Prop name controlling delayed open (ms before opening once trigger condition starts)."
            },
            "closeDelayProp": {
              "type": "string",
              "description": "Prop name controlling delayed close (ms before closing once trigger condition ends)."
            },
            "autoDismissProp": {
              "type": "string",
              "description": "Prop name controlling timed auto-dismiss (ms after which the surface closes regardless of trigger). Used by ephemeral surfaces like Toast."
            }
          }
        }
      }
    },
    "eventSignature": {
      "type": "object",
      "description": "Describes the payload of a callback.",
      "required": ["description"],
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "emittedVia": {
          "type": "array",
          "items": { "type": "string" }
        },
        "payload": {
          "type": "array",
          "items": { "$ref": "#/$defs/eventPayloadField" }
        },
        "returns": { "type": "string" },
        "cancelable": { "type": "boolean" },
        "triggeredBy": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "eventPayloadField": {
      "type": "object",
      "required": ["name", "type"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "type": { "type": "string" },
        "description": { "type": "string" },
        "optional": { "type": "boolean" }
      }
    },
    "stateMachine": {
      "type": "object",
      "description": "Declarative state-transition model. References dimension values from `states.dimensions` using \"<dimension>=<value>\" syntax.",
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "transitions": {
          "type": "array",
          "items": { "$ref": "#/$defs/stateTransition" }
        }
      }
    },
    "stateTransition": {
      "type": "object",
      "required": ["event", "to"],
      "additionalProperties": false,
      "properties": {
        "event": { "type": "string" },
        "from": {
          "description": "Source state(s). Accepts a single state name or an array of names for shared-target transitions (e.g. both 'hover' and 'focus' transitioning to 'visible' without duplicating entries).",
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" }, "minItems": 1 }
          ]
        },
        "to": { "type": "string" },
        "guard": { "type": "string" },
        "effect": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "relationship": {
      "type": "object",
      "description": "An ID-reference link between two anatomy parts, expressed so implementers know which ARIA attributes to wire.",
      "required": ["from", "to", "attribute"],
      "additionalProperties": false,
      "properties": {
        "from": { "type": "string" },
        "to": { "type": "string" },
        "attribute": { "type": "string" },
        "when": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "keyboardEntry": {
      "description": "A keyboard interaction. Three forms accepted: (1) bare string (legacy); (2) structured ours: { key, action, when?, mode? }; (3) structured portfolio: { key, when, then } — portfolio contracts import unchanged. `then` is an alias for `action`; downstream consumers should treat them equivalently.",
      "oneOf": [
        { "type": "string" },
        {
          "type": "object",
          "required": ["key", "action"],
          "additionalProperties": false,
          "properties": {
            "key": {
              "type": "string",
              "description": "Key or key combo (e.g. \"Escape\", \"Tab\", \"ArrowDown\", \"Shift+Tab\")."
            },
            "action": {
              "type": "string",
              "description": "What happens when the key is pressed."
            },
            "when": {
              "type": "string",
              "description": "Anatomy part that must hold focus for this binding to apply."
            },
            "mode": {
              "type": "string",
              "description": "State condition required for this binding (e.g. \"openness=expanded\")."
            }
          }
        },
        {
          "type": "object",
          "required": ["key", "when", "then"],
          "additionalProperties": false,
          "description": "Portfolio-shape keyboard entry. `then` is the action; treat as an alias for `action` in the structured-ours form.",
          "properties": {
            "key": { "type": "string" },
            "when": { "type": "string" },
            "then": { "type": "string" }
          }
        }
      ]
    },
    "a11y": {
      "type": "object",
      "description": "Accessibility contract for the component.",
      "additionalProperties": false,
      "properties": {
        "role": { "type": "string" },
        "labeling": {
          "type": "array",
          "items": { "type": "string" }
        },
        "keyboard": {
          "type": "array",
          "items": { "$ref": "#/$defs/keyboardEntry" }
        },
        "screenReader": {
          "type": "array",
          "items": { "type": "string" }
        },
        "roles": {
          "type": "array",
          "items": { "type": "string" },
          "description": "All ARIA roles when multiple are used. Use alongside `role` when the component takes more than one role across its anatomy."
        },
        "apgPattern": {
          "oneOf": [
            { "type": "string" },
            { "type": "null" }
          ],
          "description": "ARIA Authoring Practices Guide pattern name (e.g. \"accordion\", \"dialog\", \"combobox\"). Helps agent tooling and a11y review route to the correct interaction expectations. Nullable to make explicit absence distinguishable from forgotten field."
        },
        "obligations": {
          "type": "object",
          "additionalProperties": false,
          "description": "Typed acknowledgements of unresolved ARIA obligations the contract cannot yet satisfy (A11Y-CONTRACT-OBLIGATION-VALIDATOR-01). When the static-role validator finds a missing required attribute, an entry here downgrades the failure to an advisory diagnostic. Every suppression records the upstream blocker that would let the contract declare the real obligation.",
          "properties": {
            "suppress": {
              "type": "array",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["role", "attr", "reason"],
                "properties": {
                  "role": {
                    "type": "string",
                    "description": "ARIA role the suppression applies to, matching the role found in `anatomy.dom`."
                  },
                  "attr": {
                    "type": "string",
                    "description": "Required attribute the contract cannot yet provide (e.g. \"aria-selected\")."
                  },
                  "reason": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Why the obligation can't be met today, and the slice/issue that would unblock the real fix."
                  }
                }
              }
            }
          }
        }
      }
    },
    "a2uiContract": {
      "type": "object",
      "description": "Slim, agent-facing authoring block. Only fields that can't be derived from other contract blocks live here.",
      "required": ["category"],
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": "string",
          "enum": [
            "form-field",
            "action",
            "display",
            "container",
            "feedback",
            "navigation",
            "overlay",
            "media",
            "content",
            "layout"
          ],
          "description": "Broad agent-facing grouping. `content` for static or semi-static text/media containers (Blockquote, Postcard); `layout` for arrangement primitives (Field, List)."
        },
        "usageHints": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Short imperative notes that improve agent selection and prop-filling."
        },
        "children": {
          "type": "object",
          "description": "Whether and how this component accepts child nodes in the A2UI adjacency list.",
          "additionalProperties": false,
          "properties": {
            "allowed": { "type": "boolean" },
            "slot": { "type": "string" },
            "accepts": {
              "type": "array",
              "items": { "type": "string" }
            },
            "min": { "type": "integer", "minimum": 0 },
            "max": { "type": "integer", "minimum": 0 }
          }
        }
      }
    }
  }
}
