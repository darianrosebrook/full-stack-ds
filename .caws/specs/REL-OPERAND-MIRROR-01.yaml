id: REL-OPERAND-MIRROR-01
title: 'Declare operand mirrors: toGrain rebinds to the result grain, keep to the result fields -- the law''s other side is the pool'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Declared mirrors landed: the law''s other side is the pool, the never-discharged list is empty (round-25 eighteen resolved), no verdict moved.'
created_at: '2026-09-15T17:12:27.604Z'
updated_at: '2026-09-15T17:49:46.157Z'
created_by_session: 'session-272d1906-0cae-4fae-ba00-e86a4ac9d030'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/analytical/relation-model.ts + census.ts + erasure-plan.ts (the declared mirror, its threading, the pool and the arity floor)'
    - 'the emitted schemas and the re-recorded ledgers (both authorities move; the supersession carries two classes)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/analytical'
    - 'packages/ds-contracts/analytical-fixtures'
    - 'packages/ds-contracts/relation.contract.schema.json'
    - 'packages/ds-contracts/quotient-image.schema.json'
    - 'docs/architecture/analytical-relation-doctrine.md'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - "The mirror is a LAW fact declared on the operand map and read where every other operand fact is read: the executor never hardcodes which result slot an operand mirrors, and the pool is the mirror list itself -- so the image satisfies the law's equation by construction rather than by searching the input's namespace."
  - "The mirror rebind is the identity on every declaration that already satisfies the equation, which the corpus proves holds throughout: the three coordinates leave never-discharged as corpus-dead, and the structural fact is recorded -- no two admissible fixtures can differ only in the mirrored operand, because the law slaves it to the other side."
  - "The arity facet of a mirrored slot truncates to the mirror's length (a runtime fact) instead of the static minItems floor, and wouldChange answers by comparison there; names are preserved when truncating, so an arity erasure never rewrites occupants."
  - "Both authorities moving means the freeze is superseded with authored effects for both classes, and the pins that named the old refusals are corrected rather than deleted."
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given toGrain and keep are refused because the law writes the SAME binding a second time in the result''''s own declaration (sameSet(out.grain, d.toGrain), sameSet(fieldNames(out), d.keep)), so a one-sided rebind from the input''''s namespace leaves a declaration that disagrees with itself; when the operand map declares the mirror (the result''''s own grain / field names), the census carries it onto the locator, and the executor rebinds the operand to the mirror -- the incidence pool becomes the mirror list and the arity floor becomes the mirror''''s length; then the image satisfies the law''''s equation by construction, the three coordinates leave the never-discharged list as corpus-dead (the equation holds in every corpus declaration, so the operand spelling carries no independent information there), and the analytical suite, every gate and every ledger agree'
non_functional: {}
contracts:
  - name: 'binding-erasure'
    type: contract-test
    path: 'packages/ds-codegen/src/analytical/erasure-plan.test.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "pnpm exec vitest run packages/ds-codegen/src/analytical (19 files / 848 tests green) and all nine analytical gates green in worktree operand-mirror before merge c24b82b6; full root suite 301 files / 4069 tests, typecheck, docs gates green. Measured outcome: operandMirror is declared for toGrain (result.grain) and keep (result.fields), carried onto the locator, the pool IS the mirror, and the arity floor is the mirror's runtime length -- the image satisfies the law's equation by construction. The measurement that dissolved the planned census change: the equation holds in EVERY corpus declaration (unlawful ones included), so the NEVER-DISCHARGED LIST IS EMPTY -- the round-25 eighteen fully resolved (six discharged by the binding quotient, peers declared, two by distinctness, three by the mirror, all corpus-dead for measured reasons). Non-claims and consequences, all pinned: (1) no verdict moved -- freeze holds 160 erasures and the SAME 126 verdicts after the two-class supersession (forget-reference-arity=1, forget-reference-incidence=2, harmonised coordinateBasis first); (2) keep#arity is now dead over the SPECIMEN population too (the closure gate's blocker for it moved from no-separating-pair to erasure-is-the-identity, and an equation-breaking specimen would make it collide -- none is synthesized); (3) the specimen population fell 212 -> 211 (the mirror collapsed a duplicate by content); (4) the rebind is POSITIONAL and the cut only shortens -- a short equation break cannot be repaired, stated in the contract tests rather than hidden; (5) the only manufactured boundary defect left anywhere is bin.closure on FX_H_READINGS_BINNED_DROPS_TEMP, where C3/C3b/C3d are now based."
    recorded_at: "2026-09-15T17:49:45.397Z"