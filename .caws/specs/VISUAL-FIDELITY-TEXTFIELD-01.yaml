id: VISUAL-FIDELITY-TEXTFIELD-01
title: 'TextField visual-fidelity tokenization and verification'
risk_tier: 3
mode: feature
lifecycle_state: active
created_at: '2026-06-13T01:09:33.619Z'
updated_at: '2026-06-13T01:16:55.922Z'
blast_radius:
  modules:
    - 'TextField contract visual sidecars and regenerated TextField artifacts across admitted targets. No schema, IR, emitter, or unrelated component changes.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-contracts/components/TextField/'
    - 'packages/ds-react/src/components/TextField/'
    - 'packages/ds-vue/src/components/TextField/'
    - 'packages/ds-svelte/src/components/TextField/'
    - 'packages/ds-angular/src/components/TextField/'
    - 'packages/ds-lit/src/components/TextField/'
    - 'packages/ds-react-native/src/components/TextField/'
    - 'packages/ds-codegen/.emission-manifest.json'
    - 'test-results/'
    - 'tmp/'
    - packages/ds-figma-plugin/src/generated/components/TextField/
  out: []
invariants:
  - 'TextField visual styling remains contract-governed: generated component CSS changes come only from TextField.tokens.json / TextField.styles.json and regeneration.'
  - 'The slice uses existing global token graph paths; no new semantic token family is introduced in this first pass.'
  - 'No unrelated dirty generated output is overwritten; Button/Toast/RN work already present in the checkout is left untouched.'
acceptance:
  - id: A1
    given: 'the existing TextField contract sidecars'
    when: 'visual literals are audited'
    then: 'field background, border, typography, padding, placeholder, hover, invalid, and disabled treatment resolve through component-local slots backed by existing semantic/core tokens'
  - id: A2
    given: 'the updated TextField sidecars'
    when: 'codegen runs for TextField'
    then: 'generated target artifacts reflect the richer visual contract without hand-edited generated CSS'
  - id: A3
    given: 'the updated corpus'
    when: 'validation runs'
    then: 'generate:check passes for token/style references, or reports actionable drift before the slice is accepted'
non_functional: {}
contracts: []
