id: FEAT-COMPOUND-RN-001
title: 'Tabs compound-state path on React Native'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-08-15T18:43:25.645Z'
updated_at: '2026-08-15T19:00:29.510Z'
blast_radius:
  modules:
    - 'ds-codegen react-native emitter'
    - 'ds-react-native generated package'
    - 'admission rail emission manifest'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/react-native/component-source.ts'
    - 'packages/ds-codegen/src/frameworks/react-native/tests.ts'
    - 'packages/ds-codegen/src/frameworks/react-native/factory.ts'
    - 'packages/ds-react-native/src'
    - 'docs/current-implementation-snapshot.md'
    - packages/ds-codegen/src/validation/frameworks/react-native.ts
  out: []
invariants:
  - 'No contract or schema change — the Tabs contract already carries every compound-state fact this slice lowers.'
  - 'The RN emitter branch gates on the same isCompoundStateContainer IR fact the web emitters consume; no component-name special-casing enters the emitter.'
  - 'ds-react-native must not depend on ds-react — createCompoundContext is ported source, not an import.'
  - 'The ported primitive is committed substrate under ds-react-native/src/primitives (the same provenance tier as stack.tsx and the ds-react seed hooks), consumed by generated code and covered by the RN package typecheck/test gates; it must not import from ds-react.'
  - 'Generated trees for every component other than Tabs remain byte-identical after regeneration.'
  - 'Platform divergences (touch keyboard navigation, indicator animation, orientation layout, unmountInactive=false visual suppression) are emitter-owned, recorded as knownGaps, never pushed into the contract.'
acceptance:
  - id: A1
    given: 'the Tabs contract''s compound-state facts and the web-proven isCompoundStateContainer IR gate'
    when: 'the react-native emitter lowers a compound-state container'
    then: 'generated RN Tabs exports context-wired Tabs/TabsList/TabsTab/TabsPanel with no contract change and no per-component emitter lore'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'createCompoundContext is ported into the ds-react-native primitives as committed substrate beside stack.tsx (mirroring the ds-react seed pattern — primitives sit outside the emission manifest in both packages), with no ds-react dependency, and rail coverage flows through the RN package typecheck and test gates that consume it'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'generated react-test-renderer tests prove press fires onValueChange with the tab value, flips accessibilityState.selected, omits the inactive panel under default unmountInactive, honors controlled value, and throws for TabsTab outside Tabs'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'governed rail, root tests, and the RN framework suite pass inside the worktree with all other components'' generated trees byte-identical'
  - id: A5
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'the snapshot row and RN knownGaps record the named non-claims: touch keyboard navigation, indicator animation, orientation layout, hidden-but-mounted panels'
non_functional: {}
contracts:
  - name: 'Tabs'
    type: behavior
    path: 'packages/ds-contracts/components/Tabs/Tabs.contract.json'
