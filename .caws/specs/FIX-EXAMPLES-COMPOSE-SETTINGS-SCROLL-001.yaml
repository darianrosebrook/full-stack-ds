id: FIX-EXAMPLES-COMPOSE-SETTINGS-SCROLL-001
title: 'Compose settings lane body scroll + runtime-verification findings ledger'
risk_tier: 3
mode: fix
lifecycle_state: active
created_at: '2026-08-17T04:33:05.219Z'
updated_at: '2026-08-17T04:33:05.219Z'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'examples/settings/compose'
  out: []
invariants:
  - 'Consumer-lane-only change: no contract, IR, emitter, or generated-tree edits; the scroll defect is in the example app, not the package.'
  - 'Findings route forward, not around: the M3-default colors/chrome findings point at the reserved compose theme/token module work; no styling workarounds in the lane.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'the lane root layout is scrollable (verticalScroll), mirroring the swift lane''s ScrollView, so content below the fold is reachable when the window is smaller than the content'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'the 2026-08-16 manual runtime verification is ledgered in the lane README: confirmed behaviors (controlled toggle round-trip, state stability, stress resilience, keyboard traversal, console logging) and open findings routed to the reserved compose theme/token module work (M3 purple defaults, stock M3 chrome)'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "examples/settings/compose/src/main/kotlin/.../SettingsApp.kt @ 3a804dab — root Column wraps modifier.verticalScroll(rememberScrollState()); ./gradlew compileKotlin BUILD SUCCESSFUL exit 0 in worktree; runtime confirmation of the scroll behavior itself is a manual-rerun step (the fix mirrors the swift lane's ScrollView)"
    recorded_at: "2026-08-17T04:36:56.313Z"