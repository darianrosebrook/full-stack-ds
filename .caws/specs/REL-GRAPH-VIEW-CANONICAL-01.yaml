id: REL-GRAPH-VIEW-CANONICAL-01
title: 'A canonical graph-view declaration with a graph-valued result, authored and consumed through the project''s own path'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-09-18T08:13:07.705Z'
updated_at: '2026-09-18T08:13:09.135Z'
created_by_session: 'session-69475cd2-20cf-4633-9893-09967382f4d8'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/analytical/graph-view-model.ts (the zod model and its emission)'
    - 'packages/ds-codegen/src/analytical/graph-projection.ts (the loader and the denotation path over a parsed declaration)'
    - 'packages/ds-codegen/src/analytical/authority.ts (the new declaration family''s identity)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/analytical'
    - 'packages/ds-contracts/analytical-fixtures'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'THE DECISION, recorded: a canonical GRAPH-VIEW declaration with a GRAPH-VALUED result, alongside the existing relation-valued derivations. The existing graph derivation retains its documented edge-relation meaning; no nodeRelation operand is added to it, because the result object cannot be described as the old edge relation with one more validated input when its node population can change while the edge relation, its endpoint fields and its grain stay fixed.'
  - 'THE DECISIVE PAIR ENTERS THROUGH THE CANONICAL PATH. A test that hand-builds a declaration while the emitted schema is checked separately does not establish authorability, so the source-selection pair is parsed from the authored file through the loader and its OUTPUTS are observed.'
  - 'New declaration coordinates are accounted for explicitly. The graph-view family is its own identity with its own digest, so existing identities and their ledger stamps do not move; adding a canonical declaration is NOT retrospective ratification of stage 2, and preserving the existing unresolved count is not proof that the extension''s accounting is complete.'
  - 'Boundaries that must not be promoted: the key check establishes that the field carries key: true, NOT that every supplied identity is unique, and it is not a general source-key integrity proof. The supplied arrays are the selected populations for a snapshot; how a partially fetched population should be interpreted is NOT established, and ''not observed yet'' is not silently read as ''outside the universe''.'
  - 'Out of scope: layouts, general graph algorithms, arbitrary multigraph support, new projection families, closing the stage-2 census, and any redesign of the derivation algebra into a graph-valued one.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given graph views are authored as declarations in the canonical location; when the decisive source-selection pair is parsed through the loader and denoted; then the two already-earned graphs are reproduced from the parsed declarations rather than from hand-built objects, and the graph-valued result is what both produced representations consume'
  - id: A2
    given: 'The canonical declaration path exists.'
    when: 'Both produced representations are obtained from a parsed declaration.'
    then: >-
      Neither producer infers the universe from edges nor reconstructs a binding
      from field names; a missing population is unproven, a contradictory binding is
      refused, and the existing identity mutation still fails recovery comparison.
  - id: A3
    given: 'A new declaration family joins the analytical model.'
    when: 'The applicable governance is read.'
    then: >-
      The family carries its own authority identity and its emitted schema is
      drift-gated, the existing identities and their ledger stamps are unmoved with
      no re-record, and the integration is not treated as retrospective stage-2
      ratification.
non_functional: {}
contracts:
  - name: 'graph-view-canonical'
    type: contract-test
    path: 'packages/ds-codegen/src/analytical/graph-projection.test.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "THE DECISION, implemented as recorded: a canonical graph-view declaration whose result is a GRAPH, alongside the relation-valued derivations, and NOT a nodeRelation operand on the existing graph derivation. THE AUTHORING BOUNDARY IS REAL. graph-view-model.ts is the zod definition; analytical-fixtures/graph-view.schema.json is RENDERED FROM IT and drift-gated (analytical:check-schemas: OK, committed schemas equal the model emission); analytical-fixtures/graph-views.json is the authored source carrying the structure, the two populations and the two views; and loadGraphViews parses that file, validating it against the same definition the schema is rendered from, refusing a file that does not validate rather than defaulting. THE FALSE PASS IS PREVENTED: the test does not hand-build a declaration. It calls loadGraphViews() once and derives both bindings from the parsed file, so the decisive pair enters through the project own path and the OUTPUTS are what is observed. MEASURED: the two already-earned graphs are reproduced from the PARSED declarations over ONE unchanged source - GV_ALL_NODES selects [n1,n2,n3] with isolate n3 and GV_CONNECTED_SET selects [n1,n2], with edgeRelationIdentical true - and both preserve through both produced representations. The authored file had to be canonicalised during the work (relation names must match the structure schema lower_snake pattern), which is itself evidence the file passed through the real schema rather than bypassing it. NON-CLAIM: the graph-valued result is authored and consumed through the canonical path, but graph PROJECTION ENUMERATION, rendered behaviour and the analytical thesis remain distinct future questions. Verification: pnpm exec vitest run packages/ds-codegen/src/analytical (22 files / 948 tests), pnpm test (303 files / 4169 tests), typecheck, lint, docs:check-claims, governed:rail and test:frameworks green on merge 9111505f."
    recorded_at: "2026-09-18T08:22:18.370Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "Both representations are produced from the graph-valued result obtained through the parsed declaration, and the producers stayed downstream: they take the denoted graph and never select a relation name or decide endpoint membership. Neither infers the universe from edges - the node-from-edges counterexample is still EXHIBITED in the module and still collapses the pair (distinguishesThePair false) - and neither reconstructs a binding from conventional field names, since the binding arrives parsed. The qualification controls still hold over the parsed view: a field that RESOLVES but is not the declared key is refused (all_nodes.label), an endpoint outside the SELECTED universe is refused rather than dropped (links.dst names n9), a missing population is CARRIED under invariant:population-declared, and the node-universe-is-the-edge-relation case is refused. The identity mutation still moves the PRODUCED output (n3 -> n3-impostor) with countAndEdgesUnchanged true, and recovery comparison still fails. The incidence decoder collision control also still passes with both ambiguous edges surviving."
    recorded_at: "2026-09-18T08:22:18.836Z"