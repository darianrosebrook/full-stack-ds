id: FEAT-SWIFTUI-COVERAGE-BATCH-02
title: 'SwiftUI coverage batch 2: Checkbox, Divider, Progress, Spinner (15→19 admitted)'
risk_tier: 2
mode: feature
lifecycle_state: active
worktree: swift-coverage-batch2
created_at: '2026-08-15T19:41:50.032Z'
updated_at: '2026-08-15T19:41:50.032Z'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/frameworks/swift/swiftui/component-source.ts (boolean-channel branch + leaf classes)'
    - 'packages/ds-swiftui + fsds.targets.json (four new admitted components)'
    - 'docs/current-implementation-snapshot.md (coverage claims)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src'
    - 'packages/ds-swiftui'
    - 'fsds.targets.json'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'Every admission is IR-fact-driven (input root + channel type, bare-leaf root tag, visual-only leaf); no component-identity branching; unrealized props omitted-and-documented.'
  - 'Progress''s value semantics derive from the contract (declared range/max), never assumed; if the contract is silent the emission fails loudly rather than guessing a scale.'
  - 'Prior 15 outputs byte-stable except the newly admitted set; swift test regression suite stays green.'
acceptance:
  - id: A1
    given: 'Checkbox (input root, boolean checked channel)'
    when: 'generated'
    then: 'the value-channel control class generalizes to boolean channels emitting a Toggle(.checkbox) with the controllable-state projection; indeterminate omitted-and-documented (no SwiftUI Toggle indeterminate); compiles'
  - id: A2
    given: 'Divider (hr root, no children, no channels) and Spinner (decorative visual-only leaf)'
    when: 'generated'
    then: 'leaf classes emit — Divider lowers to the native divider with orientation axis (FsdsDivider via the reserved table), Spinner to a native progress affordance with conditional label; both compile'
  - id: A3
    given: 'Progress (value prop, track/fill anatomy, no channels)'
    when: 'generated'
    then: 'it emits a ProgressView driven by the value prop with the contract''s value semantics honored (range verified from the contract, not guessed); label lowers to accessibilityLabel; compiles'
  - id: A4
    given: 'the full worktree gate'
    when: 'typecheck, governed:rail, pnpm test, test:frameworks, swift test, swift build run'
    then: 'all pass; allowlist grows to 19; --target=all byte-stable; remaining corpus still fails loudly'
non_functional: {}
contracts:
  - name: 'swiftui-coverage-batch-02'
    type: contract-test
    path: 'packages/ds-codegen/src/frameworks/swift/swiftui/component-source.test.ts'
