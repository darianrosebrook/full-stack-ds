id: DOCS-READER-NAV-FIX-01
title: 'Docs reader navigation fixes: hash-router links, fragments, single h1, title restore, scheme hardening'
risk_tier: 2
mode: fix
lifecycle_state: active
worktree: docs-nav-fix
created_at: '2026-09-14T13:24:35.104Z'
updated_at: '2026-09-14T13:24:36.402Z'
created_by_session: 'session-670775d8-8388-4f26-b6cd-14dc1b73c8fd'
blast_radius:
  modules:
    - 'showcase docs projection and rendering'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'src/docs'
    - 'src/views'
    - 'src/router.tsx'
    - 'src/app.tsx'
  out: []
invariants:
  - 'Every href the markdown renderer emits navigates within the hash router or is an explicitly allowed external form'
  - 'A fragment-bearing docs route resolves to its document and scrolls to the slugified heading'
  - 'Each reader page renders exactly one h1 regardless of frontmatter-title/body-h1 agreement'
  - 'document.title is restored when the docs view unmounts'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a rendered markdown body link; when clicked; then the app stays in the hash router and the target reader page opens (no server-path navigation)'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a docs route with a #fragment tail; when opened; then the document resolves and the view scrolls to the slugified heading id'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a doc whose frontmatter title differs from its body h1; when the reader renders; then exactly one h1 is present and no body text is lost'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the docs view has set document.title; when the route leaves docs; then the prior document title is restored'
  - id: A5
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given markdown link hrefs with javascript:/data: schemes; when rendered; then the hrefs are neutralized to a same-page anchor'
non_functional: {}
contracts:
  - name: 'docs-reader-nav'
    type: behavior
    path: 'src/docs/routing.ts'
