id: GUARD-CMDTEXT-BOUNDARY-01
title: 'Adjudicate guard verb matchers on command text, not carried payload text'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-09-07T05:34:52.016Z'
updated_at: '2026-09-07T05:35:14.558Z'
created_by_session: 'f46106db-10cf-4869-b035-5bb63e5a1fc2'
blast_radius:
  modules:
    - 'caws hook pack'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - '.caws/hooks/worktree-guard.sh'
    - '.caws/hooks/lib/heredoc.sh'
    - '.caws/hooks/tests'
  out: []
invariants:
  - 'A heredoc body is blanked only for a safelisted file sink (cat/tee); an interpreter heredoc keeps its body fully visible, so the change can only remove false positives, never open a bypass.'
  - 'Every verb matcher that previously refused a real mutating command still refuses it; the probe asserts both directions and fails if either regresses.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a Bash command whose quoted argument or safelisted heredoc body merely NAMES a refused version-control verb; when the worktree guard adjudicates it; then the command is ALLOWED, because no such command is being invoked'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a Bash command that actually invokes one of the refused mutating verbs while worktrees are active; when the worktree guard adjudicates it; then the command is still REFUSED with its original message'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a heredoc piped to an interpreter whose body invokes a refused verb; when the worktree guard adjudicates it; then the body remains visible and the command is REFUSED'
non_functional: {}
contracts:
  - name: 'worktree-guard-cmdtext'
    type: contract-test
    path: '.caws/hooks/tests/test_worktree_guard_cmdtext.sh'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "test_worktree_guard_cmdtext.sh -> PASS (19 cases) at d308f3c5; the five payload-only cases (commit-message prose, echo label, grep argument, cat heredoc body, tee heredoc body) all read ALLOW, where the pre-fix guard read BLOCK for four of them"
    recorded_at: "2026-09-07T05:49:32.790Z"
    command: .caws/hooks/tests/test_worktree_guard_cmdtext.sh
    exit_code: 0
    commit_sha: d308f3c5
  - criterion_id: A2
    status: pass
    evidence_ref: "test_worktree_guard_cmdtext.sh -> PASS at d308f3c5; the seven real-invocation cases (stash, hard reset, amend, restore, clean, force-push, and one chained after a benign command) all still read BLOCK. Falsification: the same suite run against the pre-fix guard from HEAD reports 15 passed / 4 failed, so it can fail for the right reason"
    recorded_at: "2026-09-07T05:49:46.605Z"
    command: .caws/hooks/tests/test_worktree_guard_cmdtext.sh
    exit_code: 0
    commit_sha: d308f3c5
  - criterion_id: A3
    status: pass
    evidence_ref: "test_worktree_guard_cmdtext.sh -> PASS at d308f3c5; all four BYPASS-BLOCK cases (interpreter heredoc, second interpreter heredoc, an interpreter invoked with a sink-shaped argument as decoy, and a sink piped into an interpreter) still read BLOCK. Falsification: the same suite run against the patched guard paired with sterling's unfixed lib/heredoc.sh reports 18 passed / 1 failed, naming the piped-sink case as a bypass -- which is how the pipe disqualifier was found"
    recorded_at: "2026-09-07T05:50:10.002Z"
    command: .caws/hooks/tests/test_worktree_guard_cmdtext.sh
    exit_code: 0
    commit_sha: d308f3c5