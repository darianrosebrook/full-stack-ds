id: FEAT-COMPOSE-CALENDAR-ADMISSION-01
title: 'Shared date-grid substrate predicate; Compose Calendar admission with a real day grid'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Calendar admitted as the 45th compose component: the date-grid predicate moved to the shared substrate strengthened with the dateDayOfMonth projection fact, and Compose now realizes the declared row-per-week day grid over a committed FsdsDate runtime'
created_at: '2026-09-13T05:05:08.306Z'
updated_at: '2026-09-13T05:21:03.506Z'
created_by_session: 'session-a57bcf71-039b-4b33-98c5-60665c7bccd2'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/frameworks/native-emission-class.ts (isDateGridSurface moved from swift and strengthened with the dateDayOfMonth projection fact)'
    - 'packages/ds-codegen/src/frameworks/jetpack-compose/component-source.ts (date-grid emitter path)'
    - 'packages/ds-jetpack-compose/library/src/main/kotlin/com/fullstackds/date/ (committed JVM date substrate)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/native-emission-class.ts'
    - 'packages/ds-codegen/src/frameworks/swift/swiftui/component-source.ts'
    - 'packages/ds-codegen/src/frameworks/jetpack-compose/component-source.ts'
    - 'packages/ds-codegen/src/frameworks/jetpack-compose/component-source.test.ts'
    - 'packages/ds-jetpack-compose/library/src/main/kotlin'
    - 'packages/ds-jetpack-compose/library/src/test/kotlin'
    - 'fsds.targets.json'
    - 'scripts/compose-parity-diff.mjs'
    - 'scripts/compose-parity-diff.test.mjs'
    - 'docs/architecture/native-target-admission.md'
    - 'docs/current-implementation-snapshot.md'
    - 'AGENTS.md'
    - 'docs/normal-form.md'
  out: []
invariants:
  - 'The date-grid predicate is the complement of the Date exclusions already in isArrayIteratedList and isInteractiveComposite: moving it to the substrate leaves SwiftUI regeneration byte-identical.'
  - 'Every layeredSlot read in the emitted Calendar has a definition in the emitted tokens file, and every definition is read.'
  - 'Calendar stays inside the foundation-only compose surface: zero androidx.compose.material imports.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given Calendar declares one Date-valued channel over a header/grid anatomy whose cells project the closed dateDayOfMonth projection; when the date-grid predicate moves into the shared native substrate and Compose lowers the class to a real row-per-week day grid over a committed date substrate; then SwiftUI regeneration stays byte-identical, Calendar compiles through gradle, the compose allowlist grows 44 to 45, and the parity and doc gates stay green'
non_functional: {}
contracts:
  - name: 'date-grid-surface'
    type: behavior
    path: 'packages/ds-codegen/src/frameworks/native-emission-class.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "merge 943844da: gradlew -p packages/ds-jetpack-compose :library:compileKotlin :library:test --no-daemon -> BUILD SUCCESSFUL (FsdsDateTest 6 cases incl. midnight straddle, zone flip, day-granular toggle); pnpm run parity:compose-tokens -> self-test 16/16 and 45 allowlisted components, 0 failures; node packages/ds-codegen/dist/cli.js --target=swiftui -> packages/ds-swiftui byte-identical after the predicate move; pnpm run generate:check -> 52/52; pnpm run docs:check-claims -> 75 marked claims match (remainder derived 8 -> 7); pnpm test -> 291 files / 3905 tests pass; pnpm run test:frameworks -> exit 0; pnpm run governed:rail -> exit 0 with a clean tree; fsds.targets.json jetpack-compose allowlist 44 -> 45"
    recorded_at: "2026-09-13T05:21:02.668Z"