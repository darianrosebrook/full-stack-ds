id: FIX-CI-HISTORY-PROVISIONING-01
title: 'Provision git history for CI''s doc-freshness check'
risk_tier: 3
mode: fix
lifecycle_state: active
created_at: '2026-09-20T02:16:30.086Z'
updated_at: '2026-09-20T02:17:08.164Z'
created_by_session: 'session-8e785c23-806e-4b0b-8d1f-534c73921cef'
blast_radius:
  modules:
    - 'governed-ci'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - '.github/workflows/ci.yml'
  out: []
invariants:
  - 'The freshness check itself is untouched: no weakened rule, no baseline entries invented for unresolvable commits, no restamps to HEAD. With history available, a genuinely stale stamp must still fail.'
acceptance:
  - id: A1
    given: 'the main gate job''s checkout with fetch-depth: 0 and the pushed revision'
    when: 'the real workflow run executes docs:check-claims'
    then: 'the verification stamps resolve (no does-not-resolve findings) and the checker''s stale-stamp self-tests still run inside the same invocation, retaining sensitivity'
  - id: A2
    given: 'the same real workflow run'
    when: 'docs:check-claims passes'
    then: 'the run proceeds into and reports the downstream checks (tests, rail, drift diff) - downstream execution on a real run, not the YAML edit, is the completion evidence'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Real workflow run 35486027929 on pushed HEAD 171b64af (watched to completion via gh): the gate job (Validate, lint, typecheck, test) succeeded — docs:check-claims executed with full history and produced no unresolvable-stamp findings (the prior run 35470980084 on fe50628e failed this same step with eleven 'does not resolve to a commit' findings under depth-1). Stale-sensitivity retained: the checker's own self-test fixtures (fresh-pass/stale-fail/ledgered-pass) execute inside docs:check-claims and passed within the green gate; no rule weakened, no baseline invented, no restamps."
    recorded_at: "2026-09-20T03:31:08.340Z"
    command: gh run view 35486027929 --repo darianrosebrook/full-stack-ds
    exit_code: 0
    commit_sha: "171b64aff0b5"