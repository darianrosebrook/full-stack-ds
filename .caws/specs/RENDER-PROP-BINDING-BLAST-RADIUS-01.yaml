id: RENDER-PROP-BINDING-BLAST-RADIUS-01
title: 'Render prop->DOM binding obligation blast-radius audit (classified matrix; no fix)'
risk_tier: 3
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge render-binding at ded3dacc7baa80592edd6de332b254cdee43bbd8'
created_at: '2026-06-05T21:11:11.812Z'
updated_at: '2026-06-05T21:37:19.687Z'
blast_radius:
  modules:
    - 'scripts/render-binding-audit/ (new read-only audit tool: derives each prop''s render obligation from the contract structure and reports per-framework satisfaction)'
    - 'docs/render-binding-audit/ (emitted classified matrix report: JSON + markdown)'
    - 'READ-ONLY inputs (inspected, never modified): packages/ds-contracts/components/*/*.contract.json, packages/ds-codegen/src/ir.ts, and the five generated framework package sources'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - scripts/render-binding-audit/
    - docs/render-binding-audit/
  out: []
invariants:
  - 'Read-only: the audit makes zero changes to contracts, IR, emitters, or any generated artifact. A clean git diff over packages/** confirms nothing outside scripts/render-binding-audit and docs/render-binding-audit changed.'
  - 'Every declared prop is classified into exactly one render-obligation bucket (native-attr | aria-attr | class-state | content | behavior | no-render) derived from the contract''s own declared structure (passthrough.extends, anatomy.dom bindings/content/events, channels, variants, propType, form), never from component-name heuristics.'
  - 'Obligation satisfaction is reported per framework with a typed status (bound | derived | consumed | missing | not-applicable). Behavior and content props are never reported as missing native attributes (typed false-positive control).'
acceptance:
  - id: A1
    given: 'the 47 component contracts and the five generated framework package sources'
    when: 'the audit runs'
    then: 'it emits a stable JSON + markdown matrix (component x prop x bucket x per-framework status) covering all components and all five frameworks'
  - id: A2
    given: 'the Input declared native form-control attributes (the placeholder, name and required props) that are absent from anatomy.dom.bindings'
    when: 'audited'
    then: 'all three are reported missing across all five frameworks (known-defect detection, deterministic)'
  - id: A3
    given: 'behavior props (value/onChange/defaultValue) and content props (label/description/children)'
    when: 'audited'
    then: 'none is reported as a missing native attribute; they classify as behavior/content and resolve to consumed/not-applicable. A locked test fixture asserts a declared native form-control prop MUST bind to the host input and a behavior prop MUST NOT.'
  - id: A4
    given: 'the measured blast radius'
    when: 'the audit completes'
    then: 'it proposes the smallest source-level fix slice (contract-binding completion vs a generic IR/emitter host-capability rule) with no generated-output changes made'
non_functional: {}
contracts: []
