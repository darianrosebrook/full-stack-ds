id: DOCS-CLAIMS-DERIVED-VALUES-01
title: 'Derive stale doc claims (per-target counts, web-framework list, snapshot date) instead of authoring them'
risk_tier: 3
mode: chore
lifecycle_state: active
created_at: '2026-08-20T23:40:07.474Z'
updated_at: '2026-08-20T23:40:36.905Z'
blast_radius:
  modules:
    - 'scripts/docs-claims-check.mjs'
    - 'AGENTS.md'
    - 'README.md'
    - 'packages/ds-swiftui/README.md'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'scripts/docs-claims-check.mjs'
    - 'AGENTS.md'
    - 'README.md'
    - 'packages/ds-swiftui/README.md'
  out: []
invariants:
  - 'The gate never weakens: every marker class governed before this slice (component-count, rail-admitted-target-count, registered-target-count, icon-count) still derives from the same authorities and still fails on drift.'
  - 'Marker vocabulary stays mentionable: a marker followed by no shape-compatible value (docs tables, code spans) is skipped, and an unknown marker name still exits 2.'
  - 'Machine-local agent docs are gated present-when-here and skipped-when-absent; the tracked-file scan itself is unchanged.'
  - 'No doc value is authored where a derivation exists: per-target allowlist counts, the web-framework family, and the snapshot freshness date all derive from their live authorities.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the extended claims gate; when docs:check-claims runs over tracked docs plus machine-local CLAUDE.md; then every marked value matches its derived authority'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a planted stale value in any governed shape (int, list, or date) in any scanned doc including gitignored CLAUDE.md; when docs:check-claims runs; then it exits 1 naming the claim and the derived value'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given planted drift repaired via docs:check-claims --fix; when the rewrite completes; then values are correct and no marker is duplicated'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the bound worktree; when the full local gate runs (governed:rail, pnpm test, pnpm run test:frameworks); then every gate passes'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "pnpm run docs:check-claims at merge 950824b9 → 'OK — 34 marked claim(s) match derived values' (tracked docs + machine-local CLAUDE.md admitted via LOCAL_AGENT_DOCS)"
    recorded_at: "2026-08-20T23:43:53.217Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "planted drift one per shape at merge 950824b9 (CLAUDE.md web-framework-list truncated, AGENTS.md swiftui count 50, README.md snapshot date 2026-05-26); pnpm run docs:check-claims exited 1 naming each claim + derived value"
    recorded_at: "2026-08-20T23:43:53.562Z"