id: FEAT-VISUAL-RESIDUALS-01
title: 'Visual-fidelity residuals: RN border-inline-start mapping + Card stripe RN parity, Skeleton wipe ::after sweep'
risk_tier: 2
mode: feature
lifecycle_state: active
worktree: visual-residuals
created_at: '2026-06-13T08:57:50.867Z'
updated_at: '2026-06-13T09:07:02.568Z'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/frameworks/react-native (CSS_TO_RN_STYLE logical-border map + root accent-stripe assembler)'
    - 'packages/ds-contracts/components/Skeleton (wipe ::after sweep keyframe + styles)'
    - 'packages/ds-contracts/components/{Card,Blockquote} (border-inline-start consumers, regenerated)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - packages/ds-codegen/src/frameworks/react-native/
    - packages/ds-contracts/components/Skeleton/
    - packages/ds-contracts/components/Card/
    - packages/ds-contracts/components/Blockquote/
    - packages/ds-react/src/components/
    - packages/ds-vue/src/components/
    - packages/ds-svelte/src/components/
    - packages/ds-angular/src/components/
    - packages/ds-lit/src/components/
    - packages/ds-react-native/src/components/
    - packages/ds-figma-plugin/src/generated/components/
    - packages/ds-codegen/.emission-manifest.json
    - tmp/
    - packages/ds-tokens/build/
    - packages/ds-tokens/src/
    - packages/ds-tokens/generated/
  out: []
invariants:
  - 'The CSS_TO_RN_STYLE map additions (border-inline-start/end width+color -> borderLeft/Right) and the root accent-stripe assembler are allowlist-driven: they emit RN style props only where the component declares the corresponding token slot, so non-accent components produce no spurious output.'
  - 'border-inline-start logical borders now cross to React Native StyleSheet (Card status stripe, Blockquote bordered variant) with borderLeftWidth+borderLeftColor; RN package typechecks against ViewStyle.'
  - 'The Skeleton wipe variant renders a translating ::after gradient sweep on web (keyframe skeleton-wipe), authored in the styles sidecar; generate:check round-trips byte-stable.'
acceptance:
  - id: A1
    given: 'the RN emitter map change'
    when: 'Card and Blockquote are regenerated for react-native and the RN package is typechecked'
    then: 'Card RN root has borderLeftWidth+borderLeftColor base and each status variant emits borderLeftColor; Blockquote --bordered emits borderLeftWidth+Color; typecheck:react-native passes'
  - id: A2
    given: 'the Skeleton wipe ::after styles + skeleton-wipe keyframe'
    when: 'Skeleton is regenerated and generate:check runs'
    then: 'Skeleton.css contains a .skeleton--wipe::after rule with a translating gradient animation; generate:check passes 47/47 with zero drift'
non_functional: {}
contracts:
  - name: 'rn-logical-border-map'
    type: behavior
    path: 'packages/ds-codegen/src/frameworks/react-native'
  - name: 'skeleton-wipe-render'
    type: contract-test
    path: 'packages/ds-contracts/components/Skeleton'
