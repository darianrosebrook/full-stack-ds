id: FIX-WALKTHROUGH-VUE-ANCHOR-RESOLUTION-01
title: 'Vue selector-anchor resolution: computed caches a pre-mount null querySelector forever'
risk_tier: 2
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge vue-anchor-fix at bfbef9f3ae9c34b2e7bb3a3749efecd512a33acd'
created_at: '2026-07-23T00:42:14.470Z'
updated_at: '2026-07-23T02:18:46.998Z'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/vue/'
    - 'packages/ds-vue/src/components/Walkthrough/'
    - 'tmp/'
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'the vue emitter resolves the selector anchor via post-flush reactive effect into a shallowRef instead of a computed, so the first resolution happens after the tree is inserted and re-runs on index/steps changes'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'real-Chromium walkthrough proof passes all facts for vue including re-anchor on index change'
non_functional: {}
contracts:
  - name: 'vue-anchor-resolution'
    type: behavior
