id: FEAT-PREVIEW-CALLBACK-CAPTURE-01
title: 'Preview-shell callback capture + first runtime interaction fact (Select trigger click)'
risk_tier: 2
mode: feature
lifecycle_state: active
worktree: preview-callback
created_at: '2026-07-22T20:06:06.513Z'
updated_at: '2026-07-22T20:06:06.513Z'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'src/runtime/'
    - 'e2e/runtime-rail.spec.ts'
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'the preview shell exposes no way to observe component callbacks from Playwright'
    when: 'the fsds config message bus gains a callback-capture surface (invocations posted back as messages or recorded on a window-scoped log, uniform across the five preview mounts)'
    then: 'a Playwright test can assert a callback fired with its payload.'
  - id: A2
    given: 'the capture surface'
    when: 'the runtime rail clicks the Select trigger in react/vue/svelte/lit previews'
    then: 'the open channel change is observed (callback payload or DOM state), landing the repo''s first browser-level interaction fact, with angular recorded as skipped per the existing preview constraint.'
  - id: A3
    given: 'the full rail'
    when: 'run locally against a dedicated port'
    then: 'all existing facts stay green and the new interaction facts pass.'
non_functional: {}
contracts:
  - name: 'preview-callback-capture'
    type: behavior
