id: FEAT-SWIFTUI-SELECTION-SUBSTRATE-01
title: 'Compositional selection substrate: SelectionState over the union channel; Select first'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-08-17T03:58:18.816Z'
updated_at: '2026-08-17T03:58:18.816Z'
blast_radius:
  modules:
    - 'packages/ds-swiftui/Sources/DsSwiftUI/Presence/SelectionState.swift (runtime substrate)'
    - 'packages/ds-codegen/src/frameworks/swift/swiftui/component-source.ts (selection-control class)'
    - 'fsds.targets.json + docs (Select admitted; claims)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src'
    - 'packages/ds-swiftui'
    - 'fsds.targets.json'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'SelectionState composes ControllableValue (single + multi) with the mode-gated apply — no second state machine; membership toggle is the channelUpdate toggleMembership grammar.'
  - 'The selection-control class keys on IR facts only: exactly one union-typed (string|string[]) channel, an options array prop, a passive dom; no component identity.'
  - 'Unrealized props (searchable/filterFn/empty/disabled depth) are omitted-and-documented, never accepted-and-ignored.'
acceptance:
  - id: A1
    given: 'the spike-shaped selection semantics (union string|string[] channel, options array prop, multiple mode gate)'
    when: 'the substrate lands'
    then: 'a SelectionState runtime (single+multi ControllableValue projections behind one apply() that dispatches replace-vs-toggleMembership by the mode gate — the channelUpdate grammar) is XCTested for both modes'
  - id: A2
    given: 'Select (combobox root, selection union channel, options: SelectOption[], open boolean channel)'
    when: 'generated'
    then: 'the selection-control class emits Select through SelectionState + Menu + ForEach(options) — the channelCall binding lowers to a Menu item action; open channel rides the existing ControllableValue substrate; searchable/filterFn omitted-and-documented; compiles'
  - id: A3
    given: 'the composition-survives claim'
    when: 'this slice closes'
    then: 'the selection class gates on IR facts (union channel + options prop + non-surface dom), the state machine lives in the runtime not the emission, and SelectOption lowers from the contract''s definedTypes'
  - id: A4
    given: 'the full worktree gate'
    when: 'typecheck, governed:rail, pnpm test, test:frameworks, swift test, swift build run'
    then: 'all pass; admitted 21→22; remaining corpus fails loudly'
non_functional: {}
contracts:
  - name: 'swiftui-selection-state'
    type: contract-test
    path: 'packages/ds-swiftui/Tests/DsSwiftUITests/SelectionStateTests.swift'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "SelectionStateTests 3/3: single replace + isSelected, multi toggleMembership + change payloads, controlled write-through both modes"
    recorded_at: "2026-08-17T04:06:04.681Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "Select.swift: @StateObject SelectionState with multiple mode gate, selection.apply(option.value) Menu item actions, ControllableValue open channel; SelectOption Identifiable struct synthesized from the contract alias; compiles (swift build exit 0)"
    recorded_at: "2026-08-17T04:06:05.604Z"