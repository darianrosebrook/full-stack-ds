id: FIX-UNWIND-FREEZE-RATCHETS-01
title: 'Remove both token freeze ratchets (consumption + realization) and the golden-coverage expansion'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-08-18T04:25:03.174Z'
updated_at: '2026-08-18T04:25:03.174Z'
blast_radius:
  modules:
    - 'delete: scripts/token-consumption-audit/ and scripts/token-realization-audit/ (audit, selfcheck, seeded ledgers), packages/ds-tokens/build/runners/usage-scan.ts, both docs/ report dirs'
    - 'revert wiring: root package.json scripts, ci.yml steps, .githooks/pre-push blocks, prepush-scope.mjs pattern+flag families and their contract tests'
    - 'restore bytes: golden-fixtures.json + tokenize.test.ts CASES at abf1fed7~1; snapshot rows; AGENTS.md enumeration; mobile-parity-matrix.mjs cell note'
    - 'edit: ADR-0001 section 1.1 — drop the realization-ledger carve-out that exempted the surface the ADR was written to prevent'
    - 'explicitly untouched: resolvability, contrast, styling/behavior/a11y ledgers, .caws append-only lifecycle records'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'scripts/token-consumption-audit'
    - 'scripts/token-realization-audit'
    - 'docs/token-consumption-audit'
    - 'docs/token-realization-audit'
    - 'scripts/prepush-scope.mjs'
    - 'scripts/prepush-scope.test.mjs'
    - 'scripts/mobile-parity-matrix.mjs'
    - '.githooks/pre-push'
    - '.github/workflows/ci.yml'
    - 'packages/ds-tokens/build/runners/usage-scan.ts'
    - 'packages/ds-codegen/src/highlight/golden-fixtures.json'
    - 'packages/ds-codegen/src/highlight/tokenize.test.ts'
    - 'docs/architecture/adr/0001-structural-freeze-doctrine.md'
    - 'docs/current-implementation-snapshot.md'
    - 'AGENTS.md'
    - 'package.json'
  out: []
invariants:
  - 'Removing a gate must not damage a remaining one. scripts/prepush-scope.mjs must still declare every identifier its flags reference — the superseded attempt deleted `const resolvability` alongside the consumption binding, leaving a ReferenceError at line 101 that failed all 34 scope tests and would have broken pre-push for every agent in the repo.'
  - 'Only population pins are removed. Correctness gates (resolvability, contrast, WCAG, styling/behavior/a11y realization ledgers, governed:rail) keep their exact inputs and verdicts.'
  - 'No generated component artifact changes: golden fixtures were test-only inputs; governed:rail must end with a clean tree.'
  - 'ADR-0001 stands as doctrine, but must not exempt the surface it was written to prevent — section 1.2 and section 2 classify the realization known-gaps ledger as a population pin, so section 1.1 may not simultaneously admit it as a structural freeze.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the consumption and realization ledgers are population pins that fail when development succeeds; when the removal lands; then no audit script, seeded ledger, report dir, package script, CI step, pre-push block, or prepush-scope family remains for either surface, and a repo-wide grep finds references only in ADR-0001 and append-only .caws lifecycle records'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the golden-coverage slice froze six languages beyond the original four; when the removal lands; then golden-fixtures.json and tokenize.test.ts are byte-identical to abf1fed7~1 and the five web highlight suites pass at their restored counts'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given ADR-0001 section 1.1 exempted the realization ledgers while sections 1.2 and 2 classify that same ledger as an inadmissible population pin; when the removal lands; then the section 1.1 carve-out is removed so the doctrine no longer exempts the surface it was written to prevent'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given removing gates must not damage the gates that remain; when prepush-scope.test.mjs and the full gate suite run; then classify resolves every referenced flag with no ReferenceError, all 34 scope tests pass, and resolvability, contrast, styling, behavior and a11y ledgers plus governed:rail are green with a clean tree'
non_functional: {}
contracts:
  - name: 'CodeBlock'
    type: behavior
    path: 'packages/ds-contracts/components/CodeBlock/CodeBlock.contract.json'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Post-removal grep for token-realization-audit|audit:token-realization|token-consumption-audit|audit:token-consumption|RUN_TOKEN_REALIZATION|RUN_TOKEN_CONSUMPTION across the repo (excluding .git/node_modules/.caws) returns matches only in docs/architecture/adr/0001; both scripts/ trees, both docs/ report dirs, usage-scan.ts, both package.json scripts, both ci.yml steps and both pre-push blocks are deleted in merge fa0beae8"
    recorded_at: "2026-08-18T04:38:39.493Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "diff -q against git show abf1fed7~1 reports IDENTICAL for both golden-fixtures.json and tokenize.test.ts; canonical suite vitest packages/ds-codegen/src/highlight = 16 passed (was 30 with the six added languages); per-package highlight suites back to 8 tests each: vue 598 passed / svelte 557 passed / lit 514 passed+2 skipped"
    recorded_at: "2026-08-18T04:38:50.052Z"