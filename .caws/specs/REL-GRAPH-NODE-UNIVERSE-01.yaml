id: REL-GRAPH-NODE-UNIVERSE-01
title: 'Carry an independently declared node universe and its edge incidence through two produced representations'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-09-18T04:28:28.282Z'
updated_at: '2026-09-18T04:28:29.718Z'
created_by_session: 'session-69475cd2-20cf-4633-9893-09967382f4d8'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/analytical/graph-projection.ts (GraphBinding, evaluateGraph, the two representations and their decoders)'
    - 'packages/ds-codegen/src/analytical/graph-projection.test.ts (the decisive pair and the identity mutation)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/analytical'
    - 'packages/ds-contracts/analytical-fixtures'
    - 'docs/current-implementation-status.md'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'The node universe is DECLARED, never inferred. A consumer must not derive the node population from edge endpoints, and must not infer the binding from fields that happen to be named id, src and dst. The declaration is validated against the structure before any representation exists.'
  - 'The decisive pair is two structures with an IDENTICAL edge relation and different declared node populations. A representation that reconstructs nodes from edge endpoints collapses them, and that collapse is exhibited rather than asserted.'
  - 'The identity-preserving mutation replaces the isolated node identity while keeping the node count and the edge set. Recovery must detect the wrong IDENTITY; agreement on three nodes and one edge is not sufficient.'
  - 'The authority''s own graph operand declares no node relation, and that absence is recorded as the residual rather than repaired here: this slice declares the binding as an explicit validated premise, it does not extend the zod model, and it does not touch schema emission or the stage-2 ledgers.'
  - 'No layout, marks, interaction, accessibility implementation, general combinators, or redesign of the analytical algebra. A requirement for any of those is a stopping finding.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given two structures whose edge relation is identical and whose declared node populations differ by one isolated node; when the node universe is declared as a binding, validated against the structure, and carried through two produced representations; then both representations recover the exact node identities and the incidence, the two structures are distinguished, and a representation that derives nodes from edge endpoints is shown to collapse them'
  - id: A2
    given: 'Two structures have an identical edge relation and different declared node populations.'
    when: >-
      Both are carried through the two produced representations and through a
      representation that derives its node list from edge endpoints.
    then: >-
      Both representations recover the exact node identities, the incidence and the
      isolated node, so the pair is distinguished; the edges-derived representation
      yields the same node list for both and is exhibited as collapsing them.
  - id: A3
    given: 'A produced representation carries the graph.'
    when: 'The isolated node''s identity moves while the node count and the edge set are held fixed.'
    then: >-
      Recovery detects the wrong identity, so agreement on the number of nodes and
      edges is not sufficient, and the unmutated graph is accepted through the same
      comparison.
non_functional: {}
contracts:
  - name: 'graph-node-universe'
    type: contract-test
    path: 'packages/ds-codegen/src/analytical/graph-projection.test.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "A POSITIVE RESULT, measured through the retained ledger packages/ds-contracts/analytical-fixtures/graph-experiment.json, which the test byte-compares against a fresh computation. THE PAIR: structure A declares nodes [n1,n2,n3] with edges [(n1,n2)] and isolates [n3]; structure B declares nodes [n1,n2] with the SAME edges - edgeRelationIdentical is true, so the discriminator is the declared node population alone. THE QUESTION, ANSWERED: the node universe is DECLARED and validated rather than inferred. `declareGraphBinding` resolves every name against the structure (a missing relation, a missing key field and a missing endpoint field each throw) and refuses to let the node universe be the edge relation, so no consumer infers the binding from fields that happen to be named id, src and dst. The node population is read from the nodes relation INDEPENDENTLY of the edges, which is why `n3` - a node with no edge at all - survives into the result. RECOVERY THROUGH BOTH REPRESENTATIONS: a relational readback (two populations kept apart) and an incidence-oriented output (every node listed with its incident edges, an isolated node present WITH AN EMPTY INCIDENT LIST rather than absent) both recover nodes [n1,n2,n3], edges [(n1,n2)] and isolates [n3]; bothPreserve is true. Each decoder takes exactly one argument and receives only its own output. THE COUNTEREXAMPLE IS EXHIBITED, NOT ASSERTED: a representation deriving its node list from edge endpoints yields [n1,n2] for BOTH structures, so distinguishesThePair is false - the collapse a node-from-edges representation suffers is executed in the module and recorded rather than argued in prose. THE AUTHORITY GAP IS RECORDED IN THE ARTIFACT: authorityGap.declaresANodeRelation is false, since the relation model graph operand names {from: relation, edgeFrom: field, edgeTo: field, value: field} and no node relation; the binding is an explicit validated PREMISE and the zod model, schema emission and the stage-2 ledgers are untouched. Verification: pnpm exec vitest run packages/ds-codegen/src/analytical (22 files / 944 tests; graph-projection.test.ts 18), pnpm test (303 files / 4165 tests), typecheck, lint, docs:check-claims, governed:rail and test:frameworks green in the worktree. NON-CLAIMS: one fixture, its isolate-removal neighbour, two representations and one mutation; no layout, marks, interaction, accessibility, general combinators, algebra redesign, arbitrary graph realization, or the complete projection thesis. Stage-2 ratification is untouched and the stock example remains a regression witness."
    recorded_at: "2026-09-18T04:35:33.932Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "Identical edge relation across the pair, asserted as edgeRelationIdentical true, with different declared node populations (3 against 2). Both produced representations recover the exact node identities and the incidence for BOTH structures and so distinguish them. The node-from-edges representation is executed on both sides and returns [n1,n2] each time, so distinguishesThePair is false: it cannot see the difference at all. This is the pair the acceptance names, with the boundary that would lose it demonstrated rather than described."
    recorded_at: "2026-09-18T04:35:34.733Z"