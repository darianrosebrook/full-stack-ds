id: DOC-VERIFIED-FRESHNESS-GATE-01
title: 'Gate verified_at_commit freshness against governs globs; refresh normal-form'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-09-04T22:16:14.112Z'
updated_at: '2026-09-04T22:16:33.783Z'
created_by_session: 'sess_e3b94b1d-84ef-440f-9ea0-86af5d7e1382'
blast_radius:
  modules:
    - 'scripts/docs-claims-check.mjs'
    - 'docs/normal-form.md'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'scripts/docs-claims-check.mjs'
    - 'scripts/docs-claims-freshness.selftest.mjs'
    - 'doc-freshness-baseline.json'
    - 'docs/normal-form.md'
    - 'package.json'
    - 'scripts/prepush-scope.mjs'
  out: []
invariants:
  - '--fix never rewrites a verified_at_commit stamp; verification cannot be auto-minted'
  - 'the freshness ledger only shrinks: unledgered stale docs fail, refreshed or deleted docs must drop their entries'
  - 'existing marker-value checking behavior is unchanged'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a tracked doc whose governs globs changed after its verified_at_commit; when docs:check-claims runs; then it exits 1 naming the doc and the touching commit'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a stale doc ledgered in doc-freshness-baseline.json at its exact current verified SHA; when docs:check-claims runs; then it passes, while a ledger entry whose doc has become fresh or gone missing fails'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given freshness violations and --fix; when the fix mode runs; then verified_at_commit is never rewritten and the violations are still reported'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the refreshed normal-form.md; when its target narrative is checked; then all 9 registered targets appear with rail/explicit status stated correctly, and its verified_at_commit is fresh under the new gate'
non_functional: {}
contracts:
  - name: 'docs-claims-freshness'
    type: behavior
    path: 'scripts/docs-claims-check.mjs'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "node scripts/docs-claims-freshness.selftest.mjs — stale-fail (exit 1, names docs/a.md + lastTouch commit) and unresolvable-stamp scenarios; real-repo form: any unledgered stale stamp fails docs:check-claims"
    recorded_at: "2026-09-04T22:29:10.000Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "node scripts/docs-claims-freshness.selftest.mjs — ledgered-pass (exit 0), ledger-mismatch / obsolete-entry / dangling-entry (exit 1); real repo: doc-freshness-baseline.json exempts exactly 6 known-stale stamps, canonical run reports '9 verified stamp(s) fresh or ledgered (6 ledgered stale)'"
    recorded_at: "2026-09-04T22:29:16.473Z"
  - criterion_id: A3
    status: pass
    evidence_ref: "node scripts/docs-claims-freshness.selftest.mjs — fix-never-restamps: --fix run over a stale stamp exits 1 with 'not auto-fixable' and the stamp is byte-identical afterward"
    recorded_at: "2026-09-04T22:29:27.159Z"