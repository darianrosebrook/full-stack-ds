id: REL-TOPOLOGY-AUTHORED-STIMULUS-01
title: 'Witness a keystone topology facet with an authored stimulus whose judgment follows from an existing cause'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-09-15T06:17:34.041Z'
updated_at: '2026-09-15T06:17:35.003Z'
created_by_session: 'session-272d1906-0cae-4fae-ba00-e86a4ac9d030'
blast_radius:
  modules:
    - 'packages/ds-contracts/analytical-fixtures/witnesses.json (the nest.levels order witness)'
    - 'packages/ds-contracts/analytical-fixtures/subtraction-stage2.json (its witnessed verdict)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-contracts/analytical-fixtures'
    - 'packages/ds-codegen/src/analytical'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'The adjudication policy admits a new {base, patch} stimulus exactly where its judgment follows mechanically from an already-existing Stage-2 cause; a patch that varies ONLY the coordinate under test and flips that cause is a witness, while a patch whose difference is about a companion coordinate is not.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the sixteen keystone reference-topology facets were recorded as evidence-blocked on the committed specimens; when a {base, patch} stimulus varying exactly nest.levels order is authored against the existing grain cause; then checkWitness accepts a single-coordinate witness, the verdict is recorded witnessed, the unadjudicated count falls by one, and the snapshot records that a keystone facet is reachable by an authored stimulus so the sixteen are a per-facet obligation rather than a corpus-coverage one'
non_functional: {}
contracts:
  - name: 'authored-stimulus'
    type: contract-test
    path: 'packages/ds-contracts/analytical-fixtures/witnesses.json'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Authored a {base, patch} stimulus for relation.derivedBy.nest.levels#order: a = FX_N_NESTED_SUBTOTAL_AT_PREFIX (admissible), b = the same fixture with structure.relations.hierarchy.derivedBy.levels reordered to [state, country], outcome illegal with REL_GRAIN_SUBTOTAL_MISMATCH. Only the order moves and the declared toGrain: [country] subtotal stops being a declared prefix, so the judgment follows mechanically from CASE_NESTED_SUBTOTALS_OFF_GRAIN -- an existing cause -- and nothing was authored to reach a verdict. checkWitness ACCEPTS the single-coordinate witness; a variant predicting admissible is refused SAME_OUTCOME, which is the control that the acceptance is about the cause and not the shape. The verdict is witnessed, the unadjudicated count falls 79 -> 78, and because the coordinate is now PRIMITIVE, eight closures carry obligation 8's composite diagnosis instead of an outstanding one (the two additivity plus aggregate-to-grain~nest, join~nest, nest~bin, nest~graph, nest~normalize, nest~project). Non-claim: a witnessed verdict establishes that the declared SEQUENCE of nest levels is necessary, not that the field is a primitive analytical axis; and the remaining fifteen keystone facets are untouched -- each needs the same per-facet treatment or a corpus case."
    recorded_at: "2026-09-15T06:22:18.624Z"
    test_nodeid: "packages/ds-codegen/src/analytical/closure.test.ts > what actually blocks the closures, decomposed by coordinate kind > names the reference-topology facets as the keystone, and separates them from the candidates that block nothing"
    command: pnpm exec vitest run packages/ds-codegen/src/analytical
    exit_code: 0
    artifact_path: packages/ds-contracts/analytical-fixtures/witnesses.json
    commit_sha: "2cc24a7e"