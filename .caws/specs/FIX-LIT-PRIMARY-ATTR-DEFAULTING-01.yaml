id: FIX-LIT-PRIMARY-ATTR-DEFAULTING-01
title: 'Default-aware lit primary attribute-binding path (close the lit/angular asymmetry)'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-07-22T18:16:50.471Z'
updated_at: '2026-07-22T18:16:50.471Z'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/lit/'
    - 'packages/ds-lit/src/components/'
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'lit''s primary case-prop attribute-binding path reads bare props while angular''s single accessor already defaults that shape'
    when: 'the path routes through the default-aware accessor'
    then: 'a prop with a contract default emits the defaulted read in plain attribute bindings, pinned by codegen tests with falsification.'
  - id: A2
    given: 'call sites that already apply defaults or ifDefined-omission semantics'
    when: 'output regenerates'
    then: 'no double-defaulting and no change to props without contract defaults — every changed generated hunk is exactly the defaulted read, listed and justified.'
  - id: A3
    given: 'the full corpus regeneration'
    when: 'git status is inspected'
    then: 'only lit tree files change, all suites pass, and the governed rail passes.'
non_functional: {}
contracts:
  - name: 'lit-primary-attr-default-parity'
    type: behavior
