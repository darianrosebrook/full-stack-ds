id: RAIL-CODEBLOCK-TOKENIZER-GOLDEN-COVERAGE-01
title: 'Freeze golden token streams for all ten declared CodeBlock languages'
risk_tier: 1
mode: feature
lifecycle_state: active
created_at: '2026-08-18T00:51:17.449Z'
updated_at: '2026-08-18T00:51:17.449Z'
blast_radius:
  modules:
    - 'ds-codegen: highlight golden-fixtures.json (six new frozen cases) and tokenize.test.ts CASES (same strings run through structural property tests)'
    - 'ds-react/ds-vue/ds-svelte/ds-angular/ds-lit: highlight identity suites pick the new cases up automatically via the shared golden file — no package edits expected'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/highlight/golden-fixtures.json'
    - 'packages/ds-codegen/src/highlight/tokenize.test.ts'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'Golden expected arrays are machine-generated from the canonical tokenizer, audited, then frozen — never hand-authored.'
  - 'Fixtures freeze current behavior including imperfections; any future grammar upgrade must regenerate the goldens in the same commit that changes token streams.'
  - 'The exact frozen strings also run through the canonical structural property tests: lossless round-trip, determinism, adjacent-same-kind merge, non-empty tokens, closed enum.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the CodeBlock contract declares ten languages while the cross-framework golden ratchet freezes only four; when six new cases (css, html, javascript, jsx, markdown, tsx) are added with machine-generated expected streams; then golden-fixtures.json covers exactly the ten declared languages and every expected token kind stays inside the closed CodeBlockTokenType enum'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the extended fixtures; when the canonical tokenizer suite and all five web package highlight suites run; then every golden identity test passes in all five frameworks and the exact frozen strings also pass the canonical structural properties (lossless round-trip, determinism, adjacent-kind merge, non-empty tokens)'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given golden fixtures are test artifacts rather than codegen input; when generate:check and governed:rail run; then both pass with a clean tree proving zero generated component bytes changed'
observability:
  - 'The vitest suites are the observable: the canonical tokenizer suite and all five web-package highlight suites each gain six golden-identity cases against the exact frozen strings; CI suite counts grow accordingly.'
rollback:
  - 'git revert the slice commits to restore the four-case golden file and prior suite counts; no runtime surface exists to roll back.'
non_functional:
  security:
    - 'No runtime or codegen-input change; fixture strings deliberately include HTML-shaped hostile constructs to keep pinning the text-only doctrine.'
contracts:
  - name: 'CodeBlock'
    type: behavior
    path: 'packages/ds-contracts/components/CodeBlock/CodeBlock.contract.json'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "golden-fixtures.json case languages == [typescript, bash, json, plaintext, css, html, javascript, jsx, markdown, tsx] (exactly the ten contract CodeBlockLanguage values); expected arrays machine-generated from canonical tokenize.ts with in-script lossless+in-enum+non-empty assertions, then audited"
    recorded_at: "2026-08-18T00:57:42.058Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "scoped suites: canonical+react 30/30, vue 14/14, svelte 14/14, lit 14/14, angular (jest) 14/14; full suites: root 2160/2160 (was 2154), vue 597, svelte 515+2skip, lit 556, angular+rn green — every suite grew by the six new golden cases via the shared fixture file with zero package edits"
    recorded_at: "2026-08-18T00:57:43.110Z"