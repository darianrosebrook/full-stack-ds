id: REL-OPERAND-DISTINCTNESS-01
title: 'Declare operand distinctness: with differs from from, edgeTo from edgeFrom, so the canonical rebind skips the sibling''s binding'
risk_tier: 2
mode: feature
lifecycle_state: active
worktree: operand-distinctness
created_at: '2026-09-15T16:49:34.907Z'
updated_at: '2026-09-15T16:49:36.371Z'
created_by_session: 'session-272d1906-0cae-4fae-ba00-e86a4ac9d030'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/analytical/relation-model.ts + census.ts + erasure-plan.ts (the declared constraint, its threading, the pool skip)'
    - 'the emitted schemas and the re-recorded ledgers (both the coordinate and erasure authorities move)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/analytical'
    - 'packages/ds-contracts/analytical-fixtures'
    - 'packages/ds-contracts/relation.contract.schema.json'
    - 'packages/ds-contracts/quotient-image.schema.json'
    - 'docs/architecture/analytical-relation-doctrine.md'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - "Distinctness is a LAW fact declared on the operand map and read where every other namespace fact is read: the executor never hardcodes an operator's constraint, and the pick -- first declared name that differs from the sibling's CURRENT value -- is a function of the declarations alone, never of the slot's own value, so two stimuli differing only in the slot still reach one image."
  - 'The constraint is declared only where the law states it (join.with differs from from, graph.edgeTo from edgeFrom); nothing else changes its pool.'
  - 'Both authorities moving means the freeze is superseded with an authored effect for this record's cause, and the pins that named the old refusal are corrected rather than deleted.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given join.with and graph.edgeTo are refused because their canonical rebind lands on the sibling slot''s own binding (a self-join, a degenerate edge) while the law requires the two to differ; when the operand map declares the distinctness constraint (a namespace plus the sibling it must differ from), the census carries it onto the locator, and the executor''s pool skips the sibling''s value; then the rebind is lawful on every fixture (the boundary introduces nothing), the two coordinates leave the never-discharged list for the measured reason -- corpus-dead, because the canonical distinct pick is the binding the corpus already writes -- and the analytical suite, every gate and every ledger agree'
non_functional: {}
contracts:
  - name: 'binding-erasure'
    type: contract-test
    path: 'packages/ds-codegen/src/analytical/erasure-plan.test.ts'
