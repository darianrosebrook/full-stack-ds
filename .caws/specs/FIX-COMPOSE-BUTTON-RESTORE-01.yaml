id: FIX-COMPOSE-BUTTON-RESTORE-01
title: 'Restore the Compose Button admission reverted by a stray staged index in 465c77b5'
risk_tier: 2
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Restored the Compose Button admission swept away by a stray staged index in 465c77b5; proven reproducible from codegen'
created_at: '2026-08-18T04:44:38.863Z'
updated_at: '2026-08-18T04:48:49.820Z'
blast_radius:
  modules:
    - 'restore: packages/ds-jetpack-compose Button.kt/ButtonTokens.kt/FsdsButton.kt/Components.generated.kt (764 lines)'
    - 'restore: ds-codegen jetpack-compose component-source.ts projected-children action path (207 lines) + one swiftui component-source.ts line'
    - 'restore: fsds.targets.json compose allowlist Button entry; examples/settings/compose SettingsApp.kt five action call sites'
    - 'restore: docs/current-implementation-snapshot.md compose admission prose and backlog count 48 -> 47'
    - 'explicitly untouched: .caws lifecycle records (465c77b5 stays in history; this is a forward restore, not a rewrite)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-jetpack-compose'
    - 'packages/ds-codegen/src/frameworks/jetpack-compose'
    - 'packages/ds-codegen/src/frameworks/swift'
    - 'examples/settings/compose'
    - 'fsds.targets.json'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'Forward restore only. 465c77b5 is not rewritten, amended, or reverted as a commit — its legitimate spec-YAML half stays. Only the swept-in reversion is undone.'
  - 'Restored generated Kotlin must be REPRODUCIBLE, not merely copied: after restoring the emitter, `generate --target=jetpack-compose` must re-emit the same bytes. A restore that only matches 465c77b5~1 but not codegen output would re-drift on the next regen.'
  - 'No other path is touched. Nothing has landed on these 8 paths since the reversion, so the restore is a clean checkout with no reconciliation.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given 465c77b5 swept a staged reversion of FEAT-COMPOSE-BUTTON-ADMISSION-01 into a caws spec-create commit; when the restore lands; then Button.kt, ButtonTokens.kt, FsdsButton.kt, Components.generated.kt, the jetpack-compose and swiftui component-source.ts emitter paths, SettingsApp.kt and the fsds.targets.json compose allowlist are byte-identical to 465c77b5~1'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given generated Kotlin must be reproducible from the contract rather than restored by hand; when generate --target=jetpack-compose runs after the emitter restore; then it re-emits the restored Button bytes with no diff, proving the restore matches what codegen produces rather than a stale snapshot'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the snapshot records compose admission counts; when the restore lands; then the compose admission backlog reads 47 with the Button admission prose restored, and docs:check-claims passes'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given SwiftUI and Compose sit outside the rail drift diff so no local gate covers them; when gates run; then generate:check, governed:rail, root and framework suites, lint and docs:check-claims are green, and the Kotlin compile lane is explicitly recorded as CI-only proof not verified locally'
non_functional: {}
contracts:
  - name: 'Button'
    type: behavior
    path: 'packages/ds-contracts/components/Button/Button.contract.json'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "git diff --cached --stat 465c77b5~1 -- <the 8 swept paths> returned empty: restored tree byte-identical to the pre-reversion state across Button.kt/ButtonTokens.kt/FsdsButton.kt/Components.generated.kt, both component-source.ts emitters, SettingsApp.kt and fsds.targets.json"
    recorded_at: "2026-08-18T04:48:27.470Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "pnpm run generate -- --target=jetpack-compose after the emitter restore reported 'ADMITTED 3/3 component(s)' with GENERATED Button/Switch/ToggleSwitch, and git diff was empty afterwards — codegen re-emits the restored bytes exactly, so the restore is reproducible output not a stale copy"
    recorded_at: "2026-08-18T04:48:28.856Z"
  - criterion_id: A3
    status: pass
    evidence_ref: "snapshot compose row now reads 'jetpack-compose 3/51 admitted ... 83/83 slots realized ... the remaining 47 components being the breadth backlog' with the FEAT-COMPOSE-BUTTON-ADMISSION-01 prose restored and the reversion recorded; docs:check-claims OK - 12 marked claims match derived values"
    recorded_at: "2026-08-18T04:48:39.489Z"
  - criterion_id: A4
    status: pass
    evidence_ref: "generate:check 51/51; governed:rail Overall: PASS across all six rail targets, 306 groups / 1476 files, clean tree; root vitest 176 files / 2181 tests pass; lint 0 errors (24 pre-existing warnings). NOT verified locally: the Kotlin compile lane — kotlinc is absent on this machine, so :library:compileKotlin was not run; that proof is CI-only via the native-compile-rail job"
    recorded_at: "2026-08-18T04:48:40.885Z"