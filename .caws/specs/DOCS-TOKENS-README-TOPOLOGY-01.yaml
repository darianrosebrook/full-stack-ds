id: DOCS-TOKENS-README-TOPOLOGY-01
title: 'Truth ds-tokens README to per-framework token CSS emission'
risk_tier: 3
mode: doc
lifecycle_state: draft
created_at: '2026-09-14T03:46:13.972Z'
updated_at: '2026-09-14T03:46:13.972Z'
created_by_session: 'session-bf2f88f8-c1ec-4df7-8e1f-f78ddf354df5'
blast_radius:
  modules:
    - 'ds-tokens'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-tokens/README.md'
    - 'packages/ds-tokens/.gitignore'
    - 'packages/ds-tokens/components/.gitkeep'
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'the ds-tokens README claims component tokens CSS is emitted into packages/ds-tokens/components/ by a codegen tokens emitter target'
    when: 'the README is rewritten'
    then: 'it describes per-framework sibling .tokens.css emission inside each web framework package and contract-tokens byte-identity validation, with no reference to a ds-tokens components/ destination'
  - id: A2
    given: 'the README lists generated/ artifacts and the cascade layer order'
    when: 'rewritten'
    then: 'the artifact list is tokens.css and resolved.tokens.json with composed.tokens.json as gitignored scratch and no token-types.ts claim, and the layer order matches generated/tokens.css'
  - id: A3
    given: 'packages/ds-tokens/components/ holds only a .gitkeep'
    when: 'the placeholder is removed'
    then: 'the directory is absent from the tracked tree and the .gitignore header names only artifacts that are actually tracked'
non_functional: {}
contracts: []
