id: FIX-USAGE-COMPOSITION-IR-01
title: 'Derive usage composition from contract IR and restore complete examples'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-09-05T02:34:02.409Z'
updated_at: '2026-09-05T02:46:24.730Z'
created_by_session: '01a06ed3-9fec-7332-b38d-37990ff8be07'
blast_radius:
  modules:
    - 'contract usage grammar and semantic IR'
    - 'showcase usage renderer and runtime evidence'
    - 'generated component composition'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-contracts/component.usage.schema.json'
    - 'packages/ds-contracts/components'
    - 'packages/ds-codegen/src'
    - 'src/lib'
    - 'src/types'
    - 'src/views/sections'
    - 'e2e'
    - 'packages/ds-react/src'
    - 'packages/ds-vue/src'
    - 'packages/ds-svelte/src'
    - 'packages/ds-angular/src'
    - 'packages/ds-lit/src'
    - 'packages/ds-react-native/src'
    - vite-plugin-fsds-data.ts
    - packages/ds-figma-plugin/src/generated
    - packages/ds-swiftui/Sources/DsSwiftUI/Components
  out: []
invariants:
  - 'Usage delivery is derived from contract and IR facts; renderer and validator do not maintain component-name switches.'
  - 'Native HTML ownership and content models are preserved; Stack carries layout but never substitutes CSS display for semantic table structure.'
  - 'Invalid or undeliverable authored content fails closed and never silently flattens into an arbitrary host.'
acceptance:
  - id: A1
    given: 'every curated usage region'
    when: 'usage validation and showcase rendering run'
    then: 'validator and renderer agree on one contract-derived delivery path with zero visible fallbacks'
  - id: A2
    given: 'compound Card, Popover, Tooltip, Accordion, Tabs, and Table examples'
    when: 'they render'
    then: 'nested subcomponents preserve their declared hierarchy without invalid HTML nesting'
  - id: A3
    given: 'complex object and collection props required by curated examples'
    when: 'JSON sidecars are validated and materialized'
    then: 'values are typed against contract prop IR and reach the generated component without hardcoded fixtures'
  - id: A4
    given: 'visible glyph and indicator anatomy'
    when: 'examples render'
    then: 'content comes from componentRef or another declared composition fact rather than empty placeholder spans'
  - id: A5
    given: 'the Stack layout algebra'
    when: 'composition needs are adjudicated'
    then: 'existing flex, inline, block, contents, and native modes are used where applicable and table semantics remain native'
non_functional: {}
contracts:
  - name: 'usage-composition'
    type: behavior
    path: 'packages/ds-codegen/src/validation/usage.ts'
  - name: 'usage-tree'
    type: schema
    path: 'packages/ds-contracts/component.usage.schema.json'
