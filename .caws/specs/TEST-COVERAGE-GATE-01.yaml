id: TEST-COVERAGE-GATE-01
title: 'Measure per-package test coverage and wire a coverage threshold gate'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-09-01T00:00:48.950Z'
updated_at: '2026-09-01T00:12:48.153Z'
blast_radius:
  modules:
    - 'root workspace package.json (test:coverage scripts, devDependency)'
    - 'vitest configs (root, examples, vue, svelte, lit, react-native)'
    - 'packages/ds-angular jest coverage config'
    - '.github/workflows/ci.yml (coverage gate job)'
    - 'docs/current-implementation-snapshot.md (coverage claim row)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'package.json'
    - 'pnpm-lock.yaml'
    - 'vitest.config.ts'
    - 'vitest.examples.config.ts'
    - 'packages/ds-vue/vitest.config.ts'
    - 'packages/ds-svelte/vitest.config.ts'
    - 'packages/ds-lit/vitest.config.ts'
    - 'packages/ds-react-native/vitest.config.ts'
    - 'packages/ds-angular/jest.config.cjs'
    - '.github/workflows/ci.yml'
    - 'docs/current-implementation-snapshot.md'
    - coverage-floors.json
    - packages/ds-react/vitest.config.ts
    - packages/ds-codegen/vitest.config.ts
    - packages/ds-figma-plugin/vitest.config.ts
    - packages/ds-tokens/vitest.config.ts
    - vitest.showcase.config.ts
    - scripts/docs-claims-check.mjs
    - AGENTS.md
    - CLAUDE.md
  out: []
invariants:
  - 'Coverage instrumentation changes nothing about which tests run or pass: the same suites that pass without --coverage pass with it.'
  - 'Thresholds ratchet from measured floors, never above the measured value at wiring time; the 80% target is recorded as a declared gap in the snapshot row, not enforced as a hard floor on day one.'
  - 'Every coverage number written into prose is reproducible by the coverage command at the cited commit; the snapshot row names its measurement command.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a checkout with deps installed; when the coverage command runs; then every test runner reports coverage for its own sources without provider errors'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given measured per-package coverage; when thresholds are configured; then CI fails if coverage drops below the configured floors'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the measured numbers; when the snapshot claim ledger is updated; then the coverage row states measured aggregates and names the gap to the 80 percent target'
non_functional: {}
contracts:
  - name: 'coverage-thresholds'
    type: schema
    path: 'vitest.config.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "pnpm run test:coverage (full ten-suite chain, exit 0): react 63 files/692 tests 79.91% lines, codegen 75/1203 58.35%, figma 5/59 70.68%, tokens 3/30 5.5%, showcase 30/201 32.27%, vue 58/598 71.31%, svelte 55/557 74.43%, lit 55/516 85.91%, angular 57/479 89.99%, react-native 53/74 35.83% — every runner printed a v8 coverage table with no provider errors; codegen re-verified at main e403b0d7 exit 0"
    recorded_at: "2026-09-01T00:32:22.603Z"