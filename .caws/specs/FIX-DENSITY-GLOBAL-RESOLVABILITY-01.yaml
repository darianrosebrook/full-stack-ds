id: FIX-DENSITY-GLOBAL-RESOLVABILITY-01
title: 'Repair density token namespace and make global CSS resolvability part of the rail'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-09-04T22:33:11.858Z'
updated_at: '2026-09-04T22:33:39.303Z'
created_by_session: '01a06d90-6485-7c73-b6c8-0b0ec424c0f8'
blast_radius:
  modules:
    - 'ds-tokens'
    - 'token-resolvability-audit'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-tokens/build'
    - 'packages/ds-tokens/generated/tokens.css'
    - 'scripts/token-resolvability-audit'
    - 'docs/token-resolvability-audit'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'Global token CSS is audited as both a declaration graph and a reference source; declarations never self-exempt an undeclared read'
  - 'Unprefixed spacing.density paths lower into the core namespace exactly like their declared graph leaves'
  - 'The rail remains two-directional and component findings retain per-component identity'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given density override files reference spacing.density values; when tokens are built; then every emitted density-layer var() read names the declared --fsds-core-spacing-density-* variable'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given generated global tokens.css contains an undeclared --fsds-* read; when audit:token-resolvability runs; then it exits nonzero and identifies the global source and missing name'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the repaired generator and widened rail; when token build/check, token resolvability audit, and focused tests run; then they pass with zero unresolvable component or global references'
non_functional: {}
contracts:
  - name: 'global-token-resolvability'
    type: behavior
    path: 'scripts/token-resolvability-audit/audit.mjs'
