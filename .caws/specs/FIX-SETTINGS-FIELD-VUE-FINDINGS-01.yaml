id: FIX-SETTINGS-FIELD-VUE-FINDINGS-01
title: 'Resolve settings/Field findings: Vue keystroke input + label assoc, Field value typing, broken example'
risk_tier: 2
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge settings-field-findings at 5cff87482bd3906fabeb655e2a3e680fc7101320'
created_at: '2026-07-22T21:20:23.286Z'
updated_at: '2026-07-23T05:09:19.989Z'
blast_radius:
  modules:
    - 'packages/ds-vue/src/components/Input (keystroke input event) + Field (label association)'
    - 'packages/ds-codegen/src/frameworks/vue (input-event + label-for lowering)'
    - 'packages/ds-contracts/components/Field (value channel valueType unknown -> concrete)'
    - 'packages/ds-{react,vue,svelte,angular,lit}/src/components/Field (regenerated typed value)'
    - 'examples/settings (broken React example fixed to the real slot API)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-contracts/components/Field'
    - 'packages/ds-vue/src/components'
    - 'packages/ds-react/src/components/Field'
    - 'packages/ds-svelte/src/components/Field'
    - 'packages/ds-angular/src/components/Field'
    - 'packages/ds-lit/src/components/Field'
    - 'examples/settings'
    - packages/ds-codegen/src/frameworks/vue
    - packages/ds-codegen/src/frameworks/vue-change-event-timing.test.ts
    - packages/ds-react-native/src/components/Field
    - scripts/behavior-realization-audit/audit.mjs
    - scripts/behavior-realization-audit/realization.test.mjs
  out: []
invariants:
  - 'The Vue generated Input emits its value channel on keystroke (native input event), not on blur (change event), so keystroke-time validation matches the spec — parity with the other frameworks that already do so.'
  - 'The Vue generated Field associates its label with its control (label for / control id) so no form-field-without-label a11y violation is reported; this realizes the Field contract label->control relationship, not an example workaround.'
  - 'Field''s value channel is concretely typed (valueType is a concrete type, not unknown), following the pattern the sibling value-carrying components (Input) already use; no free-form generic is introduced.'
  - 'Fixes are realized in the contract/emitter/generated packages, never by hand-editing generated component files or adding consumer escape hatches; generated bytes stay determinism- and rail-clean.'
  - 'The settings React example consumes Field through its real public API (slot projection), typechecks, and builds with no removed/unknown props.'
acceptance:
  - id: A1
    given: 'the generated Vue Input'
    when: 'a user types a character into the input'
    then: 'the value channel onChange fires on that keystroke (input event), driving keystroke-time validation; a Vue Input test pins input-event (not change-event) wiring'
  - id: A2
    given: 'the generated Vue Field with a label and a control'
    when: 'the field renders'
    then: 'the label is associated with the control (matching for/id) and no "form field has no label" a11y issue is present; a Vue Field test pins the association'
  - id: A3
    given: 'the Field contract value channel'
    when: 'Field is generated across the five frameworks'
    then: 'value / defaultValue / onChange are concretely typed (not unknown); all five Field packages typecheck; generation is deterministic and governed:rail + committed-bytes diff pass'
  - id: A4
    given: 'the settings React example'
    when: 'it is typechecked and built'
    then: 'it consumes Field via the public slot API with no removed label prop or children escape hatch; tsc --noEmit passes (modulo the pre-existing styles.css ambient-declaration gap) and the Vite build succeeds'
non_functional: {}
contracts:
  - name: 'field-value-channel'
    type: schema
    path: 'packages/ds-contracts/components/Field/Field.contract.json'
