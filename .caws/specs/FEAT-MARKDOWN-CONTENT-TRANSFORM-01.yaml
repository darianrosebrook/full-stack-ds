id: FEAT-MARKDOWN-CONTENT-TRANSFORM-01
title: 'Markdown content transform: block-level structural output grammar (the second-transform proof)'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-08-17T19:49:11.384Z'
updated_at: '2026-08-17T20:43:23.493Z'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-contracts/component.contract.schema.json'
    - 'packages/ds-contracts/components/Markdown'
    - 'packages/ds-codegen/src'
    - 'packages/ds-react/src'
    - 'packages/ds-vue/src'
    - 'packages/ds-svelte/src'
    - 'packages/ds-angular/src'
    - 'packages/ds-lit/src'
    - 'packages/ds-react-native/src'
    - 'packages/ds-figma-plugin/src/generated'
    - 'packages/ds-swiftui'
    - 'e2e/runtime-rail.spec.ts'
    - 'docs/current-implementation-snapshot.md'
    - 'src'
    - docs/
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the schema admits a markdown transform with closed blockPart/markPart vocabularies; when the IR lowers and validates it; then every named part must exist in anatomy.details with a compatible tag (marks: span + multiple), source/gate must be bare prop bindings, and no emitter branches on a component name'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a canonical pure synchronous deterministic markdown parser over a closed v1 subset (headings, paragraphs, ordered and unordered single-level lists, fenced code blocks, blockquotes; inline emphasis, strong, code, links); when parsing hostile input; then raw HTML is never interpreted (literal text) and link hrefs are protocol-sanitized, with frozen golden fixtures and canonical unit tests'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the markdown IR fact; when emitters lower it; then all five web frameworks realize the tree (block elements from declared part tags with part classes + data-block-kind; marks as spans with data-mark-kind) while RN and SwiftUI degrade to plain text and figma omits; the parser is emitted byte-identical per web package'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a new Markdown component contract realizing the transform; when per-package identity and injection tests plus the Chromium rail facts run; then tree shape and literal-HTML rendering are proven in every framework and generate:check + governed:rail stay green with committed regeneration'
  - id: A5
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the landed slice; when docs update; then the snapshot records the grammar-generalization claim with honest v1 non-claims (no tables, images, nested lists, raw HTML; no highlight-chaining inside code blocks)'
non_functional: {}
contracts:
  - name: 'content-transform-grammar'
    type: schema
    path: 'packages/ds-contracts/component.contract.schema.json'
  - name: 'markdown-parser'
    type: behavior
    path: 'packages/ds-codegen/src/markdown/markdown.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "merge c13648f8c11508729e7c47434a5aa219692d407f: schema content.oneOf markdown variant with closed blockParts/markParts keys; IR union (MarkdownTransformIR + blockTags/markTags enrichment); validateDomNode enforces the kind DOM-contract tag table + mark multiple:true + bare-prop source/gate; single-transform-per-tree guard at IR build; grep '\"Markdown\"' over frameworks/*/component-source.ts emitters -> 0"
    recorded_at: "2026-08-17T20:43:51.156Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "19 canonical unit tests + frozen fixtures.json (full doc / hostile / edge): purity/determinism, closed v1 grammar, literal-HTML doctrine via collectMarkdownText, sanitizeHref matrix (javascript:/data:/vbscript:/unknown -> null; http/https/mailto/anchor/relative pass), unterminated-fence degradation, CRLF normalization"
    recorded_at: "2026-08-17T20:43:52.020Z"
  - criterion_id: A3
    status: pass
    evidence_ref: "five idiomatic lowerings verified per package (React JSX helpers; Lit html`` helpers with TemplateResult returns; Vue defineComponent+Fragment over h() array children; Svelte recursive snippets; Angular companion fsds-markdown-block/mark renderers, ngc-clean incl. typed union getters); RN/SwiftUI degrade via shared contentBindingOrTransformSource; figma omits; CLI emits markdown.ts byte-identical per web package (cmp-verified pattern; rail artifact-join PASS)"
    recorded_at: "2026-08-17T20:43:52.880Z"
  - criterion_id: A4
    status: pass
    evidence_ref: "Markdown contract realizes the transform (corpus 50, counts re-derived via docs:check-claims:fix); per-package identity+injection suites 6/6 in all five frameworks; e2e:rail 94 passed incl. 10 Markdown facts (golden tree shape + literal-HTML + href degradation, all five frameworks via the config bus); generate:check 50/50 + governed:rail Overall: PASS + committed regeneration (clean post-rail tree)"
    recorded_at: "2026-08-17T20:44:09.954Z"
  - criterion_id: A5
    status: pass
    evidence_ref: "snapshot 'Content transforms (markdown — the second-transform proof)' row records claim strength + honest v1 non-claims (no tables/images/nested lists/raw HTML/highlight-chaining); successor ledger updated post-merge"
    recorded_at: "2026-08-17T20:44:10.829Z"