id: FEAT-COMPOSE-PASSIVE-LEAF-FAMILIES-01
title: 'Compose passive-leaf families: prop-text + expandable content + progress/status (allowlist 16 -> 23)'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Compose admits the two passive-leaf families (16 -> 23): prop-text BasicText leaves with the font-size role, expandable disclosure content with IR-derived toggle labels, and painted progress/status indicators with progressBarRangeInfo; the parity gate''s per-path claimed sets caught and fixed two real defects'
created_at: '2026-08-31T00:27:46.154Z'
updated_at: '2026-08-31T00:46:42.791Z'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/frameworks/jetpack-compose/component-source.ts (three new emitter paths)'
    - 'packages/ds-jetpack-compose/library/src/main/kotlin/com/fullstackds/components (new generated trees + FsdsProgress substrate)'
    - 'scripts/compose-parity-diff.mjs (usage ratchet coverage of new admits)'
    - 'fsds.targets.json (jetpack-compose allowlist 16 -> 23)'
    - 'examples/settings/compose (consumer lane compile)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/jetpack-compose'
    - 'packages/ds-jetpack-compose'
    - 'scripts/compose-parity-diff.mjs'
    - 'fsds.targets.json'
    - 'docs/current-implementation-snapshot.md'
    - 'README.md'
    - AGENTS.md
  out: []
invariants:
  - 'Each new path keys off IR shape facts (content-binding kind, channel shape, root role) — zero per-component names.'
  - 'Foundation-only substrates (BasicText, Canvas-painted progress): zero androidx.compose.material imports in generated code.'
  - 'Prop-text leaves reuse the slice-2 TextStyle machinery (ProvideFsdsTextStyle / LocalFsdsContentColor) where the scopes carry typography slots.'
  - 'The usage ratchet applies to the new admits automatically: every emitted lookup declares an existing scope key and every RN-consumed chrome/text slot is consumed.'
  - 'Markdown degrades to plain text (content-transform parsing stays web-only), mirroring RN and swift.'
acceptance:
  - id: A1
    given: 'a prop-text leaf shape'
    when: 'the static root (or its code part) binds text content to a prop'
    then: 'the composable emits foundation BasicText with the bound prop as text, wrapped in the content-role TextStyle machinery — CodeBlock, CodeSnippet, and Markdown admit without throwing.'
  - id: A2
    given: 'an expandable-content shape'
    when: 'a children leaf pairs with an expand boolean channel'
    then: 'the composable emits a stateful maxLines text whose line limit is gated by the expanded channel with a show-more/less trigger wired to it — Truncate and ShowMore admit.'
  - id: A3
    given: 'a progressbar/status root'
    when: 'the component declares no value channel'
    then: 'the composable emits a foundation-only custom-painted indicator — a determinate bar driven by the 0-100 value prop or an indeterminate spinner — with progressBarRangeInfo semantics — Progress and Spinner admit.'
  - id: A4
    given: 'the whole batch'
    when: 'gated'
    then: 'fsds.targets.json lists 23 admitted components, generation succeeds, pnpm run governed:rail + pnpm test + pnpm run test:frameworks + node scripts/compose-parity-diff.mjs + docs:check-claims all pass, the gradle library plus settings example lane compile green, and the generated trees are committed.'
  - id: A5
    given: 'the parity gate'
    when: 'the new admits emit'
    then: 'the usage ratchet covers them with zero dead lookups and RN-consumed chrome/text slots consumed per path, and Markdown plain-text degradation is a named non-claim.'
non_functional: {}
contracts:
  - name: 'prop-text-leaf'
    type: behavior
    path: 'packages/ds-contracts/components/CodeBlock/CodeBlock.contract.json'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "CodeBlock/CodeSnippet/Markdown emit foundation BasicText with the bound prop as text (code-snippet: text: String -> BasicText(text = text)); the font-size role slot resolves (*.size.fontSize.default / *.typography.fontSize.default via toFsdsSp); highlight/markdown transforms degrade to their source prop; content transforms degrade — verified in generated trees + emitter unit tests"
    recorded_at: "2026-08-31T00:46:32.702Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "Truncate/ShowMore emit the expanded channel (expanded: Boolean?/defaultExpanded/onExpandedChange) with a stateful disclosure toggle whose label comes from the IR conditional content (if (resolvedExpanded) collapseText else expandText — the contract's labels, not hardcoded), gated by the contract's expandable prop when authored (Truncate), with content-color propagation; verified in generated trees + tests"
    recorded_at: "2026-08-31T00:46:33.135Z"
  - criterion_id: A3
    status: pass
    evidence_ref: "Progress/Spinner emit the foundation-only FsdsProgressIndicator (determinate linear bar from value/100, circular via the variant axis, indeterminate spinner with per-value spinner.size.*/spinner.thickness.* dim lookups and per-intent progress.color.fill.*); the substrate paints with progressBarRangeInfo semantics and the label as contentDescription — zero material imports in generated code; Skeleton stays on the static path (status-role check requires a visual child)"
    recorded_at: "2026-08-31T00:46:33.551Z"
  - criterion_id: A4
    status: pass
    evidence_ref: "fsds.targets.json jetpack-compose allowlist 16 -> 23; generation succeeds (23 file groups); pnpm run governed:rail PASS (overall: pass); pnpm test 178 files / 2209 tests; pnpm run test:frameworks 74 tests; node scripts/compose-parity-diff.mjs PASS 23/23; docs:check-claims 26/26; lint 0 errors; ./gradlew :library:compileKotlin + settings lane compileKotlin exit 0; generated trees committed"
    recorded_at: "2026-08-31T00:46:41.829Z"
  - criterion_id: A5
    status: pass
    evidence_ref: "the usage ratchet covers all 23 admits with zero dead lookups (DEAD LOOKUP fired on Progress's stale spinner.size lookups — fixed; USAGE DIVERGENCE fired on CodeSnippet when the fontSize lookup was deleted — named code-snippet.size.fontSize.default — restored by regeneration); per-path claimed sets: font-size role (prop-text), text-color role (progress); Markdown plain-text degradation is a named non-claim"
    recorded_at: "2026-08-31T00:46:42.242Z"