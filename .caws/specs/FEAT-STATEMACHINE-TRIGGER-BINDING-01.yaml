id: FEAT-STATEMACHINE-TRIGGER-BINDING-01
title: 'Bind stateMachine transition events to declared trigger referents'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'stateMachine transition events now bind to declared trigger referents via semantic Rule 6b; corpus greens, mutant killed at generate-check per targeted full-profile run'
created_at: '2026-09-19T10:51:14.185Z'
updated_at: '2026-09-19T11:25:31.267Z'
created_by_session: 'session-8e785c23-806e-4b0b-8d1f-534c73921cef'
blast_radius:
  modules:
    - 'ds-codegen-semantic-gate'
    - 'ds-contracts-schema'
    - 'contract-oracle-mutation'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-contracts/component.contract.schema.json'
    - 'packages/ds-contracts/components/Tabs/Tabs.contract.json'
    - 'packages/ds-codegen/src/contract.ts'
    - 'packages/ds-codegen/src/validation/semantic.ts'
    - 'packages/ds-codegen/src/validation/semantic.test.ts'
    - 'scripts/contract-oracle-mutation/catalog.mjs'
    - 'docs/current-implementation-snapshot.md'
    - 'docs/normal-form.md'
    - 'README.md'
    - packages/ds-figma-plugin/src/generated/components/Tabs/Tabs.figma.json
  out: []
invariants:
  - 'The rule is name-resolution only: a transition event must resolve to a declared referent (channel key, events key, prop name, dismissal trigger event, a11y.keyboard id, "<part>.<verb>" with part in anatomy.parts, or a member of the closed DOM platform set). It does not adjudicate trigger semantics, kinds, or ordering.'
  - 'The DOM platform event set is closed (animationend, transitionend) and grows only by demonstrated corpus need, mirroring the closed-projection doctrine.'
  - 'No component-source or behavior change: transition realization stays out of scope. The six rail-admitted generated trees are byte-identical; the one generated-output delta is the Figma descriptor''s faithful verbatim projection of the new a11y.keyboard id fields (Tabs.figma.json), which carries no realization semantics.'
  - 'Existing Rule 6 (from/to ⊆ states.dimensions) behavior is unchanged.'
  - 'Tabs keyboard id values exactly equal the transition event names they bind (arrow.next, arrow.prev, home, end).'
acceptance:
  - id: A1
    given: 'a contract whose stateMachine transition event resolves to no channel key, events key, prop name, dismissal trigger, a11y.keyboard id, part-qualified form, or closed DOM platform event'
    when: 'generate:check semantic validation runs'
    then: 'it fails with pointer /stateMachine/transitions/<i>/event and code STATEMACHINE_TRIGGER_UNRESOLVED'
  - id: A2
    given: 'the live corpus after Tabs keyboard entries gain stable interaction ids'
    when: 'pnpm run generate:check runs inside the spec worktree'
    then: 'it exits 0 with all 17 declared transition events resolving and no new issues'
  - id: A3
    given: 'the catalog mutant dialog-state-machine-open-event-reveal (event open to reveal) and the merged rule'
    when: 'the targeted full-profile mutation measurement runs at the committed HEAD'
    then: 'the mutant is detected at the structural stage and the catalog disposition records detected'
non_functional: {}
contracts:
  - name: 'state-machine-trigger-binding'
    type: contract-test
    path: 'packages/ds-codegen/src/validation/semantic.test.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Semantic Rule 6b unit suite at packages/ds-codegen/src/validation/semantic.test.ts (58/58 green at e8db2201). The red test asserts the exact coded issue {pointer: /stateMachine/transitions/0/event, message: [STATEMACHINE_TRIGGER_UNRESOLVED] trigger \"reveal\" ...} for an event resolving to no declared referent; eight further cases pin each resolution class (channel key, events key, prop name, dismissal trigger, keyboard id, part-qualified, DOM platform) plus two negative shapes (undeclared part in part-qualified form; bare-string keyboard entry not an id carrier). Falsification outside fixtures: perturbing the committed Dialog contract transitions[0].event open→reveal makes pnpm run generate:check exit 1 with DRIFT Dialog.contract.json and that exact pointer (observed live in the spec worktree, then re-proven mechanically by the A3 mutation run at the landed revision)."
    recorded_at: "2026-09-19T11:24:20.036Z"
    test_nodeid: "packages/ds-codegen/src/validation/semantic.test.ts::validateContractSemantics — cross-reference rules > stateMachine trigger resolution (Rule 6b, FEAT-STATEMACHINE-TRIGGER-BINDING-01) > flags a transition event resolving to no declared referent, with the exact coded message"
    command: pnpm exec vitest run packages/ds-codegen/src/validation/semantic.test.ts
    exit_code: 0
    commit_sha: e8db220185fc
  - criterion_id: A2
    status: pass
    evidence_ref: "pnpm run generate:check at the landed revision e8db2201 (post-merge f58b0c6b): exit 0, all 52 component contracts VALID, zero DRIFT. Pre-check enumeration (tmp/statemachine-trigger-precheck.mjs, canonical checkout): 17 stateMachine transition events corpus-wide; 13 resolve against pre-existing declarations (Dialog/Sheet via open prop, close events key, escape/overlayClick dismissal triggers, animationend DOM platform set; Tabs tab.click via anatomy part) and exactly the 4 Tabs keyboard-alias triggers (arrow.next, arrow.prev, home, end) resolve via the newly added a11y.keyboard ids — the rule greens the corpus with no other contract change, which is the A2 condition (no new issues)."
    recorded_at: "2026-09-19T11:24:29.625Z"
    command: "pnpm run tokens:build && pnpm run generate:check"
    exit_code: 0
    commit_sha: e8db220185fc
  - criterion_id: A3
    status: pass
    evidence_ref: "Targeted full-profile mutation measurement executed by this session at the landed revision (gitignored-and-reproducible report per runner convention, at tmp/contract-oracle-mutation/e8db220185fc-2026-09-19T11-19-42-840Z/report.md; regenerate with the cited command): pnpm run audit:contract-oracle-mutations -- --profile=full --only=dialog-state-machine-open-event-reveal exited 0. Read back from the report: Baseline passed (all 18 full-profile stages green in a clean clone of e8db2201 incl. typecheck-all and both Playwright rails); the mutant went red at its first stage generate-check in 2.1s; expected/actual detector generate-check/structural; Evidence marker found; Disposition match; Detected 1/1, survived 0; disposition/marker/provenance mismatches all 0. The catalog flip (survived→detected in scripts/contract-oracle-mutation/catalog.mjs) is therefore backed by measurement at HEAD, not assertion. Non-claim: the kill proves name-resolution binding, NOT transition realization — no emitter consumes stateMachine.transitions."
    recorded_at: "2026-09-19T11:25:16.930Z"
    command: "pnpm run audit:contract-oracle-mutations -- --profile=full --only=dialog-state-machine-open-event-reveal"
    exit_code: 0
    commit_sha: e8db220185fc