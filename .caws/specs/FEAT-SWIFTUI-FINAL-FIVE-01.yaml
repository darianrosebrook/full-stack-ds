id: FEAT-SWIFTUI-FINAL-FIVE-01
title: 'Final five: Shuttle, Accordion, Tabs, Calendar, Walkthrough (41→49) + parity diff fixture'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-08-17T05:34:33.387Z'
updated_at: '2026-08-17T05:50:43.486Z'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/frameworks/swift/swiftui (five class branches)'
    - 'packages/ds-swiftui + fsds.targets.json (49 admitted)'
    - 'scripts/swift-parity-diff.mjs + docs (parity fixture + claims)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src'
    - 'packages/ds-swiftui'
    - 'fsds.targets.json'
    - 'docs/current-implementation-snapshot.md'
    - scripts
  out: []
invariants:
  - 'Every class gates on IR facts; unrealized props omitted-and-documented; the five admissions must leave the corpus sweep at zero fail-loud names.'
  - 'The parity fixture is emission-level (API surface presence), not visual or behavioral parity — its non-claims are stated in the script header.'
acceptance:
  - id: A1
    given: 'Shuttle (ul root, selection string[] channel, array iteration over channel value rendering iterationLocal spans)'
    when: 'generated'
    then: 'the array-iterated list class emits ForEach(selection) over the ControllableValue<[String]> substrate; compiles'
  - id: A2
    given: 'Accordion (openness union channel, item/trigger/content compound shape) and Tabs (activeTab string channel, tab-list + panel)'
    when: 'generated'
    then: 'both emit as interactive-region composites on their channels — Accordion via DisclosureGroup per item region, Tabs via a tab-axis Picker/segment row driving the panel region; compile'
  - id: A3
    given: 'Calendar (Date|Date[]|null channel, header/grid anatomy) and Walkthrough (coachmark surface, step number channel, title/description slots)'
    when: 'generated'
    then: 'Calendar emits a grid surface on the value channel (mode omitted-and-documented v1); Walkthrough emits through the surface path as an anchored-overlay panel on the step channel with prev/next/onSkip wired; compile'
  - id: A4
    given: 'the smoke harness'
    when: 'swift test runs'
    then: 'the factory table reaches 49 with the completeness assertion at 49 — every corpus component body-evaluates'
  - id: A5
    given: 'the react-vs-swiftui emission diff fixture (scripts/swift-parity-diff.mjs)'
    when: 'run'
    then: 'it generates every corpus component through both the react and swiftui emitters and reports a per-component parity surface table (channel/props/regions present in both) with a nonzero exit on a component that emits for react but not swiftui; the 49/49 run reports zero divergence'
non_functional: {}
contracts:
  - name: 'swiftui-final-five'
    type: contract-test
    path: 'packages/ds-codegen/src/frameworks/swift/swiftui/component-source.test.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Shuttle.swift: @StateObject selection ControllableValue<[String]> + ForEach(selection.value, id: .self); compiled Build complete"
    recorded_at: "2026-08-17T05:52:24.969Z"