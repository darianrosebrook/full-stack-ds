id: FIX-TOKEN-SURFACE-CONTRAST-01
title: 'Separate neutral surface roles from border roles, repair two brand onColor bugs, and extend the contrast gate over the brand dimension'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-09-02T23:44:40.114Z'
updated_at: '2026-09-03T00:04:27.614Z'
blast_radius:
  modules:
    - 'ds-tokens'
    - 'ds-contracts'
    - 'ds-react'
    - 'ds-vue'
    - 'ds-svelte'
    - 'ds-angular'
    - 'ds-lit'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-tokens/src'
    - 'packages/ds-tokens/build'
    - 'packages/ds-tokens/generated'
    - 'packages/ds-contracts/components/Card'
    - 'packages/ds-react/src'
    - 'packages/ds-vue/src'
    - 'packages/ds-svelte/src'
    - 'packages/ds-angular/src'
    - 'packages/ds-lit/src'
    - 'packages/ds-react-native/src'
    - 'packages/ds-figma-plugin/src/generated'
    - 'packages/ds-swiftui/Sources/DsSwiftUI/Components'
    - 'packages/ds-jetpack-compose/library/src/main/kotlin/com/fullstackds/components'
    - packages/ds-contracts/components
    - packages/ds-tokens/usage-baseline.json
  out: []
invariants:
  - 'The contrast gate evaluates every registered brand in both themes, not only the base instantiation'
  - 'A semantic surface role and a semantic border role never resolve to the same value in the same theme'
  - 'Lightening a surface is paired with re-verifying every curated foreground that sits on it; the contrast-targeted ramp''s AA guarantees are preserved, never traded away'
  - 'Committed token outputs (resolved.tokens.json, tokens.css) are regenerated and committed with the source change that produced them'
acceptance:
  - id: A1
    given: 'the contrast gate'
    when: 'run'
    then: 'it evaluates all 10 registered brands across light and dark, and fails if any curated pair in any brand instantiation is below its WCAG level'
  - id: A2
    given: 'the corporate and monochrome brands'
    when: 'foreground.onColor is measured against background.accent at the document root'
    then: 'both meet WCAG AA 4.5:1 in both themes'
  - id: A3
    given: 'the light theme semantic layer'
    when: 'the surface roles and border roles are enumerated'
    then: 'no surface role shares a resolved value with a border role, and background.secondary is lighter than border.subtle'
  - id: A4
    given: 'an interactive Card'
    when: 'hovered'
    then: 'its background shifts to a light tint rather than a mid-grey, and its hover surface is lighter than its own border'
  - id: A5
    given: 'the full gate suite'
    when: 'run after the token change'
    then: 'tokens:check-contrast, generate:check, governed:rail and the framework test suites all pass'
non_functional: {}
contracts:
  - name: 'contrast-gate'
    type: behavior
    path: 'packages/ds-tokens/build/runners/check-contrast.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "pnpm run tokens:check-contrast now reports '484 pair(s) evaluated across 11 instantiation(s) (base + 10 brand(s)) x 2 theme(s)'; previously it evaluated 44 pairs against the base tree only. New module build/w3c/brand-overlay.ts reconstructs the per-brand/per-theme tree. Falsified live: enabling the brand dimension turned the gate RED on two pre-existing defects (corporate 3.99:1 light and dark, monochrome 1.42:1 light) and GREEN after fixing them. 15 unit tests; verified non-vacuous by mutation (stubbing overlayBrand to return the base tree unchanged is killed by 4 of them)."
    recorded_at: "2026-09-03T00:20:36.716Z"
    commit_sha: "73b1f3ac"