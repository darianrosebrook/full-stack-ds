id: DS-CONSUMER-FINDINGS-01
title: 'Consumer findings: outlined destructive Button, prop-gated Sheet/Dialog modality, Toast action content and live-region role'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-09-23T00:39:46.278Z'
updated_at: '2026-09-23T02:26:22.339Z'
created_by_session: '40ac09ec-5e89-4d7a-a01d-85b6bee26253'
blast_radius:
  modules:
    - 'packages/ds-contracts'
    - 'packages/ds-codegen'
    - 'generated framework packages'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-contracts'
    - 'packages/ds-codegen'
    - 'packages/ds-react'
    - 'packages/ds-vue'
    - 'packages/ds-svelte'
    - 'packages/ds-angular'
    - 'packages/ds-lit'
    - 'packages/ds-react-native'
    - 'packages/ds-swiftui'
    - 'packages/ds-jetpack-compose'
    - 'packages/ds-figma-plugin'
    - 'packages/ds-unity'
    - 'packages/ds-godot'
    - 'packages/ds-tokens'
    - 'examples/spatial-console'
    - 'docs'
    - 'e2e'
    - scripts/custom-region-audit/known-authored-regions.json
  out: []
invariants:
  - 'No emitter branches on a component name; each fix is a contract fact or an IR fact every emitter lowers'
  - 'Defaults are unchanged: modal omitted means modal; a Toast without action renders no action part'
  - 'Brand and core token values are not changed; the destructive Button change is a component sidecar change'
  - 'Generated bytes are regenerated, never hand-edited outside admitted @custom regions'
acceptance:
  - id: A1
    given: 'the default brand, whose primary action colour is red'
    when: 'a primary and a destructive Button render in any of the five web frameworks'
    then: 'destructive is the outlined style (transparent fill, danger border and foreground, subtle danger fill on hover) and its computed background differs from primary''s'
  - id: A2
    given: 'a Sheet or Dialog'
    when: 'modal is false'
    then: 'no focus trap, no scroll lock, no backdrop/overlay and no aria-modal=true, in all five web frameworks; with modal omitted or true, today''s modal behaviour is unchanged'
  - id: A3
    given: 'a Toast with an action node'
    when: 'it renders'
    then: 'the action node renders inside the toast__action part in all five web frameworks, and a content-less conditional part honours its if in every emitter'
  - id: A4
    given: 'a Toast'
    when: 'it renders'
    then: 'the root live region is not role=alert, its aria-live follows the politeness prop, and each item keeps role=status'
  - id: A5
    given: 'the change'
    when: 'the governed rail, generate:check, root and framework tests and the realization audits run'
    then: 'all pass and regenerated bytes are committed'
non_functional: {}
contracts:
  - name: 'button-destructive-outlined'
    type: behavior
    path: 'packages/ds-contracts/components/Button/Button.styles.json'
  - name: 'surface-modality-gate'
    type: behavior
    path: 'packages/ds-codegen/src/ir.ts'
  - name: 'toast-anatomy'
    type: schema
    path: 'packages/ds-contracts/components/Toast/Toast.contract.json'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "e2e/button-intent-rail.spec.ts on main a88dc10d: 5/5 frameworks rest transparent (rgba(0,0,0,0)), border rgb(179,27,27), label rgb(144,9,9), primary filled; counterfactual with pre-change React tokens fails (rgb(217,45,46)). Hover wash rgb(254,228,228) observed in 24/24 browser samples across all five (one-off tmp probe; a single cold-start Svelte reading of rgb(179,27,27) did not reproduce). tokens:check-contrast 594 pairs pass."
    recorded_at: "2026-09-23T02:42:18.632Z"
    command: FSDS_E2E_PORT=5392 pnpm exec playwright test e2e/button-intent-rail.spec.ts
    exit_code: 0
    commit_sha: a88dc10d
  - criterion_id: A2
    status: pass
    evidence_ref: "e2e/modality-rail.spec.ts on main a88dc10d: 20/20 (Sheet+Dialog x 5 frameworks x {default, modal=false}) assert aria-modal, backdrop display, body overflow lock, Tab escape; ungated React useSheet counterfactual fails (overflow hidden). surface-ir.test.ts pins modalityGate validation. Includes Lit FocusTrapController shadow-DOM fix (Lit default trap previously never wrapped)."
    recorded_at: "2026-09-23T02:42:28.828Z"
    command: FSDS_E2E_PORT=5392 pnpm exec playwright test e2e/modality-rail.spec.ts
    exit_code: 0
    commit_sha: a88dc10d
  - criterion_id: A3
    status: pass
    evidence_ref: "Action is a named slot: filled slot renders in .toast__action in React/Vue/Svelte/Lit/Angular Toast custom-region tests (each fails against pre-change components). if: \"slot:<name>\" guard omits the wrapper when unfilled in React/Vue/Svelte/Lit/RN; Angular renders it empty and .toast__action:empty hides it (toast-live-region-rail angular row fails without the rule). Content-less if-guards were already honoured by all five emitters at base (spec premise corrected); dom-tree.test.ts pins slot-guard lowering and validation."
    recorded_at: "2026-09-23T02:42:29.414Z"
    command: per-framework Toast suites + e2e/toast-live-region-rail.spec.ts
    exit_code: 0
    commit_sha: a88dc10d
  - criterion_id: A4
    status: pass
    evidence_ref: "Viewport role=region aria-label=Notifications, aria-live polite by default and assertive when set, item role=status, zero role=alert: 5/5 browser rail on main and per-framework unit tests (fail against pre-change components). Angular DOM-tree emitter now applies ir.root.rootRole (parity with React for Toast, Avatar, Field, NavTree, Spinner, ToggleSwitch). Not checked with a screen reader."
    recorded_at: "2026-09-23T02:42:30.000Z"
    command: e2e/toast-live-region-rail.spec.ts + Toast framework suites
    exit_code: 0
    commit_sha: a88dc10d