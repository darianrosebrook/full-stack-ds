id: FEAT-COMPOSE-BUTTON-ADMISSION-01
title: 'Compose Button admission: projected-children action path on the owned substrate'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-08-18T02:57:59.142Z'
updated_at: '2026-08-18T03:13:22.860Z'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/frameworks/jetpack-compose (action path + gate import)'
    - 'packages/ds-codegen/src/frameworks/swift/swiftui/component-source.ts (export isProjectedChildrenAction)'
    - 'packages/ds-jetpack-compose/library (hand-authored FsdsButton.kt substrate + regenerated Button tree)'
    - 'fsds.targets.json + examples/settings/compose + token-realization ledger + snapshot'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/jetpack-compose'
    - packages/ds-codegen/src/frameworks/swift/swiftui/component-source.ts
    - packages/ds-jetpack-compose/library
    - fsds.targets.json
    - examples/settings/compose
    - scripts/token-realization-audit
    - docs/current-implementation-snapshot.md
    - docs/token-realization-audit
  out: []
invariants:
  - 'The action path dispatches on the shared projected-children IR predicate (cross-framework import, precedent: vue→react hook-source), never on a component name.'
  - 'FsdsButton is foundation-only: zero androidx.compose.material imports; interaction states, focus ring, and disabled chrome are painted from the style resolved through LocalFsdsTheme.'
  - 'Variant styling layers root → variant_<size> → variant_<intent>, matching swift/RN precedence.'
acceptance:
  - id: A1
    given: 'Button as the corpus''s projected-children action consumer (root button + single children region, size/variant axes, layered variant scopes in the IR)'
    when: 'the compose emitter gains the action path on a hand-authored foundation-only FsdsButton substrate and Button joins the jetpack-compose allowlist'
    then: 'the generated tree compiles via gradle, the settings example renders its action regions through FSDS Button, the token-realization ledger loses its Button admission entry with no new gaps, and gates stay green'
non_functional: {}
contracts:
  - name: 'projected-children action lowering'
    type: behavior
    path: 'packages/ds-codegen/src/frameworks/swift/swiftui/component-source.ts'
