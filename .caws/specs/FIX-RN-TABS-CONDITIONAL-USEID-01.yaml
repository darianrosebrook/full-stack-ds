id: FIX-RN-TABS-CONDITIONAL-USEID-01
title: 'Fix conditional useId() emission in RN compound-selection components (rules-of-hooks)'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-08-16T01:37:48.782Z'
updated_at: '2026-08-16T01:37:48.782Z'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/frameworks/react-native/component-source.ts (compound-selection idBase emission)'
    - 'packages/ds-codegen/src/frameworks/rn-unconditional-useid.test.ts (new regression pin)'
    - 'packages/ds-react-native/src/components/Tabs/Tabs.tsx (regenerated bytes)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/react-native/component-source.ts'
    - 'packages/ds-codegen/src/frameworks/rn-unconditional-useid.test.ts'
    - 'packages/ds-react-native/src'
  out: []
invariants:
  - 'Generated React Native sources must call React hooks unconditionally at the top level of the component body; fallback resolution happens after the call, never inside ?? (react-hooks/rules-of-hooks).'
  - 'Only react-native generated bytes may change in this slice; the five rail-admitted families'' generated outputs stay byte-identical.'
acceptance:
  - id: A1
    given: 'a compound-selection container contract (Tabs) lowered by the RN emitter'
    when: 'the component source is generated'
    then: 'useId() is invoked unconditionally in the component body, staged in a local, with idBase coalesced after the call — no hook call inside ?? (react-hooks/rules-of-hooks clean)'
  - id: A2
    given: 'the regenerated packages/ds-react-native/src'
    when: 'pnpm run lint runs'
    then: 'it exits 0 with zero errors'
  - id: A3
    given: 'the regenerated tree'
    when: 'pnpm run governed:rail and the framework-src drift diff vs the tip run'
    then: 'both pass and the only generated-byte change is packages/ds-react-native/src/components/Tabs/Tabs.tsx'
  - id: A4
    given: 'the new emitter regression test'
    when: 'the emitter reverts to the conditional useId form'
    then: 'the test fails (mutation-falsified) while passing on the fixed emitter'
non_functional: {}
contracts:
  - name: 'rn-compound-useid-order'
    type: contract-test
    path: 'packages/ds-codegen/src/frameworks/rn-unconditional-useid.test.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "packages/ds-react-native/src/components/Tabs/Tabs.tsx:82-83 — const generatedIdBase = useId().replace(/:/g, \"\"); const resolvedIdBase = idBase ?? generatedIdBase; no ?? useId( remains; pinned by rn-unconditional-useid.test.ts test 1 (vitest, 2/2 pass)"
    recorded_at: "2026-08-16T01:42:53.795Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "pnpm run lint in the worktree post-regen: exit 0, 0 errors (21 pre-existing warnings unchanged); previously 1 react-hooks/rules-of-hooks error at Tabs.tsx:82"
    recorded_at: "2026-08-16T01:42:54.763Z"