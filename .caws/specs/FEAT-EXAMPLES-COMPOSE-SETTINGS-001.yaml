id: FEAT-EXAMPLES-COMPOSE-SETTINGS-001
title: 'Compose settings example lane consuming the generated ds-jetpack-compose package'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'Compose settings consumer lane compiled against the generated package via composite build, CI-wired as the kotlin rail''s settings-lane step'
created_at: '2026-08-16T22:59:55.813Z'
updated_at: '2026-08-16T23:10:18.010Z'
blast_radius:
  modules:
    - 'examples settings compose lane'
    - 'ds-jetpack-compose consumer boundary'
    - 'CI native compile lanes'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'examples/settings/compose'
    - 'examples/README.md'
    - 'package.json'
    - '.github/workflows/ci.yml'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'The example consumes ONLY public exports of the generated package (com.fullstackds.components.*) via Gradle composite-build substitution — no reaching into the generating repo internals, no copied source.'
  - 'No contract, schema, IR, or emitter change — this slice falsifies the existing package boundary; gaps it finds become findings, not workarounds.'
  - 'The NOT FSDS partial-realization boundary stays visible in source comments and the README ledger, mirroring the swift lane convention.'
  - 'CI wiring mirrors the swift example-lane precedent (CI-only job; the pnpm pre-push gate does not gain native compiler steps).'
  - 'No generated tree under packages/* changes — examples are pure consumers.'
acceptance:
  - id: A1
    given: 'the settings spec and the ds-jetpack-compose package emitting Switch/ToggleSwitch'
    when: 'a Gradle composite-build example app consumes the package public API'
    then: 'the Preferences section is fully realized through FSDS Switch rows (controlled checked/onChange, defaults false/true per spec) and every not-yet-emitted region is composed from plain Compose marked NOT FSDS in source'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'the lane compiles exit 0 via ./gradlew compileKotlin (JVM, real Compose runtime) with the included build substituting ds-jetpack-compose:library — consumer-side falsification that the public package boundary works outside the generating repo'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'a CI job (ubuntu, JDK 21, gradle caching) compiles the lane, closing the no-CI-lane-executes-the-Gradle-compile gap for the compose consumer graph'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'the lane is documented as locally runnable (./gradlew run opens the settings window) with claim/non-claim ledger, and examples/README + the snapshot row record the new lane and its bounded claim'
non_functional: {}
contracts:
  - name: 'settings-spec'
    type: behavior
    path: 'examples/settings/spec.md'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "examples/settings/compose/src/main/kotlin/com/fullstackds/examples/settings/SettingsApp.kt @ eecfcd52: PreferencesSection realizes both spec rows through the generated Switch (controlled checked/onChange, defaults false/true, tooltip hint via public contentDescription prop); ProfileSection/DangerZoneSection/ConfirmDeleteDialog carry NOT FSDS markers; README ledger table maps every spec region to FSDS/NOT FSDS"
    recorded_at: "2026-08-16T23:10:12.215Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "./gradlew compileKotlin BUILD SUCCESSFUL exit 0 (also via root script typecheck:example:compose); gradle dependencies shows 'ds-jetpack-compose:library -> project :ds-jetpack-compose:library' (composite substitution); disassembled example bytecode contains exactly 2 invokestatic sites to com/fullstackds/components/switch/SwitchKt.Switch with the full generated signature (Boolean,Z,Function1,SwitchSize,Z,String,String,String,Modifier,Composer,II)"
    recorded_at: "2026-08-16T23:10:13.402Z"
  - criterion_id: A3
    status: pass
    evidence_ref: ".github/workflows/ci.yml native-compile-rail job: 'Settings example compile (consumer lane over ds-jetpack-compose)' step running examples/settings/compose/gradlew compileKotlin, with cache:gradle added to setup-java — the swift-lane precedent mirrored; closes the no-CI-lane-executes-the-Gradle-compile gap"
    recorded_at: "2026-08-16T23:10:14.771Z"
  - criterion_id: A4
    status: pass
    evidence_ref: "examples/settings/compose/README.md claim + partial-realization ledger + runnable command; examples/README.md settings row gains compose/ lane; snapshot rung-1 sentence records the consumer lane; gates green in worktree (generate:check 49/49 after token bootstrap, root tests 2078/2078, diff scope had zero packages/ changes)"
    recorded_at: "2026-08-16T23:10:16.006Z"