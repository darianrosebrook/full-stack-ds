id: REL-LOWERING-SUPPORT-01
title: 'Observe output absence through the real orchestration, and enforce the lowerer''s declared support'
risk_tier: 3
mode: fix
lifecycle_state: active
created_at: '2026-09-18T04:19:24.116Z'
updated_at: '2026-09-18T04:19:25.218Z'
created_by_session: 'session-69475cd2-20cf-4633-9893-09967382f4d8'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/analytical'
    - 'packages/ds-contracts/analytical-fixtures'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'No manually authored verdict field. The output-absence limb is observed by running the SAME candidate-selection-and-lowering orchestration under the empty inventory and inspecting the outputs it produced, not by recording a boolean the test then reads back.'
  - 'A restriction that exists only in the experiment''s selection of two examples is not an enforced property. The lowerer returns unrealized outside its declared support, and the support is a declaration in the code rather than an artefact of which program the runner happened to pick.'
  - 'Only existing behaviour is corrected: admission, partition membership, single evaluation and the preservation pair are untouched, and no new analytical rule is introduced.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given an inventory under which enumeration retains no candidate; when the orchestration that selects candidates and lowers them is run; then it is executed rather than counted, and it produces an empty output collection, while the same orchestration over the full inventory produces the readback and metric witnesses and reports every program outside the declared support as unrealized'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "OUTPUT ABSENCE, OBSERVED. selectAndLower runs the same selection-and-lowering orchestration the experiment uses and returns what it PRODUCED. Measured under the empty channel inventory: considered=0, realized=[], unrealized=[] - an empty output collection from an executed path, not a count asserted beside separately produced reports, and no manually authored verdict field. The same orchestration under the full inventory returns considered=12, realized=[readback, metric], unrealized=10 with a reason each, so the empty result is ABSENCE and not a path that never produces anything. SUPPORT ENFORCED. `lower` previously branched on readback capability alone, so every zero-baseline program reached the generic metric producer; measured now: 2 realized, 10 unrealized, 12 considered, realized.length equal to LOWERING_SUPPORT.length, realized plus unrealized equal to considered, and every unrealized reason matching \"no lowering is declared\". The runner selects its witness pair FROM the support declaration rather than taking the first readback and first non-readback program, so the restriction is a property of the code and not of the example selection. The narrowed support had to include the dimension channel: at (coordinate, measure) granularity two cartesian|*|area programs matched, which is exactly the silent widening being corrected. Verification: pnpm exec vitest run packages/ds-codegen/src/analytical (21 files / 926 tests), pnpm test (303 files / 4147 tests), typecheck, lint, docs:check-claims, governed:rail and test:frameworks green on merge. NON-CLAIM: this corrects two evidence/enforcement boundaries only; admission, partition membership, single evaluation and the preservation pair are untouched and their own evidence stands."
    recorded_at: "2026-09-18T04:24:08.338Z"