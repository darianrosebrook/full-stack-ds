id: REL-PROJECTION-OBSERVER-LIMITS-01
title: 'Record and pin what the projection observers do not check: topology admissibility and grain status'
risk_tier: 3
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Recorded and pinned the two reproduced observer limits - neither observer checks topology admissibility against the capacity table nor grain status, so A3 and A4 establish that a changed analytical binding is detected and not complete admissibility checking; no behaviour changed.'
created_at: '2026-09-18T01:14:41.788Z'
updated_at: '2026-09-18T01:21:59.298Z'
created_by_session: 'session-69475cd2-20cf-4633-9893-09967382f4d8'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/analytical/projection.ts (NON_CLAIMS)'
    - 'packages/ds-codegen/src/analytical/projection.test.ts (a test pinning the recorded limitation)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/analytical'
    - 'packages/ds-contracts/analytical-fixtures'
    - docs/current-implementation-snapshot.md
  out: []
invariants:
  - 'The observers check the BINDING and the claims derived from the program. They do not check the topology own admissibility against the capacity table, and they do not consult grain status. Both were reproduced on the merged code and are recorded, not repaired here: a limitation that is left implicit reads as a claim.'
  - 'The limitation is PINNED by a test asserting the measured behaviour, so a later reader who adds the check sees the test change rather than a silent widening of what A3 and A4 establish.'
  - 'No behaviour changes. This slice corrects what the record says the evidence establishes; it does not make the observer stronger, and it does not touch the enumerator.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the observers were described as reading the program rather than its certificate; when the two reproduced limits are compared against what the retained record says; then the record states that neither observer checks topology admissibility against the capacity table nor grain status, a test pins the measured behaviour so it cannot drift silently, and the corrected account names this boundary beside the binding result'
non_functional: {}
contracts: []
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "REPRODUCED BY EXECUTION BEFORE BEING RECORDED, on the merged code at 22e3587f via tmp/probe-observer-limits.ts (gitignored scratch). LIMIT 1 - topology admissibility: a program with coordinate non-metric hosting measure area, where CAPACITY[area].spaces is [cartesian, geographic], is NOT in the retained set (the enumerator excludes it) yet programObserver.ok is true AND bindingObserver.ok is true; the blind spot is therefore not programObserver alone, bindingObserver inherits it. LIMIT 2 - grain status: with grain unknown the enumerator retains 0 and carries 53 as undecided, while programObserver.ok and bindingObserver.ok are BOTH true on a program asserting an aggregate magnitude. The record now states this in two non-claims in packages/ds-contracts/analytical-fixtures/projection-experiment.json, in correctedAccount.observerLimits, and on the durable snapshot sentence describing A3 and A4; and the limitation is PINNED by a new test that asserts the measured behaviour (non-metric/area rejected by the enumerator and accepted by both observers; unknown grain 0 retained with both observers accepting), so the day either check is added the test fails and must be updated to assert the check rather than the limitation disappearing silently. NON-CLAIM: no behaviour changed - the observers are not strengthened, the enumerator is untouched, and stage 2 remains at its existing 64 of 118 plus 2 of 3 plus 4 of 5 unresolved with drift green. Verification was pnpm exec vitest run packages/ds-codegen/src/analytical (21 files / 916 tests), pnpm test (303 files / 4137 tests), typecheck, lint, docs:check-claims, governed:rail and test:frameworks green on merge."
    recorded_at: "2026-09-18T01:21:43.878Z"