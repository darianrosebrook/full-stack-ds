id: REL-GRAPH-BINDING-SELECTS-01
title: 'Make the graph binding select and qualify its node universe, and repair the incidence decoder collision'
risk_tier: 2
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'The binding now selects its populations from one named source and qualifies them: the key must be the declared key, endpoints must belong to the selected universe, a missing population is carried, and the incidence decoder no longer collides on joined endpoint text.'
created_at: '2026-09-18T05:19:44.967Z'
updated_at: '2026-09-18T05:27:20.517Z'
created_by_session: 'session-69475cd2-20cf-4633-9893-09967382f4d8'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/analytical/graph-projection.ts (denoteGraph over a named source, key qualification, endpoint membership, the incidence decoder)'
    - 'packages/ds-codegen/src/analytical/graph-projection.test.ts'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/analytical'
    - 'packages/ds-contracts/analytical-fixtures'
  out: []
invariants:
  - 'The binding SELECTS its populations from one named source. A caller must not be able to pair a binding with independently preselected role arrays without an observable check, so the preset-array entry point is removed rather than documented.'
  - 'A name that RESOLVES is not a semantic qualification. The designated identity field must be the declared key of the node relation, and every supplied endpoint must belong to the selected universe; neither is established by the name existing.'
  - 'A missing population is CARRIED, never silently empty; a contradictory endpoint is REFUSED, never silently dropped during incidence encoding.'
  - 'The incidence deduplication key must be collision-free under concatenation ambiguity: distinct edges whose endpoint strings would compose to the same joined text are distinct edges.'
  - 'No canonical-model integration. The zod model, schema emission and the stage-2 ledgers stay untouched; this slice establishes what the binding OWNS so that a later placement decision is an integration of measured semantics.'
  - 'No layouts, general graph algorithms, arbitrary multigraph support, accessibility, combinators or stage-2 accounting. A requirement for any of those is a stopping finding.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given one named source holding two possible node universes and one edge relation; when only the explicit binding changes between them; then the binding selects the universe, the recovered graph changes accordingly, a key or population that merely resolves cannot enter, a missing population is carried, a contradictory endpoint is refused, and the incident edge set distinguishes edges whose endpoint strings would otherwise compose ambiguously'
non_functional: {}
contracts:
  - name: 'graph-binding-selection'
    type: contract-test
    path: 'packages/ds-codegen/src/analytical/graph-projection.test.ts'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "THE SELECTION CONTROL, measured into packages/ds-contracts/analytical-fixtures/graph-experiment.json and byte-compared by the test. ONE source holds two candidate node universes and one edge relation: allNodes {n1,n2,n3}, connectedSet {n1,n2}, links {(n1,n2)}. Changing ONLY the binding changes the denoted graph - selectionChanged true, byAllNodes selects [n1,n2,n3] with isolates [n3] and byConnectedSet selects [n1,n2] with no isolate - while edgeRelationIdentical is true and the source rows are unchanged. The preset-array entry point is REMOVED: `evaluateGraph(binding, rows)` no longer exists, `denoteGraph` takes a GraphSource whose rows are keyed by relation NAME, and `selected` reports which relations the binding chose, so a caller cannot pair a binding with an independently preselected arrangement. Both bindings preserve through both produced representations (relational readback and incidence output, the isolate present with an EMPTY incident list), and the node-from-edges counterexample still collapses the pair (distinguishesThePair false). QUALIFICATION, each recorded as its refusal text: `allNodes.label` RESOLVES but is not the relation declared key, so it is refused as an identity rather than accepted; an endpoint `n9` outside the SELECTED universe is refused rather than silently dropped from the incidence; and a MISSING population is CARRIED under invariant:population-declared rather than becoming a silently empty node list, with graphPreservation refusing outright because the binding does not denote a graph. The node-universe-is-the-edge-relation case remains refused. THE DECODER COLLISION, repaired: deduplicating by joined endpoint text mapped (\"a->b\",\"c\") and (\"a\",\"b->c\") to the same key and dropped one edge; the key is now an ordered JSON-encoded pair and bothSurvive is true with both edges recovered. THE MUTATION now moves the PRODUCED incidence output directly through `renameInOutput` rather than re-encoding a mutated result: the entry becomes n3-impostor, countAndEdgesUnchanged is true and detected is true. Verification: pnpm exec vitest run packages/ds-codegen/src/analytical (22 files / 945 tests), pnpm test (303 files / 4166 tests), typecheck, lint, docs:check-claims, governed:rail and test:frameworks green on merge. NON-CLAIMS: no canonical-model integration (the zod model, schema emission and stage-2 ledgers are untouched, and authorityGap.declaresANodeRelation stays false); the graph tests construct the source directly and do not invoke the analytical judge; one source, two bindings, two representations and one mutation, with no layouts, general graph algorithms, multigraph support, accessibility or combinators."
    recorded_at: "2026-09-18T05:27:19.717Z"