id: REL-ENUM-FACTORING-01
title: 'Enum factoring: the kernel carries one coordinate per distinction a value space actually has'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-09-15T04:08:17.050Z'
updated_at: '2026-09-15T04:08:24.716Z'
created_by_session: 'session-272d1906-0cae-4fae-ba00-e86a4ac9d030'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/analytical/census.ts (which coordinate kinds a k-member enum emits)'
    - 'packages/ds-contracts/analytical-fixtures (witnesses, bases, suspension ledger, freeze)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'docs/architecture/analytical-relation-doctrine.md'
    - 'packages/ds-codegen/src/analytical'
    - 'packages/ds-contracts/analytical-fixtures'
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the census decomposes a k-member enum into a leaf plus C(k,2) member pairs and at k=2 the leaf and its only pair are one distinction; when the factoring is decided; then the kernel carries one coordinate for that distinction and every evidence-machinery invariant holds for the survivor'
non_functional: {}
contracts:
  - name: 'enum-factoring'
    type: contract-test
    path: 'packages/ds-contracts/analytical-fixtures/witnesses.json'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "Factoring recorded in packages/ds-contracts/analytical-fixtures/subtraction-stage2-enum-leaf.json: field.temporality.kind is required-derived-vocabulary, derivable from its k=2 member pair, which is the ONE primitive coordinate carrying that distinction; necessity.ts accounts it via the retained mode without ratifying it. Every evidence-machinery invariant holds for the survivor: pnpm exec vitest run packages/ds-codegen/src/analytical = 786/786 (19 files), including C2 (awaiting == failing witnesses, now 2), C4b, C4c and coverage. Non-claim: stage-2's own ledger (subtraction-stage2.json) still records the leaf unresolved -- the factoring is carried by this spec's basis, not by amending a ledger another spec owns, and no stage-2 verdict moved."
    recorded_at: "2026-09-15T04:28:06.936Z"
    test_nodeid: "packages/ds-codegen/src/analytical/necessity.test.ts > C2 — every witness holds, or is held open for adjudication > accounts a retained coordinate without ratifying it"
    command: pnpm exec vitest run packages/ds-codegen/src/analytical
    exit_code: 0
    artifact_path: packages/ds-contracts/analytical-fixtures/subtraction-stage2-enum-leaf.json
    commit_sha: "515896c0"