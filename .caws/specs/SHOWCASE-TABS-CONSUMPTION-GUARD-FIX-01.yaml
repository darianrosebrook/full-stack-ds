id: SHOWCASE-TABS-CONSUMPTION-GUARD-FIX-01
title: 'Remove app .tabs/.tabs__panel consumption-guard collision in TracePanel'
risk_tier: 3
mode: fix
lifecycle_state: active
created_at: '2026-06-06T02:22:58.722Z'
updated_at: '2026-06-06T02:23:20.664Z'
blast_radius:
  modules:
    - 'src/styles/app.css — rewrite the two TracePanel inspector selectors that depend on the DS-owned .tabs / .tabs__panel classes (.trace-tabs.tabs, .trace-panel-body.tabs__panel) to .app-trace-scoped equivalents that keep the same (0,2,0) specificity without naming a DS class.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - src/styles/app.css
  out: []
invariants:
  - 'The showcase owns its TracePanel inspector layout via app-specific classes (.app-trace .trace-tabs / .trace-panel-body), never via the DS-owned .tabs / .tabs__panel selectors. The consumption guard (BARE_TABS_RULE) finds zero hits.'
  - 'No DS package file (packages/ds-*/**) is modified; the git diff is confined to src/styles/app.css. The DS Tabs component remains the single owner of .tabs / .tabs__panel.'
acceptance:
  - id: A1
    given: 'the showcase app.css, with comments stripped'
    when: 'scanned for selectors that depend on the DS-owned .tabs / .tabs__panel classes'
    then: 'no bare or compound app selector matches — the consumption-guard BARE_TABS_RULE returns zero hits and no selector names .tabs__panel'
  - id: A2
    given: 'the TracePanel two-tab inspector rendered inside the .app-trace aside'
    when: 'the page lays out'
    then: 'the tab bar still pins to the top and the active panel body still scrolls — the layout overrides retain at least the DS base specificity via the .app-trace ancestor (0,2,0 preserved)'
  - id: A3
    given: 'src/consumption/consumption-guard.test.ts'
    when: 'run under vitest'
    then: 'it passes (the A6 bare-.tabs assertion is green)'
  - id: A4
    given: 'the full root vitest suite'
    when: 'run'
    then: 'the prior 1528/1529 failure is eliminated with no new failures (suite green, modulo unrelated pre-existing breakage)'
  - id: A5
    given: 'the DS Tabs component package files'
    when: 'the slice completes'
    then: 'none under packages/ds-*/ is modified — git diff is confined to src/styles/app.css'
non_functional: {}
contracts: []
