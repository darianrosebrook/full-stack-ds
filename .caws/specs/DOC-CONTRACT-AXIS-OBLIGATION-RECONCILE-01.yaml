id: DOC-CONTRACT-AXIS-OBLIGATION-RECONCILE-01
title: 'Reconcile successor ledger: three contract-axis obligation families landed'
risk_tier: 3
mode: doc
lifecycle_state: active
worktree: obligation-reconcile
created_at: '2026-06-02T09:09:24.161Z'
updated_at: '2026-06-02T09:09:32.952Z'
blast_radius:
  modules:
    - docs (successor ledger reconciliation — single file)
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - docs/successor-work.md
  out:
    - packages
    - .caws/specs/CONTRACT-AXIS-OBLIGATION-VALIDATOR-01.yaml
invariants:
  - 'Doc-only: the change set is exactly docs/successor-work.md. No package/code/test/contract/spec-body mutation (the validator spec CONTRACT-AXIS-OBLIGATION-VALIDATOR-01.yaml is fenced out except its normal lifecycle, which is already closed).'
  - 'Records the three landed obligation families (input/form/channel coherence, surface+dismissal focus policy, A2UI children/anatomy host compatibility) with the close-commit evidence anchor (CONTRACT-AXIS-OBLIGATION-VALIDATOR-01 @ 850f764) — does not invent work not on main.'
  - 'Does NOT mark the whole obligation-lattice item complete: the remaining families (relationships↔accessible-labeling references, constrained-prop machine-checkable constraints, raw semantic token usage auditable vs category/state coverage) stay listed as open/remaining.'
  - 'Records implementation evidence without overclaiming: generate:check 47/47, typed [OBLIGATION_*] diagnostics, fail/pass fixtures, mutation-probe bite evidence, and the non-claim that no real contract / emitter / generated package changed.'
  - 'docs/current-implementation-snapshot.md is NOT edited — it carries no contract-axis/obligation row or claim-ledger wording that references this validator (verified), so there is nothing to refresh there.'
acceptance:
  - id: A1
    given: 'docs/successor-work.md, whose "Contract-axis obligation validator" row and section currently say status: partial with the three families listed only as remaining future work'
    when: 'the ledger is reconciled'
    then: 'the row/section records CONTRACT-AXIS-OBLIGATION-VALIDATOR-01 as landed (with @ 850f764) for input/form/channel coherence, surface+dismissal focus policy, and A2UI children/anatomy host compatibility'
  - id: A2
    given: 'the same ledger item, which is broader than the three landed families (it names the full obligation lattice from contract-group-axes.md)'
    when: 'the item status is updated'
    then: 'the item is NOT marked complete — relationships↔accessible-labeling references, constrained-prop machine-checkable constraints, and raw-semantic-token-usage auditing remain listed as open remaining work'
  - id: A3
    given: 'the landed work'
    when: 'evidence is recorded in the ledger'
    then: 'it cites generate:check 47/47, typed [OBLIGATION_*] diagnostics, fail/pass fixtures under test/fixtures/obligation-axes, mutation-probe bite evidence, and the non-claim that no real contract/emitter/generated-package changed — without asserting determinism, visual quality, or a11y adequacy'
  - id: A4
    given: 'the reconciled doc'
    when: 'git diff is taken over the change set'
    then: 'exactly one file changed (docs/successor-work.md); no package/code/test/contract/spec-body file appears in the diff'
non_functional: {}
contracts: []
