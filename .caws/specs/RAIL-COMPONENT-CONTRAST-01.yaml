id: RAIL-COMPONENT-CONTRAST-01
title: 'Component-derived WCAG contrast gate in the pre-emission semantics rail'
risk_tier: 2
mode: feature
lifecycle_state: closed
resolution: completed
closure_notes: 'component-derived WCAG contrast gate landed in generate:check and governed:rail pre-emission; 6 pair identities ledgered under a two-directional ratchet'
created_at: '2026-08-15T01:10:52.910Z'
updated_at: '2026-08-15T01:33:36.387Z'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/validation (new component-contrast validator + tests)'
    - 'packages/ds-codegen/src/cli.ts (semantic drift pass gains the new validator)'
    - 'root package.json (governed:rail scripts gain a pre-emission generate:check)'
    - 'docs/current-implementation-snapshot.md (token governance row)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src'
    - 'package.json'
    - 'docs/current-implementation-snapshot.md'
  out: []
invariants:
  - 'The gate derives its pair list mechanically from each contract''s styles.json/tokens.json binding chains (base scope plus variant slot redirections). It never accumulates hand-curated per-component pair lists; the curated-pair gate in ds-tokens stays the system-level contract and this gate is the component-level complement.'
  - 'The ledger only shrinks truthfully: an entry leaves only because its pair now passes or is no longer derived, enforced by the stale-entry DRIFT. Adding an entry to silence a regression introduced in the same change is forbidden.'
  - 'Disabled-state pairs are exempt per WCAG 1.4.3 Note 5; every other state (hover, focus, selected, variant scopes) is gated.'
  - 'The gate runs before emission: governed:rail runs generate:check before the generate step, so a failing pair blocks ds-framework output.'
acceptance:
  - id: A1
    given: 'a contract whose styles.json declares fg and bg color bindings'
    when: 'pnpm run generate:check runs'
    then: 'every derived pair is checked against WCAG AA 4.5:1 in light and dark themes of resolved.tokens.json and a failing pair reports DRIFT naming the tokens, themes, and ratios'
  - id: A2
    given: 'a pre-existing failing pair ledgered in component-contrast-known-gaps.json'
    when: 'generate:check runs'
    then: 'the ledgered pair reports no DRIFT, an unledgered new failure does, and a ledger entry that no longer reproduces reports DRIFT as stale'
  - id: A3
    given: 'styles blocks scoped to a disabled state'
    when: 'pairs are derived'
    then: 'they are exempt from the gate per WCAG 1.4.3 Note 5 and the exemption is documented on the validator'
  - id: A4
    given: 'the governed:rail script'
    when: 'it runs'
    then: 'generate:check executes before any ds-framework emission step'
  - id: A5
    given: 'resolved.tokens.json is not built'
    when: 'generate:check runs'
    then: 'the validator emits one instructional issue instead of crashing or silently passing'
non_functional: {}
contracts:
  - name: 'component-contrast-validator'
    type: behavior
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "pnpm run generate:check in worktree component-contrast-rail: 49/49 contracts VALID with validateComponentContrast in the semantics pass; falsified live — removing the Accordion ledger entry produced DRIFT citing light 4.38:1 and dark 1.92:1 on /styles/.accordion__trigger:hover"
    recorded_at: "2026-08-15T01:33:18.885Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "falsification: unledgered Accordion pair DRIFTed with entry removed, VALID when restored; stale-entry ratchet proven by vitest component-contrast.test.ts 17/17 (suppresses ledgered failures; flags entries that no longer reproduce)"
    recorded_at: "2026-08-15T01:33:20.135Z"
  - criterion_id: A3
    status: pass
    evidence_ref: "deriveComponentContrastPairs exempts /disabled/i scope+block keys per WCAG 1.4.3 Note 5, documented on the validator; corpus disabled pairs (Button/Input/TextField/Select) ungated; unit test 'exempts disabled blocks and disabled scopes' passes"
    recorded_at: "2026-08-15T01:33:21.388Z"
  - criterion_id: A4
    status: pass
    evidence_ref: "pnpm run governed:rail exit 0 in worktree: script runs pnpm run generate:check (Validation 49/49) before generate --target=all, then validate-cli.js --require-artifact-manifest Overall PASS; no generated-tree drift"
    recorded_at: "2026-08-15T01:33:22.831Z"
  - criterion_id: A5
    status: pass
    evidence_ref: "unit test 'emits one instructional issue when the resolved graph is missing' in component-contrast.test.ts: single /styles issue naming pnpm -F @full-stack-ds/tokens build"
    recorded_at: "2026-08-15T01:33:24.414Z"