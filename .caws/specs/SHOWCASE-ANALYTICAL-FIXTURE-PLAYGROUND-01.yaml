id: SHOWCASE-ANALYTICAL-FIXTURE-PLAYGROUND-01
title: 'Analytical fixture playground: raw-data scratch surface with realization placeholder'
risk_tier: 3
mode: feature
lifecycle_state: active
worktree: fixture-playground
created_at: '2026-09-02T21:22:55.630Z'
updated_at: '2026-09-02T21:40:37.828Z'
blast_radius:
  modules:
    - 'src/data/analytical-fixtures (new: generated fixture dump + hand-authored types)'
    - 'src/views/AnalyticalFixturesScratchView.tsx (new: URL-only scratch playground)'
    - 'src/router.tsx + src/app.tsx (scratch route wiring only)'
    - 'scripts/sync-analytical-fixtures.mjs (new: the only writer of the dump)'
    - 'AGENTS.md + package.json (governed count markers; sync script entry)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'src/data/analytical-fixtures'
    - 'src/views/AnalyticalFixturesScratchView.tsx'
    - 'src/views/AnalyticalFixturesScratchView.smoke.test.tsx'
    - 'src/router.tsx'
    - 'src/app.tsx'
    - 'scripts/sync-analytical-fixtures.mjs'
    - 'AGENTS.md'
    - 'package.json'
    - src/styles/app.css
    - README.md
  out: []
invariants:
  - 'Answer-blind showcase: the src/ dump and the playground carry no answer-key material - no corpus case ids, verdicts, diagnostics, obligations, bindings.json content, or holdout.json content; fixtures only.'
  - 'No fabricated realization: the playground draws no chart, projection, or derived output that no engine produced; the realization area renders only an explicit no-engine placeholder until a real projection feeds it.'
  - 'The dump is generated, never hand-edited: scripts/sync-analytical-fixtures.mjs is the only writer of src/data/analytical-fixtures/, derived from packages/ds-contracts/analytical-fixtures/fixtures.jsonl.'
  - 'Read-only toward the governed pack: this slice imports nothing from and writes nothing to packages/ds-contracts/analytical-pack, packages/ds-contracts/analytical-fixtures, or packages/ds-codegen/src/analytical.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the showcase app; when #/scratch/analytical-fixtures is opened; then the fixture index, each fixture''s relational structure, its evidence rows, and its L0-L2 assertions all render from the generated src/ dump'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given scripts/sync-analytical-fixtures.mjs; when it runs against packages/ds-contracts/analytical-fixtures/fixtures.jsonl; then it regenerates the src/ dump with matching fixture count and no answer-key material (case ids, verdicts, diagnostics, obligations, bindings, holdout)'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given a fixture with evidence rows; when selected in the playground; then per-observation qualifiers (null kind, uncertainty, unit, provenance) render as data, and the realization area renders only the explicit no-engine placeholder - no fabricated chart or projection'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the governed doc-claims markers over the showcase structure; when the view and data dir land; then docs:check-claims passes with the updated counts'
non_functional: {}
contracts: []
