id: FIX-LINT-ZERO-01
title: 'Clear all 26 lint findings at source; enforce eslint --max-warnings=0'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-09-04T22:11:01.001Z'
updated_at: '2026-09-04T22:11:33.252Z'
created_by_session: 'sess_b5ccec4e-abee-4004-b791-90b54bfe3547'
blast_radius:
  modules:
    - 'ds-codegen/react-native emitter'
    - 'ds-react-native generated components'
    - 'opencode caws adapter'
    - 'showcase views'
    - 'audit tooling scripts'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/react-native'
    - 'packages/ds-react-native/src'
    - '.opencode/plugins/caws.ts'
    - 'src/views/PropertiesScratchView.tsx'
    - 'src/views/SettingsView.tsx'
    - 'src/views/sections/TokensTable.tsx'
    - 'scripts/behavior-realization-audit/realization.test.mjs'
    - 'scripts/swift-parity-diff.mjs'
    - 'package.json'
  out: []
invariants:
  - 'Every change to packages/ds-react-native/src component, styles, tokens, and test files is produced by the RN emitters via pnpm run generate; the only hand-authored files are __tests__/*.behavior.test.tsx companions'
  - 'No RN emitter branch special-cases a component name; all lowerings derive from contract-declared facts (cssBlocks, tokenScopes, variants, dom bindings, channels)'
  - 'After this slice, pnpm run lint runs eslint . --max-warnings=0 and exits 0; no suppression comments (eslint-disable) are added to silence the 26 findings'
  - 'pnpm run governed:rail passes in required mode after regeneration, keeping emitted bytes bound to contract+codegen+env'
  - 'Realized RN semantics must not regress the existing web targets: generated bytes for the five web packages are unchanged by this slice'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the repo at the slice commit; when pnpm run lint executes with the updated eslint . --max-warnings=0 script; then it exits 0 reporting zero warnings and zero errors'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the RN emitter lowerings for selection, sizing, switch colors, and children slots; when packages/ds-react-native behavior tests run via pnpm run test:react-native; then hand-written behavioral tests prove Calendar day selection (accessibilityState.selected + selected style + onChange), Spinner size changes the visual ring, ToggleSwitch track colors derive from declared token slots, and children render where the web main body renders them, and no generated component destructures or imports an unused binding'
  - id: A3
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the emitter changed; when pnpm run governed:rail runs; then generate:check, full regeneration, and required-mode verification all pass with regenerated bytes committed and no hand-edits under packages/ds-react-native/src outside hand-authored __tests__/*.behavior.test.tsx files'
  - id: A4
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the emitter lowering implementations; when reviewed for the no-per-component-lore invariant; then no emitter branch keys off a component name string and every lowering is driven by contract/IR facts'
  - id: A5
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the adapter, showcase, and audit-script cleanups; when the touched suites run (root vitest for audit scripts, typecheck for views, lint); then all pass with the dead imports/vars removed, PropertiesScratchView propValues/tokenValues memoized, and caws.ts using unknown plus type guards with no any'
non_functional: {}
contracts:
  - name: 'rn-semantics-realization'
    type: behavior
    path: 'packages/ds-react-native'
  - name: 'lint-zero-gate'
    type: contract-test
    path: 'package.json'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: pnpm run lint (eslint . --max-warnings=0) exits 0 on merged main c56052e9
    recorded_at: "2026-09-04T23:06:46.493Z"
    command: pnpm run lint
    exit_code: 0
    commit_sha: c56052e9
  - criterion_id: A2
    status: pass
    evidence_ref: "pnpm run test:react-native — 163 tests pass incl. new Calendar/Spinner/ToggleSwitch behavior suites proving selection, sizing, and switch-token semantics"
    recorded_at: "2026-09-04T23:06:47.307Z"
    command: "pnpm run test:react-native"
    exit_code: 0
    commit_sha: c56052e9