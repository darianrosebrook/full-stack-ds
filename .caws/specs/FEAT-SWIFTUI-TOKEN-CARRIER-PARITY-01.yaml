id: FEAT-SWIFTUI-TOKEN-CARRIER-PARITY-01
title: 'Swift token carriers measured where they live + ref arm completes the resolution chain'
risk_tier: 2
mode: feature
lifecycle_state: active
created_at: '2026-08-18T02:09:44.054Z'
updated_at: '2026-08-18T02:11:22.672Z'
blast_radius:
  modules:
    - 'packages/ds-codegen/src/frameworks/swift/swiftui (ref emission at the 4 definition sites + tests)'
    - 'packages/ds-swiftui/Sources/DsSwiftUI/Tokens/FsdsTheme.swift (ref field + resolution arm)'
    - 'scripts/token-realization-audit (swiftui carrier path fix) + docs/token-realization-audit + snapshot correction'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-codegen/src/frameworks/swift'
    - packages/ds-swiftui
    - scripts/token-realization-audit
    - docs/token-realization-audit
    - docs/current-implementation-snapshot.md
  out: []
invariants:
  - 'The audit measures every swiftui component against the file that actually carries its scopes — the inline enum in the component source — never a convention borrowed from another target.'
  - 'Swift resolution order matches RN/Compose semantics: slot-name override, then semantic ref override, then literal, then fallback.'
  - 'Every newly surfaced real slot gap carries a ledger disposition; the 49 false carrier entries are deleted, not re-ledgered.'
acceptance:
  - id: A1
    given: 'swiftui components embed per-component token scopes inline (enum NameTokens in Name.swift) consumed via resolveFsdsComponentTokens'
    when: 'the token-realization audit points its swiftui carrier at the component file itself and FsdsComponentTokenDefinition gains a ref arm (theme.tokens[name] ?? theme.tokens[ref] ?? literal ?? fallback, ref emitted from resolvesTo)'
    then: 'the audit reports swift slot realization truthfully, real gaps (native-collapse paths with no inline scopes) are ledgered with dispositions, and swift build + codegen tests stay green'
non_functional: {}
contracts:
  - name: 'FsdsComponentTokenDefinition resolution chain'
    type: behavior
    path: 'packages/ds-swiftui/Sources/DsSwiftUI/Tokens/FsdsTheme.swift'
