id: SHOWCASE-FIXTURE-PLAYGROUND-DS-CONSUMPTION-01
title: 'Analytical fixture playground consumes DS controls'
risk_tier: 3
mode: feature
lifecycle_state: draft
created_at: '2026-09-02T22:20:11.755Z'
updated_at: '2026-09-02T22:20:11.755Z'
blast_radius:
  modules:
    - 'TODO: list one or more modules this spec touches.'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'src/views/AnalyticalFixturesScratchView.tsx'
    - 'src/views/AnalyticalFixturesScratchView.smoke.test.tsx'
    - 'src/styles/app.css'
    - 'src/consumption/consumption-guard.test.ts'
  out: []
invariants:
  - 'TODO: describe one invariant this spec guarantees.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given the playground view source; when inspected; then every control is a DS component (DS Input search, DS Select filters, DS Button index entries) and no raw <input>/<select>/<button> tags remain, enforced by a consumption-guard test; given a fixture with evidence; when rendered; then qualifier/trait/grain/count labels render as DS Badge and assertion lists render via DS List; given the DS controls; when searching by id or choosing a scale via the Select combobox interaction; then the index narrows exactly as the data implies and the current filter value is visible; given the extended guard; when the negative control runs; then it bites on raw markup and stays silent on DS component tags'
non_functional: {}
contracts: []
