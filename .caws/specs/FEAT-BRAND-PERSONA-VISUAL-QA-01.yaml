id: FEAT-BRAND-PERSONA-VISUAL-QA-01
title: 'Visual QA + iteration pass on the 5 new brand personas using the Display Case grid'
risk_tier: 2
mode: fix
lifecycle_state: active
worktree: brand-personas-qa
created_at: '2026-07-30T01:04:44.327Z'
updated_at: '2026-07-30T01:11:29.767Z'
blast_radius:
  modules:
    - 'packages/ds-tokens/src/brands (targeted value fixes on the 5 personas from FEAT-BRAND-PERSONA-SCOPE-01, informed by full-grid visual QA)'
    - 'src/views/DisplayCaseView.tsx (optional: an in-page brand switcher if the global Header toggle proves awkward for rapid brand-to-brand comparison)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-tokens/src/brands'
    - 'packages/ds-tokens/generated/tokens.css'
    - 'src/views/DisplayCaseView.tsx'
    - 'src/layout/Header.tsx'
    - 'tmp'
    - packages/ds-tokens/build/generators/global.ts
  out: []
invariants:
  - 'Fixes are confined to the 5 new personas (streaming/fintech/developer/quickserve/marketplace) and, if touched, the Display Case view — the 5 pre-existing brands (default/corporate/forest/canary/monochrome) are not modified by this pass.'
  - 'Any brand-token value change is re-verified against tokens:check-contrast and tokens:check-brand-refs before commit, not assumed safe by inspection alone.'
acceptance:
  - id: A1
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given each of the 10 brands is activated on the Display Case grid (every component, every variant, light+dark); when the grid is screenshotted per brand; then no component renders with broken/illegible contrast, a missing accent application, or a shape/elevation combination that looks unintentional'
  - id: A2
    given: 'The spec implementation is complete.'
    when: 'The acceptance statement is evaluated.'
    then: 'given issues found during the visual pass; when the corresponding brand token file is fixed; then tokens:check-contrast, tokens:check-brand-refs, and governed:rail stay green with zero drift'
non_functional: {}
contracts:
  - name: 'brand-visual-qa'
    type: contract-test
    path: 'packages/ds-tokens'
