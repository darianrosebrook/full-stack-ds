id: FIX-SWIFTUI-TOKEN-RESOLVER-01
title: 'Fix FsdsTheme nested-optional resolver trap; add XCTest regression target'
risk_tier: 2
mode: fix
lifecycle_state: active
created_at: '2026-08-15T18:42:45.282Z'
updated_at: '2026-08-15T18:58:29.757Z'
blast_radius:
  modules:
    - 'packages/ds-swiftui/Sources/DsSwiftUI/Tokens/FsdsTheme.swift (resolver staging + conditional a11y helpers)'
    - 'packages/ds-swiftui/Tests/DsSwiftUITests (new XCTest target + regression tests)'
    - 'packages/ds-swiftui/Package.swift (test target declaration)'
    - 'packages/ds-codegen/src/frameworks/swift/swiftui (emitters use conditional a11y helpers; regeneration)'
    - '.github/workflows/ci.yml (swift test step)'
    - 'docs/current-implementation-snapshot.md + packages/ds-swiftui/README.md (fix claims + ledgered gaps)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-swiftui'
    - 'packages/ds-codegen/src'
    - '.github/workflows/ci.yml'
    - 'docs/current-implementation-snapshot.md'
    - packages/ds-codegen/__golden__
  out: []
invariants:
  - 'Never assign a ?? chain directly into an optional-Value dictionary subscript — the chain type-infers at double-optional depth and nil literals wrap to .some(nil), short-circuiting later arms. Staging into a local is the enforced pattern; the regression test pins it.'
  - 'The generated-component API and FsdsTheme public surface otherwise unchanged; resolved outputs stay [String: FsdsTokenValue?] this slice (the [String: FsdsTokenValue] reshape is a follow-up design decision).'
  - 'A11y fixes must preserve intrinsic labels: modifiers attach only when a value exists, never with an empty string.'
acceptance:
  - id: A1
    given: 'a fallback-only token definition (the majority of generated slots)'
    when: 'resolveFsdsComponentTokens and resolveFsdsLayeredTokens resolve it'
    then: 'the slot resolves to the fallback value instead of nil — the nested-optional ?? chain is staged in a local before the [String: FsdsTokenValue?] subscript assignment'
  - id: A2
    given: 'a new SwiftPM test target with resolver regression tests'
    when: 'swift test --package-path packages/ds-swiftui runs'
    then: 'fallback-only, literal, theme-override, and layered-override cases all pass (they fail against the unfixed resolver)'
  - id: A3
    given: 'the macOS CI swift job'
    when: 'the workflow runs'
    then: 'it executes swift test for the package in addition to the existing smoke and example-compile steps, so resolver regressions fail CI'
  - id: A4
    given: 'the empty-string accessibility antipattern (nil-coalesced labels/identifiers erasing intrinsic text)'
    when: 'components regenerate'
    then: 'Button and Stack apply labels/identifiers through conditional runtime helpers (fsdsAccessibilityLabel/fsdsAccessibilityIdentifier) that attach nothing when nil; prior outputs byte-updated via regeneration and all 15 compile'
  - id: A5
    given: 'the deduper pilot''s remaining findings (dark-mode light-only hex, hover/focus-ring/radius realization depth, FsdsField box-in-box row form, semantic-color override case, split-repo distribution)'
    when: 'this slice closes'
    then: 'they are recorded as ledgered known gaps in the package README and snapshot, not silently dropped'
non_functional: {}
contracts:
  - name: 'fsdstheme-resolver-regression'
    type: contract-test
    path: 'packages/ds-swiftui/Tests/DsSwiftUITests/FsdsThemeTests.swift'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: "FsdsTheme.swift: both resolvers stage the chain in a local before subscript assignment; the compiler's own warning on the trap form (left operand non-optional at outer depth) confirms the mechanism"
    recorded_at: "2026-08-15T18:59:07.996Z"
  - criterion_id: A2
    status: pass
    evidence_ref: "swift test --package-path packages/ds-swiftui: 6/6 pass; mutation-falsified live — reverting one resolver to the trap form fails 2 tests (fallback-only + override), restored fix passes"
    recorded_at: "2026-08-15T18:59:10.152Z"
  - criterion_id: A3
    status: pass
    evidence_ref: "ci.yml native-compile-rail-swift job gains 'ds-swiftui token runtime tests' step (swift test) after the example compile; smoke passes untouched"
    recorded_at: "2026-08-15T18:59:12.722Z"
  - criterion_id: A4
    status: pass
    evidence_ref: Button.swift + Stack.swift use fsdsAccessibilityLabel/fsdsAccessibilityIdentifier (conditional helpers in FsdsTheme.swift); 15 components regenerated; package + example swift build exit 0; Switch golden refreshed
    recorded_at: "2026-08-15T18:59:15.764Z"
  - criterion_id: A5
    status: pass
    evidence_ref: "packages/ds-swiftui/README.md carries the pilot known-gaps ledger (dark mode, hex-only seam, hover/focus/radius, Field row form, distribution); snapshot row records the fix + pilot provenance"
    recorded_at: "2026-08-15T18:59:19.841Z"