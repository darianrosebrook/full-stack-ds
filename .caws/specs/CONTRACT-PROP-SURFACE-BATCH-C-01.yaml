id: CONTRACT-PROP-SURFACE-BATCH-C-01
title: 'Prop-surface migration Batch C (input/form/selection): styled→designed + structured propType'
risk_tier: 3
mode: refactor
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge prop-surface-batch-c at 6893148132a293c0229c229a17a94605a308913e'
created_at: '2026-06-05T17:50:35.168Z'
updated_at: '2026-06-05T17:59:59.632Z'
blast_radius:
  modules:
    - 'packages/ds-contracts/components (Batch C: Calendar, Checkbox, Field, Input, OTP, Select, Shuttle, Switch, TextField, ToggleSwitch)'
  data_migration: false
operational_rollback_slo: 5m
scope:
  in:
    - 'packages/ds-contracts/components/Calendar/**'
    - 'packages/ds-contracts/components/Checkbox/**'
    - 'packages/ds-contracts/components/Field/**'
    - 'packages/ds-contracts/components/Input/**'
    - 'packages/ds-contracts/components/OTP/**'
    - 'packages/ds-contracts/components/Select/**'
    - 'packages/ds-contracts/components/Shuttle/**'
    - 'packages/ds-contracts/components/Switch/**'
    - 'packages/ds-contracts/components/TextField/**'
    - 'packages/ds-contracts/components/ToggleSwitch/**'
  out: []
invariants:
  - 'Authority split preserved: contracts declare prop semantics, IR normalizes, emitters lower PropTypeIR, the rail validates. Contract-only batch; the two generic gaps (array defaults, builtin-ref resolution) were promoted to a shared codegen/schema fix BEFORE this batch, not patched here.'
  - 'Generated framework bytes (ds-react/vue/svelte/angular/lit src) are unchanged by every migration, proven by git diff --exit-code before and after the rail. This is the first end-to-end proof of real authored array and union shapes through all five emitters.'
  - 'array/union/callback/node/ref lower byte-identically; Date is propType {kind:ref, to:"Date"} (ambient global). ref is NOT used for the top-type keyword unknown.'
  - 'Declared residuals (kept as legacy type, reported as not-yet-Button-quality): Field.value, Field.defaultValue, Field.onChange (unknown-typed, type-erased form-wrapper affordances), Field.validate (anonymous-object param), Select.filterFn (gratuitous parenthesized callback — no clean generic printer rule, no cosmetic drift accepted). These are honest residuals, not silenced gaps.'
  - 'styled members never carry propType; designed members author exactly one of type | propType; no dual-authoring. Components whose every consumer prop is V2-expressible report migrated; Field and Select report partial due to declared residuals.'
acceptance:
  - id: A1
    given: 'Calendar (union Date|Date[]|null), Select (array SelectOption[], union string|string[]), Shuttle (array string[]) — first real authored array/union shapes'
    when: 'pnpm run generate -- --target=all <10 components> is run'
    then: 'git diff --exit-code over the five generated src trees reports zero drift — array and union are rail-proven end-to-end across all five emitters, closing the same evidence gap A closed for enum/callback and B for zero-param/non-void callbacks.'
  - id: A2
    given: 'The fully-V2-expressible Batch C components (Checkbox, Input, OTP, Switch, TextField, ToggleSwitch, Calendar, Shuttle)'
    when: 'governed:rail + PropTypeIR/A2UI/inventory focused tests run'
    then: 'They pass, rail is overall:pass, and the inventory reports them migrated (a2uiStructuredSource=true); Date refs resolve (unresolvedRefs stays 0).'
  - id: A3
    given: 'Field and Select with declared residuals (Field.unknown props + validate; Select.filterFn)'
    when: 'The batch migrates everything V2 can express cleanly'
    then: 'Those residuals keep legacy type with zero generated drift, and Field/Select are reported partial (not Button-quality) — the standard is not weakened, the residual ledger is explicit for the CERTIFY spec to decide.'
  - id: A4
    given: 'The whole corpus after Batch C merges to main'
    when: 'governed:rail runs on main followed by the generated-tree git diff'
    then: 'The rail passes in required mode and the post-rail diff over all five generated src trees is clean.'
non_functional: {}
contracts: []
