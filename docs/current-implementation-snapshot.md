---
doc_id: REF-CURRENT-IMPLEMENTATION-SNAPSHOT-001
authority: canonical
status: active
title: Current Implementation Snapshot
owner: "@darianrosebrook"
updated: 2026-09-17
governs:
  - README.md
  - docs/**/*.md
  - fsds.targets.json
  - .github/workflows/ci.yml
  - e2e/runtime-rail.spec.ts
  - packages/ds-codegen/**
  - packages/ds-contracts/**
  - packages/ds-iconography/**
---

# Current Implementation Snapshot

This is the routing document for current project state. Read it before older architecture notes, ADRs, and successor-work docs when deciding whether a surface is doctrine, partial machinery, or proven machinery.

**How to read this document's freshness.** Two different things carry evidence here, and conflating them is how this header went stale before:

1. **Last full-sweep reconciliation:** `main@c5cda2c9` (2026-07-21, `DOC-SNAPSHOT-RECONCILE-02`). At that commit — and only at that commit — *every* row was re-verified against live code by dedicated read-only passes: five drift lanes (recursive composition, composer slot-projection, textOverflow/RN IR-authority, native compile lanes + rail descriptors, iconography) plus a staleness audit of every pre-existing row.
2. **Targeted re-sweep:** `main@90804abc` (2026-08-17, `DOC-SNAPSHOT-RESWEEP-01`). All 33 rows were re-verified against live code — every cited path, count, and spec status re-derived. Result: **24 rows accurate as written, 9 drifted** — 6 stale counts and 3 under-claims. **Zero over-claims and zero broken evidence paths**: no row claimed more than the code supports, and every cited file, test, and spec resolved. The 9 were corrected in this pass.
3. **Rows amended since either sweep:** the ledger is edited continuously as slices close. Those rows carry their own evidence in-row (spec ids, merge shas, script names) and are not vouched for by this header.
4. **Targeted oracle/authorship reconciliation:** feature commit `3022618b` (2026-09-04, `FIX-A11Y-ORACLE-IDREF-01`). This was not another whole-ledger sweep. It re-derived the target-selection, runtime-preview, a11y-oracle, test-governance, and authorship-review claims amended below, and measured the initial curated contract-mutant catalog end to end.

**What the re-sweep learned about this ledger's failure mode.** Every defect was a *count* or an *omission*, never a false capability claim. The counts that rot are the ones restating a number a live source already derives — audit gap totals, corpus sizes, self-test tallies. Prefer citing the deriving source (`known-gaps.json`, the component-token audit output) over pasting its current value. Two under-claims were campaigns that finished without the row being updated, so the ledger sent readers to redo completed work — the inverse failure, and one a status-only reading of "closed specs" would have caught.

So: **trust a row's own cited evidence, not a blanket header claim.** If a row cites a spec id or merge sha, that is its provenance. If it cites neither, treat it as unverified until someone re-derives it. `git log -- docs/current-implementation-snapshot.md` shows when any given row last moved.

A re-sweep is due whenever the gap between (2) and HEAD stops being legible — at that point re-run the reconciliation and record it as a new numbered entry above, rather than editing an existing one.

Known surfaces still without a row: the visual-fidelity/token-enrichment campaign (its outcomes surface indirectly through the styling-realization audits row's gap counts) — recording it row-grade requires its own verification pass.

This document is not a replacement for the underlying architecture docs. It is an index of claim strength: what is implemented, what the evidence actually proves, what it does not prove, and which older docs should no longer be read as the only truth surface.

## Freshness rule

Use this order when answering "what is true now?":

1. Current implementation snapshot.
2. Code and workflow surfaces at the cited commit.
3. Implemented/proven docs with `verified_at_commit`.
4. Architecture and reference docs.
5. the machine-local successor ledger (`docs/internal/successor-work.md`, when present) only for remaining work, not as a statement that everything listed is unimplemented.

If an older doc says a surface is future work but this snapshot identifies an implemented surface, treat the older language as stale until that specific doc is reconciled.

## Current claim ledger

| Surface | Current claim | Claim strength | Evidence surface | Non-claims | Next admissible move |
|---|---|---|---|---|---|
| Contract corpus layout | Component contracts live under `packages/ds-contracts/components/<Name>/<Name>.contract.json`, with sidecars for tokens, styles, and usage examples. The loader, not a hand-maintained README count, is the authority for corpus discovery. | implemented | `packages/ds-codegen/src/contracts-fs.ts`; `vite-plugin-fsds-data.ts` | A visible component count in prose is not an authority surface. | Done — README no longer carries a hand-maintained count (commit `7a325195`); the loader remains the sole count authority. Remaining discipline: derived docs must reconcile on corpus growth (see the mobile-parity row's `--check` drift for a live counterexample). |
| Component design property bindings | Style sidecars expose a closed common property vocabulary through independent namespaced override addresses. Unset overrides retain the authored token/fallback chain. Shared box controls reset at component boundaries and use ordered CSS layers for shorthand, axes, and sides; defaults remain at consumers. The inspector edits design properties by source part/condition. Semantic non-use is informational. | implemented / sampled Web DOM runtime evidence | `COMPONENT-DESIGN-BINDINGS-01`; `packages/ds-codegen/src/design-properties.ts`; `scripts/migrate-design-bindings.mjs`; `e2e/design-bindings.spec.ts`; `e2e/editor-binding-rail.spec.ts`; [design and proof limits](architecture/design/component-design-bindings.md) | The new override API is Web DOM; native targets project their own consumed slots. Figma carries metadata only. Unused legacy declarations are retired by the component-token consumption gate. CSS consumption is not proof that every selector is reachable or every retokening is visually sound. | [Portability recon](architecture/design/design-contract-recon.md) retains this Web interface. `NATIVE-STYLE-SUPPORT-01` realizes static root clipping in RN and SwiftUI region composers and explicitly rejects unsupported composer radius units. The macOS Card pixel probe closes those witnesses; conditional/part styling, other native emission classes, relative radius support, and device parity remain outside this evidence. |
| Retokened media page attempts | Spotify-inspired desktop and Pinterest-inspired feed fixtures use generated React components, original artwork and scoped appearance controls. Image honors sizing hints and ratio/fit/focal-position props, with design overrides and fallback clearing tested across Web DOM targets. | bounded browser facts and reviewed page renders | `RETOKEN-PAGE-ATTEMPTS-01`; `IMAGE-MEDIA-CONTROLS-01`; `e2e/{retoken-pages,image-media}.spec.ts`; [requirements, gaps and repair](architecture/design/retoken-page-attempts.md) | No pixel-identical product reproduction, full playback, personalized feed, native media parity, image-error recovery or virtualized masonry claim. | Extend the concrete product capabilities when required; these witnesses do not justify token-contract inversion or an arbitrary CSS property bag. |
| Five Web DOM framework generation | React, Vue, Svelte, Angular, and Lit remain the primary executable Web DOM family. Codegen emits framework-specific component source, tests, styles, behavior primitives, and barrels from the same contract/IR path. | implemented / rail-admitted | `packages/ds-codegen/src/frameworks/*`; `packages/ds-{react,vue,svelte,angular,lit}/src`; `pnpm run governed:rail` | This proves cross-framework Web DOM realization, not substrate-neutral UI semantics. | Promote target-family IR boundaries out of implicit Web DOM assumptions. |
| Unity UI Toolkit pilot | The registry allowlist emits Switch, Accordion, Popover and Tabs into a local UPM package. Recorded Editor tests cover bounded behavior and runtime styling; a separate fresh macOS Player consumer loads packaged UXML/styles and passes the named interaction/layout checks. | bounded local engine witnesses; outside TypeScript admission rail and CI | `UNITY-CAPABILITY-PLAYER-01` (merge `c8b04712`); `UNITY-RUNTIME-STYLING-01` (merge `5be1f30d`); `scripts/unity-tests.mjs`; `scripts/unity-player.mjs`; [Unity target](architecture/unity-target.md). Player run `9e7113e5-da40-4802-9e9b-8b8c0df36f90` reports Unity 6000.5.3f1, OSXPlayer, `editor: false`, and all named checks passed. Local receipt: `tmp/unity-capability-player/9e7113e5-da40-4802-9e9b-8b8c0df36f90/result.json`. | Synthetic UI events are not physical-input evidence. No other-platform, declared-minimum-version, screen-reader, controller, full styling/motion or visual parity proof. The capability inventory accounts for unsupported/unassessed surfaces; it does not make them supported. | Enforce unsupported capability decisions and extend independent consumer witnesses before broadening admission. |
| Godot comparative pilot | The same registry allowlist emits GDScript, PackedScene and Theme resources. Frozen semantic traces match expected sequences in Unity Editor panels and Godot runtime/export. The Godot fixture also saves/reopens a scene through editor APIs and launches a fresh macOS export. | bounded local comparative witness; outside TypeScript admission rail and CI | `GODOT-COMPARATIVE-PILOT-01` (source `62f37334`, merge `8410fbc0`); `packages/ds-godot/fixtures/traces.json`; `scripts/{godot-pilot,engine-comparison}.mjs`; [Godot target](architecture/godot-target.md). Run `5af9bd21-83b8-4e79-ad7c-6fa825ccc9a2` passed; local receipt `tmp/godot-comparative/comparison.json` records source/trace hashes and expected state sequences. | Shared traces do not execute in a Unity Player. Godot's changed-token probe proves a loaded Theme value, not rendered consumption or changed pixels. No physical input, accessibility, arbitrary composition, full visual parity or other-platform claim. Local receipts are regenerable evidence, not committed artifacts or CI gates. | Add a rendered token-consumption oracle and independent consumer/input witnesses; admit further semantics only through explicit capability decisions. |
| Generated artifact admission rail | Required-mode rail is wired into CI and binds generated artifacts to manifest, contract bytes, codegen source bytes, framework checks, and bounded environment. CI also refuses generated drift after regeneration. | implemented / CI-gated | `.github/workflows/ci.yml`; `docs/specifications/admission-rail.md`; `docs/specifications/governed-ci.md`; `packages/ds-codegen/src/validation/**` | It does not prove determinism, full environment attestation, per-file semantic proof, visual quality, or accessibility adequacy. | The former next move (fold every rail participant into the manifest's framework vocabulary) was superseded by the closed descriptor specs (`RAIL-ADMISSION-DESCRIPTOR-INTERFACE-01`, `RAIL-TARGET-SELF-DECLARED-DESCRIPTOR-02`), which deliberately chose the opposite design: rail participants self-declare admission facts via a descriptor registry, and native lanes were admitted through a parallel narrow `RailTargetId` rather than widening `FrameworkId` (rationale committed in `validation/types.ts`). Remaining: extend the descriptor pattern to target-pack provenance in the manifest, and wire `rail-toolchain-coupling-audit --check` into CI. |
| Admission-descriptor architecture + native compile lanes | Six rail-admitted TypeScript targets (React, Vue, Svelte, Lit, Angular, React Native) self-declare `outputTreeRelPath`, source extensions, admission plan, and report rank through `AdmissionDescriptor`; registry construction rejects duplicate ids, id/plan disagreement, and non-contiguous ranks. Figma, SwiftUI, and Jetpack Compose are registered generation targets but do not declare `railFrameworkId`, so they are outside that six-target admission registry. Separately, CI runs bounded Kotlin/Swift compiler lanes and the native settings example consumers; generated SwiftUI/Compose roots are also included in byte-drift checks. | implemented / CI-gated; coupling audit remains local-only | `packages/ds-codegen/src/validation/{admission-descriptor.ts,types.ts}`; `fsds.targets.json`; `.github/workflows/ci.yml`; `scripts/rail-toolchain-coupling-audit.mjs`; specs `RAIL-ADMISSION-DESCRIPTOR-INTERFACE-01`, `RAIL-TARGET-SELF-DECLARED-DESCRIPTOR-02`, `RAIL-NATIVE-COMPILE-LANE-COMPOSE-SMOKE-03`, `RAIL-NATIVE-COMPILE-LANE-SWIFT-SMOKE-04` | Descriptor admission, registered generation, drift gating, and compiler/example admission are distinct. The compiler lanes prove their declared fixtures/consumers compile; they do not prove generated component correctness, Android SDK/device behavior, simulator behavior, native visual parity, or accessibility adequacy. | Wire `rail-toolchain-coupling-audit --check` if its independent coupling signal earns CI cost; otherwise keep target-family claims derived from their separate authorities. |
| Runtime visual proof rail | A separate CI job exercises narrow contract facts in real Chromium through React, Vue, Svelte, Lit, and Angular preview mounts. The authoritative suite is `e2e/runtime-rail.spec.ts`; `src/runtime/rail-coverage.ts` is the in-showcase coverage projection, and its coherence test fails if the named component/framework/prop surface drifts from the rail. Current fact families include CSS-var fallbacks, count/array/object iteration, relationship state, content transforms, and a first Select trigger interaction. All five previews accept non-default props through `fsds:config`; Angular uses a synthesized-host bus rather than the shared callback-capture wrapper. | implemented / CI-gated for fact assertions | `e2e/runtime-rail.spec.ts`; `src/runtime/{rail-coverage.ts,rail-coverage.test.ts,FrameworkPreview.tsx,demos.ts}`; `.github/workflows/ci.yml`; `e2e/render-binding-rail.spec.ts`; `playwright.config.ts` | The rail asserts named facts, not broad behavioral parity, accessibility adequacy, or visual correctness. The coverage projection says what committed suites assert, not whether the latest CI run passed. The Select callback-capture interaction excludes Angular because its synthesized host does not use `config-entry.ts`'s callback wrapper. Screenshot baselines are darwin-only and skipped under `CI=true`. | Grow the projection with each new runtime fact; persist freshness-bound CI status separately if the showcase should report last-run state. |
| Iteration and binding semantics | IR-level `iterate` lowers to framework-native loop constructs across the executable targets. OTP is the production count-iteration consumer; Calendar iterates `days: Date[]`; Select, Shuttle, and Walkthrough are the other production array consumers. Closed `channelCall` and `channelUpdate` bindings carry item/index payloads without arbitrary code. The grammar now also admits one closed value projection, `project:dateDayOfMonth(source)`, so Calendar renders visible day labels from the same `Date` item it commits; all six executable targets lower the projection through one shared semantic helper. | implemented / runtime-proven for prior iteration facts; cross-target codegen and showcase-render proven for date projection | `packages/ds-codegen/src/{ir.ts,dom-tree.test.ts}`; `packages/ds-codegen/src/frameworks/iteration-bindings.test.ts`; `e2e/runtime-rail.spec.ts`; contracts Select/Shuttle/Walkthrough/Calendar/OTP; spec `COMPOSITE-CONTROL-EXAMPLE-INTEGRITY-01` | `dateDayOfMonth` is the only admitted projection and requires a `Date` iteration item in its corpus witness. OTP `setCharAt` keeps only the last character of a multi-char payload; whole-paste distribution and Calendar range accumulation remain out of scope. The new projection has no dedicated cross-framework Chromium fact. | Add browser interaction/projection facts where they materially raise confidence; extend the closed projection or update vocabularies only for demonstrated corpus needs. |
| Axis-derived surface and control capabilities | Surface controller routing derives from normalized attachment (`part`, `selector`, `viewport`, `flow`) plus positioning rather than a tooltip/popover kind allowlist; a menu-shaped falsifier reaches the anchored controller without emitter changes. `formControl` declares one rendered control part, value channel/model, and commit semantic independently from `form` submission; Input, TextField, Checkbox, Switch, ToggleSwitch, and Command are its consumers. `compositeControl` separately owns an iterated item part, channel, interaction model, commit semantic, and closed update expression; Select options, Calendar days, and OTP fields are its first consumers. The IR injects the authoritative event after proving the part is interactive, multiple, and inside an iteration, so all executable targets reuse their existing binding lowering with no component-name lore. Ambient Field association still targets only `formControl.part`. | implemented / schema, fail-loud IR, and cross-target codegen-test proven | `packages/ds-contracts/component.contract.schema.json`; pilot contracts; `packages/ds-codegen/src/{ir.ts,semantics.ts,form-control-ir.test.ts,composite-control-ir.test.ts,surface-ir.test.ts}`; generated executable projections; specs `CAPABILITY-AXIS-FOUNDATION-01`, `COMPOSITE-CONTROL-EXAMPLE-INTEGRITY-01` | Composite ownership proves routing and emitted update shape, not complete selection algorithms: Calendar range accumulation and OTP whole-paste distribution remain out of scope. Button-backed native form serialization, Field form-prop propagation, and browser/assistive-technology proof also remain open. Lit intentionally does not bridge Field idrefs across shadow roots. | Add focused runtime interaction witnesses before raising the proof ceiling; model richer range/paste operations only when their closed semantics can be shared across targets. |
| Content transforms (highlight) | `anatomy.dom[].content` admits an object form — a contract-governed content-transform directive (`highlight` is the only admitted transform). A pure, synchronous, deterministic tokenizer splits the source prop into a closed `CodeBlockTokenType` token stream; every web framework renders one text-only `<span>` per token (token part class + `data-token` kind attribute) and degrades to a single plain run when the gate prop is false. The canonical tokenizer module is emitted byte-identical into all five web packages by the CLI (unconditional per build, mirroring Stack primitive emission); React-Native and SwiftUI degrade to plain text, figma descriptors by omission. Token color slots are consumed via per-kind `[data-token]` compound selectors in the styles sidecar. | implemented / rail-admitted + test-proven (identity + injection safety) + runtime-proven (Chromium rail: golden token-stream shape + gate-off plain run, all five frameworks) | merge `607f51e9` (spec `FEAT-CODEBLOCK-HIGHLIGHT-01`); `e2e/runtime-rail.spec.ts` (`RUNTIME-RAIL-CODEBLOCK-HIGHLIGHT-01` — expected kinds loaded from the frozen golden fixtures, never hand-typed); `packages/ds-codegen/src/highlight/` (tokenizer + frozen golden fixtures); `packages/ds-{react,vue,svelte,angular,lit}/src/primitives/highlight/` (emitted runtime + `__tests__`); `pnpm run governed:rail` | Regex-approximate lexing, not a parse tree — a syntax palette, not semantic highlighting. Proven: cross-framework token-stream identity (golden fixtures, unit + Chromium rail) and no-HTML-interpretation; NOT proven: visual quality; a11y adequacy of the colored output is component-gated by the ancestry contrast lens (failing pairs ledgered — see the Token governance row). | A second transform name requires extending the schema's closed set, `ContentTransformIR`, and every emitter's lowering — grammar-first, no per-component-name lore. |
| Content transforms (markdown — the second-transform proof) | The content grammar admits a SECOND transform, proving the family generalizes beyond inline spans: `markdown` declares closed blockPart/markPart vocabularies (heading/paragraph/unorderedList/orderedList/listItem/codeBlock/blockquote; emphasis/strong/code/link), every kind mapping to an anatomy part whose tag matches the kind's DOM contract (schema + IR validated). One canonical pure/synchronous/deterministic parser (v1 closed subset; raw HTML NEVER interpreted; link hrefs protocol-sanitized at parse time) is emitted byte-identical into every web package. The IR fact lowers idiomatically in all five frameworks — React module-scope recursive JSX helpers, Lit recursive html`` helpers, Vue local defineComponent tree over h()/Fragment, Svelte 5 recursive snippets, Angular companion standalone renderers (fsds-markdown-block/fsds-markdown-mark) with a markdownBlocks class getter — with RN/SwiftUI plain-text degradation and figma omission. The Markdown component contract (corpus grows) realizes it. | implemented / rail-admitted + test-proven (identity + injection) + runtime-proven (Chromium rail: golden tree shape + literal-HTML doctrine, all five frameworks) | spec `FEAT-MARKDOWN-CONTENT-TRANSFORM-01`; `packages/ds-codegen/src/markdown/` (parser + frozen golden fixtures); `packages/ds-*/src/primitives/markdown/` (emitted runtime + `__tests__`); `e2e/runtime-rail.spec.ts`; `pnpm run governed:rail` | v1 closed subset: no tables, images, nested lists, raw HTML, or highlight-chaining inside code blocks — each a recorded follow-up. Regex-free but line-oriented; not a CommonMark-conformant parser. Proven: cross-framework tree identity + no-HTML-interpretation + href sanitization; NOT proven: visual quality or a11y adequacy beyond the contrast-gated root pair. | Third transform needs the same grammar-first path (schema closed set, IR union member, every emitter's lowering) — which is now proven twice-shaped. |
| Contract-declared behavior realization (2026-07-22 batch) | Contract-declared interactivity now lowers to live behavior where the corpus declares it: Select's trigger toggles the `open` channel in all six targets (`FIX-SELECT-OPEN-TOGGLE-WIRE-01`, `9a64f310` — pure contract wire, zero emitter change; lit/angular generated-test emitters now derive toggle expectations from the channel's contract default); Accordion is a live contract-driven repeated-disclosure compound in all five web frameworks — context provider + Item/Trigger/Content subcomponents, ancestor-aware `getInteractiveItemPart` + `isDisclosureContainer` in the single shared detection module, no tab-ARIA leakage, no component-name literals (`FIX-COMPOUND-CONTAINER-ANCESTOR-PREDICATE-01`, `2cccbdf7`); lit/angular prop reads are default-aware at the accessor layer, restoring undefined-prop parity with react's parameter defaults — Icon size hint-map miss and Skeleton `decorative` a11y divergence fixed (`FIX-UNDEFINED-PROP-ACCESSOR-DEFAULTING-01`, `2800d15b`); portal computation is wired-or-deleted — React portals Dialog/Sheet/Toast root-to-body via the generic `portalsRootToBody` predicate, all other portal scaffolding removed corpus-wide (`FIX-PORTAL-CONSUMPTION-01`, `6051c40a`). | implemented / test-proven per framework (jsdom + Jest), falsified per slice; portal DOM-parent proof jsdom-level | merge commits `9a64f310`, `2cccbdf7`, `2800d15b`, `6051c40a`; `packages/ds-codegen/src/frameworks/react/{hook-source.ts,compound-container-detection.test.ts}`; `frameworks/{lit,angular}/binding-defaults.test.ts`; `packages/ds-codegen/src/semantics.ts` (`portalsRootToBody`); per-framework Accordion/Select behavioral tests | Real-browser interactivity proof exists for exactly one interaction: Select trigger click → `onOpenChange(false)` + listbox unmount, asserted in Chromium for react/vue/svelte/lit via the preview callback-capture surface (`FEAT-PREVIEW-CALLBACK-CAPTURE-01` `867c0431` + `FIX-INTERACTION-FACT-SKIP-SCOPE-01` `7de140f8` — the first landing silently skipped all four facts via a describe-scoped `test.skip`; 74 passed / 1 named angular skip after the fix). Everything else remains jsdom/Jest-proven only; focus order, `:has()`, and stacking-context escape are still unproven in-browser. Angular/Lit read uncontrolled `defaultValue` at construction (pre-existing Tabs-shaped input-timing constraint). Anchored surfaces (Popover/Tooltip) are positioned AND content-portaled in all five frameworks (`FEAT-ANCHORED-SURFACE-XFW-01`, `c99cf445`): each package ships a mirrored anchored-position primitive (same options/state shape and flip-shift/clamp math as react's `useAnchoredPosition`), and the surface emitters wire fixed positioning + `data-placement` + hidden-until-ready plus per-framework content portal (vue Teleport, svelte `use:portal`, angular body-append, lit content-host move) via the shared `anchoredPortalsContentToBody` predicate. Real-Chromium placement facts pass for vue/svelte/lit (body-parenting, `position:fixed`, exact 8px anchor gap, sub-pixel centering, `data-placement`) — that proof immediately caught two defects the static gates structurally cannot see, fixed under `FIX-ANCHORED-BROWSER-DEFECTS-01` (`ebeb5309`): lit's `display:contents` content host made `position:fixed` a layout no-op, and svelte's placement option called `getContext` on post-init recomputes (position tracking died after the first frame). Angular anchored placement has no browser-level proof (no harness path through its AOT preview pipeline); its proof is jsdom/Jest-level only. | All named successors executed: lit primary-attr defaulting (`FIX-LIT-PRIMARY-ATTR-DEFAULTING-01`, `dd5c8861`), portal contract adjudication (`FIX-PORTAL-CONTRACT-ADJUDICATION-01`, `d0d4d7e9` — Command portals via a new centered surface block; Calendar/Select/Walkthrough overclaims removed), and full-overlay portal parity in all five frameworks (`FEAT-PORTAL-MECHANISM-CROSS-FRAMEWORK-01`, `04999ceb` — vue `<Teleport to="body">`, a new svelte `portal` action, angular lifecycle body-append, lit host-element move preserving shadow styles; falsified DOM-parent tests per framework; vue/svelte generated tests resolve portaled roots from document.body with afterEach resets since neither package auto-cleans). The anchored-surface successor landed (`FEAT-ANCHORED-SURFACE-XFW-01` + `FIX-ANCHORED-BROWSER-DEFECTS-01`; see the non-claims column). The Walkthrough `steps[].anchor` residual landed (`FEAT-WALKTHROUGH-ANCHOR-CONSUMPTION-01`, merge `4eee3141`): the contract schema gained a selector-source anchor variant (`surface.anchor.selector = {prop, path, indexChannel}`, oneOf with the trigger-part form), the IR a `SurfaceSelectorAnchorIR` fact with fail-loud validation (prop exists + array-typed, channel exists; openTriggers stays optional — tours open programmatically), and Walkthrough declares a `kind: coachmark` surface with anchored positioning and honestly re-admits `portal.enabled`. All five web emitters' GENERIC component path (not the compound surface emitter) consume the shared `selectorAnchoredRootPortal` predicate: the whole root panel portals to body (react `renderInPortal`, vue Teleport, svelte `use:portal`, angular body-append, lit host move + inline `display:block`) and tracks `document.querySelector(steps[step].anchor)` through the mirrored anchored-position primitives, re-resolving on step change. Real-Chromium proof: 36/36 facts across react/vue/svelte/lit — body parenting, `position:fixed`, `data-placement`, exact 8px gap, 0px center delta, AND live re-anchoring to `#step-2` when the index changes — with zero page errors. That proof caught a third browser-only defect class (`FIX-WALKTHROUGH-VUE-ANCHOR-RESOLUTION-01`, merge `bfbef9f3`): vue's anchor resolution via `computed` first evaluated while the app tree was still detached, cached the null `querySelector` result forever (no reactive dep changes to re-run it), and the panel stayed hidden — jsdom suites and typecheck stayed green throughout; fixed with a post-flush `watchEffect` into a `shallowRef`. Non-claims: Angular's Walkthrough lowering is jsdom/Jest-proven only (no browser harness path), and its anchor-resolution `effect()` tracks only the step signal — a `steps` input mutation alone does not re-resolve the anchor (latent, unprovable here). Post-merge runtime rail: 74 passed / 1 named angular skip. |
| Behavior-realization audit rail | A CI-gated static audit (`pnpm run audit:behavior-realization`) derives behavior obligations from the live contract corpus each run — 20 event-wire obligations × 5 frameworks (all four `events` value forms incl. `channelCall` and the `channelUpdate` operations — OTP's input wire is a derived obligation, not allow-listed), 2 compound/disclosure realizations, portal positive/orphan-negative obligations (Command joined the positives, Calendar/Select/Walkthrough dropped out by derivation after `FIX-PORTAL-CONTRACT-ADJUDICATION-01`) — and verifies each is realized in the committed generated source, importing detection predicates from compiled `ds-codegen/dist` (no re-implemented detection, no hardcoded component names; a source-audit test enforces the latter). Runs in the CI gate job after `governed:rail` + drift check, ~55ms over 49 components; wired green at birth. | implemented / CI-gated; static source-level | `scripts/behavior-realization-audit/{audit.mjs,realization.test.mjs}` (self-tests whose count is derived, not frozen — several `check(...)` sites loop over corpus-derived obligations, so the executed total grows with the corpus (31 at the 2026-08-17 sweep): derived floors ≥ not frozen =, typed skips, OTP zero-derivation, synthetic falsifications); `.github/workflows/ci.yml`; falsified live (stripped Select option handler → exit 1 naming the obligation); spec `RAIL-BEHAVIOR-REALIZATION-AUDIT-01` (closed, merge `9a8d7eb6`) | A realized handler token proves the wire exists in emitted source, NOT that it fires, updates state, or is a11y-adequate — runtime truth stays with the Playwright rails. react-native/figma are not audited. | Portal positives are now five-framework (`RAIL-AUDIT-PORTAL-XFW-OBLIGATIONS-01`, `98eadc41` — `consume-root-portal` checks each framework's mechanism token, falsified by stripping vue Dialog's Teleport; the audit's own self-test, found silently stale after the channelUpdate landing, is now wired into the `audit:behavior-realization` script so it runs in CI). Landed: the Select trigger event-wire obligation now carries browser-level proof (the runtime rail's interaction facts, `7de140f8`); the capture surface covers the four shared-pipeline preview mounts only (Angular's synthesized-host pipeline has no interception seam) and one component's one callback — widening is additive. The audit also derives anchored obligations (`FEAT-ANCHORED-SURFACE-XFW-01`): `consume-anchored-positioning` and `consume-anchored-content-portal` per framework for every contract with `positioning.strategy: "anchored"` + portal (Popover/Tooltip → 20 obligations), both matchers mutation-falsified at landing; consumption tokens prove source wiring only — the real-browser placement facts live in the behavior-realization batch row. The anchored family gained a selector-anchored branch (`FEAT-WALKTHROUGH-ANCHOR-CONSUMPTION-01`): contracts whose anchor is selector-sourced (Walkthrough) derive `consume-root-portal` (the root panel moves, so the root-portal mechanism tokens apply) plus `consume-anchored-positioning` per framework, are excluded from `consume-anchored-content-portal` (`anchoredPortalsContentToBody` returns false for selector anchors — nothing content-level portals), and are exempt from the orphan invariant; portal-family obligations 265→270, both new derivations mutation-falsified at landing (stripped react `renderInPortal` and lit `AnchoredPositionController` → exit 1 naming the exact obligation). |
| A11y-realization audit rail + label/id association lowering | The CI-gated static audit derives `relationships[]`, keyboard, and focus obligations from the live contracts for every AdmissionDescriptor family. IDREF checks now require the declared carrier, the expected per-instance target id, and the exact carrier→target endpoint rather than accepting an attribute substring anywhere in generated source. Semantic validation also rejects labeling relationships whose endpoint is a decoration, so a real-but-wrong part id is no longer merely self-consistent. React/Vue/Svelte/Lit generated axe checks keep the independent axe-core oracle active for naming/structure rules: only page-level `region` is disabled, and each test first proves the scan root contains the component. | implemented / CI-gated; static topology plus generated runtime axe checks | `scripts/a11y-realization-audit/`; `packages/ds-codegen/src/{id-relationships.ts,relationship-lowering.test.ts}`; `packages/ds-codegen/src/validation/{semantic.ts,semantic.test.ts}`; `packages/ds-codegen/src/frameworks/{react,vue,svelte,lit}/tests.ts`; spec `FIX-A11Y-ORACLE-IDREF-01` | Exact source topology still does not prove the contract selected the semantically correct non-decorative label, runtime assistive-technology behavior, or cross-shadow-root associations. Axe is an external rules oracle, not an exhaustive WCAG proof. Ledgered gaps remain accounted-for obligations, not correctness waivers. | Expand contract mutation cases across relationship endpoint classes and add browser/AT-facing witnesses where static topology and axe cannot distinguish intent. |
| CSS custom-property bindings | Prop-to-CSS custom-property lowering exists and is runtime-checked for both default/fallback and explicit non-default cases in Progress, Truncate, and ShowMore. Non-default props are mounted through the preview `fsds:config` message bus for all five frameworks. | implemented / runtime-proven for defaults plus selected non-default cases | `packages/ds-codegen/src/frameworks/css-var-bindings.test.ts`; `e2e/runtime-rail.spec.ts` | The rail does not prove every non-default computed visual geometry, every component/prop pair, or every token cascade branch. | Widen the non-default surface beyond the three components/props, and bind rail outcomes into component evidence pages. |
| DOM-property reflection bindings | `DomNodeIR.propertyBindings` carries DOM-property-only facts (facts with no HTML attribute form) as a distinct IR binding class, validated at IR build via `DOM_PROPERTY_ONLY_KEYS` and host-eligibility checks. Checkbox `indeterminate` lowers across all five web frameworks in each framework's own verified idiom — React `useRef`+`useEffect` imperative assignment, Angular direct `[indeterminate]` template property binding, Vue and Svelte through their existing binding emission (their runtimes/compilers already route the key to a property write), Lit through a dedicated `.prop=` PropertyPart form — with `aria-checked="mixed"` lowering alongside. | implemented / test-proven (jsdom) | `packages/ds-codegen/src/ir.ts`; `packages/ds-codegen/src/frameworks/{react,vue,svelte,angular,lit}/component-source.ts`; generated Checkbox test pairs (initial mount + reactive update, falsified per framework); spec `DOM-PROPERTY-REFLECTION-IR-CHECKBOX-INDETERMINATE-01` (closed; merged via PR #10 `e3d54481`) | Not runtime-rail-proven — the proof is jsdom mount/update tests, not the Chromium fact rail. The class currently has one member fact (`indeterminate`). RN is unchanged: it lowers indeterminate via `accessibilityState` (a plain prop), not this class. | Assert the indeterminate property fact in the runtime rail, or admit a second DOM-property-only fact into the class. |
| Recursive component composition (componentRef) | A contract node can declare `componentRef: "fsds.<Name>"` to render an owned child component by reference. The IR resolves it once into a typed `ComponentInstanceIR` fact — target layer plus each binding/attr/event pre-classified (`kind:"prop"` vs `kind:"attribute"`, events resolved to the target handler prop, e.g. `click`→`onClick`) — so emitters do last-mile syntax only. Layer ordering (target rank ≤ host rank in primitive<compound<composer<assembly) is enforced twice: at IR build (throws) and at `generate:check` (cross-graph semantic rule). All six targets lower the resolved fact; Angular/Lit/RN branch on the classification directly (their attribute-vs-property syntax diverges), React/Vue/Svelte pass through structurally (their prop syntax is classification-invariant). | implemented / test-proven | `packages/ds-codegen/src/ir.ts` (`ComponentInstanceIR`, `resolveComponentInstances`, `LAYER_ORDER`); `validation/semantic.ts` cross-graph layer check + tests; `frameworks/component-ref-imports.test.ts` (11 tests, per-framework import-path resolution); contracts Alert/Chip (→`fsds.Button`), Avatar (→`fsds.Image`), all compound-layer; spec `CODEGEN-RECURSIVE-COMPOSITION-01` (closed `9db0c81b`) | Only three compound-layer contracts exercise it; no composer/assembly-layer usage and no nesting deeper than one hop is proven. The "composers must not componentRef a control" rule is convention upheld by precedent (TextField→Input dropped), NOT stated in committed architecture docs and NOT mechanically enforced — `LAYER_ORDER` only forbids upward-rank references. | Commit the composer-forbidden doctrine to the architecture docs or add a mechanical schema/semantic check rejecting composer-layer componentRef, so the rule stops being convention-only. |
| Composer named-slot projection | A compound/composer owning multiple content regions routes each region through a named slot (`{"tag":"slot","name":X}`), never a content prop for the same region. `validateSlotPropNamespaces` throws at IR build on any slot-name/prop-name collision (fires for all six targets, not only under svelte-check). All five web emitters plus RN lower named slots to their idiom. Five components use the pattern: Field (5 slots), TextField (3), Sheet (2), Walkthrough (2), Dialog (1). | implemented / test-proven (RN slot-exactness for Field) | `docs/architecture/composer-slot-projection.md` (`verified_at_commit: 2ab2509a`); `ir.ts` `validateSlotPropNamespaces`; `packages/ds-react-native/src/components/Field/__tests__/Field.runtime.test.tsx` (3/3 pass at `c5cda2c9`: exactly-once region rendering, single control host, clean omission); commit `e8b23a04` | The collision check proves namespace hygiene at IR build, not corpus-wide render exactness — only Field (RN) has a dedicated runtime exactness test. 40+ non-slotted components still use the older shape; the pattern is proven and admitted, not corpus-wide adopted. No Chromium runtime-rail assertion covers slot regions. | Extend the Field-style runtime exactness test to TextField/Sheet/Walkthrough/Dialog, and/or add a slot-region assertion to `e2e/runtime-rail.spec.ts` for one web framework. |
| textOverflow IR fact (dormant authority) | `ComponentIR.textOverflow` (`TextOverflowIR { kind: "line-clamp"; line }`) is a target-neutral IR fact built from `contract.textOverflow` for ShowMore (`prop:maxLines`) and Truncate (`prop:lines`), schema-validated and pinned by direct IR/contract/semantic unit tests (64/64 at `c5cda2c9`). | implemented IR fact / test-pinned — dormant (no consumer) | `packages/ds-codegen/src/ir.ts` (`TextOverflowIR`, `buildTextOverflowIR`); `ir.test.ts`, `contract.test.ts`, `validation/semantic.test.ts`; spec `CODEGEN-RN-EMITTER-IR-AUTHORITY-01` (closed via PR #10 `e3d54481`) | Zero emitters consume it — line-clamp behavior is still realized entirely through the pre-existing CSS-variable binding mechanism, unchanged (grep-verified across `frameworks/`). `DomGuardIR` and `NativeStyleDeclarationIR` were explicitly deferred by the spec and remain unintroduced. RN's `isCheckboxRootPattern` (structural DOM-shape check, not name lore) was knowingly tolerated at closure per the spec's own invariant. | Wire an emitter consumer of `TextOverflowIR` under a follow-up spec if native line-clamp realization is needed; otherwise the fact stays dormant scaffolding and this row's strength must not be read as behavioral. |
| Target-pack registry | Target-pack manifest and registry config machinery exists. Built-in targets are described through manifests; local target packs can be declared, resolved, validated, fingerprinted, and described. | implemented foundation | `fsds.targets.json`; `packages/ds-codegen/src/target-packs/{manifest,builtin,config,local}.ts`; `docs/architecture/design/target-pack-registry.md` | Local target packs are metadata-only in this slice: their emitter entrypoints are not imported/executed. npm/package-source loading is not implemented. | Add executable local target-pack loading after manifest validation while keeping file writing centralized. |
| Figma descriptor target + plugin | `figma` is a built-in design-tool target that emits descriptor artifacts and README projections from ComponentIR; the target-pack manifest declares descriptor-only token strategy and non-behavioral capability limits. The Figma plugin consumes those descriptors through a plan-driven materializer (`planner.ts` owns state semantics, `materialize-state.ts` realizes them; component sets for the Button/Chip/Status allowlist, annotated placeholders for the rest), an interactive Svelte UI (filterable descriptor catalog, materialization status/report, variant-label and description drafting) built from ds-svelte's own components, and a Dev Mode surface that returns per-framework usage-snippet previews for the selected node from `fsds.component`/`fsds.variantRow` plugin data. Eligibility classification has a single authority module (`eligibility.ts`) shared by the plugin core and the UI model. | implemented; unit-proven against a mocked `figma` global (2026-07-21) | `packages/ds-figma-plugin/src/{plugin.ts,planner.ts,materialize-state.ts,eligibility.ts,ui-model.ts,codegen-preview.ts,FigmaPluginApp.svelte}`; package test suite 59/59 (`pnpm exec vitest run packages/ds-figma-plugin`); build emits `dist/plugin.js` + `dist/ui.html` with inlined assets; spec `FIGMA-PLUGIN-UI-PORT-01` | This does not prove live-canvas behavior in a real Figma desktop session (all runtime evidence in this repo is mocked-global unit tests — 59 passing across 5 files. Source purpose-built for the real `figma.*` API exists and is NOT reflected in this row's evidence column: `live-materialize.ts`, `live-run.ts`, `live-audit.ts`, `live-style.ts`, `live-token-resolve.ts`, backing the closed `FIGMA-LIVE-MATERIALIZATION-IDEMPOTENCY-01` and `FIGMA-STYLE-RESOLUTION-PROJECTION-01`. Treat those as shipped-but-not-durably-evidenced: all eight FIGMA specs closed with **zero** acceptance-criterion evidence recorded between them (61 criteria), and the one live run's artifact is gitignored scratch. See `docs/architecture/figma-plugin.md` for the full claim-strength split), library publication, or MCP tool availability. Dev Mode previews are usage snippets (a pure function of descriptor + variant row), not the ds-codegen emitter pipeline. Component-set materialization is allowlist-limited to three components. The plugin package is not in the admission rail or CI gates. | Capture live-canvas evidence from a real Figma desktop run; widen the component-set allowlist; add the package build/tests to CI. |
| Component evidence/showcase pages | The loader-discovered corpus is reviewable in the showcase. Design derives usage, anatomy, states, props, accessibility, A2UI, and evidence/residuals; the separate component-token view exposes token bindings; Developer renders five-framework preview/source/trace tabs. The shared Properties inspector drives live prop and token overrides, including Angular through its synthesized-host `fsds:config` bus. Built brand CSS is discovered from emitted `[data-brand]` selectors rather than a second hard-coded list. Together these surfaces are the authorship-time human review loop: a reviewer can reject an outcome, then change the owning contract/token/style/brand source. | implemented; human review available but not persisted as required sign-off | `src/{app.tsx,router.tsx}`; `src/views/{DesignView.tsx,DeveloperView.tsx,ComponentTokensView.tsx}`; `src/layout/{TracePanel.tsx,Header.tsx}`; `src/components/properties-panel/`; `src/runtime/{FrameworkPreview.tsx,demos.ts}`; `packages/ds-tokens/build/generators/global.ts` | The showcase is an independent human judgment surface, not automated proof. It records neither reviewer identity nor a freshness-bound approval for each component. Preview overrides are ephemeral; durable changes must return to source. EvidencePanel reports committed coverage, not latest-run status. | If durable approval provenance becomes necessary, add a freshness-bound review artifact without turning subjective acceptance into a generated self-attestation. |
| Curated usage examples | Usage JSONL sidecars are loaded into component bundles and rendered as live React in the Design view where present. Contract-derived composition metadata owns slot/subcomponent delivery and JSON-to-runtime prop materialization (currently ISO strings to `Date`/`Date[]`). Every curated frame is now test-audited for a text/structural content witness, visible fallback absence, and confinement of its component root to the frame; named frame attributes make failures identify the exact component/example. The audit surfaced and repaired two real empty-content examples: Calendar day cells and Avatar initials. | implemented / corpus-wide jsdom audit; focused browser review | `packages/ds-contracts/components/*/*.usage.jsonl`; `vite-plugin-fsds-data.ts`; `packages/ds-codegen/src/usage-composition.ts`; `src/lib/{render-usage.tsx,render-usage.test.tsx}`; `src/views/sections/UsageExamples.tsx`; spec `COMPOSITE-CONTROL-EXAMPLE-INTEGRITY-01` | Curated examples are documentation inputs, not codegen inputs. A structural witness proves non-empty delivery and portal locality, not visual quality, semantic usefulness, accessible naming, or geometry at every viewport. | Add a real-browser corpus sweep if computed style and geometry need a blocking proof ceiling; keep component-specific semantic assertions for content the generic witness cannot judge. |
| Token governance | Token build, DTCG validation, committed-build drift check, contrast validation, and brand reference validation are blocking in CI before codegen checks. Token *usage* (share of the vocabulary referenced) is report-only — see the token-resolvability row for why the blocking question is the inverse. Component-derived contrast complement (`RAIL-COMPONENT-CONTRAST-01`): `generate:check` derives fg×bg pairs mechanically from every contract's `styles.json`/`tokens.json` binding chains (base scope plus variant/part slot redirections, never a hand-maintained pair list) and gates them at WCAG AA 4.5:1 in light + dark themes of `resolved.tokens.json`; disabled-state pairs are exempt per WCAG 1.4.3 Note 5. Pre-existing debt was ledgered at landing in `component-contrast-known-gaps.json` under a two-directional ratchet (unledgered failures fail; stale entries fail); `FIX-CONTRAST-DEBT-01` BURNED the ledger to empty — the resolver now preserves DTCG alpha (`core.color.mode.transparent` resolves to `#00000000`, which the WCAG math cannot consume and skips; its previous opaque `#000000` resolution was the phantom behind the Walkthrough finding), `semantic.interaction.text.hover` became theme-aware (the unthemed value measured 1.92:1 in dark), `foreground.hover` dark brightened to neutral.200 (was 3.60:1), and `foreground.secondary` light darkened to neutral.700 (was 4.38:1) — each pair stopped reproducing, so the ratchet itself demanded every entry's removal. Corpus-wide re-measure after the burn: 132 pair/theme checks, 0 failing. `RAIL-COMPONENT-CONTRAST-ANCESTRY-01` widened the lens with background inheritance: a color-only element block now pairs against the nearest UNCONDITIONAL ancestor background — ancestry from `anatomy.dom` parent chains, transform-generated parts (content.tokenPart) hanging off their container node, pseudo-state/attribute-conditional ancestor blocks never contributing — so token-span palettes and inherited-surface text are gated, not just same-block pairs. `governed:rail` runs `generate:check` before any ds-framework emission step. | implemented / CI-gated | `.github/workflows/ci.yml`; `packages/ds-tokens/**`; `packages/ds-codegen/src/validation/component-contrast.ts` + `component-contrast-known-gaps.json` (ledger burned to empty by `FIX-CONTRAST-PALETTE-REPAIR-01`; 27 tests); `packages/ds-tokens/build/generators/resolve.ts` alpha preservation (+5 tests); `package.json` (governed:rail scripts); `docs/architecture/tokens-architecture.md` | Token gates do not prove visual design quality or complete accessibility adequacy. No blocking gate detects a semantic or component token that loses its last consumer. The component contrast gate models declared color declarations only — foreground inherited across blocks (a title reading root's `color` without declaring its own) stays unmodeled; non-text (border) contrast (WCAG 2.2 SC 1.4.11) and `var()` fallback literals remain out of the model. | Connect token gate summaries to component evidence pages. Palette repair LANDED (`FIX-CONTRAST-PALETTE-REPAIR-01`): the syntax palette is theme-aware (ramp step ≥700 light / ≤300 dark via `fsds.light`/`fsds.dark` extensions) and Select `emptyState` rebinds to `foreground.secondary`; all 10 ancestry-lens entries stopped reproducing and the stale ratchet itself demanded the burn back to an empty ledger. The ratchet still guards any future debt. `interaction.text.active` remains unthemed (same shape as the hover defect FIX-CONTRAST-DEBT-01 burned) but is not currently derived into a failing pair by any contract. |
| Token-reference resolvability | Every `var(--fsds-*)` read by generated global token CSS or React component CSS must resolve to a name declared by the token graph, the component's own slots, or its runtime setter. An unresolvable reference does not error — CSS falls back to the literal second argument, so the property renders a plausible value while the token layer is decorative for it and no brand, theme, or density re-point can reach it. The camelCase lowering defect this rail was built to find is **closed**: 64 references across 23 names burned to **0** under `FIX-TOKEN-SLUG-KEBAB-CASE-01`. Widening the same rail to its global input exposed 28 more undeclared reads across three namespace families (`spacing.density`, `elevation.surface`, `interaction.stateLayer`); `FIX-DENSITY-GLOBAL-RESOLVABILITY-01` corrected their core/semantic namespace classification and burned that set to **0**. | implemented / gated at pre-push + CI (ledger ratchet) | `scripts/token-resolvability-audit/{audit,selfcheck}.mjs` (18 falsification checks); `packages/ds-tokens/build/core/{index.test.ts,token-path.test.ts}`; `docs/token-resolvability-audit/resolvability-matrix.{json,md}`; specs `RAIL-TOKEN-REFERENCE-RESOLVABILITY-01`, `FIX-TOKEN-SLUG-KEBAB-CASE-01`, `FIX-DENSITY-GLOBAL-RESOLVABILITY-01` | **Not a consumer-coverage gate**: a token that loses its last consumer is not detected here. Component styles are sampled through generated React CSS as the reference framework; the global `tokens.css` graph is now audited directly. Zero findings means those scanned stylesheets read no undeclared name — it does not mean every read resolves to the *intended* value, wins the cascade, or changes computed style. A mutation to a shared lowering authority can move producer and consumer together; focused unit cases are the independent falsifier for namespace classification. | Explicit-light precedence was already repaired and browser-proven under `FIX-BRAND-LAYER-THEME-LEAK-01`. Keep global sheets inside this audit permanently: the density defect survived while the rail scanned only component output, demonstrating that a repair can make a sibling surface reachable without bringing that surface under the original ratchet. |
| Icon catalog runtime delivery | `packages/ds-iconography` is the icon authoring source of truth: <!-- icon-count -->29 icons under `icons/<Name>/<Name>.icon.json` validated against `icon.contract.schema.json`, two authored size variants (16/24) each, with a CASR-style content-addressed emission ledger (`emission-ledger.json`) maintained per batch. `build/build.mjs` emits a committed package-root `index.mjs`/`index.d.ts` (named export per icon + catalog + `resolveIcon`); `generated/` stays gitignored scratch. Codegen carries a dedicated `IconGlyphIR` fact parsed from `anatomy.dom[].iconGlyph`, lowered with zero per-icon emitter lore into generated `Icon` components across all five web frameworks, RN, and the Figma descriptor (Angular `ICON_GLYPH_SIZE_HINTS[(this.size ?? "")]`, Lit `svg\`\`` + `ifDefined`). `validateContractIconRefs` runs inside `generate:check` and fails on unknown icon references. | implemented / CI + pre-push drift-gated | `packages/ds-iconography/{icons,build,index.mjs,index.d.ts,icon.contract.schema.json,emission-ledger.json}`; `.github/workflows/ci.yml`; `.githooks/pre-push`; `scripts/prepush-scope.test.mjs`; specs `ICON-CATALOG-RUNTIME-DELIVERY-01`, `ICONOGRAPHY-TOKEN-DISCIPLINE-02`, `FIX-ICONOGRAPHY-COMMITTED-EXPORT-DRIFT-01` | Two independent checks now run in order whenever iconography changes: `iconography:build:check` rebuilds and byte-diffs the committed package-root exports; `iconography:ledger:check` separately attests the regenerated attachment edge set. The second cannot substitute for the first because its own rebuild makes producer and attestation self-consistent. Brand-swappable glyphs remain absent (zero icon keys under `src/brands/`). The bundle-size probe is ungated. RN/Figma Icon realizations exist as generated code but sit outside the admitted Web-DOM rail family. | Keep byte identity and edge-set attestation separate; add brand-swappable glyph vocabulary only when a consuming contract requires it. |
| Non-web generation (substrate) | React Native is a rail-admitted default target and `--target=all` emits its full loader-discovered corpus. SwiftUI and Jetpack Compose are also registered builtins in `fsds.targets.json`, so `--target=all` includes their allowlisted projections (SwiftUI <!-- target-component-count:swiftui -->52; Compose <!-- target-component-count:jetpack-compose -->52). Their generated trees are drift-gated in CI and pre-push, and separate native compiler/example-consumer lanes establish bounded compilation facts. SwiftUI and Compose remain outside the six-target AdmissionDescriptor rail because they do not declare a `railFrameworkId`. SwiftUI's CI-run `swift test` additionally evaluates every admitted component body and pins state, native token-resolver, and sampled token-paint facts; its separately invoked `PressProofHarness` drives named interactions through a real macOS AppKit accessibility path. Popover now lowers its contract-authored click trigger to a native Button without component-name lore; the host observes AXPress → first `onOpenChange` value `true` → visible content (`FIX-SWIFTUI-POPOVER-TRIGGER-01`). Compose runs the shared token-resolver conformance fixture but has no UI-runtime lane. The framework-neutral emission-class predicates both native emitters dispatch on live once in `packages/ds-codegen/src/frameworks/native-emission-class.ts` (identical predicates shared, divergent gates composed from the same atoms at each target) — the swift emitter's byte-identical regeneration after the move is drift-gated proof. Native admission criteria are codified as the C1–C7 ladder in `docs/architecture/native-target-admission.md` (class dispatch on structural predicates, allowlist registration, byte drift, native compile, token parity, foundation-only substrate discipline, ledgered divergences); Compose's boolean-control and bare-rule-leaf classes landed on it (`FEAT-COMPOSE-ADMISSION-SUBSTRATE-01`: Checkbox via the painted `FsdsCheckbox` substrate, Divider via `FsdsRule`, both gradle-compiled and parity-gated), then the glyph pipeline (`FEAT-COMPOSE-GLYPH-ADMISSION-01`: a generated `FsdsGlyphCatalog` from the iconography corpus plus the committed `FsdsSvgPath` runtime — the same arc→cubic endpoint parameterization the swift spike validated — admitting Icon through the glyph-host class and Alert/AlertNotice/Badge through icon-decorated content, with member-named variant slots incl. the error↔danger family closure; NavTree stays deliberately unadmitted with the decision ledgered), then the radio-collection and disclosure classes (`FEAT-COMPOSE-DISCLOSURE-01`, `FEAT-COMPOSE-RADIO-ADMISSION-01`: Details on the native-disclosure collapse with a toggleable header and animated content; RadioGroup on the radio-group facts extracted once into the shared substrate and consumed by both native emitters — the swift regeneration after that extraction is byte-identical; `FEAT-COMPOSE-SHUTTLE-ADMISSION-01` moved the array-iterated-list predicate to the same substrate and admitted Shuttle on it, again with byte-identical swift regeneration; `FEAT-COMPOSE-OTP-ADMISSION-01` moved the count-iterated field-group predicate to the same substrate and admitted OTP on it, likewise drift-proven; `FEAT-COMPOSE-TEXTFIELD-ADMISSION-01` moved the labeled text-control predicate to the substrate and admitted TextField on it with label/description/error regions; `FEAT-COMPOSE-SELECT-ADMISSION-01` moved the selection-control predicate to the substrate and admitted Select on a Popup-backed option list with single/multi union projections; `FEAT-COMPOSE-DIALOG-ADMISSION-01` opened the surface family with the centered-surface class over the foundation Dialog host, and `FEAT-COMPOSE-EDGE-SURFACES-01` added the viewport-edge class with Sheet's placement enum and Toast's real duration-driven auto-dismiss; `FEAT-COMPOSE-ANCHORED-SURFACES-01` completed the surface family with the anchored class (Popover click trigger, Tooltip hover/focus trigger) on measured anchor offsets, and closed a real scaffolding gap: surface components now emit their token-scope files through the surface path; `FEAT-COMPOSE-COMMAND-ADMISSION-01` admitted Command on the same centered class by moving the surface's optional second string channel into the substrate (`surfaceStringChannel`) and lowering it to a foundation `BasicTextField` above the consumer content, and corrected the parity gate's path classification — a surface is now classified by its own host marker before any marker whose host it reuses, so an anchored or search-bearing surface can no longer satisfy its chrome-role claim as whichever lower-level class happens to own that host; `FEAT-COMPOSE-CALENDAR-ADMISSION-01` moved the date-grid predicate out of the swift emitter into the shared substrate — strengthened there with the closed `dateDayOfMonth` projection fact, over which exactly one corpus component matches — and made Compose realize the declared grid itself: one row per calendar week of the contract's own `days` prop, each day a selectable cell labelled by that projection and activated on the channel the IR's `compositeControl` names, over a committed `FsdsDate` day-granular runtime with its own Kotlin test lane; the same class still delegates to a platform `DatePicker` on SwiftUI, which the shared predicate now makes reachable for it; `FEAT-COMPOSE-REFERENCE-REALIZATION-01` gave Compose the missing capability the corpus already depended on — a declared `componentRef` now lowers to a call on the referenced *generated* composable, its package and axis enums imported, every fact in the node mapped by one declared vocabulary that fails loudly outside it — admitting Chip (composed from two referenced Button controls, one guarded by the contract's `if: dismissible`) and adding the C8 reference-realization rung: the parity gate reads each contract's declared references and requires every one to be realized or ledgered, in both directions, which turned six silently dropped references in already-admitted components into explicit entries; `FEAT-COMPOSE-FIELD-ADMISSION-01` moved the last target-local composer predicate into the substrate (`isNamedSlotComposer` plus its dom atoms, SwiftUI regeneration byte-identical) and admitted Field as the 47th component — every declared slot a null-safe content region in document order, the status axis driving chrome through the layered lookup). | RN rail-admitted; SwiftUI/Compose registered, allowlisted, drift-gated, and compile-lane covered; SwiftUI has bounded test/paint/host-interaction facts | `fsds.targets.json`; `packages/ds-codegen/src/{registry.ts,validation/admission-descriptor.ts}`; `packages/ds-codegen/src/frameworks/native-emission-class.ts`; `packages/ds-codegen/src/frameworks/swift/swiftui/surface-emit.ts`; `packages/ds-codegen/src/frameworks/{swift/swiftui,jetpack-compose}/component-source.ts`; `packages/ds-codegen/src/frameworks/jetpack-compose/icon-glyph.ts`; `packages/ds-{react-native,swiftui,jetpack-compose}/`; `packages/ds-swiftui/{Package.swift,Tests/}`; `.github/workflows/ci.yml`; `.githooks/pre-push`; `node scripts/swift-parity-diff.mjs`; `node scripts/compose-parity-diff.mjs`; `swift test --package-path packages/ds-swiftui`; `swift run --package-path packages/ds-swiftui PressProofHarness`; `docs/architecture/native-target-admission.md`; specs `DOC-README-ORACLE-NATIVE-RECONCILE-01`, `FIX-SWIFTUI-POPOVER-TRIGGER-01`, `FEAT-COMPOSE-ADMISSION-SUBSTRATE-01`, `FEAT-COMPOSE-GLYPH-ADMISSION-01`, `FEAT-COMPOSE-COMMAND-ADMISSION-01`, `FEAT-COMPOSE-CALENDAR-ADMISSION-01`, `FEAT-COMPOSE-REFERENCE-REALIZATION-01`, `FEAT-COMPOSE-FIELD-ADMISSION-01` | Registration, generation, byte drift, admission, compilation, CI tests, sampled paint, and host interactions are different claims. The PressProof harness is reproducible but not a required CI step and its macOS host facts are not iOS simulator/device evidence. Tooltip's declared focus trigger remains unlowered on SwiftUI; hover is live. OTP text entry and native modal-dismissal callback remain blocked by named host limitations. None of these surfaces proves visual parity, platform accessibility adequacy, full corpus parity for Compose, or substrate-neutral behavior. | Keep each native proof rung separately named; add a CI-safe platform interaction lane only if it can preserve the harness's current explicit skips and host limitations rather than converting them into passes. |
| Mobile-vs-web parity (measured) | `scripts/mobile-parity-matrix.mjs` derives react-native as admitted (registered target + rail plan + package root) with full generated coverage; its `--check` self-consistency gate passes (exit 0) after `RAIL-COVERAGE-DERIVATION-FIX-01` deleted the frozen-partition sub-check (a point-in-time A/C/D sweep classification compared against the live corpus count — a pin against a moving target, retired rather than repaired; the sweep counts remain as historical provenance in the matrix output). The remaining self-checks (dimension completeness, admission symmetry) assert internal consistency of live-derived output only. | measured, script-derived, reproducible | `scripts/mobile-parity-matrix.mjs` (`--check` exit 0, verified 2026-07-21; re-verified 2026-08-17 — "50 components, 8 targets, 8 dimensions; derivations consistent"); `docs/internal/reference/successor-work-mobile-parity-matrix.md` (`REF-MOBILE-PARITY-MATRIX-001`; machine-local — `docs/internal/` is gitignored, so this path is absent in a fresh clone) | The matrix measures structural admission facts, not visual/runtime parity — and those facts are byte-redundant with `fsds.targets.json` + the governed rail + CI's six-tree diff (the matrix is a convenience projection, not an independent proof). | Re-run after any SwiftUI/Compose elevation; retire the script entirely if its projection stops earning its upkeep. |
| Styling-realization audits (variant + pseudo/state, measured) | Read-only classifiers measure whether declared variant and pseudo/state styling obligations are present in generated React CSS. Their known-gap ledgers are two-directional ratchets. | measured, script-derived, reproducible | `scripts/variant-style-audit/`; `scripts/pseudo-state-audit/`; `docs/pseudo-state-audit/pseudo-state-matrix.{json,md}` | A green known-gap ledger means accounted-for findings, not repaired behavior. Selector presence does not establish runtime reachability. The former dead-slot ledger is retired; the component-token consumption row governs token declarations. | Repair supported state styling with runtime evidence; do not manufacture selectors merely to satisfy a classifier. |
| Examples consumer-app portfolio | `examples/` is the consumer-side falsification surface, governed by its own `examples/README.md` (claim/falsifier/non-claim discipline per app shape). `settings/` has **five** implemented lanes: React and Vue (build/typecheck), RN (typecheck-only), and — added since the last sweep and omitted here until 2026-08-17 — Swift and Compose, both **CI-compiled**: `.github/workflows/ci.yml` runs `./examples/settings/compose/gradlew … compileKotlin` in `native-compile-rail` and `swift build --package-path examples/settings/swift` in `native-compile-rail-swift` (`FEAT-EXAMPLES-SWIFT-SETTINGS-01`, `FEAT-EXAMPLES-COMPOSE-SETTINGS-001`). Those two lanes are compile-verified only — no runtime, visual, or device proof. Three assembly-layer shapes (operations-dashboard, storefront-checkout, social-feed) are spec scaffolds with a shared, committed isomorphic data/API seam: lane-local fixtures + adapter (`?raw` bundler modules, no node-only specifiers) + promise-returning functional API, tested by `pnpm run test:examples:data-api` (47/47 pass at `d82f1046`, node/Vitest). | implemented narrow lanes + measured data seam; assembly shapes spec-scaffold only | `examples/README.md` (invariants 1–10, incl. the isomorphism invariant #10); `examples/*/src/{data,api}/`; `vitest.examples.config.ts`; specs `EXAMPLES-OPS-DASHBOARD-ISOMORPHIC-SEAM-01` (closed `d82f1046`), `EXAMPLES-DATA-API-TEST-LANE-01` and the app-shape scaffold specs (closed) | No assembly shape has an implemented framework lane; five-framework app parity is not proven by examples. The data/API test lane runs under node only and is structurally blind to browser-runtime regressions (governed by invariant #10's browser-render falsifier, which no automated lane executes today). Not in CI. | Implement the first framework lane for one assembly shape, and decide whether `test:examples:data-api` joins CI. |
| Fallback gates (presence + graph-truth) | Two hard invariants over the same surface, both inside `generate:check`'s semantic pass. **Presence** (`[FALLBACK_MISSING]`): every contract slot declaring `resolvesTo` MUST declare `fallback` — the codegen emits `var(--ref, fallback)` only when present, so without it the property silently drops when the token is unavailable. **Correctness** (`[FALLBACK_STALE]`): a present fallback MUST equal the light-mode terminal value of its `resolvesTo` chain in the resolved token graph — a fallback is a cached copy of a graph value, and truth is read from the graph, never inferred from literal frequency (the removed FALLBACK-DIVERGENCE-01 advisory's lesson: `8px` at 25 sites vs `6px` at 1 — the majority was the defect). Component-layer chains resolve intra-contract to their terminal graph value. Serialized DTCG **shadow** composites are lowered by a narrow mirror of the tokens build's `shadowValueToCSS` (structured dimensions, srgb→hex8 with `Math.round(a*255)` alpha, layers joined `", "`), drift-locked by an oracle test against the committed `tokens.css`; composites outside that envelope (oklch colors, string dimensions, typography) are still refused rather than guessed. Canonicalization is suppress-only: rem≡px, hex-case, hex3≡hex6, rgba/rgb≡hex, bare-`0`≡`0px`. | implemented / CI-gated (`generate:check`) | `packages/ds-codegen/src/validation/fallback-completeness.ts` + `.test.ts` (35 cases, incl. the mirror-vs-emitter oracle); `cli.ts` semantic-check registration; specs `ICONOGRAPHY-TOKEN-DISCIPLINE-02` A1/A2 (presence + 883-entry repair), `FALLBACK-STALE-01` (graph-truth + 695 literals derived, divergence advisory removed as subsumed), `FALLBACK-SHADOW-LOWERING-01` (shadow lowering + 23 shadow readings repaired across 10 components, incl. the single-site Dialog/Popover blind spot no divergence scan could see) | Correctness is a **light-mode-only** claim — dark-mode fallback correctness is a mechanism limitation (a single static literal cannot encode both theme modes). The shadow mirror is local, not exported from the tokens build (not importable: tsx-run, no compiled output); emitter drift is caught by the oracle test rather than shared code. Box-model ref values are not checked against the primitive's merged defaults. | (No open move — both gates enforced. If shadow tokens ever become theme-split, the light projection applies before lowering.) |
| State-suppression rail | A third styling obligation axis, independent of realization: a contract may declare `states.dimensions.<dim>.suppresses.categories` (e.g. `availability=disabled` suppresses `interaction`). `scripts/state-suppression-audit/` checks whether the generated React CSS honours that claim — every property set under an interaction pseudo must either be reset in the suppressing block (winning it back at equal specificity) or be guarded so the selector cannot match. Leaks are graded by **reachability**: `:hover` matches a disabled element, but `:focus-visible`/`:active` cannot match a natively-disabled form control, and no pseudo needing focus can match a non-focusable root. Current: 16 components declare interaction suppression, **13 guarded, 0 leaks** — the codegen emits the guard from the declared fact (`:not(:disabled)` on a native form-control root, the `aria-disabled` form otherwise, a contract-declared `a11y.attribute` winning over both). The remaining 3 declare suppression but author no root interaction block, so there is nothing to guard. | implemented / gated at pre-push (ledger ratchet) | `scripts/state-suppression-audit/{audit,selfcheck}.mjs` (11 falsification checks); ledger `known-leaks.json`; `docs/state-suppression-audit/state-suppression-matrix.{json,md}`; spec `RAIL-STATE-SUPPRESSION-01` | Detects a *declared* suppression that the CSS does not honour — it does NOT prove the converse (a component with no `suppresses` declaration is unchecked). Classification is static CSS analysis against ds-react only; reachability is derived from the contract's root tag and tabindex, not from a browser. A green ledger means every leak is ACCOUNTED FOR, not fixed. | Ledger fully burned under `FIX-STATE-SUPPRESSION-LEAKS-01`. Remaining gap: the scan is root-anchored, so an interaction pseudo wrapped in a compound selector (`:has(.x__input:hover)`) is outside it — zero such blocks exist in the corpus today, but the guard would not be emitted for one either. |
| Stack primitive layout-mode authority | `Stack.primitive.json` declares `layout.displayByMode.native = null` — "native lets the rendered host keep its own display/layout behavior" — and the IR derives `axisModes` from it as exactly the flex-bearing modes. Both the display rule AND the flex axis are now gated on that fact in all five web targets. Previously only Angular consumed `axisModes`; React/Vue/Svelte/Lit emitted an unscoped `.stack--vertical { flex-direction: column }` (`:host([variant])` in Lit), so every `layout="native"` host silently inherited a column axis it never declared. 43 of 50 generated components render `Stack layout="native"` and none passes a variant (re-derived 2026-08-17 by grepping `layout="native"` across `packages/ds-react/src/components/`). | implemented / test-proven + browser-proven | `packages/ds-codegen/src/primitive-contract.ts` (`axisModes`); `frameworks/{react,vue,svelte,lit}/primitive-source.ts`; `primitive-emit.test.ts` (per-framework axis-gating case + over-correction guard, mutation-verified); specs `FIX-STACK-NATIVE-AXIS-LEAK-01` (merge `ed2d8ca5`), `FIX-ALERT-DECLARE-AXIS-01` (merge `1198287a`) | Browser-measured in Chromium on the showcase, not in an automated rail — no Playwright fact pins the axis, so a future regression would be caught by the emitter test but not by rendered geometry. The corpus sweep covered every `layout="native"` host with 2+ in-flow children across all 49 design pages at one viewport; a usage example not rendered on a design page, or a layout that only breaks at another width, was not measured. Angular's realization is a `@HostBinding` style, not a CSS rule, so the two mechanisms are proven equivalent only for the modes the contract declares. | Alert's anatomy is flat (`root > icon, title, body, dismiss`) and cannot express the conventional alert layout — icon beside a COLUMN of title+body, dismiss at the end. `FIX-ALERT-DECLARE-AXIS-01` restored column as the better of the two axes a flat anatomy admits; giving Alert/AlertNotice a content-wrapper part is unresolved and changes the public DOM contract of six targets. Separately, `.stack { gap }` is still emitted unconditionally while Angular gates gap on `axisModes` — the same divergence class as the axis, unmeasured and unfixed. |
| Anchored-surface part-key lowering | The anchored-surface family (Tooltip, Popover) marks `trigger`/`content` with `data-<prefix>-<part>` and emits NO part class — the trigger may be the consumer's own adopted element (React `asChild`, Vue slot-props host adoption) and the content is portaled out of the `.<prefix>` ancestor. `expandOptionsForContract` now lowers a bare part KEY to the marker selector for that family, so part-keyed styles reach the rendered element. Before this, Tooltip's `content` block compiled to `.tooltip__content`, matching zero elements: all 11 declared surface properties were inert and the tooltip rendered as bare text. Popover had worked around it with hand-written `[data-popover-content]` styles keys AND left a duplicate copy of the surface chrome on `root`, so a closed Popover painted its trigger as a filled panel. Popover is migrated to part keys; the `data-*` markers are untouched (behavioural marker vs styling hook). | implemented / test-proven + browser-proven | `packages/ds-codegen/src/ir.ts` (`expandOptionsForContract`, `surfacePartSelectors`); `frameworks/surface-part-class.test.ts` (7 cases, mutation-verified); `packages/ds-contracts/components/{Popover,Tooltip}`; specs `FIX-SURFACE-PART-CLASS-01` (merge `67d7edad`), `FIX-INVERSE-SURFACE-FOREGROUND-01` (merge `c5605ef6`) | Fixed at the selector layer, so no emitter template changed and the two callsites share one options helper. Browser-measured in Chromium at one viewport, not pinned by an automated runtime rail. **A2 only partially met:** the closed Popover host no longer paints (no background, border, shadow or radius) but still carries `padding: 16px`, because `renderBoxModelConsumers` emits `box-model.padding-*` unconditionally on root and Popover binds those to `semantic.surface.size.padding-*` — surface facts applied to the host. Unchanged by this slice (16px before and after), so pre-existing, not a regression. | Two findings this surfaced. (1) **5 orphaned part classes remain**, all Card's — `__badge`/`__media`/`__actions`/`__note`/`__link`, styled in generated CSS and carried by nothing. The other 9 (Checkbox `__indicator`/`__input`, Button `__spinner`/`__loadingText`, Skeleton `__shape`/`__stack`, Text `__error`/`__success`, Tabs `__root`) are resolved under `ADJUDICATE-WEB-STYLE-REALIZATION-DEBT-01`; see `docs/carrier-reachability-audit/adjudication.md` for the per-carrier evidence. Now gated, both directions, by `RAIL-WEB-STYLE-CARRIER-REACHABILITY-01` (`pnpm run audit:carrier-reachability` / `audit:variant-realization`; ledgers `scripts/carrier-reachability-audit/known-unreachable.json` and `scripts/variant-style-audit/known-unrealized.json` — cite those files, not this row). Carrier vocabulary is one IR fact, `deriveWebDomCarriers`: `classRecipe` owns modifier spelling, the `anatomy.dom` walk plus compound subcomponents plus content-transform vocabularies own part carriers, `expandOptionsForContract` owns the `data-*` markers. Forward findings **0**: the original 18 rows were 18 selector occurrences over 11 unique carriers, all resolved — 7 by realizing declared anatomy (Checkbox's composite control, Button's spinner/loadingText, Skeleton's stack/shape), 2 by selector repair (`Details`' namespaced `.details--icon-*` keys against the disjoint `icon` axis, Tabs' part-suffixed root key), 2 by contract retirement (Text's error/success blocks, which no variant, prop or token could select). Card's 5 do NOT appear: it carries no `anatomy.dom`, so its producible part set is UNKNOWN and part adjudication is skipped rather than inferred from missing information. They are adjudicated separately as a realization gap on contract-sourced evidence (Card's `a2ui.usageHints` tell consumers to use `Card.Link`/`Card.Actions`/`Card.Media`); the cause is the hardcoded `COMPOUND_PARTS` name allowlist in `semantics.ts`, and the repair is deferred because that predicate is applied to every component, not only dom-less ones. Inverse findings **43** across 20 axes, after the census predicate dropped `stylingIntent` as an admission rule — 41 of the original 47 were invisible under the old rule, including every runtime-confirmed dead knob (`Checkbox.size`, `Progress.size`/`variant`, `ToggleSwitch.size`). Each now carries an `adjudication` (22 `structural-behavioral`, 10 `visual-obligation`, 8 `unresolved-contract-semantics`, 3 `stale-vocabulary`), stamped by `scripts/variant-style-audit/apply-adjudication.mjs`, which throws on any unadjudicated finding. The inverse ledger is a ratchet, not a defect list, and is not expected to reach zero. The forward validator lives in `validation/styles.ts` and is still NOT wired into `generate:check`; its ledger is now zero, so promotion at that baseline is a separate slice rather than a blocked one. Non-claim: reachability proves a rule has a producible attachment point, not that it wins the cascade, changes computed style, supplies content the part needs, or realizes any visual distinction — `Details`' `__icon` span is empty, so repairing its selectors is necessary and not sufficient.

Which side is wrong is per-carrier and not mechanically inferable, so these are **orphaned style carriers requiring disposition**, not overclaims. Nine were adjudicated and resolved under `ADJUDICATE-WEB-STYLE-REALIZATION-DEBT-01`, and the reading that they were missing realization rather than stale contract held in seven of the nine (Checkbox's contract described the composite control while `anatomy.dom` collapsed to a bare `<input>`; Button's contract says `loading` "replaces content with a spinner"; Skeleton's curated usage example passes `lines: 3`). Two were genuine contract retirements, justified without reference to generated output: Text declares no error/success variant, prop or token. Card's five remain, and its A2UI guidance telling consumers "Use Card.Link" is the contract-sourced evidence that they too are missing realization. This is a different class from the Tooltip defect, and no existing rail sees them: the dead-slot audit asks whether a token slot has a `var()` consumer, and here every consumer exists — it just sits in a rule whose selector matches nothing. (2) **23 camelCase custom properties are read by generated CSS and declared nowhere**, so those bindings silently fall back to their literals: codegen preserves camelCase in var names while the token build kebab-cases them, so any contract `resolvesTo` with a camelCase segment cannot resolve. Worst reach is `semantic.shape.control.border.defaultWidth` (17 components — Button's border width); also `core.dimension.actionMinHeight*`, the whole `semantic.glyph.badge.size.*` group, `foreground.linkHover`/`linkVisited`, `interaction.stateLayer.*`, `background.accentSubtle`, `spacing.gap.gridSmall`. `foreground.on-inverse` is deliberately kebab-named to sidestep this. |
| Component token consumption | Component declarations must reach a property or behavior. Web output and native dictionaries project their actual consumers. Disconnected aliases and cycles cannot count as consumption; unused semantic vocabulary remains valid. Legacy gaps have been retired or retained with a real native/behavior consumer. | implemented / zero-gap source and emitted CSS gates | `COMPONENT-TOKEN-CONSUMPTION-01`; `packages/ds-codegen/src/validation/component-token-consumption.ts`; `scripts/dead-slot-audit/audit.mjs`; [contract and retirement decisions](architecture/design/component-token-consumption.md) | No known-dead allowance or reseed path remains. Static consumption is not selector reachability or visual proof. Native dictionaries are source-projected; device parity is not claimed. | Introduce new component decisions with their consumers and tests; extend semantic vocabulary independently. |
| Component identification attribute | Every generated component root across react/vue/svelte/angular/lit carries `data-fsds-component=<cssPrefix>` for audit/devtools discoverability (e.g. `document.querySelector('[data-fsds-component="button"]')`). React/Vue/Svelte: template attribute on the root element (threaded through each framework's `RenderContext` from `ir.cssPrefix`). Angular: `host: { "data-fsds-component": ... }` in the `@Component` decorator (4 emit sites). Lit: `this.setAttribute(...)` in `connectedCallback` (not the constructor — the custom-elements spec forbids attribute writes there; several `connectedCallback` emission sites across `component-source.ts` + `surface-emit.ts` each carry it, verified across all 50 Lit components). | implemented across all 5 DOM frameworks | `packages/ds-codegen/src/frameworks/{react,vue,svelte,angular,lit}/component-source.ts`; `frameworks/lit/surface-emit.ts`; regenerated component source across all 5 packages; spec `ICONOGRAPHY-TOKEN-DISCIPLINE-02` A5 | **Identification only — NOT selector scoping.** Does not change any CSS selector. The collision-isolation concern this attribute was a prerequisite for is now partially resolved by `@layer components` wrapping (see the resolved finding below) — but that fix works at the layer level, not the selector level, so `[data-fsds-component]` selector-prefixing (per-component isolation granularity, heavier: 5 framework emitters + every selector) remains undone. Native (RN/iOS/Android) ID surfaces are a follow-up (RN `testID`, Swift `accessibilityIdentifier`, Jetpack Compose `testTag`). | Selector-prefixing with `[data-fsds-component]` for per-component isolation granularity, if still wanted now that layer-wrapping covers the collision case; add native ID surfaces. |
| Test coverage governance | Every test runner carries a coverage floor declared once in `coverage-floors.json` and enforced by its own runner config — vitest `coverage.thresholds` for react/codegen/vue/svelte/lit/react-native/figma-plugin/tokens/showcase and jest `coverageThreshold` for angular. `pnpm run test:coverage` runs all ten scoped coverage suites and fails on any floor regression; CI's `coverage-gate` job runs it on every push. Per-package line floors: react <!-- coverage-floor-lines:react -->81, codegen <!-- coverage-floor-lines:codegen -->82, vue <!-- coverage-floor-lines:vue -->87, svelte <!-- coverage-floor-lines:svelte -->87, lit <!-- coverage-floor-lines:lit -->83, angular <!-- coverage-floor-lines:angular -->87, react-native <!-- coverage-floor-lines:react-native -->83, figma-plugin <!-- coverage-floor-lines:figma-plugin -->93, tokens <!-- coverage-floor-lines:tokens -->80, showcase <!-- coverage-floor-lines:showcase -->80. Versus the 80%-lines target: react <!-- coverage-vs-target:react -->above, codegen <!-- coverage-vs-target:codegen -->above, vue <!-- coverage-vs-target:vue -->above, svelte <!-- coverage-vs-target:svelte -->above, lit <!-- coverage-vs-target:lit -->above, angular <!-- coverage-vs-target:angular -->above, react-native <!-- coverage-vs-target:react-native -->above, figma-plugin <!-- coverage-vs-target:figma-plugin -->above, tokens <!-- coverage-vs-target:tokens -->above, showcase <!-- coverage-vs-target:showcase -->above. | implemented / CI-gated | `coverage-floors.json` (single authority); ten runner configs; `.github/workflows/ci.yml` `coverage-gate` job; `scripts/docs-claims-check.mjs` (coverage marker families); `scripts/custom-region-audit/` | Floors ratchet from measured values, not from the 80% target. Thresholds are per-package aggregates, not per-file, and coverage percentage is not assertion depth. `audit:custom-regions` derives the current generated/authored-region census and requires every occupied authored test region to be ledgered; its `3022618b` run found 79 occupied generated-test regions and no occupied generated component-source regions. That is evidence that an authored test layer exists, not that every contract defect class reaches it. Mutation evidence is measured separately in the next row. | Add per-file thresholds where runner support is practical; use mutation survivors, not aggregate coverage alone, to prioritize authored tests. |
| Contract-oracle mutation measurement | A deterministic catalog perturbs one contract fact at a time, restores it after each run, and records the first failing stage plus evidence class. Reviewed dispositions ratchet the detected-vs-survived outcome, exact first stage/class, and a stable failure marker. A red earns `detected` only when all three detector facts match; otherwise it is `inconclusive`, remains visible for adjudication, and fails the run without improving the score. Mixed-suite markers name the authored assertion specifically, so a generated failure cannot silently replace it. Verification is full-profile-only because shortening or reordering the stage set changes first-detector meaning. The expanded clean run at `71dbe276` measured 7/11 detected and 4/11 survived: the existing `rtl.flipIcon` and `ssr.hydrateOn` sentinels still survived; Dialog `stateMachine.transitions[0].event` and Card `slots.actions.required` also survived every structural, audit, test, and browser stage. `textOverflow.line` repointed to the real-but-wrong `defaultExpanded` prop was killed by the hand-authored IR-authority assertion; Accordion motion `height`→`width` was killed by the contract-derived motion ledger. The first attempt correctly refused to score mutants because a newly authored Status test region was unledgered; `known-authored-regions.json` now records that independent glyph oracle and the baseline passes. | implemented / catalog self-check CI-gated / full measurement scheduled | `scripts/contract-oracle-mutation/{catalog.mjs,selfcheck.mjs,run.mjs,README.md}`; `scripts/custom-region-audit/known-authored-regions.json`; `.github/workflows/{ci.yml,contract-oracle-mutations.yml}`; `package.json`; report `tmp/contract-oracle-mutation/71dbe2766356-2026-09-05T02-21-34-130Z/report.md` (gitignored, reproducible); spec `RAIL-CONTRACT-INFLUENCE-PROVENANCE-01` | The 7/11 result is a curated influence sample, not a general mutation score, statistical coverage estimate, or proof that any original value is right. Each survivor establishes only that exact leaf's non-influence under this ordered profile; it does not prove its whole field is dead or should be deleted. A structural or contract-derived kill proves rejection or realized consistency, not semantic correctness. The Dialog default-size mutant is first killed by the variant-realization audit, not an independent visual oracle; the variant rename is first rejected at admission. Every credited kill requires its reviewed marker. A later full verification exposed why: an unrelated showcase-test timeout occurred while the SSR mutant was active; the runner now reports that class as inconclusive rather than falsely calling it a new SSR oracle. | Product-intent review now has a measured queue: implement, explicitly mark advisory, or remove the four surviving facts. Expand next across channel wiring and token repoints; add runtime witnesses where a derived kill is insufficient. |
| Analytical relation kernel (L0-L2) | A single zod authority (`packages/ds-codegen/src/analytical/relation-model.ts`) emits `relation.contract.schema.json`, the assertion grammar and the fixture schema; a three-valued engine (`admissible` / `illegal` / `unproven`) adjudicates each corpus case against a frozen answer key it never imports and never names. Obligations narrow a judgment, they do not block it. Stage 2 added the **L3 derivation algebra**: seven typed operators (`aggregate-to-grain`, `join`, `nest`, `bin`, `normalize`, `project`, `graph`) under which a derivation of a relation is itself a relation, a boundary authority (`derivation.ts`) that certifies a *declared* result against the grain the operator determines, and peer-projection conservation obligations. Coordinate standing is earned, not assumed: every independently variable coordinate of the kernel carries a necessity witness whose outcome comes from an oracle case, or has left the kernel as `not-yet-admitted` with the stage that may re-earn it, or is **retained as `required-derived-vocabulary`** — accounted, because a name an external authority governs has no independent degree of freedom to witness, and deliberately NOT ratified, so a retained name never reads as a necessity claim. Those are three accounting modes and not three grades of the same one. | implemented / oracle-gated, **ratification bounded to L0-L2** | `packages/ds-codegen/src/analytical/**` (21 test files, 924 tests, re-derived 2026-09-17); `packages/ds-contracts/analytical-pack/` (corpus + vocabulary + form-name denylist, custody-digested by `corpus-integrity.ts`); `packages/ds-contracts/analytical-fixtures/` (23 files: answer-free fixtures plus the binding ledger, `census-stage1.json`, `witnesses.json`, `removals.json`, `baseline-stage1.json`, `holdout.json`, the `subtraction-stage2{,-holders,-interaction}.json` and `subtraction-stage2-enum-leaf.json` bases, `closures-stage2.json`, `erasure-footprints.json`, `stage2-freeze.json`, `stimulus-receipts-stage2.json`, `legacy-erasure-comparison.json`, `evidence-map.json`, `projection-experiment.json` (the bounded stage-3 candidate sets)); `pnpm run analytical:check-schemas` / `analytical:check-baseline` / `analytical:subtraction:gate` / `analytical:closures:check` / `analytical:footprints:check` / `analytical:freeze:check` / `analytical:final-quotient` / `analytical:stimuli` / `fixtures:check`; doctrine `ARCH-ANALYTICAL-RELATION-001`; specs `REL-FIELD-ALGEBRA-01/02`, `REL-A11Y-CORPUS-EXTEND-01` (closed), `REL-VIEW-ALGEBRA-01` (active) | **Nothing past L2 is ratified.** The doctrine's `status: active` ratifies that the L0-L2 kernel survives construction and subtraction, and nothing further. Stage 2 is a view *algebra*, not projection enumeration: there is no projection space, no channels, no enumerator, and no realization. The thesis this line of work exists to test — that realization is a compositional byproduct rather than a selection — is **not yet falsifiable**; making it so is stage 3's precommitted experiment, whose success condition and four gameability closures are recorded on the durable surface in the doctrine's stage-3 precommit. **Passing the oracle is not correctness.** The corpus is the answer key; an engine that agrees with itself proves nothing, and a case the corpus does not contain returns `unproven`, never `admissible`. **The holdout reaches derivations, and agreement is not correctness.** Nine of its seventeen items carry hand-derived expectations for the derivation boundary, covering all seven operator laws and both occurrence kinds (a diagnostic and an obligation); `stage1.test.ts` fails when a fixture newly reaches the boundary without a re-derived expectation. Agreement still does not show the cited law is the right law. **One locus states the per-operator transition law.** `OPERATOR_LAWS` in `derivation.ts` co-locates each operator's prose statement with its executable certifier, and a test pins that every operator kind carries both, so an eighth operator arrives failing rather than silent. Grain determination, semantic findings and identity remain separate per-kind switches (`determinedGrain`, `semanticFindings`, `derivationKey`), and a certifier can still stop matching its own sentence. **The stage-2 coordinate subtraction is open** — run `pnpm run analytical:subtraction` for the live disposition split rather than reading a count here. | Adjudicate every candidate in the frozen stage-2 basis to a reasoned verdict (`witnessed`, `representation-artifact`, or `not-yet-admitted` with a re-earning stage), which is `REL-VIEW-ALGEBRA-01`'s close condition; start with the `derivedBy.kind` family, which tests whether operator identity is itself primitive. **The 22 closure carriers are blocked by evidence, not by an undecided verdict.** Measured over the committed 212-specimen population, each of the seventeen unresolved footprint dependencies has an erasure that DOES collide specimens — so none is `unseparated`, the instrument reaches them — but every collided pair the oracle binds carries the same outcome on both sides. No witness is constructible for any of them, so no verdict exists that this corpus would support, and the four that are `witnessed` (`nonAdditiveAlong#arity`, `toGrain#order`, `join.cardinality:one-to-many~many-to-many`, `nest.levels#order`) are primitive by stage-1 re-earning and therefore decided *against* those closures. Closing the subtraction requires adding discriminating corpus cases first, or deciding those coordinates by policy deliberately; filing them to unblock the gate is the move the standing index exists to prevent. **Four of the 22 dependency coordinates are adjudicated `witnessed`, one is retained as `required-derived-vocabulary` and the remaining seventeen are not, and the difference is measured in three stages.** An oracle-separated collided pair is necessary and not sufficient: of the ten candidates that have one, `checkWitness`'s isolation clause refused two of the three `relation.derivedBy.bin.closure` coordinates, and the reason was the clause's SCOPE rather than its principle: it exempted a semantic finding that the erasure produced, but stated that rule for one class (a member pair on a discriminator) while measuring something else — whether the erasure reached beyond the coordinate's own slot. Stated for what it measures, the exemption admits the ideal pair (one fixture IS the other minus the declaration, `REL_BIN_CLOSURE_UNDECLARED` against admissible), so both coordinates are now adjudicated `witnessed`; the third still fails `NO_COLLISION` because the corpus has no pair varying only which side is closed. The well-formedness line did not move: a slot-local erasure that introduces `REL_DERIVATION_*` is still refused, and the doctrine records that no fifth disposition was needed — a declared necessity is an ordinary necessity whose pair happens to be one fixture minus the declaration. The footprint audit separately refuses `evidence.rows.*#present`, and a sweep of every unresolved candidate for a pair the erasure is a no-op on one side of finds none, so that refusal is a MISSING FIXTURE SHAPE rather than a mis-chosen pair: 162 of 212 specimens carry no rows, yet no fixture IS another fixture plus rows, which is what collision requires once the erasure leaves a row-less fixture untouched. The nineteen adjudicated so far carry single-coordinate witnesses, and two more are RETAINED rather than ratified: `relation.derivedBy.kind` and `relation.derivedBy.join.cardinality`. Invariant 17's factoring is measured for both -- footprint(leaf) is exactly the leaf plus its member pairs (22 and 7) and every pair's footprint is exactly itself -- so each leaf's necessity is implied by any single pair and it is recorded `required-derived-vocabulary`. That answers the `kindInterpretation.openQuestion` the doctrine left open for the operator discriminator, and it moves the closure gate: `relation.derivedBy.join.cardinality` is one of the footprint dependencies, so the gate reports 21 unsettled dependencies rather than 24. Leaving 64 unresolved candidates. **And a sweep must bind outcomes by CONTENT, not by id.** The population deliberately holds 26 ids more than once — `separatingPairs` clones a base fixture and keeps ITS id, and `specimens()` dedupes by content — so 85 of the 212 specimens carry an oracle outcome by id that is not about their content. Resolved by id a witnessability sweep reports two discriminable candidates; resolved by content it reports one (`evidence.rows.*#present`), which then fails the presence test. **Most unresolved candidates are evidence-blocked, and the reference-topology operand family is not.** The ones whose own erasure is answerable wait on a discriminating pair the corpus does not yet carry — but the `reference-topology` operand family waited on the QUOTIENT instead, and that quotient has LANDED: the erasure rebinds a bound slot to the operand's own declared names (`x-fsds-operands` on the branch, the pool read off the fixture), so measured over the 34 live ones the never-discharged count fell 18 → 6, with 6 more discharged and 6 corpus-dead (their canonical bind is the binding the corpus already writes — the slot holds the first-declared relation, a corpus fact C1e enumerates, not a walk gap). No production module resolves outcomes over the population by id (all four `outcomeOf` call sites are corpus-keyed); the hazard is confined to ad-hoc sweeps and is now pinned. **And the obligation is targeted, not sixty-four-wide.** Decomposed by coordinate kind the 64 unresolved are 23 `reference-topology` facets, 28 member pairs, 8 member absences and 5 leaves; decomposed by what the closures wait on, every blocked closure depends on at least one reference-topology facet and sixteen depend on nothing else. Only 10 of the 23 facets block any carrier and 49 of the 64 block nothing at all, so the corpus obligation is those ten facets plus the six closures that also need a member pair. **And a keystone facet is reachable by an AUTHORED stimulus, not only by a corpus case.** The adjudication policy admits a new `{base, patch}` exactly where its judgment follows mechanically from an already-existing cause, and `relation.derivedBy.nest.levels#order` is the first instance: reordering the declared levels to `[state, country]` removes `country` from the declared prefixes, so the existing `toGrain: [country]` subtotal becomes off-grain under `CASE_NESTED_SUBTOTALS_OFF_GRAIN`, and nothing else about the fixture moves. `checkWitness` accepts it and the verdict is `witnessed`, which makes the ORDER PRIMITIVE -- and that turns six more closures (`aggregate-to-grain~nest`, `join~nest`, `nest~bin`, `nest~graph`, `nest~normalize`, `nest~project`) into obligation 8's composite diagnosis rather than an outstanding one, joining the two additivity closures. **And the route reaches a coordinate with no corpus anchor at all.** `relation.derivedBy.aggregate-to-grain.toGrain#order` has no committed instance to build a pair from — every corpus `toGrain` names one column — so BOTH sides are authored, sharing the surrounding shape (the result relation's declared grain and fields) identically and differing at exactly the two `toGrain` positions: `[country, state]` admissible against `[state, country]` illegal with `REL_GRAIN_SUBTOTAL_MISMATCH`. `checkWitness` accepts the single-coordinate witness and `checkIsolation` discharges on both sides, so the verdict is `witnessed` and the other five `aggregate-to-grain~X` closures carry obligation 8's composite diagnosis too. **And the keystone partitions into three kinds of gap, not one.** Of the unadjudicated `reference-topology` facets the picture moved with the landed quotient: 0 have an erasure that is never discharged anywhere it moves — the list is empty (six until the peers declaration, five until the distinctness constraint, three until the mirror took the two-sided facts by taking the law's other side as the pool); 6 erase nothing on any committed fixture because their canonical bind IS the binding (the slot holds the first-declared relation), a corpus-SHAPE gap a fixture declaring its input second would close; and the discharged remainder await separating pairs or newly admissible witnesses, a corpus-COVERAGE gap the landing widened the door to. **The arity facets are now settled, and they split three ways by REACH rather than by kind.** Three carried an authored `{base, patch}` pair whose sides differ at exactly that coordinate and are now `witnessed`: `nonAdditiveAlong#arity` (the near-neighbour blocks one dimension, adding the dimension the assertion sums along fires `CASE_INVENTORY_SUMMED_OVER_TIME`), `grainWitness#arity` (a one-column witness repeats and fans out, the pair does not — the corpus triad's own hostile and satisfying sides), and `peers[]#arity` (the divergence law is conditioned on EVERY member being an aggregate, so the count of members decides whether it applies — a non-claim, since that route reaches admissibility by adding a non-aggregation). The other two cannot be witnessed and are NOT the same fact: `toGrain#arity` is **not independently erasable**, because the law requires the declared result grain to equal the target grain as a set, so truncating `toGrain` alone leaves the image unable to type its own result; and `nest.levels#arity` is **evidence-blocked**, because a three-level nest is admissible exactly as a two-level one is, so no corpus cause separates the two arities. **And the quotient blocker is now a LANDED erasure with six residues, not eighteen.** Canonical REBINDING -- replacing the slot's names with the operand's own first k declared names -- is the production erasure for every bound slot: `x-fsds-operands` declares each derivation branch's operand namespaces, the census carries the namespace on the locator, and the executor rebinds to declared names in declaration order. Declared distinctness then took two (`distinctFrom` on the operand map: the canonical rebind skips the sibling operand's binding, so `join.with` never lands on `from` nor `edgeTo` on `edgeFrom`, and both are corpus-dead — the sibling-skipping pick is the binding the corpus writes). Three coordinates remain refused by the boundary's own well-formedness line, each for a named reason (`toGrain` rebinds to a field the derivation cannot reach that way, `keep` both facets to a selection that drops a field the result still declares, and `structure.peers[]#incidence` -- which the peers DECLARATION has since mended: the structure carries the same `x-fsds-operands` map the branches carry, an array's elements inherit their annotated property's namespace, the peer sets rebind to declared relation names, and the coordinate is discharged wherever it moves -- lawful in BOTH binding regimes (a base-relation bind leaves the image clean; an aggregate bind fires REL_PEER_GRAIN_DIVERGENCE, the semantic distinction under test, pinned in necessity.test.ts) -- leaving zero residues — the mirror measurement showed the law's equation holds in every corpus declaration, so taking the OTHER side as the pool needed no census change at all). The prototype's thirteen-of-twenty-one division was pessimistic in one place: its refusal test was coordinate-wise while evidence is refused per-stimulus, so `bin.field` is discharged where its rebinding lands on a declared field the boundary accepts; `toGrain`, `keep` and `edgeTo` really are refused, and their coordinate's extent being wider than its locator remains the census change a quotient for them has to make. **The round-25 stimulus is admitted for `nest.levels#incidence`** -- the pair that showed the distinction is real now carries a witness -- and what the discharge COSTS is measured and POLICED at admission: the rebinding lands on one arrangement, so forgetting incidence also forgets order and the committed `#order` witness's arrangement-only pair collides under `#incidence` too -- but `DIFFERENCE_MISATTRIBUTED` refuses the recast (the difference is order's degree of freedom) while the pair stays admissible for `#order` alone and for the joint set, and no committed witness is refused (the hazard was authored-only; authored stimuli are unbounded, so the rule lives at admission rather than as a pre-filed ratchet). `C1f` pins the six and both admitted pairs; `C1h` keeps the prototype and pins that the landed executor agrees with it on every coordinate both express. **No verdict moved on the erasure side**: `witnesses.json` is unchanged and the supersession classes were over images, not verdicts. Filing under the new quotient then happened, and it was REVERSED by the standing repair: the round-25 pair was filed for `nest.levels#incidence` (freeze split briefly 17 witnessed / 73 unresolved), and the verdict returned to unresolved because forgetting incidence lands on ONE canonical arrangement and takes the sibling facets `#order` and `#arity` with it while the list survives -- so the witness HOLDS and stays filed as evidence, but it does not ratify PRIMITIVE standing, and `witnessed` means exactly that here. The repair is one classifier (`support.ts`) consumed by `primitiveRatified`, the witness audit, the closure ledger and `final-quotient`, plus the drift check wired into `analytical:subtraction:gate`; the test is the CARRIER rather than the leaf, so a leaf's own member refinements and a deleted holder's contents remain sub-distinctions and keep primitive standing (58 ratified of 60 holding single-coordinate witnesses), while a facet destroyed in place does not. The published ledger needed no retroactive demotion: `along#present`'s collateral is conditional on the presence it denies. **The candidates are TRIAGED before filing**: read as single-coordinate witnesses, the live basis partitions 74 atomic / 8 own-refinements / 2 sibling-facet / 1 outside — 61 of the 64 unresolved could in principle be ratified, while exactly three (`nest.levels#incidence` and `toGrain#incidence`, each taking its `#order`; `structure.peers[]#present`, reaching outside its holder) can never be ratified by a primitive claim and are owned by the interaction family. Pinned two-directionally, so a future erasure change cannot silently widen the unwitnessable set. **And non-identity is not evidence**: of the 60, exactly ONE is separable by a committed fixture pair (`evidence.rows.*#present`, whose pair is about row CONTENT and so was never filable), 17 have a separating SHAPE among the synthesized specimens (authoring backlog, not citable stimuli), and 44 have none — so the remaining work is a discriminating corpus case or an authored stimulus, and NOT further instrument repair. Pinned three ways. **And the evidence pool is a CLAUSE rather than a stated intention, which moves one of those seventeen out of the backlog.** The doctrine always said a witness side's required outcome must be a judgment the corpus supplies and that the boundary's well-formedness refusals are not in that pool — but nothing enforced it, because a hand adjudication's `cause` is free text. `checkWitness` now reads the pool off the SAME oracle the sides resolve against and refuses an authored outcome that names no diagnostic, names a `REL_DERIVATION_*` code, or cites a code or term the corpus does not carry (`OUTCOME_NOT_CORPUS_SUPPLIED`). All thirty-seven hand-adjudicated sides on file cite the corpus, so the clause refuses no existing witness, and the pin runs both directions. Re-measured over the three remaining single-path candidates as each would actually be filed, `relation.derivedBy.graph.value#incidence` and `relation.derivedBy.normalize.field#incidence` author cleanly (their COMMITTED sides are illegal under `REL_FLOW_NOT_CONSERVED` and `REL_ADDITIVITY_NORMALIZE_NONADDITIVE`), while `relation.derivedBy.project.from#incidence` does not: the side that fails to type is the AUTHORED one, so its specimen shape is not evidence and sixteen of the seventeen remain authorable. Which side carries the refusal is invisible in the specimen population, because `separatingPairs` clones a base fixture and keeps its id. **And the first witness under the repaired standing is filed**: `evidence.grainWitness#incidence` moves unresolved -> witnessed on an authored single-path stimulus that follows from the corpus's fan-out cause (`REL_GRAIN_FANOUT`, instance evidence — the declared witness column repeats across rows), classes ATOMIC so it ratifies primitive standing (the ratified set was 59 of the 60 holding single-coordinate witnesses), and passes the drift check on all three bases. **And a SECOND witness under the repaired standing is filed**: `relation.derivedBy.aggregate-to-grain.from#incidence` moves unresolved -> witnessed on an authored pair over the committed `FX_NESTED_SUBTOTAL_OFF_HIERARCHY` — the a-side is illegal under the existing `CASE_NESTED_SUBTOTALS_OFF_GRAIN` (a subtotal at a grain the nest does not declare) and the b-side moves `structure.relations.subtotals.derivedBy.from` to `sales`, where the same declaration is admissible and nothing else about the fixture differs — classes ATOMIC, so it ratifies primitive standing (60 of the 60 holding single-coordinate witnesses) and passes the drift check on all three bases. Filing it takes the subtraction to 66 unresolved of 118, the freeze split to 18 witnessed / 72 unresolved, and the ratifiable unresolved set to 63. **And the filings move the specimen population, which is why the counts above are measured rather than assumed**: an authored patched side is minted with a `_PATCHED` id, so it takes the id slot of the synthesized specimen it pre-empts (the same content, previously reachable by the base fixture's id), which is the whole of the movement from 110 distinct ids and 167 oracle-bound specimens to 111 and 166, and it adds the two generated per-witness cases (`C2`, `C7`) that take the analytical suite to 20 files and 866 tests. **And a THIRD witness is filed on a member pair**: `field.additivity.kind:non-additive~ratio-measure` moves unresolved -> witnessed on an authored pair over the committed `FX_GDP_PER_CAPITA_ROLLUP_MEAN` — its `gdp_per_capita` field is a ratio measure averaged to a region grain, so the committed side is illegal `REL_RATIO_MEASURE_AVERAGED`, and declaring the same field non-additive makes the judgment admissible with nothing else differing — classes ATOMIC, so it ratifies primitive standing (61 of the 61 holding single-coordinate witnesses) and passes the drift check on all three bases. Filing it takes the subtraction to 65 unresolved of 118, the freeze split to 19 witnessed / 71 unresolved, and the ratifiable unresolved set to 62. The population lesson repeats with a twist: this patched side carries a `_PATCHED` id that ALREADY exists (the additive-against-ratio-measure witness minted it), so it swaps one synthesized specimen without adding a distinct id — 26 ids now repeat, 165 specimens are oracle-bound, 130 of the row-less ones are, and the suite is 868 tests. **And a FOURTH witness is filed, on the null enum's second member pair**: `observation.null:censored~suppressed` moves unresolved -> witnessed on an authored pair over the committed `FX_SURVIVAL_MEAN_WITH_CENSORED_ROWS` — the committed side reads a censored bound as an observed value and is illegal `REL_NULL_CENSORED_AS_OBSERVED`, while writing `suppressed` at that one slot leaves `null:missing-mechanism` outstanding instead, so the two members are told apart by which finding they attract rather than by the encoding — classes ATOMIC, so it ratifies primitive standing (62 of the 62 holding single-coordinate witnesses) and passes the drift check on all three bases. Filing it takes the subtraction to 64 unresolved of 118, the freeze split to 20 witnessed / 70 unresolved, and the ratifiable unresolved set to 61. Its `_PATCHED` id also already exists — the absent-against-censored witness mints the same one over the same base — so again no distinct id is added and the repeated-id count holds at 26, while the oracle-bound figure falls 165 -> 164 and the suite is 870 tests. The legacy ledger keys its divergence records by specimen id, so it is re-recorded on any population-id movement. **And a FIFTH witness is filed, on a derivation's FIELD binding**: `relation.derivedBy.normalize.field#incidence` moves unresolved -> witnessed on an authored pair over the committed `FX_SHARE_OF_AVERAGE_SCORE` — the committed declaration normalizes the non-additive `avg_score`, which is illegal `REL_ADDITIVITY_NORMALIZE_NONADDITIVE` under `CASE_NORMALIZED_AVERAGES`, and binding the relation's key field `region` instead makes the same declaration admissible — classes ATOMIC, so it ratifies primitive standing (63 of the 63 holding single-coordinate witnesses) and passes the drift check on all three bases. Filing it takes the subtraction to 63 unresolved of 118, the freeze split to 21 witnessed / 69 unresolved, and the ratifiable unresolved set to 60 (1 committed pair / 16 specimen-shape / 44 none). It is the FIRST filing that blocks a closure carrier: two `normalize~X` closures now carry obligation 8's composite diagnosis instead of an outstanding one, while the non-blocking unresolved count stays at 48 — the previous four filings blocked nothing, and the pin records the difference. This `_PATCHED` id is new, so the distinct-id count rises 111 -> 112 and the oracle-bound figure falls 164 -> 163.  **And the evidence pool is enforced, but VOCABULARY is not APPLICABILITY.** Two clauses now stand where one did: `OUTCOME_NOT_CORPUS_SUPPLIED` refuses an authored outcome whose codes or terms the corpus does not carry, and `OUTCOME_NOT_OBSERVED` refuses one the ENGINE does not produce for that stimulus — a hand adjudication is a claim about what the engine says, and until this clause nothing checked it. Together they retire the fifth filed witness: `evidence.grainWitness#incidence` had changed its witness column from `order_id` to `orders`, which is the RELATION name and not a declared column, so every row produced the missing-value sentinel and the rule reported a repetition the corpus never observed. `hasDuplicates` now reads a witness only from rows that carry a value for every named column, so an unresolved column and an unobservable one leave the `grain:declared` obligation outstanding — distinguished by evidence class, schema against instance — and a duplicated readable value still produces the instance finding. **The two directions are not symmetric, because they range over different populations.** A repeat among the readable rows refutes uniqueness over the SUPPLIED population, which contains them; the absence of a repeat over a proper subset establishes nothing about the rows that were discarded, each of which may carry a key that repeats one already read. A unique readable value is therefore admissible only when EVERY supplied row was readable, and a partially readable witness leaves the same INSTANCE-class obligation standing instead of admitting the assertion on evidence that never covered the population the conclusion ranges over. The fabricated entry is removed rather than re-pointed; the coordinate is `unresolved` with that reason, and the replacement pair over two real declared columns is the next slice. The rule digest therefore moved and the holdout was re-verified item by item (17 items, 0 mismatches: no holdout fixture declares a `grainWitness`) before it was re-recorded. NON-CLAIM: the two states are distinguished by EVIDENCE CLASS and detail, not by two catalogued codes — naming the declaration-level defect with its own diagnostic and corpus case is still owed, and obligation 8's eligibility is still the complement of three blockers rather than a positive list. **And retention cannot absorb the three that cannot be ratified**: `required-derived-vocabulary` requires the retained verdict to name a live retained coordinate the content is derivable from (`final-quotient`'s close condition 4 — invariant 17's factoring, which is why `derivedBy.kind` and `join.cardinality` are retained rather than witnessed). `nest.levels#incidence` satisfies the policy's candidacy clause (a committed cause needs the distinction, and erasing exactly it makes that distinction unexpressible) and FAILS admission, so it stays `unresolved`; the same holds for `toGrain#incidence` and `peers[]#present`. Since obligation 8 admits only non-primitive, non-interaction, adjudicated dependencies, those three have no terminal class today — a decision about the vocabulary (remove, re-earn at a later stage, or name a sixth accounting mode) rather than a workaround, and it is the last structural blocker on closing the subtraction now that the instrument, the standing and the evidence are all measured. **And the six join-cardinality member pairs are now triaged rather than pending.** One carries a holding witness: `one-to-many~many-to-many`, whose two sides are BOTH corpus-licensed — the a-side is the corpus case `CASE_REVENUE_DOUBLE_COUNTED_AFTER_JOIN` and the b-side is the same declaration at `many-to-many`, whose required outcome adds the corpus's `grain:declared` obligation because a join that multiplies on both sides determines no result grain. The other five are refused as EVIDENCE and stay unresolved, for two measured reasons: `one-to-one~many-to-one` because the authority determines the same result grain for both members, so no corpus cause separates them; and the four pairing `one-to-one`/`many-to-one` against `one-to-many`/`many-to-many` because the ONLY distinguishing outcome on one side is `REL_DERIVATION_RESULT_NOT_DERIVABLE`, which the doctrine states carries no corpus case, so those pairs do not follow from an already-existing Stage-2 cause. **Whether such a pair should count as evidence is a policy question this triage opens rather than settles**, and it is the first refusal that turns on the KIND of required outcome rather than on the erasure or the corpus. `C1f` in `necessity.test.ts` pins the partition, so reading a keystone facet as simply a missing corpus case is refuted for the first class. **And the ORDER facet is now emitted only where the declaration says the sequence is read.** A JSON array cannot say whether a collection is a set, so the census used to emit `#order` for both kinds. Every array-of-Name in `relation-model.ts` now carries `x-fsds-sequence`, `listFacets` THROWS on a list that states neither value, and the five set-valued facets (`keep`, `along`, `nonAdditiveAlong`, `peers[]`, `grainWitness`) LEFT the kernel with `representation-artifact` verdicts — taking the unadjudicated count from 77 to 72 and the closure dependency set from 23 to 21. `final-quotient` recognizes them as a third factorization form (`declared-set-order`), checked against the same declaration the walk read, so `nest.levels#order` — which IS ordered — can never be filed that way. The two ordered lists keep their facet and are semantically real: `levels` order moves the judgment, and `toGrain` order is reached by an AUTHORED pair because no committed fixture carries a two-element target grain. `C1g` pins the totality of the declaration, the partition, the shape-identity that made the old reading impossible, and the `relation.grain` asymmetry (a name list declared as a union, so the walk emits the leaf and none of the three facets, inert over its 32 multi-element instances). **The carriers are blocked FIRST by absent stimuli, and a receipt is not automatically the remedy.** Nineteen of the twenty-two closure records declare no controlled stimuli, so obligations 3-6 are not evaluable for them; the stage-2 receipts supply a pair only where that closure's own obligations accept it, which was measured rather than assumed — four of the six (every `~graph` pair) fail obligation 5, so carrying them across would have manufactured four `refuted` promotions, while the two `~project` pairs discharge 3-6 and are carried (five records now carry stimuli, up from three). Then stage 3's precommitted experiment: can analytical realization be *derived* rather than selected? **A bounded version of that experiment runs, and it is an EXPERIMENT, not a ratification.** `projection.ts` enumerates projection programs for one admitted object (`FX_N_STOCK_SUM_ALONG_PRODUCT`) as the product of declared coordinate, channel and task choices and filters them by channel capacity and the task's required claims: 42 structurally distinct topologies over a post-exclusion population of 99 (168 triples considered, 69 excluded where a channel has no slot for a scale). **The all-pass interpretation of the first run is withdrawn.** That run carried no bound analytical operation, so the same topology could display a sum over one dimension, a sum over another, or the raw observations, and attributing established preservation and loss to it was an overclaim. Every program now carries a `BoundOperation` — relation, field, aggregate, the dimensions summed over and the result grain — bound from the fixture's own admitted assertion; a binding that does not resolve against the structure, or is absent, is carried as the `grain:declared` obligation rather than admitted on a favorable branch. **Preservation is now observed through two PRODUCED representations, which is what the earlier consumers did not establish.** Both of those called `evaluateOperation`, so nothing representation-dependent lay between them: their agreement showed the binding and channel guards admit one shared evaluation, not that a value survived an encoding. The enumerator now filters on facts derived from the operation and the relation it NAMES — the result's own group column and the aggregate, never a source column the operation summed away — and validates the operation before any candidate exists (a relation it does not declare, a grain it contradicts, or an aggregate other than `sum` is REFUSED rather than executed as a sum). **Structural resolution is not semantic admission, and a preservation result is not an admission result.** A cross-date sum over a measure declared non-additive along date RESOLVES perfectly and was admitted, enumerated and preserved while contradicting the declaration; the operation is now judged by the same engine that judges the corpus, so an illegal one is refused with the corpus's own causes and an unproven one is carried. A declared composition partition that is not a column of the RESULT is refused at the same boundary. **And PROGRAM CHOICE now decides which representation is produced:** `produce` alone could build both from any result, so with an empty channel inventory enumeration retained nothing while the standalone report still emitted both outputs. `lower` consumes an actual retained program, its measure channel selects the representation or refuses to produce one, and the preserved round trip is reported per program — readback recovers 30 and 105 with a key-binding mutation detected at an unchanged total, metric recovers them from extents 60 and 210 under a declared scale with an extent mutation detected. One evaluated result produces a readback representation and a metric representation, and each is decoded by a function that receives ONLY its own output: the metric decoder recovers 30 and 105 from extents 60 and 210 and the DECLARED scale. A bumped extent is detected, and a shifted group binding is detected while the total is held at 135, so preservation is distinguished from agreement on a total. **What the observers do NOT check is recorded and pinned:** neither validates a topology against the capacity table and neither consults grain status, so a program whose coordinate cannot host its measure channel, and one asserting an aggregate magnitude under an unknown grain, are refused or left undecided by the ENUMERATOR while both observers accept them. A3 and A4 therefore establish that the observer reads the program rather than its certificate and that it detects a changed analytical BINDING; they do not establish complete admissibility checking. **Two of the four precommitted metamorphic controls carry REFUTED concrete predictions, recorded as refutations rather than as vacuous passes:** the value-readback family was named as preservable under ratio-to-ordinal and none survived, and it was treated as grain-independent when a bound program's result grain makes every topology grain-dependent. A composition whose partition is OMITTED is no longer resolved to the favorable completion (53 candidates carried under `invariant:exhaustive`), and a measure whose additivity KIND is `non-additive` or `ratio-measure` is refused over any partition rather than only where a dimension list names it. Stage 2 stays unratified, no coordinate is adjudicated, and the result is conditional on that basis, that population and the declared candidate universe: it does not establish rendered behaviour, cross-substrate behaviour, that any topology can be realized, or that the representation's coordinates are necessary. |

## Resolved architectural finding: component CSS collision isolation (option a)

Research during `ICONOGRAPHY-TOKEN-DISCIPLINE-02` (Phase 5) recorded a real, unmitigated collision concern. Fix option (a) — `@layer` wrapping — shipped under `FEAT-BRAND-PERSONA-SCOPE-01` (closed, worktree-merge `8107091b`) and `FEAT-BRAND-PERSONA-VISUAL-QA-01` (closed, worktree-merge `3c5f398f`):

- **Component CSS is now layered.** `generateLayerDeclaration()` (`packages/ds-tokens/build/generators/global.ts:917`) declares a 6-layer order — `@layer core, semantic, components, theme, brand, density;` — and `emitTokensCss` (`packages/ds-codegen/src/css.ts:379`) wraps every generated component's structure/tokens CSS in `@layer components`. This closes the original gap: FSDS component rules are no longer unlayered.
- **Why this also fixes collision isolation, not just intra-FSDS overrides.** Per the CSS Cascade Layers spec, unlayered styles always win over layered styles regardless of specificity or layer order. Moving component CSS into `@layer components` means a consuming app's plain (unlayered) CSS now reliably beats FSDS's component CSS without `!important` or specificity tricks — the collision concern this section originally flagged.
- **What it unlocked inside FSDS:** `components.<Name>.<path>` brand overrides (`packages/ds-tokens/src/brands/_schema.json`, `components` property; implemented via `componentNameToKebab`/`componentTokenPathToCSSVar`/`walkComponentBrandSubtree`/`processComponentBrandTokens` in `packages/ds-tokens/build/generators/global.ts`) land in `@layer brand`, which is declared after `@layer components`, so a brand's component-scoped override now deterministically wins over that component's own default without needing to out-specificity it.
- **Non-claims, carried forward:** this closes fix option (a) only. Option (b) — prefixing every generated selector with `[data-fsds-component="<cssPrefix>"]` for per-component isolation granularity — remains undone; the identification attribute from Phase 5 is still identification-only and does not prefix any selector. The layer mechanism is a `<link>`/`<style>` document-level cascade feature — it does not reach Lit's shadow DOM (`emitLitInlineCss` renders inside a separate cascade context `@layer` cannot cross), so Lit's style isolation still comes from shadow DOM boundaries, not from this layer order.

## Stale-doc corrections this snapshot makes explicit

Several older docs are still useful, but their frontmatter/status or prose lags the code:

- the machine-local successor ledger (`docs/internal/successor-work.md`) must not be read as "all listed surfaces are unimplemented." Some items are now partial or promoted, especially target-pack registry, component evidence pages, usage examples, and runtime rail.
- README no longer carries a hand-maintained component count (removed in `7a325195`); loader discovery under `packages/ds-codegen/src/contracts-fs.ts` is the count authority. Derived docs must still reconcile on corpus growth — `docs/internal/successor-work-mobile-rail-sweep.md` (machine-local, gitignored) is the current live counterexample (see the mobile-parity row).
- The five-framework claim is still true but narrower than the broader target story. Web DOM retains the end-to-end emit → admission rail → runtime evidence described above; React Native is also rail-admitted. Native and engine targets have separate bounded compiler or runtime witnesses, including the Unity/Godot rows above. Those witnesses do not establish general cross-substrate parity. Local target packs remain metadata-only, and Figma descriptor emission does not prove live publication.
- Runtime rail evidence is newer than the original architecture docs. Any answer that treats runtime preview/fact testing as future work is stale.
- The behavior-realization batch (2026-07-22) landed after the whole-ledger reconciliation: any doc or row text describing Accordion as behaviorally dead, Select/Calendar events as unwired, Calendar as count-iterated (`daysShown`), portal computation as orphaned, or the behavior-realization audit as future work is stale — the batch rows and the corrected runtime-rail/iteration/substrate rows are authoritative.
- The whole ledger was reconciled at `main@c5cda2c9` (2026-07-21, `DOC-SNAPSHOT-RECONCILE-02`) — every row re-verified against live code, and the formerly-absent surfaces (recursive composition, slot-projection, textOverflow, native compile lanes, iconography, examples portfolio) gained rows. The two live defects that sweep surfaced and recorded rather than fixed silently are **both now green**, re-run at the 2026-08-17 sweep: the render-binding rail's matrix path (`node scripts/render-binding-audit/audit.mjs` exit 0 → `docs/internal/render-binding-audit/render-binding-matrix.md`, 50 components / 309 prop rows, 0 failing class A, 0 failing class B) and the mobile-parity `--check` red (`node scripts/mobile-parity-matrix.mjs --check` exit 0, "50 components, 8 targets, 8 dimensions; derivations consistent"). Both artifacts land under gitignored `docs/internal/`, so they are reproduced per run, not committed.
- The 2026-08-17 re-sweep (`DOC-SNAPSHOT-RESWEEP-01`) re-derived every row again at `main@90804abc`. It changed no capability claim — the corrections were 6 stale counts (a11y ledger, dead-slot totals and dispositions, corpus 49→50 in three rows, behavior-audit self-test tally) and 3 under-claims (two finished campaigns still listed as pending work: `FEAT-A11Y-RELATIONSHIP-STRAGGLERS-01` and the dead-slot `repoint` disposition; plus two example lanes and one closed spec's evidence missing from their rows).
- The visual-fidelity/token-enrichment campaign still has no dedicated row (its outcomes surface via the styling-realization audits row); recording it row-grade requires its own verification pass.

## Current bounded thesis

The project is a contract-governed design-system substrate with generated Web DOM framework realizations, target-pack admission scaffolding, Figma descriptor projection, token governance, generated-artifact admission, runtime fact assertions, and partially derived component evidence pages.

The claim boundary is bounded. It does not yet prove production adoption ergonomics, complete accessibility adequacy, visual quality, npm-distributed packages, live Figma library publication, executable external target packs, or substrate-neutral UI semantics. The non-web mobile arc has narrowed — but not closed — the substrate-neutrality question: React Native is now a default-rail target with a generated package, typed native token projection, native variant/state style realization, real package-type admission, focused host-render + surface tests, and a public-export settings example typecheck lane, while simulator/device runtime behavior, full surface parity, compound-part emission, part-scoped/boolean-modifier styling, and platform accessibility parity remain unproven.
