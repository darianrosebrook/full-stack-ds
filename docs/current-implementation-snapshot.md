---
doc_id: REF-CURRENT-IMPLEMENTATION-SNAPSHOT-001
authority: canonical
status: active
title: Current Implementation Snapshot
owner: "@darianrosebrook"
updated: 2026-07-22
governs:
  - README.md
  - docs/**/*.md
  - fsds.targets.json
  - .github/workflows/ci.yml
  - e2e/runtime-rail.spec.ts
  - packages/ds-codegen/**
  - packages/ds-contracts/**
  - packages/ds-iconography/**
---

# Current Implementation Snapshot

This is the routing document for current project state. Read it before older architecture notes, ADRs, and successor-work docs when deciding whether a surface is doctrine, partial machinery, or proven machinery.

Snapshot point: `main@c5cda2c9` (2026-07-21, full reconciliation under `DOC-SNAPSHOT-RECONCILE-02`). Every row was re-verified against live code at this commit by dedicated read-only verification passes: five drift lanes (recursive composition, composer slot-projection, textOverflow/RN IR-authority, native compile lanes + rail descriptors, iconography) plus a staleness audit of every pre-existing row. Known surfaces still without a row: the visual-fidelity/token-enrichment campaign (its outcomes surface indirectly through the styling-realization audits row's gap counts) — recording it row-grade requires its own verification pass.

This document is not a replacement for the underlying architecture docs. It is an index of claim strength: what is implemented, what the evidence actually proves, what it does not prove, and which older docs should no longer be read as the only truth surface.

## Freshness rule

Use this order when answering "what is true now?":

1. Current implementation snapshot.
2. Code and workflow surfaces at the cited commit.
3. Implemented/proven docs with `verified_at_commit`.
4. Architecture and reference docs.
5. the machine-local successor ledger (`docs/internal/successor-work.md`, when present) only for remaining work, not as a statement that everything listed is unimplemented.

If an older doc says a surface is future work but this snapshot identifies an implemented surface, treat the older language as stale until that specific doc is reconciled.

## Current claim ledger

| Surface | Current claim | Claim strength | Evidence surface | Non-claims | Next admissible move |
|---|---|---|---|---|---|
| Contract corpus layout | Component contracts live under `packages/ds-contracts/components/<Name>/<Name>.contract.json`, with sidecars for tokens, styles, and usage examples. The loader, not a hand-maintained README count, is the authority for corpus discovery. | implemented | `packages/ds-codegen/src/contracts-fs.ts`; `vite-plugin-fsds-data.ts` | A visible component count in prose is not an authority surface. | Done — README no longer carries a hand-maintained count (commit `7a325195`); the loader remains the sole count authority. Remaining discipline: derived docs must reconcile on corpus growth (see the mobile-parity row's `--check` drift for a live counterexample). |
| Five Web DOM framework generation | React, Vue, Svelte, Angular, and Lit remain the primary executable Web DOM family. Codegen emits framework-specific component source, tests, styles, behavior primitives, and barrels from the same contract/IR path. | implemented / rail-admitted | `packages/ds-codegen/src/frameworks/*`; `packages/ds-{react,vue,svelte,angular,lit}/src`; `pnpm run governed:rail` | This proves cross-framework Web DOM realization, not substrate-neutral UI semantics. | Promote target-family IR boundaries out of implicit Web DOM assumptions. |
| Generated artifact admission rail | Required-mode rail is wired into CI and binds generated artifacts to manifest, contract bytes, codegen source bytes, framework checks, and bounded environment. CI also refuses generated drift after regeneration. | implemented / CI-gated | `.github/workflows/ci.yml`; `docs/specifications/admission-rail.md`; `docs/specifications/governed-ci.md`; `packages/ds-codegen/src/validation/**` | It does not prove determinism, full environment attestation, per-file semantic proof, visual quality, or accessibility adequacy. | The former next move (fold every rail participant into the manifest's framework vocabulary) was superseded by the closed descriptor specs (`RAIL-ADMISSION-DESCRIPTOR-INTERFACE-01`, `RAIL-TARGET-SELF-DECLARED-DESCRIPTOR-02`), which deliberately chose the opposite design: rail participants self-declare admission facts via a descriptor registry, and native lanes were admitted through a parallel narrow `RailTargetId` rather than widening `FrameworkId` (rationale committed in `validation/types.ts`). Remaining: extend the descriptor pattern to target-pack provenance in the manifest, and wire `rail-toolchain-coupling-audit --check` into CI. |
| Admission-descriptor architecture + native compile lanes | Six rail-admitted TS-toolchain targets (react/vue/svelte/lit/angular/react-native) self-declare their admission facts (`outputTreeRelPath`, `sourceExtensions`, `plan`, `reportRank`) via `AdmissionDescriptor`, aggregated and load-time-validated centrally (`buildRegistry` enforces id↔plan agreement, no duplicates, contiguous rank); `figma` stays rail-excluded. Former parallel literals in four consumers are now derived from the registry. Separately, the rail admits two non-pnpm native compile lanes — Kotlin/`kotlinc` and Swift/`swiftc` — via `RailTargetId` (the union of `FrameworkId` and `NativeLaneId`), each CI-wired with real positive (valid fixture compiles) and negative (broken fixture must fail) compiler passes. | implemented / CI-gated (native lanes); local-only oracle (coupling audit) | `packages/ds-codegen/src/validation/admission-descriptor.ts`; `validation/types.ts` (`FrameworkId` unchanged 6-member, `NativeLaneId`, `RailTargetId`); `packages/ds-compose-smoke/`, `packages/ds-swift-smoke/`; `.github/workflows/ci.yml` (`native-compile-rail`, `native-compile-rail-swift`); `scripts/rail-toolchain-coupling-audit.mjs` (`--check` exit 0 at `c5cda2c9`); specs `RAIL-ADMISSION-DESCRIPTOR-INTERFACE-01`, `RAIL-TARGET-SELF-DECLARED-DESCRIPTOR-02`, `RAIL-NATIVE-COMPILE-LANE-COMPOSE-SMOKE-03`, `RAIL-NATIVE-COMPILE-LANE-SWIFT-SMOKE-04` (all closed) | The lanes compile hand-authored fixtures, not generated components — they prove the rail can invoke non-pnpm compiler families, nothing about component correctness. No Compose Material3/Android SDK or SwiftUI framework compilation in these lanes, no emulator/simulator runtime. The jetpack-compose emitter is still scaffold-only (throws "not implemented"). The SwiftUI emitter is past scaffold (`FEAT-SWIFTUI-EMITTER-WIRING-01`): registered as a builtin target in `fsds.targets.json` with a declared-admission component allowlist (`--target=all`/`governed:rail` emit the allowlisted set; explicit requests bypass it; no `railFrameworkId`, no rail admission — `FEAT-SWIFTUI-DEFAULT-ADMISSION-01`) emitting the native-toggle collapse path (Switch/ToggleSwitch), the Stack primitive (`emitPrimitives`, `FEAT-SWIFTUI-STACK-PRIMITIVE-01`), the projected-children action path (`FsdsButton` via the multi-part emitter + token scopes through the hand-authored `FsdsTheme` runtime, `FEAT-SWIFTUI-MULTIPART-BUTTON-01`), the compound-part composer path (`Card`, one ViewBuilder region per compound part, `FEAT-SWIFTUI-CARD-COMPOSER-01`), the named-slot composer path (`FsdsField`, one region per dom slot with the all-leaves-are-slots gate, `FEAT-SWIFTUI-FIELD-SLOTS-01`), the value-channel text-control path (`Input`, string channel → SwiftUI TextField with the controllable-state projection, `FEAT-SWIFTUI-INPUT-TEXT-01`), the centered-modal surface path (`Dialog` via surface-emit as a sheet whose native dismissal drives the openness channel, `FEAT-SWIFTUI-DIALOG-SURFACE-01`), and the anchored-tooltip surface path (`Tooltip`: hover-driven open channel into a popover with grammar-table placement, `FEAT-SWIFTUI-TOOLTIP-ANCHOR-01`), and the coverage batch (`native-disclosure` collapse → DisclosureGroup for Details; sheet routed through the centered-modal branch; static-content class for Label/Blockquote/Links/List; a second string channel projected as the palette search field for Command — `FEAT-SWIFTUI-COVERAGE-BATCH-01`, 8→15 admitted) into the SwiftPM package `packages/ds-swiftui`; the macOS CI swift job compiles the settings example lane over that tree (`FEAT-EXAMPLES-SWIFT-SETTINGS-01`) but no lane drift-gates the generated bytes yet, and component-instance composers (TextField) still throw; anchored popovers route to surface-emit and throw there. 15 of 49 corpus components emit under the current classes (sweep-verified); the remaining 34 fail loudly. The deduper consumption pilot (2026-08-15) caught the nested-optional `??` resolver trap that made every fallback-only token slot resolve nil (`FIX-SWIFTUI-TOKEN-RESOLVER-01`: staged assignment + a CI-wired XCTest regression target — the compile-only gates could not see it); its remaining findings (dark-mode light-only hex, hex-only override seam, hover/focus/radius realization depth, Field row form, split-repo distribution) are ledgered known gaps in the package README. The coupling-audit `--check` oracle is a local developer command, wired into neither CI nor package.json. | Wire `rail-toolchain-coupling-audit --check` into CI/pre-push so descriptor and lane drift fails automatically; then decide whether a generated-native-emission slice justifies the `RailTargetId` migration already paid for. |
| Runtime visual proof rail | A separate CI job runs after the main gate and exercises Progress, Truncate, ShowMore, OTP, and Calendar in real Chromium through React, Vue, Svelte, Lit, and Angular preview mounts. It asserts contract facts, not screenshot pixels. Non-default prop facts are asserted too: all five frameworks use the same `fsds:config` preview message bus (ShowMore `maxLines=7`, Progress `value=50`, Truncate `lines=5`). Angular preview is executable in both surfaces — the showcase `FrameworkPreview` and this rail. | implemented / CI-gated for fact assertions | `e2e/runtime-rail.spec.ts`; `.github/workflows/ci.yml`; commits `9c7bead`, `11193fb`, `df722b2`; specs `RUNTIME-RAIL-ANGULAR-01`, `RUNTIME-RAIL-ANGULAR-NONDEFAULT-02`; also `e2e/render-binding-rail.spec.ts` (`RENDER-PROP-BINDING-PLAYWRIGHT-RAIL-01`, revived by `RAIL-COVERAGE-DERIVATION-FIX-01`) — a second, CI-wired Playwright rail proving prop→DOM attribute binding across React/Vue/Svelte/Lit/Angular against a corpus-derived matrix regenerated on every run (`pnpm run e2e:render-binding`; the audit classifier resolves componentRef bindings as delegated prop-mappings, not DOM-attr obligations). Its revival immediately caught the lit/angular count-iteration default-clobbering defect (fixed under `FIX-COUNT-ITERATION-DEFAULT-THREADING-01`). Calendar's rail facts follow the `days: Date[]` contract (unset days → zero cells; five injected dates → five indexed cells — a live array-iteration runtime fact), rewritten with the `FEAT-BINDING-CALL-WITH-ARG-01` migration; 70/70 at merge `24b69caa`. `playwright.config.ts` honors `FSDS_E2E_PORT`/`FSDS_E2E_BASE` so a foreign dev server on :5173 cannot be silently reused (`FIX-E2E-BASEURL-ENV-OVERRIDE-01`). | Behavioral parity beyond DOM shape is not asserted. Screenshot baselines are darwin-only and skipped under `CI=true`. The render-binding rail asserts attribute presence/sentinel-value, not interactivity — dead-handler/unwired-state defects are the behavior-realization audit rail's job (see its row), and that rail is static source-level, so runtime interactivity remains asserted nowhere beyond these Playwright facts. | Widen the non-default surface (more components/props); add an interaction fact (click → state change) to the runtime rail for one wired component. |
| Iteration and binding semantics | IR-level `iterate` lowers to framework-native loop constructs across the five Web DOM frameworks. OTP is the production count-iteration consumer; Calendar migrated to array iteration over `days: Date[]` (`FEAT-BINDING-CALL-WITH-ARG-01`, merge `24b69caa`); Select (`prop:options`), Shuttle (`channel:selection.value`, commit `73dc83cf`), and Walkthrough (`prop:steps`) are production array-iteration consumers. The `BindingExpression` grammar gained a closed `channelCall` kind — `channel:X.onChange(arg)`, arg limited to `iterationLocal[.path]`/`prop[.path]`/`literal`, event-position only — so event wires carry per-item payloads: Select option click → `setSelection(item.value)`, Calendar day click → `setValue(item)` (a real Date), lowered idiomatically in all five frameworks. | implemented / runtime-proven (count defaults + Calendar array facts in the Chromium rail); channelCall codegen test-proven per framework | commits `bf73ca4`, `894381c`, `457de31`, `9c7bead`, `73dc83cf`, `24b69caa`; `packages/ds-codegen/src/frameworks/{iteration-bindings,channel-call-with-arg}.test.ts`; `e2e/runtime-rail.spec.ts`; contracts Select/Shuttle/Walkthrough/Calendar The grammar further gained a closed `channelUpdate` operation vocabulary — `channel:X.onChange:<op>(operands)`, ops limited to `setCharAt(index)` and `toggleMembership(member, modeGate)`, each admitted only with a corpus consumer (`FEAT-CHANNEL-UPDATE-OPERATIONS-01`, merge `123fdf0d`): OTP field input composes set-char-at-index through the canonical setter in all five frameworks (Angular via synthesized methods — template expressions forbid arrows), and Select in multiple mode toggles membership while single mode keeps replace, dispatched by the contract-authored `prop:multiple` mode-gate operand. | implemented / runtime-proven (count defaults + Calendar array facts in the Chromium rail); channelCall + channelUpdate codegen test-proven per framework, behaviorally proven in react+vue | commits `bf73ca4`, `894381c`, `457de31`, `9c7bead`, `73dc83cf`, `24b69caa`, `123fdf0d`; `packages/ds-codegen/src/frameworks/{iteration-bindings,channel-call-with-arg}.test.ts`; `e2e/runtime-rail.spec.ts`; contracts Select/Shuttle/Walkthrough/Calendar/OTP | Calendar's visible day-number label is still not inlined (projecting `Date → getDate()` is a method call the closed grammar refuses; consumers render labels). OTP `setCharAt` keeps only the last character of a multi-char payload — whole-paste distribution across fields is out of scope. No runtime (Chromium) interaction fact exists for either operation: the preview shell exposes no callback-capture surface, so behavioral truth is pinned at the jsdom component-test tier. | Add a preview-shell callback-capture surface so the runtime rail can carry the first interaction fact; decide whether day-label projection justifies a governed value-projection form or stays consumer-side. |
| Contract-declared behavior realization (2026-07-22 batch) | Contract-declared interactivity now lowers to live behavior where the corpus declares it: Select's trigger toggles the `open` channel in all six targets (`FIX-SELECT-OPEN-TOGGLE-WIRE-01`, `9a64f310` — pure contract wire, zero emitter change; lit/angular generated-test emitters now derive toggle expectations from the channel's contract default); Accordion is a live contract-driven repeated-disclosure compound in all five web frameworks — context provider + Item/Trigger/Content subcomponents, ancestor-aware `getInteractiveItemPart` + `isDisclosureContainer` in the single shared detection module, no tab-ARIA leakage, no component-name literals (`FIX-COMPOUND-CONTAINER-ANCESTOR-PREDICATE-01`, `2cccbdf7`); lit/angular prop reads are default-aware at the accessor layer, restoring undefined-prop parity with react's parameter defaults — Icon size hint-map miss and Skeleton `decorative` a11y divergence fixed (`FIX-UNDEFINED-PROP-ACCESSOR-DEFAULTING-01`, `2800d15b`); portal computation is wired-or-deleted — React portals Dialog/Sheet/Toast root-to-body via the generic `portalsRootToBody` predicate, all other portal scaffolding removed corpus-wide (`FIX-PORTAL-CONSUMPTION-01`, `6051c40a`). | implemented / test-proven per framework (jsdom + Jest), falsified per slice; portal DOM-parent proof jsdom-level | merge commits `9a64f310`, `2cccbdf7`, `2800d15b`, `6051c40a`; `packages/ds-codegen/src/frameworks/react/{hook-source.ts,compound-container-detection.test.ts}`; `frameworks/{lit,angular}/binding-defaults.test.ts`; `packages/ds-codegen/src/semantics.ts` (`portalsRootToBody`); per-framework Accordion/Select behavioral tests | Real-browser interactivity proof exists for exactly one interaction: Select trigger click → `onOpenChange(false)` + listbox unmount, asserted in Chromium for react/vue/svelte/lit via the preview callback-capture surface (`FEAT-PREVIEW-CALLBACK-CAPTURE-01` `867c0431` + `FIX-INTERACTION-FACT-SKIP-SCOPE-01` `7de140f8` — the first landing silently skipped all four facts via a describe-scoped `test.skip`; 74 passed / 1 named angular skip after the fix). Everything else remains jsdom/Jest-proven only; focus order, `:has()`, and stacking-context escape are still unproven in-browser. Angular/Lit read uncontrolled `defaultValue` at construction (pre-existing Tabs-shaped input-timing constraint). Anchored surfaces (Popover/Tooltip) are positioned AND content-portaled in all five frameworks (`FEAT-ANCHORED-SURFACE-XFW-01`, `c99cf445`): each package ships a mirrored anchored-position primitive (same options/state shape and flip-shift/clamp math as react's `useAnchoredPosition`), and the surface emitters wire fixed positioning + `data-placement` + hidden-until-ready plus per-framework content portal (vue Teleport, svelte `use:portal`, angular body-append, lit content-host move) via the shared `anchoredPortalsContentToBody` predicate. Real-Chromium placement facts pass for vue/svelte/lit (body-parenting, `position:fixed`, exact 8px anchor gap, sub-pixel centering, `data-placement`) — that proof immediately caught two defects the static gates structurally cannot see, fixed under `FIX-ANCHORED-BROWSER-DEFECTS-01` (`ebeb5309`): lit's `display:contents` content host made `position:fixed` a layout no-op, and svelte's placement option called `getContext` on post-init recomputes (position tracking died after the first frame). Angular anchored placement has no browser-level proof (no harness path through its AOT preview pipeline); its proof is jsdom/Jest-level only. | All named successors executed: lit primary-attr defaulting (`FIX-LIT-PRIMARY-ATTR-DEFAULTING-01`, `dd5c8861`), portal contract adjudication (`FIX-PORTAL-CONTRACT-ADJUDICATION-01`, `d0d4d7e9` — Command portals via a new centered surface block; Calendar/Select/Walkthrough overclaims removed), and full-overlay portal parity in all five frameworks (`FEAT-PORTAL-MECHANISM-CROSS-FRAMEWORK-01`, `04999ceb` — vue `<Teleport to="body">`, a new svelte `portal` action, angular lifecycle body-append, lit host-element move preserving shadow styles; falsified DOM-parent tests per framework; vue/svelte generated tests resolve portaled roots from document.body with afterEach resets since neither package auto-cleans). The anchored-surface successor landed (`FEAT-ANCHORED-SURFACE-XFW-01` + `FIX-ANCHORED-BROWSER-DEFECTS-01`; see the non-claims column). The Walkthrough `steps[].anchor` residual landed (`FEAT-WALKTHROUGH-ANCHOR-CONSUMPTION-01`, merge `4eee3141`): the contract schema gained a selector-source anchor variant (`surface.anchor.selector = {prop, path, indexChannel}`, oneOf with the trigger-part form), the IR a `SurfaceSelectorAnchorIR` fact with fail-loud validation (prop exists + array-typed, channel exists; openTriggers stays optional — tours open programmatically), and Walkthrough declares a `kind: coachmark` surface with anchored positioning and honestly re-admits `portal.enabled`. All five web emitters' GENERIC component path (not the compound surface emitter) consume the shared `selectorAnchoredRootPortal` predicate: the whole root panel portals to body (react `renderInPortal`, vue Teleport, svelte `use:portal`, angular body-append, lit host move + inline `display:block`) and tracks `document.querySelector(steps[step].anchor)` through the mirrored anchored-position primitives, re-resolving on step change. Real-Chromium proof: 36/36 facts across react/vue/svelte/lit — body parenting, `position:fixed`, `data-placement`, exact 8px gap, 0px center delta, AND live re-anchoring to `#step-2` when the index changes — with zero page errors. That proof caught a third browser-only defect class (`FIX-WALKTHROUGH-VUE-ANCHOR-RESOLUTION-01`, merge `bfbef9f3`): vue's anchor resolution via `computed` first evaluated while the app tree was still detached, cached the null `querySelector` result forever (no reactive dep changes to re-run it), and the panel stayed hidden — jsdom suites and typecheck stayed green throughout; fixed with a post-flush `watchEffect` into a `shallowRef`. Non-claims: Angular's Walkthrough lowering is jsdom/Jest-proven only (no browser harness path), and its anchor-resolution `effect()` tracks only the step signal — a `steps` input mutation alone does not re-resolve the anchor (latent, unprovable here). Post-merge runtime rail: 74 passed / 1 named angular skip. |
| Behavior-realization audit rail | A CI-gated static audit (`pnpm run audit:behavior-realization`) derives behavior obligations from the live contract corpus each run — 20 event-wire obligations × 5 frameworks (all four `events` value forms incl. `channelCall` and the `channelUpdate` operations — OTP's input wire is a derived obligation, not allow-listed), 2 compound/disclosure realizations, portal positive/orphan-negative obligations (Command joined the positives, Calendar/Select/Walkthrough dropped out by derivation after `FIX-PORTAL-CONTRACT-ADJUDICATION-01`) — and verifies each is realized in the committed generated source, importing detection predicates from compiled `ds-codegen/dist` (no re-implemented detection, no hardcoded component names; a source-audit test enforces the latter). Runs in the CI gate job after `governed:rail` + drift check, ~55ms over 49 components; wired green at birth. | implemented / CI-gated; static source-level | `scripts/behavior-realization-audit/{audit.mjs,realization.test.mjs}` (22 self-tests: derived floors ≥ not frozen =, typed skips, OTP zero-derivation, synthetic falsifications); `.github/workflows/ci.yml`; falsified live (stripped Select option handler → exit 1 naming the obligation); spec `RAIL-BEHAVIOR-REALIZATION-AUDIT-01` (closed, merge `9a8d7eb6`) | A realized handler token proves the wire exists in emitted source, NOT that it fires, updates state, or is a11y-adequate — runtime truth stays with the Playwright rails. react-native/figma are not audited. | Portal positives are now five-framework (`RAIL-AUDIT-PORTAL-XFW-OBLIGATIONS-01`, `98eadc41` — `consume-root-portal` checks each framework's mechanism token, falsified by stripping vue Dialog's Teleport; the audit's own self-test, found silently stale after the channelUpdate landing, is now wired into the `audit:behavior-realization` script so it runs in CI). Landed: the Select trigger event-wire obligation now carries browser-level proof (the runtime rail's interaction facts, `7de140f8`); the capture surface covers the four shared-pipeline preview mounts only (Angular's synthesized-host pipeline has no interception seam) and one component's one callback — widening is additive. The audit also derives anchored obligations (`FEAT-ANCHORED-SURFACE-XFW-01`): `consume-anchored-positioning` and `consume-anchored-content-portal` per framework for every contract with `positioning.strategy: "anchored"` + portal (Popover/Tooltip → 20 obligations), both matchers mutation-falsified at landing; consumption tokens prove source wiring only — the real-browser placement facts live in the behavior-realization batch row. The anchored family gained a selector-anchored branch (`FEAT-WALKTHROUGH-ANCHOR-CONSUMPTION-01`): contracts whose anchor is selector-sourced (Walkthrough) derive `consume-root-portal` (the root panel moves, so the root-portal mechanism tokens apply) plus `consume-anchored-positioning` per framework, are excluded from `consume-anchored-content-portal` (`anchoredPortalsContentToBody` returns false for selector anchors — nothing content-level portals), and are exempt from the orphan invariant; portal-family obligations 265→270, both new derivations mutation-falsified at landing (stripped react `renderInPortal` and lit `AnchoredPositionController` → exit 1 naming the exact obligation). |
| A11y-realization audit rail + label/id association lowering | A CI-gated static audit (`pnpm run audit:a11y-realization`) derives accessibility obligations from the live contract corpus — `relationships[]` idref attributes, `a11y.keyboard`, and `focus.strategy` (trap/roving) — and verifies realization in generated output for every family in the AdmissionDescriptor registry (react/vue/svelte/lit/angular/react-native; figma excluded by construction), with a committed two-directional ledger (`scripts/a11y-realization-audit/known-gaps.json`): an unledgered gap fails AND a stale entry fails, so the ledger only shrinks. The first burn-down lane is landed: `relationships[]` with `for`/`aria-labelledby`/`aria-describedby` lower through `resolveIdRelationships` (`packages/ds-codegen/src/id-relationships.ts`) into generated per-instance ids + conditional idref attributes (React/Vue `useId()`, Svelte `$props.id()`, Angular module-counter, Lit shadow-root-scoped literals), slot-presence gates on refs whose target content is consumer-slotted (Angular is the one documented ungated family), and a field-association mechanism for composer→slotted-control association (React context, Vue provide/inject, Svelte setContext getter, Angular element-injector DI service; controls opt in via contract `fieldAssociation: "control"` — Input). | implemented / CI-gated; rail static source-level, association runtime-proven per framework (jsdom) | rail: `scripts/a11y-realization-audit/`; lowering: `packages/ds-codegen/src/{id-relationships.ts,relationship-lowering.test.ts}` (13 IR tests) + `frameworks/idref-slot-gate.test.ts` (per-framework gate idiom, falsified against the collapse defect it pins); per-framework `FieldAssociation` tests in ds-react/ds-vue/ds-svelte/ds-angular; specs `RAIL-A11Y-REALIZATION-01` (merge `4cf16a0c`), `FEAT-A11Y-LABEL-ID-ASSOCIATION-01` (merge `c0a6f765`, ledger 213→181), `FIX-A11Y-ASSOCIATION-REVIEW-01` (post-merge review fixes: Lit single-ref slot-gate collapse, Toast title content, Field provider memo) | The rail checks attribute/wiring PRESENCE in emitted source — it cannot see gate quality (the Lit collapse defect shipped rail-green), cross-component delivery (Field's describedby lands in the consumer's file; its 4 entries stay ledgered with notes), or runtime AT behavior. Lit cannot realize field association at all (idrefs cannot cross the shadow boundary — platform limit, ledgered). Vue peer floor is now ^3.5 (`useId`). | Burn the remaining 181 ledgered gaps under `FEAT-A11Y-COMPOSITE-KEYBOARD-01` (140: composite-widget keyboard/roving) and `FEAT-A11Y-RELATIONSHIP-STRAGGLERS-01` (35); extend the rail with a cross-component delivery check so Field's describedby residue can burn. |
| CSS custom-property bindings | Prop-to-CSS custom-property lowering exists and is runtime-checked for both default/fallback and explicit non-default cases in Progress, Truncate, and ShowMore. Non-default props are mounted through the preview `fsds:config` message bus for all five frameworks. | implemented / runtime-proven for defaults plus selected non-default cases | `packages/ds-codegen/src/frameworks/css-var-bindings.test.ts`; `e2e/runtime-rail.spec.ts` | The rail does not prove every non-default computed visual geometry, every component/prop pair, or every token cascade branch. | Widen the non-default surface beyond the three components/props, and bind rail outcomes into component evidence pages. |
| DOM-property reflection bindings | `DomNodeIR.propertyBindings` carries DOM-property-only facts (facts with no HTML attribute form) as a distinct IR binding class, validated at IR build via `DOM_PROPERTY_ONLY_KEYS` and host-eligibility checks. Checkbox `indeterminate` lowers across all five web frameworks in each framework's own verified idiom — React `useRef`+`useEffect` imperative assignment, Angular direct `[indeterminate]` template property binding, Vue and Svelte through their existing binding emission (their runtimes/compilers already route the key to a property write), Lit through a dedicated `.prop=` PropertyPart form — with `aria-checked="mixed"` lowering alongside. | implemented / test-proven (jsdom) | `packages/ds-codegen/src/ir.ts`; `packages/ds-codegen/src/frameworks/{react,vue,svelte,angular,lit}/component-source.ts`; generated Checkbox test pairs (initial mount + reactive update, falsified per framework); spec `DOM-PROPERTY-REFLECTION-IR-CHECKBOX-INDETERMINATE-01` (closed; merged via PR #10 `e3d54481`) | Not runtime-rail-proven — the proof is jsdom mount/update tests, not the Chromium fact rail. The class currently has one member fact (`indeterminate`). RN is unchanged: it lowers indeterminate via `accessibilityState` (a plain prop), not this class. | Assert the indeterminate property fact in the runtime rail, or admit a second DOM-property-only fact into the class. |
| Recursive component composition (componentRef) | A contract node can declare `componentRef: "fsds.<Name>"` to render an owned child component by reference. The IR resolves it once into a typed `ComponentInstanceIR` fact — target layer plus each binding/attr/event pre-classified (`kind:"prop"` vs `kind:"attribute"`, events resolved to the target handler prop, e.g. `click`→`onClick`) — so emitters do last-mile syntax only. Layer ordering (target rank ≤ host rank in primitive<compound<composer<assembly) is enforced twice: at IR build (throws) and at `generate:check` (cross-graph semantic rule). All six targets lower the resolved fact; Angular/Lit/RN branch on the classification directly (their attribute-vs-property syntax diverges), React/Vue/Svelte pass through structurally (their prop syntax is classification-invariant). | implemented / test-proven | `packages/ds-codegen/src/ir.ts` (`ComponentInstanceIR`, `resolveComponentInstances`, `LAYER_ORDER`); `validation/semantic.ts` cross-graph layer check + tests; `frameworks/component-ref-imports.test.ts` (11 tests, per-framework import-path resolution); contracts Alert/Chip (→`fsds.Button`), Avatar (→`fsds.Image`), all compound-layer; spec `CODEGEN-RECURSIVE-COMPOSITION-01` (closed `9db0c81b`) | Only three compound-layer contracts exercise it; no composer/assembly-layer usage and no nesting deeper than one hop is proven. The "composers must not componentRef a control" rule is convention upheld by precedent (TextField→Input dropped), NOT stated in committed architecture docs and NOT mechanically enforced — `LAYER_ORDER` only forbids upward-rank references. | Commit the composer-forbidden doctrine to the architecture docs or add a mechanical schema/semantic check rejecting composer-layer componentRef, so the rule stops being convention-only. |
| Composer named-slot projection | A compound/composer owning multiple content regions routes each region through a named slot (`{"tag":"slot","name":X}`), never a content prop for the same region. `validateSlotPropNamespaces` throws at IR build on any slot-name/prop-name collision (fires for all six targets, not only under svelte-check). All five web emitters plus RN lower named slots to their idiom. Five components use the pattern: Field (5 slots), TextField (3), Sheet (2), Walkthrough (2), Dialog (1). | implemented / test-proven (RN slot-exactness for Field) | `docs/architecture/composer-slot-projection.md` (`verified_at_commit: 2ab2509a`); `ir.ts` `validateSlotPropNamespaces`; `packages/ds-react-native/src/components/Field/__tests__/Field.runtime.test.tsx` (3/3 pass at `c5cda2c9`: exactly-once region rendering, single control host, clean omission); commit `e8b23a04` | The collision check proves namespace hygiene at IR build, not corpus-wide render exactness — only Field (RN) has a dedicated runtime exactness test. 40+ non-slotted components still use the older shape; the pattern is proven and admitted, not corpus-wide adopted. No Chromium runtime-rail assertion covers slot regions. | Extend the Field-style runtime exactness test to TextField/Sheet/Walkthrough/Dialog, and/or add a slot-region assertion to `e2e/runtime-rail.spec.ts` for one web framework. |
| textOverflow IR fact (dormant authority) | `ComponentIR.textOverflow` (`TextOverflowIR { kind: "line-clamp"; line }`) is a target-neutral IR fact built from `contract.textOverflow` for ShowMore (`prop:maxLines`) and Truncate (`prop:lines`), schema-validated and pinned by direct IR/contract/semantic unit tests (64/64 at `c5cda2c9`). | implemented IR fact / test-pinned — dormant (no consumer) | `packages/ds-codegen/src/ir.ts` (`TextOverflowIR`, `buildTextOverflowIR`); `ir.test.ts`, `contract.test.ts`, `validation/semantic.test.ts`; spec `CODEGEN-RN-EMITTER-IR-AUTHORITY-01` (closed via PR #10 `e3d54481`) | Zero emitters consume it — line-clamp behavior is still realized entirely through the pre-existing CSS-variable binding mechanism, unchanged (grep-verified across `frameworks/`). `DomGuardIR` and `NativeStyleDeclarationIR` were explicitly deferred by the spec and remain unintroduced. RN's `isCheckboxRootPattern` (structural DOM-shape check, not name lore) was knowingly tolerated at closure per the spec's own invariant. | Wire an emitter consumer of `TextOverflowIR` under a follow-up spec if native line-clamp realization is needed; otherwise the fact stays dormant scaffolding and this row's strength must not be read as behavioral. |
| Target-pack registry | Target-pack manifest and registry config machinery exists. Built-in targets are described through manifests; local target packs can be declared, resolved, validated, fingerprinted, and described. | implemented foundation | `fsds.targets.json`; `packages/ds-codegen/src/target-packs/{manifest,builtin,config,local}.ts`; `docs/architecture/design/target-pack-registry.md` | Local target packs are metadata-only in this slice: their emitter entrypoints are not imported/executed. npm/package-source loading is not implemented. | Add executable local target-pack loading after manifest validation while keeping file writing centralized. |
| Figma descriptor target + plugin | `figma` is a built-in design-tool target that emits descriptor artifacts and README projections from ComponentIR; the target-pack manifest declares descriptor-only token strategy and non-behavioral capability limits. The Figma plugin consumes those descriptors through a plan-driven materializer (`planner.ts` owns state semantics, `materialize-state.ts` realizes them; component sets for the Button/Chip/Status allowlist, annotated placeholders for the rest), an interactive Svelte UI (filterable descriptor catalog, materialization status/report, variant-label and description drafting) built from ds-svelte's own components, and a Dev Mode surface that returns per-framework usage-snippet previews for the selected node from `fsds.component`/`fsds.variantRow` plugin data. Eligibility classification has a single authority module (`eligibility.ts`) shared by the plugin core and the UI model. | implemented; unit-proven against a mocked `figma` global (2026-07-21) | `packages/ds-figma-plugin/src/{plugin.ts,planner.ts,materialize-state.ts,eligibility.ts,ui-model.ts,codegen-preview.ts,FigmaPluginApp.svelte}`; package test suite 59/59 (`pnpm exec vitest run packages/ds-figma-plugin`); build emits `dist/plugin.js` + `dist/ui.html` with inlined assets; spec `FIGMA-PLUGIN-UI-PORT-01` | This does not prove live-canvas behavior in a real Figma desktop session (all runtime evidence is mocked-global unit tests), library publication, or MCP tool availability. Dev Mode previews are usage snippets (a pure function of descriptor + variant row), not the ds-codegen emitter pipeline. Component-set materialization is allowlist-limited to three components. The plugin package is not in the admission rail or CI gates. | Capture live-canvas evidence from a real Figma desktop run; widen the component-set allowlist; add the package build/tests to CI. |
| Component evidence/showcase pages | The showcase has moved beyond doctrine: Design view derives usage examples, anatomy, variants, states, props, accessibility, and token sections from component bundles, and additionally renders an Evidence/Residuals panel (provenance, per-framework generated-source presence, committed runtime-rail coverage projection) and an A2UI descriptor panel derived live from the contract; Developer view renders per-framework preview/source tabs and trace regions. | implemented (evidence/residuals + A2UI panels landed; CI-status binding still open) | `src/views/DesignView.tsx`; `src/views/DeveloperView.tsx`; `src/views/sections/EvidencePanel.tsx`; `src/views/sections/A2UIDescriptorPanel.tsx`; `src/runtime/rail-coverage.ts`; `src/runtime/FrameworkPreview.tsx`; specs `COMPONENT-EVIDENCE-RESIDUALS-01`, `COMPONENT-EVIDENCE-RAIL-COVERAGE-BINDING-01` (both closed) | Admission-rail and token-gate last-run status are still not bound in-app (reserved `COMPONENT-EVIDENCE-CI-STATUS-BINDING-01`); the runtime-rail binding shows coverage (what the rail asserts), not last-CI-run pass/fail. | Bind admission-rail and token-gate last-run status into the evidence page (`COMPONENT-EVIDENCE-CI-STATUS-BINDING-01`). |
| Curated usage examples | Usage JSONL sidecars are loaded into component bundles and rendered as live React in the Design view where present. | implemented narrow path | `packages/ds-contracts/components/*/*.usage.jsonl`; `vite-plugin-fsds-data.ts`; `src/lib/render-usage.tsx`; `src/views/sections/UsageExamples.tsx`; commit `33f7c8e` | Curated examples are not codegen inputs, and existing examples can still contain product/content curation issues. | Expand usage coverage and decide whether usage examples should have their own evidence/validation report. |
| Token governance | Token build, DTCG validation, committed-build drift check, contrast validation, and brand reference validation are blocking in CI before codegen checks. Token *usage* (share of the vocabulary referenced) is report-only — see the token-resolvability row for why the blocking question is the inverse. Component-derived contrast complement (`RAIL-COMPONENT-CONTRAST-01`): `generate:check` derives fg×bg pairs mechanically from every contract's `styles.json`/`tokens.json` binding chains (base scope plus variant/part slot redirections, never a hand-maintained pair list) and gates them at WCAG AA 4.5:1 in light + dark themes of `resolved.tokens.json`; disabled-state pairs are exempt per WCAG 1.4.3 Note 5. Pre-existing debt was ledgered at landing in `component-contrast-known-gaps.json` under a two-directional ratchet (unledgered failures fail; stale entries fail); `FIX-CONTRAST-DEBT-01` BURNED the ledger to empty — the resolver now preserves DTCG alpha (`core.color.mode.transparent` resolves to `#00000000`, which the WCAG math cannot consume and skips; its previous opaque `#000000` resolution was the phantom behind the Walkthrough finding), `semantic.interaction.text.hover` became theme-aware (the unthemed value measured 1.92:1 in dark), `foreground.hover` dark brightened to neutral.200 (was 3.60:1), and `foreground.secondary` light darkened to neutral.700 (was 4.38:1) — each pair stopped reproducing, so the ratchet itself demanded every entry's removal. Corpus-wide re-measure after the burn: 132 pair/theme checks, 0 failing. `governed:rail` runs `generate:check` before any ds-framework emission step. | implemented / CI-gated | `.github/workflows/ci.yml`; `packages/ds-tokens/**`; `packages/ds-codegen/src/validation/component-contrast.ts` + `component-contrast-known-gaps.json` (ledger empty; +18 tests); `packages/ds-tokens/build/generators/resolve.ts` alpha preservation (+5 tests); `package.json` (governed:rail scripts); `docs/architecture/tokens-architecture.md` | Token gates do not prove visual design quality or complete accessibility adequacy. No blocking gate detects a semantic or component token that loses its last consumer. The component contrast gate checks same-block fg+bg declarations only: cross-block color inheritance, non-text (border) contrast (WCAG 2.2 SC 1.4.11), and `var()` fallback literals are out of its model. | Connect token gate summaries to component evidence pages. The contrast ledger is empty; any new component-level contrast debt needs a fresh ledger entry naming its owning spec. `interaction.text.active` remains unthemed (same shape as the hover defect FIX-CONTRAST-DEBT-01 burned) but is not currently derived into a failing pair by any contract. |
| Token-reference resolvability | Every `var(--fsds-*)` read by generated React CSS must resolve to a name the token graph or the component's own slots declare. An unresolvable reference does not error — CSS falls back to the literal second argument, so the property renders a plausible value while the token layer is decorative for it and no brand or theme re-point can reach it. The defect class this rail was built to find — codegen lowering a `resolvesTo` verbatim while the token build kebab-cased the same path, so any camelCase segment emitted a name nothing declared — is **closed**: 64 references across 23 names, burned to **0**, and the ledger file deleted rather than left as an empty shell. `packages/ds-codegen/src/token-path.ts` is now the single lowering authority both sides call. | implemented / gated at pre-push + CI (ledger ratchet) | `scripts/token-resolvability-audit/{audit,selfcheck}.mjs` (16 falsification checks); `packages/ds-tokens/build/core/token-path.test.ts`; `docs/token-resolvability-audit/resolvability-matrix.{json,md}`; specs `RAIL-TOKEN-REFERENCE-RESOLVABILITY-01`, `FIX-TOKEN-SLUG-KEBAB-CASE-01` | **Not a superset of the gate it replaced**: a token that loses its last consumer is no longer detected by any blocking gate, and that signal is not preserved elsewhere. Scanned against generated React CSS only (the reference framework). Zero findings means no generated React CSS reads an undeclared name — it does not mean every read resolves to the *intended* value; the rail cannot see a chain that resolves to the wrong token. The corpus agreement test pins non-divergence, not the lowering's behavior — a mutation to the shared function moves both sides together and leaves it green (its unit cases are what fail). | Two pre-existing defects the repair *exposed* by making the chains live, both out of scope here and unfixed: (1) `--fsds-spacing-density-{compact,spacious,tight}-*` is read by the density layer but declared nowhere (namespace inference drops the `core-` prefix for `spacing.density.*`), so `[data-density]` re-points collapse to their fallback; (2) the brand layer emits its `@media (prefers-color-scheme: dark) { :root { … } }` block *after* its `[data-theme="light"]` block at equal specificity, so an explicit light theme loses to a dark OS preference. |
| Icon catalog runtime delivery | `packages/ds-iconography` is the icon authoring source of truth: 29 icons under `icons/<Name>/<Name>.icon.json` validated against `icon.contract.schema.json`, two authored size variants (16/24) each, with a CASR-style content-addressed emission ledger (`emission-ledger.json`) maintained per batch. `build/build.mjs` emits a committed, drift-gated package-root `index.mjs`/`index.d.ts` (named export per icon + catalog + `resolveIcon`); `generated/` stays gitignored scratch. Codegen carries a dedicated `IconGlyphIR` fact parsed from `anatomy.dom[].iconGlyph`, lowered with zero per-icon emitter lore into generated `Icon` components across all five web frameworks, RN, and the Figma descriptor (Angular `ICON_GLYPH_SIZE_HINTS[(this.size ?? "")]`, Lit `svg\`\`` + `ifDefined`). `validateContractIconRefs` runs inside `generate:check` (required CI step) and fails on unknown icon references. | implemented / partially CI-gated | `packages/ds-iconography/{icons,build,index.mjs,icon.contract.schema.json,emission-ledger.json}`; `packages/ds-codegen/src/ir.ts` (`IconGlyphIR`); `validation/icon-refs.ts`; generated `Icon` in all 7 targets; spec `ICON-CATALOG-RUNTIME-DELIVERY-01` (closed, worktree-merge `ff1d463d`); commits `b93a5edb`, `10327e77`, `b1a435e0`, `a00e9220`, `4851de49` | The committed `index.mjs` drift gate (`iconography:build:check`) is wired into neither CI nor pre-push, and CI's generated-drift diff scopes only the six framework src trees (`react, vue, svelte, angular, lit, react-native`) — a hand-edited or stale-rebuilt `index.mjs` passes every automated gate today. **The emission ledger gate (`ledger-icons.mjs --check`) IS now wired into CI and pre-push** (`ICONOGRAPHY-TOKEN-DISCIPLINE-02` Phase 4, 2026-07-27): a new `iconography:ledger:check` root script + a CI step after the six-tree drift diff + a `RUN_ICONOGRAPHY`-gated pre-push block. The pre-push classifier (`scripts/prepush-scope.mjs`) now also recognizes `packages/ds-react-native/` (closing a latent gap where the six-tree diff included it but the classifier omitted it). Brand-swappable glyphs are an absent feature, not an open decision (zero icon keys under `src/brands/`, grep-verified). The bundle-size probe exists but is ungated. RN/Figma Icon realizations exist as generated code but sit outside the admitted Web-DOM rail family. | Wire the `index.mjs` drift check into CI alongside the ledger gate, or fold both into one iconography drift step. |
| Non-web generation (substrate) | React Native is a **default-rail target** (`FEAT-MOBILE-RN-001`): it is registered in `fsds.targets.json`, so `--target=all` and the default `pnpm run governed:rail` emit and verify the full loader-discovered contract corpus (49 contracts at the 2026-07-21 verification; `packages/ds-codegen/src/contracts-fs.ts` is the count authority) into `packages/ds-react-native/`, and CI/pre-push include `packages/ds-react-native/src` in the generated-tree drift check. The package typechecks against real React Native types; token realization follows the web token normal form (`ComponentIR.tokenScopes` mirrored as typed native scopes consumed through `FsdsTheme`); variant styling is realized natively — per-variant `StyleSheet` entries joined from root cssBlocks declarations × `variant_*` token scopes, or dereferenced through the owning scope for direct-declaration variant blocks, with synthesized union-typed axis props mirroring web. Generated tests cover host-render archetypes, variant/state-style proofs, and admitted presence surfaces. The compound-selection path is admitted (`FEAT-COMPOUND-RN-001`): Tabs lowers through the same `isCompoundStateContainer` IR gate the web emitters consume into a context+provider root with `TabsList`/`TabsTab`/`TabsPanel` (press-driven `onValueChange`, `accessibilityState.selected`, controlled channel, `unmountInactive` panel suppression), a ported `createCompoundContext` primitive committed beside `stack.tsx`, and generated interaction tests. SwiftUI is an explicit-only builtin target (`--target=swiftui` → `packages/ds-swiftui`, SwiftPM; Switch/ToggleSwitch via the native-collapse path, the Stack primitive, `FsdsButton` through the projected-children multi-part path, `Card` through the compound-part composer path (region closures per compound part), `FsdsField` through the named-slot composer path (region closures per dom slot), `Input` through the value-channel text-control path (string channel → TextField), `Dialog` through the centered-modal surface path (sheet + openness channel), and `Tooltip` through the anchored-surface path (hover + popover), all with token scopes resolved through the hand-authored `FsdsTheme` runtime — the RN normal form; in `fsds.targets.json` via the components allowlist (full-corpus runs emit the admitted set), outside the rail and CI drift checks, but compiled by the macOS CI swift job via the `examples/settings/swift` consumer lane — `FEAT-SWIFTUI-EMITTER-WIRING-01`, `FEAT-EXAMPLES-SWIFT-SETTINGS-01`, `FEAT-SWIFTUI-STACK-PRIMITIVE-01`, `FEAT-SWIFTUI-MULTIPART-BUTTON-01`); Compose remains a recon/prototype lane. | RN default-rail target; native variant style realization; typed native token slice; host-render + variant/state/surface tests; other native targets recon | `fsds.targets.json`; `packages/ds-codegen/src/frameworks/react-native/**`; `packages/ds-codegen/src/validation/frameworks/react-native.ts`; `packages/ds-react-native/**`; `.github/workflows/ci.yml` (six-tree diff); `.githooks/pre-push`; `pnpm run governed:rail`; `FEAT-MOBILE-IR-001` (closed `eb933df`); `FEAT-MOBILE-DISCLOSURE-001` (closed `f00110c`); `FEAT-MOBILE-RN-001` | The RN rail proves generated TS package admission, host-render behavior slices, variant + pressed/disabled state style realization, and the presence-surface substrate: Dialog/Sheet lower to RN Modal with escape/outside-click dismissal (FEAT-MOBILE-RN-SURFACE-001); Toast lowers to a live region with the dwell-token auto-dismiss timer; Tooltip/Popover lower to the anchored Modal substrate with measureInWindow positioning and documented touch divergences (FEAT-SURFACE-DWELL-ANCHOR-001). The motion.dwell.* presence-budget tokens flow into the autoDismiss behavior primitive across all five web packages and RN; the hover/focus pause claim remains web-only. It does not prove simulator/device execution, native visual parity, platform accessibility parity, anchored collision handling, disclosure-family compound emission (Accordion), or part-scoped/boolean-modifier styling. The RN Tabs compound lowering carries named touch divergences: no keyboard navigation or roving tabindex, a static indicator ornament, no orientation layout effect, and `unmountInactive: false` renders inactive panels mounted-but-suppressed rather than hidden. | Accordion RN disclosure emission (the reserved `FEAT-ACCORDION-RN-001` — its IR fact `isDisclosureContainer` and the web behavioral bar already landed), Walkthrough RN (the reserved `FEAT-WALKTHROUGH-RN-001`), part-scoped variant styling, anchored collision handling, Menu/Select/Command surface migrations. Requirements for the Tabs/Accordion/Walkthrough residuals are specced in the machine-local roadmap `SCOPE-RESIDUAL-ADMISSION-001` (docs/internal/; reserves FEAT-COMPOUND-RN-001, FEAT-ACCORDION-WEB/RN-001, FEAT-WALKTHROUGH-TAX/RN-001). Standing finding, updated 2026-07-22: the web halves of the former Accordion/Walkthrough finding are resolved — Accordion is behaviorally alive in all five web frameworks (`FIX-COMPOUND-CONTAINER-ANCESTOR-PREDICATE-01`, merge `2cccbdf7`) and Walkthrough's orphaned portal computation was removed corpus-wide (`FIX-PORTAL-CONSUMPTION-01`, merge `6051c40a`). Still open: RN disclosure-family emission (Accordion) remains unproven, and Walkthrough's anchored coachmark positioning is realized on all five WEB frameworks only (`FEAT-WALKTHROUGH-ANCHOR-CONSUMPTION-01`, merge `4eee3141`) — the RN lowering still does not consume `steps[].anchor`. |
| Mobile-vs-web parity (measured) | `scripts/mobile-parity-matrix.mjs` derives react-native as admitted (registered target + rail plan + package root) with full generated coverage; its `--check` self-consistency gate passes (exit 0) after `RAIL-COVERAGE-DERIVATION-FIX-01` deleted the frozen-partition sub-check (a point-in-time A/C/D sweep classification compared against the live corpus count — a pin against a moving target, retired rather than repaired; the sweep counts remain as historical provenance in the matrix output). The remaining self-checks (dimension completeness, admission symmetry) assert internal consistency of live-derived output only. | measured, script-derived, reproducible | `scripts/mobile-parity-matrix.mjs` (`--check` exit 0, verified 2026-07-21); `docs/successor-work-mobile-parity-matrix.md` (`REF-MOBILE-PARITY-MATRIX-001`) | The matrix measures structural admission facts, not visual/runtime parity — and those facts are byte-redundant with `fsds.targets.json` + the governed rail + CI's six-tree diff (the matrix is a convenience projection, not an independent proof). | Re-run after any SwiftUI/Compose elevation; retire the script entirely if its projection stops earning its upkeep. |
| Styling-realization audits (variant + pseudo/state, measured) | Two read-only classifier rails measure whether declared styling obligations are realized in the generated React CSS: `scripts/variant-style-audit/` (variant axes) and `scripts/pseudo-state-audit/` (state dimensions plus the empty-field input prompt, classified with the codegen realization vocabulary and false-positive suppression for base values, channel-driven states, suppressed categories, and `focus.strategy=none` roots). At the 2026-07-21 run the pseudo-state matrix covers 49 components / 231 obligations / 62 realized / 19 gaps — headline gaps: `:placeholder-shown` never emitted for Input/TextField, Tabs realizes selection as `--active` (diverging from the `aria-selected` truth), an availability=disabled cluster (Chip/Command/Details/Calendar/Walkthrough), and Calendar presentation states. | measured, script-derived, reproducible | `scripts/variant-style-audit/`; `scripts/pseudo-state-audit/audit.mjs` + locked discriminator test (`realization.test.mjs`, 17/17); `docs/pseudo-state-audit/pseudo-state-matrix.{json,md}`; spec `PSEUDO-STATE-STYLING-RAIL-01` (closed `fc33671a`) | Gated by two-directional ledgers at pre-push (`audit:pseudo-state`, `audit:dead-slots`): a finding absent from the ledger, or a ledger entry that no longer reproduces, fails the push. The ledger being green means every finding is ACCOUNTED FOR, not that it is fixed — 134 dead slots and 19 state gaps remain ledgered. For several pseudo-state entries the audit's own prescribed selector is unsatisfiable on the component's root element (`:disabled` on a `<span>`/`<div>`), so authoring CSS to match it would score realized while changing nothing at runtime; burning those requires runtime proof, not selector presence. Classification is against generated React CSS only (a web-only proxy for the corpus). A realized selector proves presence, not visual quality. | Burn the ledgers down under the dispositions the classifier assigns: FIX-DEAD-SLOT-BOXMODEL-SHADOW-01 (17 shadowed), FIX-DEAD-SLOT-VARIANT-REPOINT-01 (18 repoint), FEAT-DEAD-SLOT-WIRE-01 (22 wire), FIX-CONTRACT-OVERCLAIM-SWEEP-01 (10 delete + 6 state overclaims), FEAT-STYLES-KEY-ELEMENT-AWARENESS-01 (4 unsatisfiable-selector gaps), FEAT-STATE-ANCHOR-LOWERING-01 (9 descendant-anchored gaps), TRIAGE-DEAD-SLOT-REVIEW-01 (67 unclassified — the honest residual). |
| Examples consumer-app portfolio | `examples/` is the consumer-side falsification surface, governed by its own `examples/README.md` (claim/falsifier/non-claim discipline per app shape). `settings/` has implemented React, Vue (build/typecheck), and RN (typecheck-only) lanes. Three assembly-layer shapes (operations-dashboard, storefront-checkout, social-feed) are spec scaffolds with a shared, committed isomorphic data/API seam: lane-local fixtures + adapter (`?raw` bundler modules, no node-only specifiers) + promise-returning functional API, tested by `pnpm run test:examples:data-api` (47/47 pass at `d82f1046`, node/Vitest). | implemented narrow lanes + measured data seam; assembly shapes spec-scaffold only | `examples/README.md` (invariants 1–10, incl. the isomorphism invariant #10); `examples/*/src/{data,api}/`; `vitest.examples.config.ts`; specs `EXAMPLES-OPS-DASHBOARD-ISOMORPHIC-SEAM-01` (closed `d82f1046`), `EXAMPLES-DATA-API-TEST-LANE-01` and the app-shape scaffold specs (closed) | No assembly shape has an implemented framework lane; five-framework app parity is not proven by examples. The data/API test lane runs under node only and is structurally blind to browser-runtime regressions (governed by invariant #10's browser-render falsifier, which no automated lane executes today). Not in CI. | Implement the first framework lane for one assembly shape, and decide whether `test:examples:data-api` joins CI. |
| Fallback gates (presence + graph-truth) | Two hard invariants over the same surface, both inside `generate:check`'s semantic pass. **Presence** (`[FALLBACK_MISSING]`): every contract slot declaring `resolvesTo` MUST declare `fallback` — the codegen emits `var(--ref, fallback)` only when present, so without it the property silently drops when the token is unavailable. **Correctness** (`[FALLBACK_STALE]`): a present fallback MUST equal the light-mode terminal value of its `resolvesTo` chain in the resolved token graph — a fallback is a cached copy of a graph value, and truth is read from the graph, never inferred from literal frequency (the removed FALLBACK-DIVERGENCE-01 advisory's lesson: `8px` at 25 sites vs `6px` at 1 — the majority was the defect). Component-layer chains resolve intra-contract to their terminal graph value. Serialized DTCG **shadow** composites are lowered by a narrow mirror of the tokens build's `shadowValueToCSS` (structured dimensions, srgb→hex8 with `Math.round(a*255)` alpha, layers joined `", "`), drift-locked by an oracle test against the committed `tokens.css`; composites outside that envelope (oklch colors, string dimensions, typography) are still refused rather than guessed. Canonicalization is suppress-only: rem≡px, hex-case, hex3≡hex6, rgba/rgb≡hex, bare-`0`≡`0px`. | implemented / CI-gated (`generate:check`) | `packages/ds-codegen/src/validation/fallback-completeness.ts` + `.test.ts` (35 cases, incl. the mirror-vs-emitter oracle); `cli.ts` semantic-check registration; specs `ICONOGRAPHY-TOKEN-DISCIPLINE-02` A1/A2 (presence + 883-entry repair), `FALLBACK-STALE-01` (graph-truth + 695 literals derived, divergence advisory removed as subsumed), `FALLBACK-SHADOW-LOWERING-01` (shadow lowering + 23 shadow readings repaired across 10 components, incl. the single-site Dialog/Popover blind spot no divergence scan could see) | Correctness is a **light-mode-only** claim — dark-mode fallback correctness is a mechanism limitation (a single static literal cannot encode both theme modes). The shadow mirror is local, not exported from the tokens build (not importable: tsx-run, no compiled output); emitter drift is caught by the oracle test rather than shared code. Box-model ref values are not checked against the primitive's merged defaults. | (No open move — both gates enforced. If shadow tokens ever become theme-split, the light projection applies before lowering.) |
| State-suppression rail | A third styling obligation axis, independent of realization: a contract may declare `states.dimensions.<dim>.suppresses.categories` (e.g. `availability=disabled` suppresses `interaction`). `scripts/state-suppression-audit/` checks whether the generated React CSS honours that claim — every property set under an interaction pseudo must either be reset in the suppressing block (winning it back at equal specificity) or be guarded so the selector cannot match. Leaks are graded by **reachability**: `:hover` matches a disabled element, but `:focus-visible`/`:active` cannot match a natively-disabled form control, and no pseudo needing focus can match a non-focusable root. Current: 16 components declare interaction suppression, **13 guarded, 0 leaks** — the codegen emits the guard from the declared fact (`:not(:disabled)` on a native form-control root, the `aria-disabled` form otherwise, a contract-declared `a11y.attribute` winning over both). The remaining 3 declare suppression but author no root interaction block, so there is nothing to guard. | implemented / gated at pre-push (ledger ratchet) | `scripts/state-suppression-audit/{audit,selfcheck}.mjs` (11 falsification checks); ledger `known-leaks.json`; `docs/state-suppression-audit/state-suppression-matrix.{json,md}`; spec `RAIL-STATE-SUPPRESSION-01` | Detects a *declared* suppression that the CSS does not honour — it does NOT prove the converse (a component with no `suppresses` declaration is unchecked). Classification is static CSS analysis against ds-react only; reachability is derived from the contract's root tag and tabindex, not from a browser. A green ledger means every leak is ACCOUNTED FOR, not fixed. | Ledger fully burned under `FIX-STATE-SUPPRESSION-LEAKS-01`. Remaining gap: the scan is root-anchored, so an interaction pseudo wrapped in a compound selector (`:has(.x__input:hover)`) is outside it — zero such blocks exist in the corpus today, but the guard would not be emitted for one either. |
| Stack primitive layout-mode authority | `Stack.primitive.json` declares `layout.displayByMode.native = null` — "native lets the rendered host keep its own display/layout behavior" — and the IR derives `axisModes` from it as exactly the flex-bearing modes. Both the display rule AND the flex axis are now gated on that fact in all five web targets. Previously only Angular consumed `axisModes`; React/Vue/Svelte/Lit emitted an unscoped `.stack--vertical { flex-direction: column }` (`:host([variant])` in Lit), so every `layout="native"` host silently inherited a column axis it never declared. 42 of 49 generated components render `Stack layout="native"` and none passes a variant. | implemented / test-proven + browser-proven | `packages/ds-codegen/src/primitive-contract.ts` (`axisModes`); `frameworks/{react,vue,svelte,lit}/primitive-source.ts`; `primitive-emit.test.ts` (per-framework axis-gating case + over-correction guard, mutation-verified); specs `FIX-STACK-NATIVE-AXIS-LEAK-01` (merge `ed2d8ca5`), `FIX-ALERT-DECLARE-AXIS-01` (merge `1198287a`) | Browser-measured in Chromium on the showcase, not in an automated rail — no Playwright fact pins the axis, so a future regression would be caught by the emitter test but not by rendered geometry. The corpus sweep covered every `layout="native"` host with 2+ in-flow children across all 49 design pages at one viewport; a usage example not rendered on a design page, or a layout that only breaks at another width, was not measured. Angular's realization is a `@HostBinding` style, not a CSS rule, so the two mechanisms are proven equivalent only for the modes the contract declares. | Alert's anatomy is flat (`root > icon, title, body, dismiss`) and cannot express the conventional alert layout — icon beside a COLUMN of title+body, dismiss at the end. `FIX-ALERT-DECLARE-AXIS-01` restored column as the better of the two axes a flat anatomy admits; giving Alert/AlertNotice a content-wrapper part is unresolved and changes the public DOM contract of six targets. Separately, `.stack { gap }` is still emitted unconditionally while Angular gates gap on `axisModes` — the same divergence class as the axis, unmeasured and unfixed. |
| Anchored-surface part-key lowering | The anchored-surface family (Tooltip, Popover) marks `trigger`/`content` with `data-<prefix>-<part>` and emits NO part class — the trigger may be the consumer's own adopted element (React `asChild`, Vue slot-props host adoption) and the content is portaled out of the `.<prefix>` ancestor. `expandOptionsForContract` now lowers a bare part KEY to the marker selector for that family, so part-keyed styles reach the rendered element. Before this, Tooltip's `content` block compiled to `.tooltip__content`, matching zero elements: all 11 declared surface properties were inert and the tooltip rendered as bare text. Popover had worked around it with hand-written `[data-popover-content]` styles keys AND left a duplicate copy of the surface chrome on `root`, so a closed Popover painted its trigger as a filled panel. Popover is migrated to part keys; the `data-*` markers are untouched (behavioural marker vs styling hook). | implemented / test-proven + browser-proven | `packages/ds-codegen/src/ir.ts` (`expandOptionsForContract`, `surfacePartSelectors`); `frameworks/surface-part-class.test.ts` (7 cases, mutation-verified); `packages/ds-contracts/components/{Popover,Tooltip}`; specs `FIX-SURFACE-PART-CLASS-01` (merge `67d7edad`), `FIX-INVERSE-SURFACE-FOREGROUND-01` (merge `c5605ef6`) | Fixed at the selector layer, so no emitter template changed and the two callsites share one options helper. Browser-measured in Chromium at one viewport, not pinned by an automated runtime rail. **A2 only partially met:** the closed Popover host no longer paints (no background, border, shadow or radius) but still carries `padding: 16px`, because `renderBoxModelConsumers` emits `box-model.padding-*` unconditionally on root and Popover binds those to `semantic.surface.size.padding-*` — surface facts applied to the host. Unchanged by this slice (16px before and after), so pre-existing, not a regression. | Two findings this surfaced, both unfixed. (1) **14 orphaned part classes remain** — styled in generated CSS, never rendered in source: Checkbox `__indicator`/`__input`, Card `__badge`/`__media`/`__actions`/`__note`/`__link`, Button `__spinner`/`__loadingText`, Skeleton `__shape`/`__stack`, Text `__error`/`__success`, Tabs `__root`. These are never-rendered-part overclaims, a different class from the Tooltip defect, and no existing rail sees them: the dead-slot audit asks whether a token slot has a `var()` consumer, and here every consumer exists — it just sits in a rule whose selector matches nothing. (2) **23 camelCase custom properties are read by generated CSS and declared nowhere**, so those bindings silently fall back to their literals: codegen preserves camelCase in var names while the token build kebab-cases them, so any contract `resolvesTo` with a camelCase segment cannot resolve. Worst reach is `semantic.shape.control.border.defaultWidth` (17 components — Button's border width); also `core.dimension.actionMinHeight*`, the whole `semantic.glyph.badge.size.*` group, `foreground.linkHover`/`linkVisited`, `interaction.stateLayer.*`, `background.accentSubtle`, `spacing.gap.gridSmall`. `foreground.on-inverse` is deliberately kebab-named to sidestep this. |
| Dead-slot audit | Read-only audit (mirroring `PSEUDO-STATE-STYLING-RAIL-01`'s shape) classifying every token/style slot a contract declares as **consumed** or **dead** by scanning the generated ds-react structure CSS (`<Component>.css`) for `var(--fsds-<slug>)` references. Consumption counts a `var()` READ in either the structure CSS or another slot's declaration in `<Component>.tokens.css` — overriding a slot that another slot reads does change what that slot resolves to, so the knob works. A slot still cannot consume ITSELF: the scan matches `var(--slot`, never the `--slot:` declaration. Initial matrix against the post-fallback-repair corpus: 49 components, 894 slots declared, 760 consumed, **134 dead** — including the 3 Button size slots that motivated the audit (`button.size.padding-block.medium`, `padding-inline.medium`, `minHeight.medium`, consumed nowhere across all 5 frameworks). | implemented / gated at pre-push (ledger ratchet, RAIL-STYLING-REALIZATION-LEDGERS-01) | `scripts/dead-slot-audit/audit.mjs` + `classification.test.mjs` (6 discriminator checks, standalone Node); `docs/dead-slot-audit/dead-slot-matrix.{json,md}`; spec `ICONOGRAPHY-TOKEN-DISCIPLINE-02` A3 | Gated at pre-push by a two-directional ledger (`scripts/dead-slot-audit/known-dead.json`): an unledgered dead slot or a stale entry fails the push. Green means every INERT slot is ACCOUNTED FOR, not fixed — 115 ledgered: **98 defects + 17 inert-by-design**. A slot is inert when no rule reads it, so setting the custom property has no effect (measured: each is declared in 9 emitted files and read by `var()` in zero). A declared-but-unread slot is therefore NOT an override escape hatch — the hatch requires a consumer rule. `shadowed` entries are inert by design (an author root rule deliberately overrides the box-model primitive) and are not defects, but they stay ledgered: an entry leaves only by becoming consumed or by having its declaration deleted. Dispositions are machine-computed; `review` (67) means no rule matched and needs human adjudication, NOT that the slot is safe to delete. `shadowed` is a THIRD status, not a dead disposition: `renderBoxModelConsumers()` emits the primitive consumer on every root and author `styles.root` rules spread after it, so the author wins the property and the primitive var never reaches the CSS — the layering working as designed. Falsified end-to-end in both directions: deleting an author override returns the slot to `consumed`, not `shadowed`; and deleting a `shadowed` ledger entry fails the audit as unledgered, so inert-by-design is accounted for rather than exempt. Consumption is scanned in ds-react CSS only (the reference framework); a slot consumed only in another framework's bespoke CSS would register as dead here. The 134 dead slots are real drift, not classification error (grep-verified). | Burn the 98 down by disposition (wire 19 / delete 10 / unconsumed-vocabulary 3 / review 66); the 18 `repoint` entries burned under `FIX-DEAD-SLOT-VARIANT-REPOINT-02` under the named FIX-*/FEAT-*/TRIAGE-* specs recorded in the ledger. |
| Component identification attribute | Every generated component root across react/vue/svelte/angular/lit carries `data-fsds-component=<cssPrefix>` for audit/devtools discoverability (e.g. `document.querySelector('[data-fsds-component="button"]')`). React/Vue/Svelte: template attribute on the root element (threaded through each framework's `RenderContext` from `ir.cssPrefix`). Angular: `host: { "data-fsds-component": ... }` in the `@Component` decorator (4 emit sites). Lit: `this.setAttribute(...)` in `connectedCallback` (not the constructor — the custom-elements spec forbids attribute writes there; several `connectedCallback` emission sites across `component-source.ts` + `surface-emit.ts` each carry it, verified across all 49 Lit components). | implemented across all 5 DOM frameworks | `packages/ds-codegen/src/frameworks/{react,vue,svelte,angular,lit}/component-source.ts`; `frameworks/lit/surface-emit.ts`; regenerated component source across all 5 packages; spec `ICONOGRAPHY-TOKEN-DISCIPLINE-02` A5 | **Identification only — NOT selector scoping.** Does not change any CSS selector. The collision-isolation concern this attribute was a prerequisite for is now partially resolved by `@layer components` wrapping (see the resolved finding below) — but that fix works at the layer level, not the selector level, so `[data-fsds-component]` selector-prefixing (per-component isolation granularity, heavier: 5 framework emitters + every selector) remains undone. Native (RN/iOS/Android) ID surfaces are a follow-up (RN `testID`, Swift `accessibilityIdentifier`, Jetpack Compose `testTag`). | Selector-prefixing with `[data-fsds-component]` for per-component isolation granularity, if still wanted now that layer-wrapping covers the collision case; add native ID surfaces. |

## Resolved architectural finding: component CSS collision isolation (option a)

Research during `ICONOGRAPHY-TOKEN-DISCIPLINE-02` (Phase 5) recorded a real, unmitigated collision concern. Fix option (a) — `@layer` wrapping — shipped under `FEAT-BRAND-PERSONA-SCOPE-01` (closed, worktree-merge `8107091b`) and `FEAT-BRAND-PERSONA-VISUAL-QA-01` (closed, worktree-merge `3c5f398f`):

- **Component CSS is now layered.** `generateLayerDeclaration()` (`packages/ds-tokens/build/generators/global.ts:917`) declares a 6-layer order — `@layer core, semantic, components, theme, brand, density;` — and `emitTokensCss` (`packages/ds-codegen/src/css.ts:379`) wraps every generated component's structure/tokens CSS in `@layer components`. This closes the original gap: FSDS component rules are no longer unlayered.
- **Why this also fixes collision isolation, not just intra-FSDS overrides.** Per the CSS Cascade Layers spec, unlayered styles always win over layered styles regardless of specificity or layer order. Moving component CSS into `@layer components` means a consuming app's plain (unlayered) CSS now reliably beats FSDS's component CSS without `!important` or specificity tricks — the collision concern this section originally flagged.
- **What it unlocked inside FSDS:** `components.<Name>.<path>` brand overrides (`packages/ds-tokens/src/brands/_schema.json`, `components` property; implemented via `componentNameToKebab`/`componentTokenPathToCSSVar`/`walkComponentBrandSubtree`/`processComponentBrandTokens` in `packages/ds-tokens/build/generators/global.ts`) land in `@layer brand`, which is declared after `@layer components`, so a brand's component-scoped override now deterministically wins over that component's own default without needing to out-specificity it.
- **Non-claims, carried forward:** this closes fix option (a) only. Option (b) — prefixing every generated selector with `[data-fsds-component="<cssPrefix>"]` for per-component isolation granularity — remains undone; the identification attribute from Phase 5 is still identification-only and does not prefix any selector. The layer mechanism is a `<link>`/`<style>` document-level cascade feature — it does not reach Lit's shadow DOM (`emitLitInlineCss` renders inside a separate cascade context `@layer` cannot cross), so Lit's style isolation still comes from shadow DOM boundaries, not from this layer order.

## Stale-doc corrections this snapshot makes explicit

Several older docs are still useful, but their frontmatter/status or prose lags the code:

- the machine-local successor ledger (`docs/internal/successor-work.md`) must not be read as "all listed surfaces are unimplemented." Some items are now partial or promoted, especially target-pack registry, component evidence pages, usage examples, and runtime rail.
- README no longer carries a hand-maintained component count (removed in `7a325195`); loader discovery under `packages/ds-codegen/src/contracts-fs.ts` is the count authority. Derived docs must still reconcile on corpus growth — `docs/successor-work-mobile-rail-sweep.md` is the current live counterexample (see the mobile-parity row).
- The five-framework claim is still true but narrower than the broader target story. The current executable family is Web DOM; target packs and Figma descriptors extend the projection model but do not yet prove external pack execution or non-web runtime semantics.
- Runtime rail evidence is newer than the original architecture docs. Any answer that treats runtime preview/fact testing as future work is stale.
- The behavior-realization batch (2026-07-22) landed after the whole-ledger reconciliation: any doc or row text describing Accordion as behaviorally dead, Select/Calendar events as unwired, Calendar as count-iterated (`daysShown`), portal computation as orphaned, or the behavior-realization audit as future work is stale — the batch rows and the corrected runtime-rail/iteration/substrate rows are authoritative.
- The whole ledger was reconciled at `main@c5cda2c9` (2026-07-21, `DOC-SNAPSHOT-RECONCILE-02`) — every row re-verified against live code, the formerly-absent surfaces (recursive composition, slot-projection, textOverflow, native compile lanes, iconography, examples portfolio) now have rows, and two live defects surfaced during verification are recorded in their rows rather than fixed silently: the render-binding rail's broken matrix path, and the mobile-parity `--check` red. The visual-fidelity/token-enrichment campaign still has no dedicated row (its outcomes surface via the styling-realization audits row); recording it row-grade requires its own verification pass.

## Current bounded thesis

The project is a contract-governed design-system substrate with generated Web DOM framework realizations, target-pack admission scaffolding, Figma descriptor projection, token governance, generated-artifact admission, runtime fact assertions, and partially derived component evidence pages.

The claim boundary is bounded. It does not yet prove production adoption ergonomics, complete accessibility adequacy, visual quality, npm-distributed packages, live Figma library publication, executable external target packs, or substrate-neutral UI semantics. The non-web mobile arc has narrowed — but not closed — the substrate-neutrality question: React Native is now a default-rail target with a generated package, typed native token projection, native variant/state style realization, real package-type admission, focused host-render + surface tests, and a public-export settings example typecheck lane, while simulator/device runtime behavior, full surface parity, compound-part emission, part-scoped/boolean-modifier styling, and platform accessibility parity remain unproven.
